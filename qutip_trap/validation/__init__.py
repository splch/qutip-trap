"""Analytic reference formulas used by the tests (PLAN.md Section 3.2).

The check and benchmark scripts behind every ``[recomputed here]`` number live at the repository root under
``validation/scripts/`` with their committed outputs (Appendix D); ``validation/scripts/run_checks.py`` re-runs
them on the pinned toolchain as the first CI job. Reference formulas join this package with the milestones.
"""

from __future__ import annotations

__all__: list[str] = []
