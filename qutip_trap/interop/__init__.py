"""Adapters that put qutip-trap behind another SDK's execution interface. Each imports its SDK at module import and is an
optional extra of the package (``qiskit``: ``uv sync --extra qiskit``); this package imports none of them."""
