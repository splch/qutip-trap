# Physics notes: what qutip-trap integrates, and where each equation comes from

qutip-trap simulates a trapped-ion quantum computer by integrating its physics: the ions' internal states and motional
modes evolve under the Hamiltonian the laser and microwave fields produce, with the dissipation the environment and the
scattered photons cause, from the cooled and pumped initial state to the detected photons. Circuits are compiled to native
gates and scheduled as pulses; the pulses are what the simulator integrates. No gate matrix is ever applied to the
simulated state (the compiler and the validation suite are the only places ideal unitaries appear).

These notes list the equations as implemented, each with the section of [`PLAN.md`](../PLAN.md) that specifies and tags
it, the module that implements it, and the provenance record of [`docs/provenance/ledger.yaml`](provenance/ledger.yaml)
(`conv.*` for a convention row, `anchor.*` for a number a test pins) that a reader can follow to the source and to the
check that established it. Tags follow the plan's Appendix D: **verified** (checked against the primary source by
independent verifiers), **corrected** (a printed or extracted error found and the corrected form stated), **recomputed
here** (a committed script under `validation/scripts/` reproduces the number), **derived** (computed by the package from
cited inputs), **background** (textbook physics the author supplies), **contested**. A tag records what checking was done,
never that a value is final: the tests of Section 9 define correctness.

Notation: internal frequencies are angular (rad/s), public ones ordinary (Hz), converted with one explicit 2π; ħ is kept in
the displayed Hamiltonians and divided out once when the master equation is integrated (Section 5.6; `conv.frequencies`).
The full convention table is in [conventions.md](conventions.md).

## 1. One master equation

Everything the simulator integrates is one master equation on the joint space of the ions' internal levels and the
resolved motional modes (Section 5.7):

    ρ̇ = −i[H(t), ρ] + Σ_k D[L_k] ρ,      D[L] ρ = L ρ L† − ½{L†L, ρ},      H(t) = H_phys(t)/ħ,

run as `sesolve` on the pure branches of the initial mixture, `mcsolve` with keyed quantum-jump trajectories, or `mesolve`
on the density matrix where the Liouvillian fits (Section 5.3; `dynamics/evolve.py`, the `dop853` → `vern9` escalation
ladder of `conv.solver_integrators`). The one Hamiltonian builder is `dynamics/hamiltonian.py` (Section 3.1: every pulse,
whether a calibration scan, a single-qubit gate, a Mølmer-Sørensen gate or a cooling pulse, is a set of drive terms on a
shared space assembled by the same builder); the collapse operators come from `dynamics/channels.py`,
`noise/scattering.py` and the readout layer. Every term below is switchable and every switch is recorded in
`Result.diagnostics.approximations`.

## 2. The Hamiltonian

H(t) = H_mot + H_int + Σ_i H_drive,i(t) + Σ_i H_Stark,i(t) + H_anh + H_curv (Section 5.7).

- **Motion.** H_mot = Σ_m ħ ω_m a_m† a_m over the resolved modes, the zero-point energy dropped (`conv.zero_point_energy`),
  the frequencies the calibrated (noise-shifted) secular frequencies of the pseudopotential frame (Sections 4.1.3, 4.1.5)
  with per-sample offsets from the quasi-static noise. Modes are indexed by position in `Crystal.modes`, ordered axial,
  transverse_1, transverse_2, ascending in frequency within a family, eigenvectors unit norm with the last component positive
  (`conv.mode_index`, `conv.mode_eigenvector_gauge`).
- **Internal offsets.** H_int = Σ_i (ħ Δ_i/2) σ_z^i (Σ_p ħ Δ_{i,p} |p⟩⟨p| when the register factor has d > 2), the offset of
  ion i's true transition from the frame frequency: the magnetic-field sample through the exact Zeeman diagonalization at
  the shifted field (Section 6.3), the ac Zeeman shift of a microwave drive (Section 4.3.3), the qubit frequency the table
  believes against the one the atom has (Section 7.3). σ_z = |1⟩⟨1| − |0⟩⟨0| in the computational ordering
  (`conv.computational_ordering`).
- **Drives.** For every tone of every pulse on ion i (Sections 4.3.1, 5.2; `conv.rabi_frequency`, `conv.detuning`,
  `conv.drive_coefficient_and_phase_continuity`)

      H_drive,i(t) = (ħ/2) Σ_tones Ω_{i,tone}(t) e^{−i(μ_tone t − φ_tone(t))} σ₊^i ⊗ Π_m D_m(i η_{i,m}) + h.c.,

  with the displacement operators D(iη) = exp[iη(a + a†)] built as exact matrix exponentials and asserted against the
  analytic Laguerre elements ⟨n′|D|n⟩ over the populated range (Section 5.1.1; `anchor.m2.displacement_table`,
  `conv.oracle_margin_rule`), or held as tensor factors and applied mode by mode by the matrix-free kernel of
  `dynamics/kernels.py` (Section 11.3 item 4; `anchor.m9b.factorized_kernel_exactness`). Ω comes from the beam intensities
  and the dipole matrix elements (Sections 4.3.2, 4.5; `light/raman.py`, `light/microwave.py`), never from a table: the
  scheduler requests Ω_table and the played chain of Section 7.10 delivers Ω_phys = Ω_req Ω_derived/Ω_table
  (`conv.played_chain`). The same term on a neighbour j with Ω → ε_{ij} Ω is addressing crosstalk (Section 6.6; the ratio is a
  Rabi ratio, `conv.crosstalk_ratio` in Section 13); the coefficient multiplied by e^{iβ cos(Ω_rf t + δ)} is excess
  micromotion (Section 4.3.6); Ω_i → Ω_i(1 + (Ω″/2Ω) x̂_i²) is Cetina's beam-curvature coupling (`anchor.m2.cetina_curvature`);
  a sampled φ(t) or Ω(t) is laser phase or intensity noise (Sections 6.3, 6.4). Frozen spectator modes leave the space and
  enter through per-shot Debye-Waller factors e^{−η²(n + ½)} drawn from their thermal distribution
  (`conv.frozen_spectator_shot_sample`, `anchor.m2.debye_waller_identity`).
- **Spin and motion phases.** A bichromatic pulse's two tones at phases φ_b, φ_r define the spin phase φ_s = (φ_b + φ_r)/2
  and the motion phase φ_m = (φ_b − φ_r)/2; the force axis carries a further π/2 from the i of the sideband coupling
  (Section 4.3.4; `conv.spin_motion_phases`, `conv.motion_phase_default`). The Lamb-Dicke parameter is η_{i,m} =
  (Δk · ê_m) c_{i,m} √(ħ/(2 m_i ω_m)) with the ion's own mass and the mass-weighted eigenvector component, C₀ = 1 + 3q²/16
  applied once (Sections 4.1.1, 4.1.7; `conv.lamb_dicke`, `conv.micromotion_correction`, `anchor.trap.c0_wronskian`).
- **Light shifts.** H_Stark,i(t) = (ħ/2) δ_St,i(t) σ_z^i, proportional to the instantaneous intensity, the two-photon shift
  scaling linearly and a single-photon shift quadratically with the amplitude (Section 4.3.2; `conv.stark_scaling_with_amplitude`);
  the scheduler compensates the believed shift by detuning every tone and absorbs the accumulated z-rotation into the
  virtual-Z frame (Section 7.5 item 7; `conv.stark_compensation`).
- **Anharmonicity.** H_anh is the cubic Coulomb coupling of Section 4.1.4, opt-in because it couples the three mode
  families; the resonance checker and the directly integrated phase estimate are on by default (`anchor.trap.nonlinearity_epsilon`).

## 3. The collapse operators

- **Heating.** √(Γ(N̄ + 1)) a_m and √(Γ N̄) a_m† per mode, with Γ_h = Γ N̄ the quoted heating rate d⟨n⟩/dt at n = 0 and
  Γ_h = e² S_E/(4 m ħ ω) from the single-sided electric-field noise density, projected on the mode with the configured
  correlation length for a multi-ion crystal (Sections 4.1.5, 6.2; `conv.heating_master_equation`,
  `conv.electric_field_noise`, `conv.heating_rate_meaning`; `trap/heating.py`, `noise/model.py`).
- **Motional dephasing.** √(2/τ_m) a_m† a_m from the white rf-amplitude noise on the rf-derived modes (Section 6.2;
  `conv.motional_dephasing_from_rf_noise`).
- **Qubit dephasing.** √(γ_φ/2) σ_z^i with γ_φ = 2π² (dν/dB)² S_B,white from the computed Zeeman sensitivity, so a Ramsey
  coherence decays as e^{−γ_φ t} and γ_φ = 1/T₂ for the white part; the slow part is a sampled Δ_i in H_int
  (Section 6.3; `conv.qubit_dephasing_operator`, `conv.qubit_dephasing_from_field_noise`).
- **Photon scattering.** One Raman operator √Γ_{p→q} |q⟩⟨p| ⊗ R per channel from the signed amplitudes of the atomic layer,
  including channels out of the qubit pair when d > 2 (leakage into a sink level, `noise/levels.py`), the elastic Rayleigh
  channel as c = ½√Γ_el σ_z (the dissipator prefactor Γ_el/4, the coherence decaying at Γ_el/2; `conv.rayleigh_dissipator`),
  and R the recoil operator D(i(η_abs − η_em)) with the emission direction on a moment-exact quadrature about B
  (Sections 4.2.8, 4.5.5, 6.5; `conv.scattering_channels`, `anchor.m3.recoil_energy_identity`). At d = 2 the run reports the
  scattering probabilities as estimates in the intrinsic budget instead.
- **Laser intensity noise.** √D H_drive(t), the white part of the intensity spectrum (Section 6.4; `conv.intensity_noise_channel`).
- **Readout.** The bright/dark/shelf continuous-time Markov chain of Section 8.2 with its photon-counting operator, and the
  shelf decay √(1/τ_D) |S⟩⟨D| of Section 8.1.

Every spectrum is stored two-sided in angular frequency with the e^{−iωt} kernel and a separately declared white level, the
white level the Lindblad route and the tabulated band the sampled-trajectory route (Sections 6.1, 9.17; `conv.noise_routing`).

## 4. Trap, crystal and modes

- **Mathieu equation** d²x/dξ² + [a − 2q cos 2ξ] x = 0, ξ = ω_rf t/2, the stability edge and the characteristic exponent from
  the monodromy matrix, the Floquet function's Wronskian normalization Im(u* u̇) = ν giving C₀ (Section 4.1.1;
  `conv.mathieu_sign`, `conv.floquet_phase_origin`, `conv.floquet_branch_normalization`; `trap/mathieu.py`).
- **Pseudopotential** ψ = Q²|E|²/(4 m Ω²) with V_rf the peak of the cos drive (Section 4.1.6; `conv.rf_amplitude_pseudopotential`;
  `trap/pseudopotential.py`, `trap/surface.py` for gapless-plane electrodes with the five-wire height √(a(a + 2b))/2,
  `anchor.trap.house_five_wire`).
- **Equilibrium positions and modes.** The N-ion equilibrium from the Coulomb-plus-trap potential, the mass-weighted Hessian
  and its eigenproblem on all three axes, the ordering checks (axial COM lowest, transverse COM highest, the zigzag
  threshold), the per-species eigenvector components c_{i,m} = √m_i b_i (Sections 4.1.2, 4.1.3, 4.1.7;
  `conv.mass_weighted_eigenvectors_parity`; `anchor.trap.james_spectrum`, `anchor.trap.zigzag_threshold`,
  `anchor.trap.mixed_two_ion_axial`; `trap/crystal.py`).
- **Heating from S_E(ω)** as an input spectrum (Section 12: no first-principles model exists), with the multi-ion
  projection and the S_E round trip (`anchor.trap.heating_round_trip`).

## 5. Atomic structure and the light-atom coupling chain

- **Hyperfine and Zeeman structure** at the operating field by exact diagonalization of H_hfs + H_Z on every fine-structure
  level, with A_hfs signed, the measured g_J, g_I = −(μ_I/(I μ_N))(m_e/m_p), the Breit-Rabi formula as the J = 1/2 check,
  labels connected adiabatically through avoided crossings, the clock points found from dν/dB = 0 (Section 4.5.1;
  `conv.sign_A_hfs_and_g_factors`, `conv.nuclear_g_sign`; `anchor.ca43.clock_point`, `anchor.yb171.quadratic_zeeman`;
  `species/zeeman.py`).
- **Dipole matrix elements** by Wigner-Eckart, ⟨F m_F|d_q|F′ m_F′⟩ = ⟨J‖d‖J′⟩ × (6j and 3j factors), the reduced element from
  the measured lifetime through Γ = ω³ (2J + 1)|⟨J‖d‖J′⟩|²/(3π ε₀ ħ c³ (2J′ + 1)), the saturation intensity I_sat = π h c Γ/(3λ³)
  with the angular partial rate (Sections 4.5.2, 9.13; `conv.hyperfine_dipole_element`, `conv.dipole_normalization_stack`,
  `conv.saturation_intensity`; `anchor.yb171.i_sat`, `anchor.yb171.branching_third`; `species/dipole.py`).
- **Polarization** decomposed about B into σ⁺, π, σ⁻ components in the spherical basis, the helicity index of absorption
  against the operator index q_op = m_lower − m_upper (Section 4.5.3; `conv.polarization_components`; `species/polarization.py`).
- **Raman couplings** Ω_R = Σ_e Ω₂* Ω₁/(2Δ_e) with the single-photon Rabi frequencies in the ħΩ/2 convention, light shifts
  δ_g = Σ|Ω|²/(4Δ_e), the differential shift of the qubit pair, and the scattering amplitudes (Raman, Rayleigh, the
  differential-Rayleigh dephasing) from the explicit intermediate-state sums (Sections 4.5.4, 4.5.5; `conv.raman_beam_labels`,
  `anchor.be9.ozeri_2005_amplitudes`, `anchor.be9.wineland_clock_light_shift`; `species/raman.py`, `light/raman.py`,
  `light/scattering.py`).
- **Electric-quadrupole coupling** for optical qubits, Ω = (e E₀ k/(2ħ)) |⟨S‖r² C^{(2)}‖D⟩| |Λ(m, m′)| |g^{(q)}| with the
  Racah-normalized rank-2 tensor and the upper-level degeneracy in the lifetime formula (Section 4.5.7;
  `conv.quadrupole_element_normalization`; `anchor.ca40.reduced_element_e2`, `anchor.sr88.reduced_element_e2`;
  `species/quadrupole.py`).
- **The multi-level optical Bloch builder** of Section 4.2.8 (`dynamics/multilevel.py`, `light/bloch.py`): every dressed
  sublevel of every included level, one frame frequency per manifold with the inconsistency detector and the Floquet
  fallback, one collapse operator per decay line and emitted polarization from the dipole operator itself (Σ C†C = Γ P_e),
  the steady state as the single scattering-rate object W(Δ) = Σ Tr(C†C ρ_ss) that cooling, detection and pumping share
  (`conv.scattering_rate_object`, `conv.multilevel_frame_assignment`; `anchor.m3a.yb171_detection_rate`,
  `anchor.m3a.dark_states`).

## 6. Cooling and state preparation

- **Doppler cooling** from the rate framework of Section 4.2.2: per illuminated ion and mode the heating and cooling rates
  c²_{i,m}[S(±ν) + 2D] from the dipole-force spectrum of the ion's steady state under all its beams and the emission-recoil
  diffusion, one detuning per beam group optimized over the participation-weighted modes, the RMP force model and the
  Doppler limit (Γ/4ν)(1 + α) as guarded cross-checks where ν ≪ Γ (Section 4.2.1; `conv.level_a_rates_participation`,
  `conv.doppler_stage_producer`; `anchor.m3.doppler_rate_framework`, `anchor.m3a.doppler_limit`; `prep/doppler.py`,
  `prep/rates.py`).
- **Resolved-sideband and Raman sideband cooling**: the rate coefficients A_± = W(Δ ∓ ν) + (η̃²/η²) W(Δ) with the carrier
  weight, Stenholm's floor (Γ/2ν)² [α/cos²θ_L + 1/4], the exact pulsed transfer matrices with higher sidebands and the
  Laguerre nodes where a single order strands population, the repump recoil kernel, and the sideband-ratio thermometry
  R_k = (n̄/(1 + n̄))^k exact for every pulse duration (Sections 4.2.2, 4.2.7; `conv.pulsed_transfer_matrix`;
  `anchor.m3.laguerre_nodes_and_che_schedules`, `anchor.m3.thermometry_exactness`; `prep/sideband.py`).
- **EIT cooling** with Δ > 0 and Ω_r² = 4ν(ν + Δ) in the plan's sign (Section 4.2.3; `conv.eit_plan_sign_and_composition`;
  `anchor.m3.eit_fixture_and_lechner`; `prep/eit.py`); **polarization-gradient cooling** in Joshi's analytic model and the
  Lindblad layer (Section 4.2.4; `conv.pgc_two_models`; `prep/polarization_gradient.py`).
- **Optical pumping** by the exact exponential of the multi-level Liouvillian from the scrambled manifold, returning the
  preparation error, the photon count and the per-mode recoil heating (Section 4.2.6; `anchor.m3.optical_pumping_recoil`;
  `prep/pumping.py`); the stages run Doppler → sideband/EIT → final pump (`conv.preparation_stage_order`,
  `conv.preparation_recipe`; `prep/sequence.py`, `prep/recipe.py`).
- **Recoil.** The emission kernel over a direction quadrature that derives every angular factor (α = 2/5 for the σ pattern
  along the beam, 1/5 for π perpendicular, 1/3 isotropic), the per-ion participation and the joint multi-mode kick, and the
  recoil-energy identity (Section 4.2.8; `anchor.m3.recoil_energy_identity`; `light/recoil.py`).

## 7. Gates

- **Single-qubit gates.** A native GPi(φ) or GPi2(φ) is a resonant carrier pulse of area π or π/2 at phase φ relative to
  the ion's frame, R(θ, φ) = exp[−iθ σ_φ/2] (Section 4.3.5); the exact dynamics carry the off-resonant sideband coupling
  (ηΩ/ω_m)², the Debye-Waller factors and the addressing crosstalk sin²(ε_{ij} θ/2) on the neighbours, which the intrinsic
  budget reports (Section 9.6). Virtual RZ(θ) is a frame update, φ → φ − θ on every later pulse (Section 7.6;
  `conv.virtual_z_propagation`, `conv.virtual_z_concrete_sequence`, `conv.compiler_frame_absorption`). The single-qubit
  generator carries θ/2 while the two-qubit XX(χ) carries χ with no ½, and no helper computes both (Section 13;
  `conv.rotation_generators`). The composite-pulse library of Section 4.3.5 (SK1, BB1, CORPSE, SCROFULOUS, the
  Low-Yoder-Chuang families) is `control/composite.py`, whose internal figure of merit is F_K = ¼|Tr(U_id†U)|² with F_C and
  F_avg as conversions (Section 13; `conv.gate_fidelity_measure`; `anchor.m2.composite_order_ladder`,
  `anchor.m2.mount_pd6`).
- **The Mølmer-Sørensen gate.** A bichromatic pulse with tones at ±μ from the carrier on both ions; in the plan's per-tone
  convention the spin-dependent force on S_y is −(ħηΩ/2)(a† e^{iεt} + h.c.) and the maximally entangling closure of a
  single-mode square pulse is ηΩ/ε = 1/(2√K) (Section 4.4.1; `conv.ms_closure`; `anchor.m4.ms_closure_through_the_builder`).
  The closed-form displacement α_m(t) and entangling angle χ_ij = Σ_m η_im η_jm ∫∫[Ω_i(t)Ω_j(t′) + Ω_j(t)Ω_i(t′)] … of Section
  4.4.3 (Choi's form with sin(μt) in the integrand, the symmetrized two-body kernel) drive the AM, FM and Fourier pulse
  solvers that close every resolved mode's loop (`conv.entangling_angle`; `anchor.m4.symmetrized_kernel`,
  `anchor.m4.choi_closure_counts`; `control/shaping.py`); the played waveform's χ_m and α_m per mode decide the mode classes
  (`conv.mode_classes_and_tolerances`). The gate's error model (residual displacement ε_ent = Σ|α|²(2n̄ + 1), the thermal
  Debye-Waller error (π²/4)η⁴⟨(n − n_ref)²⟩ with its calibration reference, spectator loops, dephasing, intensity noise) is
  Section 4.4.7 (`anchor.m4.debye_waller_references`, `anchor.m4.residual_displacement_conversions`, `anchor.m4.ballance_alpha_k`).
- **The light-shift (σ_z σ_z) gate** from the two-photon self-couplings of the two qubit levels, and the ZZ native gate as the
  inferred GPi2-wrapper construction on an MS device (Sections 4.4.4, 7.1; `conv.light_shift_force`,
  `conv.zz_wrapper_construction`; `anchor.m4.light_shift_gate_ca40`).
- **Native matrices and the compiler.** GPi, GPi2, MS(φ₀, φ₁, θ), ZZ(θ) and RZ as `control/native.py` defines them
  (`conv.native_ms_matrix`, `conv.gate_parameters`), the ZXZXZ decomposition of any SU(2), Maslov's one-XX CNOT template with
  its sign e^{iπvs/4}, the exact CP(θ) = [RZ(θ/2) ⊗ RZ(θ/2)] ZZ(−θ/2), every block verified against its target up to a global
  phase (Sections 7.2, 7.7; `conv.compiler_single_qubit_decomposition`, `conv.cnot_template_sign`, `conv.cp_template_exact`;
  `control/compiler.py`); an arbitrary SU(4) through the KAK decomposition in the magic basis, (a, b, c) in the Weyl
  chamber π/4 ≥ a ≥ b ≥ |c|, at most three entangling gates (`control/two_qubit.py`; `conv.kak_weyl_chamber`).
- **Calibration.** The scheduler reads only the calibration table (Section 7.3): the surrogate of Section 7.5 (closed-form
  waveforms corrected by exact spot checks, derived values as `seed` entries) or the table fitted by simulated experiments in
  the dependency order field → micromotion → modes → Rabi, Stark, qubit frequency → crosstalk → entangling scans → detection,
  heating, every entry with its uncertainty, age and noise sample (`conv.calibration_graph`, `conv.sideband_lineshape`,
  `conv.servo_residual`; `anchor.m8.*`; `calibration/`, `experiments/`).

## 8. Readout

- **The rate object.** R_∘ (the photon rate of the conditional bright state), R_d and R_b (the off-resonant pumping rates out
  of the bright and dark manifolds) from the slow-manifold analysis of the detection beams' Bloch steady state, asserted
  against the equal-population ceiling n_e/(n_e + n_g) (Γ/4 for 171Yb+'s F = 1 → F′ = 0 cycle) and reproducing the (Γ/18)
  s_∘/[1 + (2/9) s_∘] form and Noek's leakage prefactors (Sections 8.1, 8.8; `conv.saturation_ceiling`; `anchor.m5.yb171_rate_object`;
  `readout/fluorescence.py`).
- **Photon records.** The bright/dark/shelf continuous-time Markov chain: the exact count distribution over a window as one
  matrix exponential of the augmented generator, the sampled chain with Poisson counts at the piecewise rates, dead time,
  afterpulsing, neighbour crosstalk as added counts, a camera point-spread function (Section 8.2; `conv.readout_chain_exact`,
  `conv.readout_crosstalk_added_counts`, `conv.detection_efficiency_once`; `readout/detection.py`).
- **Discriminators.** Thresholding with the interior optimum over (n_c, t_b), Myerson's time-resolved maximum likelihood,
  adaptive early termination, first-photon protocols, camera decoders (Section 8.3; `conv.readout_likelihood_first_order`;
  `anchor.m5.myerson_optimum_and_recursion`; `readout/discriminate.py`).
- **The measurement operator.** The joint internal outcome is sampled projectively from the final state so that entangled
  correlations survive; the product POVM at zero crosstalk and the register-wide confusion tensor otherwise; ε = ½(ε_B + ε_D)
  reported per ion (Sections 8.4, 5.7; `conv.readout_figure_of_merit`, `conv.readout_polarity_scheme`; `anchor.m5.povm_domains`).

## 9. Noise as physical processes

Section 6.1's rule routes every noise source as physics, never as a phenomenological channel: (b) a white spectral part as a
Lindblad operator, (c) a slow part as a quasi-static parameter drawn per dynamical sample at the shot clock (an
Ornstein-Uhlenbeck chain with its correlation time, high-passed by an optional servo), (d) a band as a sampled time series
the drive coefficients see, (e) rare events (collisions) as heralded shots (`conv.noise_routing`, `conv.seed_keys`;
`noise/`). The control hardware chain of Section 7.10 quantizes phases and amplitudes to the DDS words, applies the
modulator's first-order response and the amplifier bandwidth, and carries the beat-note phase reference at the gate start
(`conv.hardware_chain`, `conv.response_phase_reference`, `conv.beat_phase_at_gate_start`; `control/hardware.py`). The
filter-function layer of Section 6.9 (`noise/decoupling.py`) computes χ = (2/π)∫(dω/ω²) S_b(ω) F(ωτ) with the full
toggling-frame control matrix, the dc frozen-noise coefficients and the decoupling sequences (`conv.filter_function_machinery`).
The circuit-level summaries of Section 6.8 (`noise/summary.py`) are what the simulator exports, never what it integrates:
the entanglement and average gate infidelity of a simulated channel, its Pauli twirl, the depolarizing rate ε with
Λ_ε(ρ) = (1 − ε)ρ + ε/(4ⁿ − 1) Σ_{P≠I} PρP whose ε is the entanglement infidelity (`conv.depolarizing_normalization`,
the same channel as Trout 2018's uniform p/3 and p/15 Kraus weights, with Qiskit's λ = p 4ⁿ/(4ⁿ − 1) as a conversion — the
Forte medians 2.0 × 10⁻⁴ and 46.4 × 10⁻⁴ enter Qiskit as 2.67 × 10⁻⁴ and 49.5 × 10⁻⁴), and the RB conversions of Section 13
(`conv.rb_error_rate`). Section 6.8 also lists Bermudez et al.'s three-variant mapping of ε onto Pauli channels as
provided, but the plan writes no formulas for the three variants, so this release omits it and records the omission
(`conv.bermudez_pauli_mapping`).

## 10. Fidelity levels and numerics

- **JOINT_EXACT** evolves the full state of all ions and all resolved modes through every pulse; the only approximation is
  the truncation (Section 5.4), and the truncation policy is enforced: caps from the pulse's closed-form coherent excursion at
  the 10⁻⁶ tail plus the Section 5.1.1 margin, the boundary monitor after every pulse, cap growth with a repeated run
  (Section 5.5; `anchor.m9a.cap_rule_populated_range`; `hilbert/truncation.py`, `run/space.py`).
- **GATE_LOCAL** above the guards (joint dimension 4096, 2 × 10⁷ drive non-zeros): every gate step exactly on its own space,
  its channel by state-based process tomography (the Π d_i² product inputs, the Choi matrix by least squares, Dykstra's
  projection onto the CP cone and the TP affine set with both residuals reported), applied to the register as Kraus
  operators, the motional model tracked between steps, the residual displacement and purity deficit reported and held to a
  JOINT_EXACT comparison (Sections 5.4, 9.8; `anchor.m9a.tomography_projection`, `anchor.m9a.gate_local_bell`;
  `dynamics/tomography.py`, `run/gate_local.py`).
- **The initial mixture as branches** (Section 5.3; `conv.fock_sum_branches`), **the matrix-free kernel** and its measured
  cost model (Sections 11.1 to 11.3; `anchor.m9b.cost_model_constants`), **parallelism** over trajectories, branches and
  samples through QuTiP's maps with keyed seeds, agreement over 1 and 18 workers to 10⁻¹² (Sections 3.4, 9.9;
  `anchor.m9b.*`; `dynamics/parallel.py`), **the propagator cache** on internal-state-only spaces (Section 11.3 item 5).
- **Results.** Shots drawn from the dynamical samples with the effective sample size behind every error bar (Section 3.4;
  `run/job.py`), the bitstring order of `conv.result_bit_order`, the IonQ formats (`conv.ionq_json_encoding`;
  `anchor.ionq.*`), and the diagnostics block that records the level, the space, the mode classes, the boundary populations,
  the integrators, the seeds, the calibration and every approximation.

## 11. Benchmarks and their budget (milestone M10)

- **Randomized benchmarking** (`benchmarks/rb.py`, `benchmarks/clifford.py`): m uniform Cliffords and the inverse of their
  product, compiled and played as pulses; the mean survival fitted to F(m) = A p^m + B; r = (1 − p)(2ⁿ − 1)/2ⁿ the average
  error per Clifford (Section 13; `conv.rb_error_rate`), beside the entanglement infidelity (4ⁿ − 1)(1 − p)/4ⁿ of the
  depolarizing channel with the same p. The 24 single-qubit Cliffords are the closure of {H, S}, of which 20 cost exactly
  one pulse (16 GPi2, 4 GPi) and four — the Z-rotation subgroup, one sixth — cost none, a group mean of 0.8333 pulses per
  Clifford; the 11520 two-qubit ones are sampled uniformly from the four double cosets L g L, g ∈ {1, CNOT, iSWAP, SWAP},
  of the local group L = C1 ⊗ C1, with sizes 576, 5184, 5184, 576 from the stabilizers 576, 64, 64, 576
  (`conv.two_qubit_clifford_classes`). Simultaneous RB on several ions fits each ion's OWN marginal survival and reports
  r_q = (1 − p_q)/2 per ion beside the r that ion measures alone (Gambetta et al. 2012), keeping the joint decay's
  (1 − p)(2ⁿ − 1)/2ⁿ as the per-layer correlation diagnostic and never as an n-qubit Clifford rate
  (`conv.simultaneous_rb_units`); `variant="knill"` runs Section 7.9's Knill-style sequences instead — π/2 pulses with an
  interleaved π Pauli or an identity and one final π/2 into the computational basis — fitted in the published form
  B p^L + ½ (`anchor.m10.knill_style_rb`).
- **GHZ fidelity** (`benchmarks/ghz.py`): P_0 = P(0…0), P_1 = P(1…1) and the parity ⟨Π Z⟩ after GPi2(φ) on every qubit,
  oscillating as C cos(Nφ + φ₀) with C = 2|ρ_{0…0,1…1}|. The laboratory's (P_0 + P_1 + C)/2 is then exactly
  max_θ ⟨GHZ_θ|ρ|GHZ_θ⟩ and therefore an UPPER bound on the fidelity against a fixed-phase GHZ state, not a lower one
  (Section 7.9; `conv.ghz_parity_bound`); the simulator reports both exact numbers beside it, the phase-optimised fidelity
  the bound estimates and the fixed-phase one it exceeds.
- **Quantum volume** (`benchmarks/volume.py`): square circuits of Haar-random SU(4) on random pairings, heavy outputs above
  the median of the ideal distribution, and the pass criterion of Cross et al. 2019 Appendix C Eq. (32) — mean − 2σ > 2/3
  with the per-circuit binomial σ = √(h(1 − h)/n_c), over at least the protocol's 100 circuits, the standard error of the
  mean reported beside it as a diagnostic and never as the criterion (`conv.heavy_output_criterion`).
- **The budget alongside** (`benchmarks/budget.py`): the Section 9.6 closed-form scales per unit; the Section 6.8 channels of
  the native gate kinds by GATE_LOCAL tomography, reduced to the benchmarked qubits with the crosstalk neighbours traced out
  in |0⟩, composed to first order (r_channel = Σ counts × ε_avg; F_gates = Π(1 − ε)); the SPAM offsets. The composition is an
  estimate, not a bound in either direction: coherent errors of one Clifford's pulses partly cancel, so the measured r sits
  at about 0.8 of r_channel on the example device, while the coherent crosstalk of a GHZ circuit's five consecutive carrier
  pulses on one neighbour adds up faster than the product (`anchor.m10.*`, `conv.benchmark_budget_composition`). The
  Section 9.6 scales are summed over each native kind's whole schedule entry, the crosstalk on ions OUTSIDE the benchmarked
  set included, so r_intrinsic bounds a larger error than r_channel measures and the gap between them is partly a
  difference of scope.

## Appendix. The numerical anchors, by milestone

Every number below is pinned by a test (`tests/`) or a committed check script (`validation/scripts/`, re-run by CI against
its committed output). The table is generated from the ledger; the `anchor.m*.*` families record what each milestone's
tests established and, in the last column, what they overturned in the plan or in a source.

<!-- BEGIN generated from docs/provenance/ledger.yaml by tools/docs_from_ledger.py: anchors; do not edit by hand -->

219 anchor records in 27 families. Each row is a number or identity a test pins: **what** the check established (the ledger's equation field), **where** it lives (script, test, module), and **corrected** what the check overturned or narrowed, if anything.

#### Trap, crystal and modes (Sections 4.1, 9.1, 9.10, 9.12, 9.13; M1)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.trap.be9_infinite_chain` | omega_r (force balance) | 9.1 Zigzag threshold (infinite chain); 4.1.2 | omega_r^2 = (7 zeta(3)/(8 pi eps0)) e^2/(m s_c^3): 7.806 MHz for 9Be+ at s_c = 3 um with m = 9 u (Wineland 1998's example); 7.8006 MHz with the isotope mass 9.0121822 u; Landsman's dispersion vanishes at kappa d = pi for eps = 2/(7 zeta(3)) | tests/test_crystal.py | the plan's row does not state its mass; the 0.07% difference is the 9 u versus 9.012 u choice (M1 finding) | recomputed here |
| `anchor.trap.berkeland_excess_micromotion` | u0, beta_mm, J0 | 9.12 Excess micromotion from a stray field; 4.1.1 | 171Yb+ at 1 MHz, q = 0.28: E_dc = 1, 10, 100 V/m give u0 = 14.3, 143, 1429 nm, beta = 0.034, 0.340, 3.40 at 369.5 nm, J0 = 0.9997, 0.971, -0.365; J1^2/J0^2 -> (beta/2)^2 | tests/test_trap_model.py | the carrier sign inverts past the first zero at beta = 2.4048; Berkeland's Eq. 21 prints a stray factor Omega | recomputed here |
| `anchor.trap.c0_wronskian` | C0(q) | 9.10 Micromotion carrier and comb; 4.1.1 | C0 = 1.001890, 1.007741, 1.018161 at q = 0.1, 0.2, 0.3 (tolerance 1e-6) from the recursion null vector AND from direct integration; equal to 1 + 3q^2/16 to O(q^4); even in q | check_c0_floquet.py; tests/test_mathieu.py | (1 + q/2)^-1 = 0.952381, 0.909091, 0.869565 and the u(0) = 1 average are the negative controls | recomputed here |
| `anchor.trap.comb_phase_per_rf_period` | nu T_rf | 9.10 Micromotion carrier and comb; 4.1.1 | beta pi = 0.222580 at a = 0, q = 0.1 from the exact monodromy exponent, against 0.222144 from the pseudopotential exponent sqrt(a + q^2/2); the difference is the O(q^4) correction to beta (1.96e-3 relative) | tests/test_mathieu.py; FloquetCoefficients.comb_phase_per_rf_period_rad | the RMP prints beta omega_rf T, exactly twice this, because nu = beta omega_rf/2 (0.445161) | recomputed here |
| `anchor.trap.companion_sign_guard` | beta_j of the coupled system | 9.13 Floquet companion-matrix sign guard | A = diag(-0.004, -0.004, 0.008), Q rotated by 20 deg at q = 0.30: all \|lambda\| = 1, beta = {0.0894 = sqrt 0.008 exactly, 0.2061 (x2)}; with House's printed sign \|lambda\| = 1.3244 and 0.7550 | tests/test_mathieu.py | the Hamiltonian-matrix check K F + F^T K = 0 passes for both signs and cannot detect the misprint | recomputed here |
| `anchor.trap.cubic_gate_species` | eps, g = eps omega_z | 9.12 Cubic anharmonicity gate; 4.1.4 | (171Yb+, 0.2 MHz, 3.793e-4, 76 Hz), (40Ca+, 2 MHz, 7.093e-4, 1419 Hz), (9Be+, 5 MHz, 1.059e-3, 5295 Hz), (112Cd+, 2.8 MHz, 6.319e-4, 1769 Hz) | Marquet 2003 Table 1; tests/test_anharmonic.py | the row supplies only the mass NUMBER in each ion's label; eps moves by 1.4e-4 relative between 112 u and the 111.902757 u isotope mass, inside the printed digits | verified |
| `anchor.trap.depth_optima` | b/a optima, kappa_max | 9.13 Five-wire null and depth; 4.1.6 | depth maximal at b/a = 1.1914879 = cbrt(54 - 6 sqrt 33)/6 + cbrt(54 + 6 sqrt 33)/6 for fixed a; at fixed ion height b/a = 3.676 with kappa = 0.022542 = (5 sqrt 5 - 11)/8 for c = b, and 4.902 with 0.020038 = 8/9 of it for c = b/2; Nizamani's kappa equals House's Eq. 29 for c = b | tests/test_surface.py | the c = b/2 branch extrapolates a formula House derived for c = b only: the House formula applied to unequal rails is 8 to 40% off the numerical saddle at Nizamani's design point | recomputed here |
| `anchor.trap.floquet_sign_ratios` | c_{+-1}/c_0 | 9.17 Mathieu sign of the Floquet function; 4.1.1 | under a - 2q cos 2xi at q = 0.1: c_+1/c_0 = -0.023322, c_-1/c_0 = -0.026875 (= -q/((2 +- beta)^2 - a)), u'(0)/(i nu u(0)) = 1.1036 (1 + q); trajectory ratios (c_1 + c_-1)/c_0 -> -q/2 and (c_2 + c_-2)/c_0 -> q^2/32 | check_critique_v3.py; tests/test_mathieu.py | the RMP's quantum section, Wineland's trajectory and the plan's second revision use the opposite rf phase origin (+0.0233, 1 - q) | recomputed here |
| `anchor.trap.four_wire_escape_height` | (y_E - y0)/a | 9.13 Universal fixed-height depth bound; 4.1.6 | sqrt(2 + sqrt 5) - 1 = 1.0581710273 = Wesenberg's \|p_s\|/d for the symmetric four-wire layout (a = c), reproduced to 10 digits by GaplessPlaneTrap.escape_point(); Ubar_s = (5 sqrt 5 - 11)/(2 pi^2) U0 = 0.009136125 U0 = 4 KAPPA_MAX/pi^2 with U0 = e^2 V_rf^2/(4 m Omega^2 d^2) | House 2008 Eq. 29; Wesenberg 2008 Eqs. 29-30; tests/test_surface.py | the U0 normalization is not printed in either source; it is the one the two printed forms (House's kappa = (5 sqrt 5 - 11)/8 and Wesenberg's Ubar_s) imply, and the identity 0.009136125 = 4 kappa/pi^2 is exact | verified |
| `anchor.trap.heating_dynamics` | n_bar(t), coherence decay | 9.1 Heating dynamics; 4.1.5 | mesolve with sqrt(Gamma_h) a and sqrt(Gamma_h) a^dag gives n_bar(t) = Gamma_h t to 2e-3; the coherence of (\|n0> + \|m0>)/sqrt 2 decays at Gamma_h (n0 + m0 + 1): 2.01, 4.03, 10.09 Gamma_h over a short window for (0, 1), (1, 2), (3, 6) | tests/test_heating.py (mesolve) | not Gamma_h (2 N_bar + 1)(n0 + m0); the +1 is the absorption channel on the lower level | recomputed here |
| `anchor.trap.heating_round_trip` | n_dot | 9.15 heating round trip; 9.1 Heating conversion, Multi-ion heating | S_E = 2.2250e-13 (V/m)^2/Hz for 9Be+ at 3.6 MHz gives 40 quanta/s; uniform noise heats a 9-ion COM at 9 x 7.2 = 65 quanta/s and no other equal-mass mode; uncorrelated noise heats every mode at the single-ion rate; the two-ion mixed sum rule sum hbar omega_k n_dot_k = (e^2 S_E/4)(1/m1 + 1/m2) holds for every mu | tests/test_heating.py | a two-sided density needs the factor 2 at the boundary; the correlation length is a required argument, never defaulted to uniform | recomputed here |
| `anchor.trap.home_2013_table` | Be+/Mg+ modes | 9.13 Mixed-species two-ion table; 9.13 Home mixed-species end to end | single-ion (12.26, 11.19, 2.69) and (4.82, 3.72, 1.65) MHz: x 12.11/4.67, y 11.03/3.53, z 4.04/1.90 MHz; axial eigenvectors (-0.926, 0.379), (0.379, 0.926); off-species amplitudes 0.018 (x), 0.020 (y); separation 4.76 um; eta_Be = 0.1844 on the 1.90 MHz mode at 313 nm, 90 deg crossing; eta_Be/eta_Mg = 91 on the 12.11 MHz mode | check_home_table.py; tests/test_crystal.py | the caption's transposed (3.72, 4.82) order gives 0.017 and 0.022 (negative control) | recomputed here |
| `anchor.trap.home_trap_inversion` | omega_i^2(m) = P_i/m^2 + S_i/m | 9.13 Home mixed-species end to end; 4.1.7 | P_x = P_y, P_z = 0 and sum_i S_i = 0 close the inversion: 2 P_x/m^2 = sum_i omega_i^2, S_z = m omega_z^2. Be+ [9.7, 12.9, 4.6] MHz gives Mg+ [1.5233, 5.4308, 2.8197] MHz (the row prints 1.52, 5.43, 2.82); omega_x^2 - omega_y^2 scales exactly as m_ref/m, so sign(omega_x^2 - omega_y^2) is preserved across species | Home 2013 Tables I-II, Eqs. 6-19; trap/pseudopotential.trap_inversion_p_s; tests/test_crystal.py | the shipped exact-exponent route (Trap.single_ion_frequencies_rad_s, which inverts the monodromy at the trap's own Omega_rf) is this inversion plus the O(q^4) Floquet correction and reaches it only as Omega_rf -> infinity: Mg = 1.656/4.528 MHz at 40 MHz, 1.520/5.399 at 200 MHz, 1.523/5.430 at 1 GHz. S_x < 0 in this fixture, so the pseudopotential stops confining x above 3.01 m_Be and the helper refuses rather than returning nan | recomputed here |
| `anchor.trap.house_five_wire` | x0, y0, y_E, psi_E, Q11 | 9.13 House five-wire fixture; 9.13 Five-wire null and depth; 4.1.6 | a = 100 um, b = c = 120 um, V_rf = 300 V, Omega = 2 pi x 50 MHz, m = 1.46e-25 kg: x0 = 50.000000 um, y0 = 92.195445 um, y_E = 169.655527 um, psi_E = 0.184465 eV, Q11 = -Q22 = 0.2284260, Q12 = 0, 4.038 MHz pseudopotential; the exact monodromy exponent gives 4.0804 MHz | check_surface_mixed.py; tests/test_surface.py | the plan's '4.077 MHz Floquet' is the preprint closed form [(a + q^2/2)/(1 - 3q^2/8)]^(1/2), 0.06% below the exact exponent (M1 finding); House's 11.5 MHz drops a 1/(2 sqrt 2); his gapped escape radical needs 2, not 1/2 (1.69656 against 1.16494 at a = 1, b = 1.2) | recomputed here |
| `anchor.trap.integrated_cubic_phase` | phi_anh(t) | 9.12 Cubic anharmonicity gate; 4.1.4; 5.7 | two modes with g (a + a^dag)^2 (b + b^dag) at g/2pi = 1.419 kHz over 100 us: the directly integrated phase on \|1, 0> is 0.0041, 0.0034, 0.0032, 0.0032 rad at mismatches 1, 0.3, 0.1, 0.03 MHz (essentially flat) and -0.01589 rad on the resonant pair \|2, 0> <-> \|0, 1> at 0.1 MHz; leaked population below 1e-6 throughout | check_anharmonic.py; trap/anharmonic.integrated_cubic_phase_rad; tests/test_m1_anharmonic_estimator.py | the second-order label g^2 t/Delta_res gives 0.0013, 0.0042, 0.0127, 0.0422 rad at the same mismatches (3x low at 1 MHz, 13x high at 0.03 MHz) and is kept only as an explicit upper bound; the first version's g t = 0.892 rad is the negative control (an infidelity of 0.19, two hundred times the Ballance gate). The numbers were previously reachable only through a standalone script that does not import the package; they are now a library function, and the Section 5.7 checker plus this estimate run on every build with an AnharmonicTerms record, whatever include_anharmonic says [corrected: M1 audit, fix batch] | recomputed here |
| `anchor.trap.james_spectrum` | mu_p(N) | 9.1 Equilibrium positions, Axial modes; 9.12 Equilibrium and Hessian fixtures | u = -+(1/2)^(2/3) (N = 2), -+(5/4)^(1/3), 0 (N = 3); mu_1 = 1, mu_2 = 3 for all N; mu_3 = 29/5, 5.81, 5.818, 5.824, 5.829, 5.834, 5.838, 5.841 (N = 3..10); mu_N = 3.0000, 5.8000, 13.4748, 43.2405, 86.5334, 107.3793, 288.6657 (N = 2, 3, 5, 10, 15, 17, 30) | tests/test_crystal.py | James Table 1 positions are not printed in the plan; they are reproduced through these eigenvalue anchors and the closed forms | recomputed here |
| `anchor.trap.kielpinski_three_ion` | zeta_{1,2,3}(mu) | 9.13 Three-ion mixed closed forms; 9.13 Three-ion mixed crossing and gaps | 0.7811, 1.7321, 1.8881 at mu = 24/9 and 1.1093, 1.7321, 3.5454 at 9/24 from both the closed forms and the mass-weighted Hessian (1e-6); zeta_2 = sqrt 3 with zero impurity amplitude for all mu; zeta_3(17/3) = sqrt 3; zeta_3(7.3) = 1.7034 < zeta_2; gaps 1.560 and 6.228 MHz at f_z = 10 MHz | tests/test_crystal.py | mode order is not monotonic in mu, so modes are identified by eigenvector structure, not by index | recomputed here |
| `anchor.trap.marquet_n6_example` | mu_5, mu_6, D_655 (N = 6) | 9.12 Re-verified items; 4.1.4 | mu_5 = 13.513882, mu_6 = 18.265709, D_655 = 4.2527958 from the James equilibrium of six ions, reproduced to the printed digits | Marquet 2003; tests/test_anharmonic.py | the paper's 7.3556 comes from the rounded mu_5 = 13.51 (exact 7.35510); alpha_res = 0.09150975 needs Marquet's Eq. 4.10, which is NOT implemented in any form - the resonance checker uses the direct condition \|omega_p +- omega_q - omega_r\| < width instead | verified |
| `anchor.trap.marquet_selection_rules` | D_mn1, D_mn2, D_222 | 9.1 Anharmonic selection rules; 4.1.4 | D_mn1 = 0 and D_mn2 = ((1 - mu_m)/(2\|u\|)) delta_mn to 1e-12 for N = 2, 3, 4, 6 with C_mnp = (1/6) d^3 F/du^3; D_222 = -1.1225 at N = 2; the Cartesian third-derivative route reproduces the closed form to 1e-9 | tests/test_anharmonic.py | the cubic energy along the stretch mode is D_222 s^3 < 0 (the Coulomb spring softens on expansion), so the potential's cubic term is +sum D xi xi xi in this convention; Section 4.1.4 prints a leading minus sign, inconsistent with its own D_222 unless C carries the opposite sign (M1 finding) | recomputed here |
| `anchor.trap.mixed_two_ion_axial` | omega_-+^2/omega_z1^2 | 9.13 Mixed two-ion axial modes; 9.13 Mixed two-ion axial closed forms | 0.500847 and 2.250636 at mu = 23.985042/9.0121822 (sum 2.751484); mu = 0.675: (1.0828427249, 1.9468987123, b1^2 = 0.6839213464); mu = 40/27: (0.8896460800, 1.5995405129, 0.3160786536); (1, 3) at mu = 1; the mass-weighted Hessian reproduces all to 1e-9 | check_surface_mixed.py; tests/test_crystal.py | the natural-abundance mass 24.305 u gives 0.495185 and 2.246406; renormalizing in raw displacements gives 0.40641 instead of 0.31608 (negative control) | recomputed here |
| `anchor.trap.morigi_walther_eta` | eta_Mg, eta_In | 9.13 Mixed Lamb-Dicke signs and participation | 115In+/25Mg+ at mu = 4.6: \|beta'-\| = (0.26543, 0.96413), \|beta'+\| = (0.96413, 0.26543), Omega+/Omega- = 2.63550; with eta_Mg+ = 0.5: \|eta_Mg-\| = 0.22347, \|eta_In-\| = 0.37846, eta_In+ = -0.06418, compared up to a per-mode sign | tests/test_crystal.py | the published ratio 2.6377 is a ratio of rounded values; W^max by the explicit sum (0.193) belongs to the cooling layer (M3) | recomputed here |
| `anchor.trap.nizamani_figure_of_merit` | q_N versus q | 9.13 Nizamani figure of merit versus Mathieu q | a = 60, b = 300, c = 150 um, 171 u, Omega = 2 pi x 55 MHz, V_rf = 500 V: h = 82.46 um, q_N = 0.6948, \|Q11\| q = 0.1976, principal-axis q = sqrt(Q11^2 + Q12^2) = 0.2171 (ratio 3.2002), 4.222 MHz; 450 V at h = 85 um gives q_N = 0.589 | tests/test_surface.py | q_N must never enter omega = q Omega/(2 sqrt 2) or a stability boundary; his quoted h = 85 um and q = 0.7 are not self-consistent | recomputed here |
| `anchor.trap.nonlinearity_epsilon` | eps, g = eps omega_z | 9.12 Cubic anharmonicity gate; 4.1.4 | eps = x0/(4 l) = 1.06e-3 (9Be+, 5 MHz), 7.09e-4 (40Ca+, 2 MHz), 3.79e-4 (171Yb+, 0.2 MHz); g/2pi = 1419 Hz and 76 Hz; the axial coefficient of X_p X_q X_r is 2 eps omega_z D_pqr (mu_p mu_q mu_r)^(-1/4) times the multiplicity | tests/test_anharmonic.py | eps is reverse-engineered from the plan's three numbers as x0/(4 l); Marquet's own -3 eps hbar omega_3 operator form was not re-derived (the coefficient stored is first-principles) | recomputed here |
| `anchor.trap.stability_edge` | q(beta = 1, a = 0) | 9.1 Monodromy beta; 4.1.1 | the monodromy trace reaches -2 at q = 0.9080 for a = 0 (Mathieu tables: 0.908); beta^2 - (a + q^2/2) = O(q^4) | tests/test_mathieu.py | the printed continued fractions of RMP 2003 Eqs. 10-11 carry an index error and are not used | recomputed here |
| `anchor.trap.yb171_lamb_dicke_triple` | eta_COM(N) | 9.16 row 13-6; 13 Lamb-Dicke base | 171Yb+ at 355 nm counter-propagating (\|Delta k\| = 3.5398227e7 1/m), f = 3.045 MHz, ion mass 170.93578 u: 0.1103011324, 0.0493281660, 0.0267519541 for N = 1, 5, 17 (reproduced to 2e-10) | tests/test_crystal.py | with 171 u exactly the N = 17 value rounds to 0.0267 instead of 0.0268: the anchor must state its mass | recomputed here |
| `anchor.trap.zigzag_threshold` | (omega_r/omega_z)_crit | 9.1 Zigzag threshold; 9.12 Exact zigzag bound; 9.17 Zigzag pair | alpha_crit = 2/(mu_N - 1) = 1, 5/12 at N = 2, 3; (omega_r/omega_z)_crit = 1.0000, 1.5492, 2.4975, 4.5957, 6.5396, 7.2931, 11.9930 for N = 2, 3, 5, 10, 15, 17, 30; solve_crystal raises ZigzagError below it | check_critique_v3.py; tests/test_crystal.py | the fit 0.73 N^0.86 is 13 to 32% off (negative control); sqrt(12/5) is the ratio, not alpha_crit | recomputed here |

#### 171Yb+ atomic anchors (Sections 4.5, 9.13; M0a)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.yb171.branching_third` | b(P1/2 F'=1 -> S1/2 F=0) = 1/3 | 9.13 Wigner-Eckart branching; 4.5.2; 8.1 | P1/2 F' = 1 decays 1/3 to S1/2 F = 0 and 2/3 to F = 1; F' = 0 decays only to F = 1; {1/2 1/2 1; 1 0 1/2}^2 = 1/6 | check_atomic.py; Olmschenk 2007 | 1/2 or 1/4 means the 6j normalization or the sum direction is wrong | recomputed here |
| `anchor.yb171.i_sat` | I_sat(171Yb+ 369.5 nm) | 9.13 Gamma to reduced element to I_sat; 9.12 171Yb+ constants | with Gamma_S/2pi = 19.62 MHz partial (19.72 total x 0.99499): \|<J\|\|d\|\|J'>\| = 1.752 e a0, I_sat = 50.83 mW/cm^2, Omega(I = I_sat) = Gamma/sqrt 2 | check_atomic.py; check_critique_v3.py | 1.751 e a0 and 50.77 mW/cm^2 at the retired 19.6 MHz reading; 8.09 mW/cm^2 with gamma_hz unconverted | recomputed here |
| `anchor.yb171.p32_doublet` | Omega_R propto omega_f/[Delta(Delta - omega_f)] | 4.5.4; 4.5.5; 9.13 | 171Yb+ 6p 2P3/2: tau = 6.15(9) ns, branchings 0.9875(6) (329.0315 nm), 0.0017(1) (1345.636 nm), 0.0108(5) (1650.290 nm), A_hfs = 875.4(10) MHz (Berends and Maleki's first measurement 877(20) MHz agrees at 0.1 sigma); omega_f/2pi = 99.8432 THz. The two paths interfere DESTRUCTIVELY at every detuning (the P3/2/P1/2 partial-sum ratio is real and negative), and \|Omega_R\|\|Delta(Delta - omega_f)\|/omega_f is flat to 27% over Delta/2pi = -50 to -400 THz where \|Omega_R Delta\| varies by 2.6x | Pinnington, Rieger, Kernahan 1997; Feldker et al. 2018; Berends and Maleki 1992; tests/test_m0a_branchings.py | the record had only P1/2 until 2026-09-08, so its intermediate sum was single-path and Omega_R fell as 1/Delta: the plan's headline species could not reproduce its own scaling law and the 4.5.5 sentence closing Section 12's 'worked for 9Be+ only' gap was unmet. NOTE 171Yb+'s fine structure is 99.84 THz, so the asymptotic 1/Delta^2 regime (\|Delta\| >> omega_f) is not reachable at optical frequencies; the closed form itself is the discriminator, not the exponent (audit items E4, B2) | corrected |
| `anchor.yb171.quadratic_zeeman` | taylor_c2(171Yb+ clock) | 9.13 171Yb+ clock quadratic shift; 4.5.1 | (g_J - g_I)^2 mu_B^2/(2 h^2 A) = 310.87 Hz/G^2 with g_J = 2.002615 and A = 12.642812118 GHz in Hz; tolerance +-0.02 Hz/G^2 from the g_J uncertainty; Han's own 31.0869(22) mHz/uT^2 = 310.869 Hz/G^2 | check_critique_v3.py; Han et al. 2025; Olmschenk 2007 / Fisk | 310.85 used an uncited g_J = 2.00254 and printed 2hA (one factor of h short); d2nu/dB2 = 621.55 Hz/G^2 is the other convention | recomputed here |
| `anchor.yb171.repump_935_branching` | b(3[3/2]1/2 -> D3/2) = 1 - A(297) tau | 4.5.6; 4.2.6; 8.1 | tau(3[3/2]1/2) = 37.7(5) ns and A(297.143 nm) = 2.61e7 s^-1 give 0.98397 into S1/2 and 0.01603 into D3/2 at 935.186 nm, the branch that closes the repump cycle | Pinnington, Berends, Ji 1994; Sansonetti and Martin 2005 (NIST ASD T7227); tests/test_m0a_branchings.py | the 37.7 ns is NOT primary-verified (APS 403 on Phys. Rev. A 50, 2758; corroborated to 1% by the 4.2 MHz natural linewidth quoted in arXiv:2111.11504) and is tagged extracted, not verified. The 297 nm channel is DECLARED as Level.untabulated_branching rather than tabulated as a Transition, deliberately: an E1 record would put a level whose lifetime is not primary-verified into every Section 4.5.4 intermediate sum out of S1/2, which the plan specifies as the P1/2 + P3/2 doublet | derived |

#### 40Ca+ atomic anchors (Sections 4.5.7, 9.14; M0a)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.ca40.blackbody_m1_mixing` | W_12 = A_12 n_bar(nu, T) | 9.15 Blackbody M1 mixing rate; 4.5.7 | h nu/kT = 0.29115, n_bar = 2.95884, W_12 = 7.2491e-6 s^-1 at 300.0 K and 7.2297e-6 at 299.3 K (the printed 7.23e-6) from A_12 = 2.45e-6 s^-1 and the quoted 1.82 THz; upward 1.0874e-5 s^-1 with g_u/g_l = 6/4; the 40Ca+ level energies put the D3/2-D5/2 line at 1.8194 THz, 3e-4 away in the rate | Kreuter 2005 Eq. 1; Ali and Kim 1988; tests/test_metastable.py | the Bose occupation MULTIPLIES the rate; dividing by it is low by n_bar^2 = 8.75x. Default off (8.5e-6 of the natural rate); the M1 line is a Transition of the 40Ca+ table with A_12 tau = 2.9e-6 of the D5/2 decay, the E2 branch carrying the remainder (MetastableChannels.bbr_rate_hz, milestone M0a closed 2026-09-07) | recomputed here |
| `anchor.ca40.collision_rates` | R = sum_s Gamma_s p_s/(k_B T) | 9.15 Collision-rate construction; 4.5.7; 13 Collision-induced rates | n = 2.4143e5 cm^-3 per partner at 300 K and 1e-11 mbar; quenching 37e-12 (H2) + 170e-12 (N2) cm^3/s gives R^q = 5.00e-5 s^-1, j-mixing 3e-10 + 13e-10 cm^3/s gives R^j = 3.86e-4 s^-1, total 4.36e-4 s^-1; j-mixing coefficients 7.73x the quenching ones | Knoop et al. 1995 via Kreuter 2005 Sec. III B; tests/test_metastable.py | the source's printed bound '< 3e-4 s^-1' is 1.45x below its own coefficients and is a rounded estimate; either value is 5e-4 of the natural rate. Specific coefficients in cm^3/s, never rates; a partner or species without a cited coefficient raises (MetastableChannels.collision_rates_hz) | contested |
| `anchor.ca40.gamma_dependent_repins` | the 40Ca+ numbers that moved with Gamma(P1/2), Gamma(P3/2) and b(P1/2 -> D3/2) | 9.3 Doppler anchors; 4.2.6; 4.4.4; 8.1; 9.13; M6 | the flip of conv.ca40_linewidth_reading moved three inputs: Gamma(P1/2)/2pi 21.5700 -> 23.052570 MHz (+6.87 %), Gamma(P3/2)/2pi 23.4000 -> 23.972728 MHz (+2.45 %) and b(P1/2 -> D3/2) 0.06 -> 0.06435 (+7.25 %, conv.ca40_branching). The PARTIAL rates that carry them into everything else: 397 nm 20.2758 -> 21.5691 MHz, 866 nm 1.2942 -> 1.4834 (+14.6 %), 393 nm 21.8717 -> 22.4071, 854 nm 1.37358 -> 1.40720, 850 nm 0.154674 -> 0.158460 MHz | qutip_trap/species/ca40.py; qutip_trap/device/presets.py; tests/test_doppler.py; tests/test_m6_ca40_optical.py; tests/test_light_shift_gate.py; tests/test_species_tables.py; validation/scripts/check_atomic.py | every re-derived pin, with its cause. SPECIES TABLE: tau(P1/2) 7.3785 -> 6.904 ns, tau(P3/2) 6.8015 -> 6.639 ns; 397 nm reduced element 1.9832 -> 2.0455 e a0 and I_sat 42.367 -> 45.069 mW/cm^2 at the vacuum wavelength; 393 nm 2.8747 -> 2.9097 e a0 and 46.925 -> 48.074 mW/cm^2 (anchor.ca40.i_sat_chain). ROOS 2000 DOPPLER FIXTURE (tests/test_doppler.py, 397 nm at -20 MHz, s = 1 and 3): eight-state nbar_z 3.74 -> 4.02 and nbar_y 8.74 -> 9.41, two-level 3.65 -> 3.90 and 8.50 -> 9.08 (both now pinned); the measured 6.5(1.0) and 16(2) are still not reproduced and the two models still agree to 6 %. ca40_optical PRESET: Doppler nbar 15.06/9.64/10.04 -> 12.90/8.88/9.11; the Lamb-Dicke guard eta^2 (2 nbar + 1) 0.604 -> 0.559 at the rejected 1.0 MHz axial and 0.164 -> 0.141 at the adopted 2.0 MHz, so the 2.0 MHz choice stands with a larger margin; preparation error 6.11e-5 -> 1.06e-5 (T1 alone 2.81e-5, the branching alone 2.51e-5, i.e. about evenly split, both acting through I_sat(866)); pumped-state errors 1.8e-1 -> 1.43e-1 at (0.5, 3.0) in 20 us and 8.7e-9 -> 8.2e-10 at (0.02, 20) in 200 us; detection R_o 3.57e6 -> 3.49e6 /s at (2, 10), 1.99e6 -> 1.31e6 at (10, 30), 3.06e5 -> 1.96e5 at (50, 100), so 13.6 -> 13.3 detected photons in the 2 ms window; eps_B 2.5e-4 -> 3.4e-4 with eps_D unchanged at 1.8e-3 (that one is the 1.168 s shelf against the window, not Gamma). LIGHT-SHIFT GATE (tests/test_light_shift_gate.py, a 398.5 nm pair 3 THz below the S-P1/2 line): the S1/2 two-photon self-coupling -9973.10 -> -10554.37 Hz and the D5/2 one 12.9929 -> 13.3109 Hz, so Omega_LS/2pi 4993.04 -> 5283.84 Hz and the static Stark shift 9986.09 -> 10567.69 Hz; level_weights (-1.99740, 0.00260) -> (-1.997481, 0.002519) and spin_flip_weight -0.17366 -> -0.16812, both because 2 up/(up - dn) mixes a +2.45 % numerator with a +5.82 % denominator; the S-D Raman rabi_hz -867.1 -> -888.3 Hz. UNMOVED, and checked: every 729 nm E2 quantity (the derived 2.00144e5 Hz Rabi frequency, the D-state lifetimes and quadrupole elements), the Lamb-Dicke parameters, and Myerson et al. 2008's MEASURED apparatus rates in the detector preset - only the model-DERIVED rates move. TWO SIGNS WERE SURPRISING AND WERE DIAGNOSED, not assumed: nbar and R_o both FALL although Gamma rises, where nbar_D ~ Gamma/4nu would rise. At a fixed s = I/I_sat and a detuning of -Gamma/2 the optical-Bloch problem is scale-invariant in Gamma except for the FIXED B = 5 G, and the magic-angle LINEAR polarization of the 397/866 cycle puts it in the coherent dark-state regime, where the controlling parameter is the Zeeman splitting measured in linewidths. Scaling B by the same 1.0687 restores linear scaling for nbar (it then rises 5.1-7.4 % against the expected 6.9 %) and recovers 80 % of the R_o drop, which is what identifies the fixed field rather than a modelling error | recomputed here |
| `anchor.ca40.i_sat_chain` | Gamma -> \|<J\|\|d\|\|J'>\| -> I_sat | 9.13 Gamma -> reduced element -> I_sat; 13 Saturation intensity; 4.5.7 | PLAN.md 9.13 prints 2.045 e a0 / 45.11 mW/cm^2 (397 nm) and 2.972 / 50.25 (393 nm) at lambda = 396.85 and 393.37 nm, the AIR wavelengths, under the PARTIAL reading of the quoted 21.57 and 23.4 MHz | PLAN.md 9.13; validation/scripts/check_atomic.py; tests/test_species_tables.py | the PARTIAL reading is now the table's too (conv.ca40_linewidth_reading), so the 397 nm pair COMES OUT OF THE TABLE: Hettrich et al. 2015's tau(P1/2) = 6.904(26) ns (total 23.0526 MHz) times Ramm et al. 2013's S1/2 branching 0.93565(7) gives a partial rate of 21.5691 MHz, 4.0e-5 from Hettrich's printed gamma_PS = 2 pi x 21.57(8) MHz, hence 2.0446 e a0 / 45.106 mW/cm^2 at the plan's air wavelength and 2.0455 / 45.069 at the vacuum 396.959 nm Section 13 requires. Both pins MOVED with the flip of 2026-09-08, from the previous TOTAL reading's 1.9832 / 42.367. The 393 nm pair does NOT come out of the table: Meir et al. 2020's tau(P3/2) = 6.639(42) ns times Gerritsma et al. 2008's 0.93469 gives 22.4071 MHz, so 2.9085 / 48.113 (air) and 2.9097 / 48.074 (vacuum), against the plan's 2.972 / 50.25 - 2.1 % on the element and 4.2 % on the intensity, disagreeing with an unsourced quoted linewidth (anchor.ca40.p32_linewidth_readings). check_atomic.py prints the plan's closed forms at both wavelengths and the table's own chain beside them; tests/test_species_tables.py pins all of it (audit items E13, B12) | corrected |
| `anchor.ca40.lamb_dicke_729` | eta_729(40Ca+, 1 MHz) | 9.3 40Ca+ Lamb-Dicke recomputation; 9.14 | x0 = 11.246 nm for 40 u at 2 pi x 1 MHz; k x0 = 0.0969 at 729 nm and 0.180 at 393 nm (published 0.096, 0.179) | Roos thesis p. 28; check_recoil2.py | Schindler's 6% and 2.5% are 1.49x smaller and refer to an unnamed mode | recomputed here |
| `anchor.ca40.p32_linewidth_readings` | gamma(4p 2P3/2)/2pi of 40Ca+ | 9.13 Gamma -> reduced element -> I_sat; 4.5.6; 12 | PLAN.md 9.13 quotes 23.4 MHz for the 393 nm line and names no source for it. The measurements: Meir et al. 2020 tau = 6.639(42) ns -> 23.9727 MHz TOTAL and 22.4071 MHz PARTIAL into S1/2 with Gerritsma's share 1 - 0.0587 - 0.00661 = 0.93469; Jin and Church 1993 tau = 6.924(19) ns -> 22.9860 TOTAL and 21.4848 PARTIAL | PLAN.md 9.13; Meir, Sinhal, Safronova, Willitsch, Phys. Rev. A 101, 012509 (2020); Jin and Church, Phys. Rev. Lett. 70, 3213 (1993); Gerritsma et al., Eur. Phys. J. D 50, 13 (2008); qutip_trap/species/ca40.py; tests/test_species_tables.py | 23.4 MHz matches none of those four: 2.4 % below Meir's total, 4.4 % above Meir's partial, 1.8 % above Jin and Church's total, 8.9 % above their partial - and the two lifetimes are themselves 6 sigma apart. The table adopts Meir's lifetime as ca40.P32.lifetime_s (verified) and reads the quoted 23.4 MHz as a partial rate for consistency with the settled 397 nm line (conv.ca40_linewidth_reading), keeping it only as a contested cross-check. CONSEQUENCE: PLAN.md 9.13's printed 393 nm anchors 2.972 e a0 and 50.25 mW/cm^2 are NOT reproduced by the table. tests/test_species_tables.py::test_ca40_393_nm_partial_rate_disagrees_with_the_plans_quoted_23_4_mhz pins them ONLY as closed forms evaluated at the plan's own 23.4 MHz and AIR 393.37 nm (2.9722 / 50.246 exactly), labelled as the plan's, and pins the table's own 2.9085 / 48.113 at that wavelength and 2.9097 / 48.074 at the vacuum 393.478 nm beside them. Closing the row needs either the source of 23.4 MHz or a third P3/2 lifetime measurement; ca40.MISSING records the negative search | contested |
| `anchor.ca40.reduced_element_e2` | \|<S\|\|r^2 C^(2)\|\|D5/2>\| | 9.14 Reduced matrix element; 4.5.7 | 2.724e-20 m^2 = 9.73 a0^2 with tau = 1.168 s, vacuum lambda 729.347 nm, 2j' + 1 = 6; Kreuter's Q = 9.740(47) a.u. reproduces tau_theory = 1165.5 ms | James 1998; Roos 2000; Kreuter 2005; Barton 2000; check_constants.py | 10.29 a0^2 with Roos's 1.045 s; 5.7 a0^2 if 2j + 1 = 2 is used (wrong by sqrt 3) | recomputed here |
| `anchor.ca40.roos_e2_rabi` | Omega/2pi (729 nm stretched line) | 9.14 Roos's worked Rabi frequency | E0 = 2.3e5 V/m, lambda_air 729.147 nm, tau 1.045 s: 1.1495 MHz; E0 = 2.3e5 V/m, lambda_vac 729.347 nm, tau 1.168 s: 1.0877 MHz and a 460 ns pi time; E0 = 2.309e5 V/m (100 mW in 30 um): 1.0920 MHz | Roos 2000 p. 31; James 1998 Eqs. 5.11, 5.13; tests/test_quadrupole.py | Section 9.14 prints 1.088 MHz 'with 2.309e5 V/m'; that value belongs to E0 = 2.3e5 V/m, a 0.4% inconsistency in the plan's row (2026-09-04, M0a) | recomputed here |

#### 43Ca+ atomic anchors (Section 9.13; M0a)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.ca43.clock_point` | B0, f0 (43Ca+ \|4,0> - \|3,+1>) | 9.13 43Ca+ clock point; 4.3.3 | stationary at B0 = 146.0942 G, f0 = 3,199,941,076.93 Hz, curvature d2nu/dB2 = 2.415 mHz/mG^2 (1.2077 mHz/mG^2 as a coefficient of dB^2) with A = -806.4020716 MHz, g_J = 2.00225664, mu_I = -1.31535 mu_N | Harty 2014; check_atomic.py | Harty prints 146.094 G, 3,199,941,077 Hz, 2.4 mHz/mG^2; Harty's g_I^(N) with Langer's sign gives 146.3015 G and 3,199,857,314.5 Hz (must fail) | verified |

#### 88Sr+ atomic anchors (Sections 4.5.7, 9.14; M0a)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.sr88.clock_wavelength` | lambda_vac = c/nu_clock | 4.5.7; 9.14 | the 88Sr+ S1/2-D5/2 Transition takes lambda_vac = c/444 779 044 095 484.6 Hz = 674.025591 nm from the stored clock frequency (1.3 ppb), not from the 0.01 cm^-1 level energy 14 836.24 cm^-1, which gives 674.02522 nm | Sansonetti 2012; qutip_trap/species/sr88.py; tests/test_m0a_branchings.py | the two disagree by 5.4e-7 relative and the module's note claimed agreement 'to 1e-7', a 5x overstatement; clock_frequency_hz was stored and never used. Species.__post_init__'s rel_tol = 1e-6 cross-check against the level energies still passes (audit item E13b, defect B15) | corrected |
| `anchor.sr88.reduced_element_e2` | \|<S\|\|r^2 C^(2)\|\|D5/2>\| (88Sr+) | 9.14 Reduced matrix element; 4.5.7 | 13.81(3) a0^2 with tau = 0.3908(16) s, A = 2.558854 s^-1, A/2pi = 0.4073 Hz, lambda_vac = 674.025591 nm from the clock frequency; blackbody deshelving 4e-14 s^-1 at 300 K | Letchumanan 2005; Sansonetti 2012; check_sr_lifetime_quad.py; check_sr_lifetime_bbr.py | 14.70 a0^2 with the superseded 0.345 s (negative control); NIST's air Ritz 673.8392 nm is the 276 ppm trap | recomputed here |

#### 9Be+ atomic anchors (Sections 4.5.5, 9.13; M0a)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.be9.clock_point` | B0, f0 (9Be+ \|2,0> - \|1,+1>) | 9.13 9Be+ clock point, further anchors | 119.446 G, 1,207,495,853.5 Hz with A = -625.008837048 MHz, mu_I = -1.177432 mu_N, g_J = 2.00226; tolerance 20 Hz (the 10 Hz residual is the precision of the g-factors); curvature 0.3049 Hz/uT^2; slopes -17.64 and 21.0 kHz/uT; further pairs at 119.643 G and 223.073 G | Langer 2005; check_atomic.py | Langer prints 119.45 G, 1,207,495,843 Hz and both slopes as unsigned magnitudes; A and the g-factors are not printed there and must be cited from elsewhere | verified |
| `anchor.be9.ozeri_2005_amplitudes` | a^(1/2), a^(3/2) | 9.10 Rayleigh amplitudes; 4.5.5 | Raman a^(1/2) = -sqrt2/3, a^(3/2) = +sqrt2/3; Rayleigh with sigma+ on m = -1/2: 2/3 and 1/3; with pi on m = -1/2: 1/3 and 2/3; stretched state under sigma+: 0 and 1 | Ozeri 2005; tests/test_raman_scattering.py | the P1/2 and P3/2 Raman amplitudes are equal in magnitude and opposite in sign; the Rayleigh amplitudes share a sign but not a magnitude | recomputed here |
| `anchor.be9.wineland_clock_light_shift` | delta_{0<->0} | 9.13 Three-index detuning; 4.5.4 | -(g_b^2 + g_r^2)(omega_0/3)[1/Delta^2 + 2/(Delta - omega_F)^2] from the explicit sum with per-ground-state detunings; the summed line strength is identical for both clock states, so a single Delta per intermediate level returns exactly zero | Wineland 2003 Eqs. 2.10-2.18; tests/test_raman_scattering.py | - | recomputed here |

#### 25Mg+ atomic anchors (Section 9.13; M0a)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.mg25.clock_point` | B0, f0 (25Mg+ \|3,+1> - \|2,+1>) | 9.13 25Mg+ clock point; 4.4.5 | 212.78 G, 1.686462 GHz with A = -596.254376 MHz, mu_I = -0.85545 mu_N, g_J = 2.00226 | Srinivas 2021; check_atomic.py | Srinivas prints 212.8 G, 1.686 GHz | verified |

#### 87Rb reference checks of the angular algebra (Section 4.5.2; M0a)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.rb87.hyperfine_sum_rules` | S_FF', b(F'->F) (87Rb D lines) | 9.13 Hyperfine sum rules and stretched elements | D2 S_FF' rows {1/20, 1/4, 7/10} (F = 2) and {1/6, 5/12, 5/12} (F = 1), D1 {1/2, 1/2} and {1/6, 5/6}, each summing to 1; \|<J\|\|er\|\|J'>\| = 4.227524 e a0 (D2, tau = 26.2348 ns) and 2.993125 (D1, tau = 27.679 ns), stretched element sqrt(1/2) x 4.227524 = 2.989311 e a0 | Steck Rb87 Eqs. 34-42, Tables 7-8 | the rounded 26.24 ns already gives 4.227104 e a0, so elements are derived from the stored lifetime and never typed in | verified |
| `anchor.rb87.saturation_intensity` | I_sat (87Rb D2, D1) | 9.13 Saturation intensity, both normalizations | cycling pi h c Gamma/(3 lambda^3) = 1.669325 mW/cm^2 (D2) and 1.495851 (D1); random-orientation hbar omega^3 Gamma/(4 pi c^2) = 5.007975 and 4.487552, exactly 3.0000x; sigma_0 = 3 lambda_vac^2/(2 pi) = 2.9066929377205e-9 cm^2 | Steck Rb87 Eqs. 61-62, Table 7 | - | verified |
| `anchor.rb87.second_order_zeeman` | taylor_c2 (87Rb clock) | 9.13 Second-order Zeeman from Breit-Rabi | (g_J - g_I)^2 mu_B^2/(2 h^2 Delta_hfs) = 575.146 Hz/G^2 with g_J = 2.002331070, g_I = -0.0009951414, Delta_hfs = 6.834682610904290 GHz, reproduced by the numerical diagonalization | Steck Rb87 Eq. 29, Table 6 | - | recomputed here |

#### Derivation-audit regressions (Section 9.16; M0a)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.audit.sqrt_gamma_inside` | Gamma_{a->b} = sum_q' \|sum_e sqrt(Gamma_e) c_{e->b q'} Omega_ea/(2 Delta_e)\|^2 | 9.16 row 4.5-7; 4.5.5 | with Gamma(P1/2) != Gamma(P3/2) the master equation matches the sqrt(Gamma_e)-inside form to 5e-3 and the Gamma_e-outside form by neither shared Gamma | derivation audit 2026-09-04; tests/test_raman_scattering.py (toy level scheme, dressed initial state) | the second revision placed Gamma_e outside the sum | recomputed here |
| `anchor.audit.total_rate_m_independent` | Gamma_Steck tau = 1 | 9.16 row 4.5-1 | omega^3/(3 pi eps0 hbar c^3) sum_{m,q} \|<J_g m\|d_q\|J_e m'>\|^2 = 1/tau for all 16 excited sublevels of the 87Rb D2 manifold, m'-independent | derivation audit 2026-09-04 | 1.000, not 0.5: no second factor 2 | recomputed here |
| `anchor.audit.uncoupled_vs_6j` | <F mF\|d_q\|F' mF'> | 9.16 row 4.5-4 | Steck's 6j factorization equals the uncoupled-basis (spectator m_I, no 6j) evaluation with \|F mF> in the \|(J I) F> coupling order for I up to 7/2 and (J, J') in {(1/2,1/2), (1/2,3/2), (3/2,3/2), (3/2,5/2)}; the selection rule mF = mF' + q holds | derivation audit 2026-09-04 | the \|(I J) F> order flips the sign of some elements by (-1)^{2I+J+J'-F-F'} | recomputed here |

#### IonQ formats (Section 8.6; M0)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.ionq.bit_order_roundtrip` | IonQ result keys | 9.13 IonQ result bit order | {"5": 0.5, "7": 0.5} on three qubits <-> 101, 111; ["6","1","0","7"] <-> 110, 001, 000, 111 | qiskit-ionq tests | - | verified |
| `anchor.ionq.circuit_json_roundtrip` | IonQ circuit JSON | 9.13 IonQ circuit JSON round trip | {gate: ms, targets: [0, 1], phases: [0, 0.25], angle: 0.25} and {gate: gpi2, target: 0, phase: 0.75} import to MS(0, 0.25, 0.25) and GPi2(0.75) and export identically; zz carries angle only | IonQ OpenAPI v0.4 | - | verified |

#### Single ion, spin-motion coupling, single-qubit gates (Section 9.2; M2)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m2.cetina_curvature` | C = prod (1 + theta^2 Omega^2 t^2)^(-1/2), phi = sum arctan(theta Omega t) | 6.2, 9.2 Beam-curvature Rabi decay, 9.12 Cetina curvature numbers | Omega''/Omega = -2/w^2 = -2.6424e12 m^-2 at w = 870 nm; xi_0 = 17.19 nm at 100 kHz and 14.53 nm at 140 kHz for 171Yb+; the exact Omega(x) = Omega_0(1 + (Omega''/2 Omega_0) x^2) drive on a thermal axial mode (nbar = 4, d = 60, mesolve) reproduces the frozen-Fock secular sum sum_n P_n sin^2(Omega_n t/2), Omega_n = Omega_0(1 + kappa xi^2 (n + 1/2)), to 1e-6 and Cetina's continuum form to 2e-3, the latter converging as nbar grows (5e-5 at 20) | tests/test_builder_physics.py | phi is a phase LAG (the source's Eq. 2 prints the wrong sign); the mechanism is distinct from the Debye-Waller factor (modes perpendicular to the beam, algebraic contrast) | recomputed here |
| `anchor.m2.comb_drive_through_the_builder` | comb_drive(derived, comb, ...) -> Drive | 4.3.7 ('a comb drive is a SET of tones'), 5.2, 9.17; light/raman.py | light.raman.comb_drive sets Drive.comb, tones from comb.tones() and stark_shift_hz from comb.stark4_hz() on the same gate-time cut, and evaluates CombSpec.guards() at construction, warning with the failing clauses and the un-evaluable ones by name. On the single-ion 171Yb+ fixture at 80 MHz and 10 ps the cut keeps ONE beat note, of envelope Omega_0 sech(pi x 158 x nu_rep tau) = 0.926025 (the plan's 0.925994 is the same factor at the zero-field splitting), and a pulse of pi/(Omega_0 sech e^{-eta^2/2}) inverts the qubit to 3e-6, the residual being the off-resonant sidebands (eta Omega/omega_m)^2. Widening the cut to 10/(1.5 nu_rep) keeps three tones at mu = 0, +-80 MHz and does NOT raise the drive-term count, because a tone set is one operator with a summed coefficient | tests/test_m2_comb_integration.py | Drive.comb was a dead field whose only reader was its own kind validation; light/raman.py contained zero comb references and no test integrated a comb drive at all. guards()'s 'adiabatic_elimination' and 'small_pulse_area' were the literal True and guards() was never called from qutip_trap/, so its docstring's 'the engine logs before every solve' was false; the two clauses are now evaluated when their inputs are supplied and ABSENT (with unevaluated_guards naming them) when they are not, so 'not evaluated' can no longer be read as 'satisfied'. The intensity-to-Rabi chain that would supply theta per pulse is still missing (Section 12) (M2 audit E8) | recomputed here |
| `anchor.m2.comb_numbers` | sech(pi l nu_rep tau), C_00,10, q, lock | 4.3.7, 9.15 | pair weight 0.863911 at l = 105 (0.595332 with the single-tooth argument), exact convolution 0.821; sum rule 1.000000000000 at nu_rep tau = 1.68e-3 and 3.14 at 1; C_00,10 = 0.746343, 0.453162, 0.450920, 0.669087, 0.940671, 0.943780, 0.943789 for \|k\| <= 0, 5, 10, 100, 500, 1000, 5000; C_10,11 = C_10,1-1 = 0.988841; Hayes q = 156.5090, 313.0181, 469.5213; Islam n = 157, \|Delta nu_M\| = 11.381 MHz; Rosen-Zener 0.720860 and 0.914142; widths 1.6768 and 1.1222 tau; amplitude-noise 5.6179e-3 (Hz) against (2 pi)^2 x that | tests/test_comb_drive.py; check_comb.py | the tone set excludes the self-beat l = 0 pair (it is the intensity, not a drive) and cuts at \|mu_j - mu_res\| <= 10/t_g, the rest folded into the fourth-order shift | recomputed here |
| `anchor.m2.composite_filter_and_dc_floor_methods` | CompositePulse.filter_function_amplitude, CompositePulse.dc_floor | 6.9, 9.15 amplitude filter function and dc floors; Appendix E (PLAN.md:2819, 2821) | the two Appendix E methods now delegate to noise/decoupling.py (composite_segments + amplitude_filter_function; the free dc_floor) through a function-local import that keeps the control layer free of a module-scope dependency on the noise layer. c-hat_{m+1} is FITTED, not read off at one eps: leading_coefficient(eps) carries a truncation error in even powers of eps and a round-off error growing as eps falls, so fitted_leading_coefficient least-squares c-hat + a eps^2 + b eps^4 over the eps ladder whose operator distance is still above 1e-10 and returns the constant. Measured relative agreement with Section 4.3.5 at theta = pi: SK1 22.8302557111 to 4.1e-10, SCROFULOUS 4.566051 to 9.2e-8, BB1 9.388566343 to 1.5e-7, CORPSE 0.006500751892 to 1.6e-7, PB1 118.295935928 to 1.9e-7, CinSK 0.75 to 4.2e-7, CinBB 0.75 to 2.1e-6. The dc floors follow at 2.5e-7 relative: 5.87365e-6 (SK1), 3.53675e-9 (BB1), 1.67248e-9 (CORPSE) at <beta^2> = 2.07e9/pi (rad/s)^2 and Omega = 1.5e6 rad/s. m is the pulse's order FOR THE CHANNEL, so a family that does not correct a channel enters that channel at m = 0. The amplitude-filter low-frequency slopes are 2.0 (primitive, CORPSE) and 4.0 (SK1, BB1, PB1, SCROFULOUS, CinSK, CinBB), and the dc polygon closes for the six amplitude correctors and equals (pi, 0, 0) for primitive and CORPSE | tests/test_composite_pulses.py; tests/test_decoupling.py | the two methods raised NotImplementedError naming milestone M7 although M7 had shipped the equivalents; the stale M7 constant is deleted. phi_B2 = 1.696124158 / 1.823476582 / 1.955193101 at theta_t = pi/2, pi, 3pi/2 replaces a tautology (acos(-PI/(4 PI)) == acos(-0.25)) and W1 = +I is asserted at four target angles rather than one (M2 audit E13, E14, E15) | recomputed here |
| `anchor.m2.composite_order_ladder` | slopes of 1 - F_K | 4.3.5, 9.15 | amplitude/detuning infidelity slopes at theta = pi: primitive 2/2, SK1 4/2, BB1 6/2, CORPSE 2/4 (fitted below 1e-4), SCROFULOUS 4/2, CinSK 4/4 and CinBB 6/4 with 4 under simultaneous error; coefficients SK1 22.830256 eps_a^4, BB1 and CinBB 9.388566 eps_a^6, SCROFULOUS 4.566051 eps_a^4 and 4.0 eps_d^2, CORPSE 6.5008e-3 eps_d^4 degraded to 0.75 by either concatenation; the printed SK1 sign is twice the bare error, the three-pulse P2 3700x worse than P2; f_j = 4, 24, 720, 90720 and 2, 12, 360, 45360; toggled PD2 = BB1; durations 5 pi, 5 pi - 4k, 9 pi - 4k (1500 degrees) | tests/test_composite_pulses.py; check_composite.py | below 1 - F ~ 1e-8 the spectral-norm distance E converts as 1 - F_K ~ E^2 (1 - F_C ~ E^2/2): the plan's 'E^2/2' (Sections 4.3.5, 13) is the F_C form and halves every F_K coefficient if applied to F_K (M2 finding, pinned by the primitive's 1 - F_K = eps_d^2 at theta = pi); short-CORPSE's (0, 1, 0) winding returns -R, a global phase | recomputed here |
| `anchor.m2.debye_waller_identity` | sum_n P_n e^{-eta^2/2} L_n(eta^2) | 4.2.7, 9.2 Debye-Waller, 9.10, 9.12 Debye-Waller identity | 0.9851119396031 at eta^2 = 0.01, nbar = 1 and 0.8203698531378 at eta^2 = 0.09, nbar = 1.7 to 13 digits; the sum from n = 1 in the second case is 0.4662967 (0.4876057 in the first); 100 modes at nbar = 0.1, eta = 0.01: rms 3.3e-4 and P(\|dOmega/Omega\| < 1e-4) = erf(1e-4/sqrt(2 x 1e-6 x 0.11)) = 0.237, 0.330 without the 2 | tests/test_hilbert_layer.py | the thermal mean equals exp[-eta^2(nbar + 1/2)] EXACTLY (a thermal state's characteristic function is Gaussian), so Section 4.2.7's 'approximately' is an identity; Section 9.12's 'the n = 1 start gives 0.4662967' attaches to the (0.09, 1.7) case, not to the (0.01, 1) case it follows in the sentence (M2 finding) | recomputed here |
| `anchor.m2.derived_raman_drive` | Omega_R, Delta k, eta, delta_St, Gamma | 3.3, 4.3.2, 4.5.4, 4.5.5, 6.6 | 171Yb+ clock transition, counter-propagating 355 nm pair along x with lin-perp-lin polarizations (y, z) and B along x, 10 mW in 20 um waists: Omega_R/2pi = 30.28 kHz, \|Delta k\| = 2k, eta_x = 0.1111 = the 0.1103011 anchor scaled to 3.0 MHz, Stark shift -11.5 Hz (blue-detuned light raises both levels), Raman probability per pi pulse 1.6e-6; a co-propagating pair has Delta k = 0 and eta = 0 on every mode; pi-pi polarizations give no clock coupling (the vector light shift vanishes); the neighbour ratio under beams pointed at ion 0 is the product of two Gaussian FIELD factors exp(-s^2/w^2) | tests/test_light_layer.py | the two-ion 90-degree fixture of tests/fixtures.py (both beams polarized along B) cannot drive the clock transition and is a data-model fixture only (M2 finding) | recomputed here |
| `anchor.m2.displacement_table` | max \|D_expm - D_analytic\|, norm loss | 5.1.1 table; 9.13 Displacement operator | block n, n' < d/2: 2e-13/2e-6/1e-3 (d = 8), 3e-16/7e-8/2e-4 (10), 1e-16/1e-15/3e-9 (20), <= 1e-14 (40) at eta = 0.1/0.5/1.0, the round-off rows compared at the 1e-12 floor; expm unitary to 1e-13; analytic top-column norm loss 9.3% at (10, 0.1); both branches alpha^(n'-n) below and (-alpha*)^(n-n') above the diagonal match qutip.displace for complex alpha to 1e-13 | tests/test_hilbert_layer.py; bench_numerics.py | the d = 40 rows read 6e-15/5e-15/7e-16 here against the table's 7e-16/8e-16/7e-16: BLAS round-off, inside the 1e-12 floor | recomputed here |
| `anchor.m2.harty_randomized_benchmarking` | EPG | 4.3.3, 9.2 Microwave RB, 9.12 Harty budget decomposition | Pauli (two pi/2 pulses about +-x, +-y; +-z as a delay plus a frame rotation by pi; +-I as delays) then Clifford (one pi/2 about +-x, +-y) per computational gate, 14 us dead time after every pulse, 2000 gates, final pi/2 pulses into \|down> or \|up>; 12.1 us pulses, +4.5 Hz and a constant 5e-4 Rabi error give 0.77(11)e-6 over sets of 32 sequences (paper 0.81(14)e-6), the Rabi error alone 0.31e-6 (paper 0.3e-6), the detuning alone 0.59e-6 with the -1.0 Hz ac Zeeman shift applied during pulses only and 0.80e-6 with 5.5 Hz everywhere (paper 0.7e-6) | qutip_trap/validation/harty_rb.py; tests/test_harty_rb.py; Harty et al. 2014 (arXiv:1403.1524) main text and supplement | the paper leaves the identity timing ambiguous ('delays of the same duration (12 us) as the pi/2-pulses'); one delay per replaced pi/2 pulse (two slots each followed by the dead time) reproduces the budget, a single delay gives 0.53e-6; delta_eff = delta - delta_ac = +5.5 Hz (Section 9.2) | recomputed here |
| `anchor.m2.micromotion_carrier_factor` | J_0(beta) | 4.3.6, 9.2 Micromotion factors | 171Yb+ at omega_x/2pi = 3 MHz in a 30 MHz rf trap with a 50 V/m stray field along x: beta = 0.409 for the counter-propagating 355 nm pair (u_0 = 80 nm, q = 0.28), J_0 = 0.9586; the unlocked drive's carrier pi time is pi/(J_0 Omega e^{-eta^2/2}) to 1e-5 in population, the rf-locked modulated drive e^{i beta cos(Omega_rf t + delta)} agrees to 1e-4, C0 sits inside eta (0.11289 against 0.11113 without the rf record) | tests/test_builder_physics.py | an unlocked drive with micromotion='modulated' and no sampled rf phase is refused: the default is the J_0 average (Section 4.3.6) | recomputed here |
| `anchor.m2.mount_pd6` | phi_PD6:k | 4.3.5, 9.2 Composite pulses, 9.10 Composite pulses | PD6 phases (0.38266, -2.51430, -1.75192, 0.05941, 2.67572, 0.39344) for theta_t = pi and (0.34769, -3.06979, 1.55852, -0.70890, 3.09692, -0.62174) for pi/2, twelve pi pulses palindromic after the target: 2.447e-2 -> 3.713e-11 at theta_t = pi, eps = 0.1, and 6.156e-3 -> 1.348e-11 at pi/2, the cross-keyed rows leaving 6.156e-3; B2 below 1% for \|eps\| < 0.4 (6.3e-3 at 0.4, 2.2e-2 at 0.5) and PD6 for \|eps\| < 0.6 (6.5e-3 at 0.6, 3.3e-2 at 0.7) in 1 - F_K | Mount et al. 2015 Table I (arXiv:1504.01440); tests/test_composite_pulses.py | the table's phi_t column is theta_t (the plan's source typo); Section 9.10's 'target-first and target-last orderings separate at delta-hat = 0.1 (4.33e-3 vs 1.84e-3)' is not reproduced: at theta_t = pi, eps_d = 0.1 the two orderings give 9.80e-3 and 1.00e-2 for B2 and 1.00e-2 and 9.87e-3 for PD6 in 1 - F_K, coinciding at eps_d = 0 (M2, recorded rather than absorbed) | recomputed here |
| `anchor.m2.photons_per_stark_radian` | Gamma_total/Delta_St -> 0.9579 gamma/Delta_hf | 4.3.2 (PLAN.md:447), 9.2 Scattering rates, 9.10 Rayleigh amplitudes; validation/atomic_closed_forms.py | R_SE/\|delta_{0<->0}\| = gamma/omega_0 EXACTLY for any detuning and any polarization, because Eqs. 2.17 and 2.18 share one (g_b^2 + g_r^2)/3 prefactor and one [1/Delta^2 + 2/(Delta - omega_F)^2] bracket (asserted to 1e-12 over five detunings and three polarization pairs). Ozeri's saturated coefficient 0.9579 is a 4.2 percent correction to that identity, so a factor error in either cannot hide; it is TRANSCRIBED, since deriving it needs the four Rayleigh amplitudes at first power in the detuning | Ozeri et al. 2005; Wineland et al. 2003 Eqs. 2.17-2.18; tests/test_m2_stark_null.py | the plan's printed product 0.9579 gamma/Delta_hf = 0.015396 does not close on any pair of cited 9Be+ constants: with gamma/2pi = 19.4 MHz (Monroe 1995, species/be9.py) it needs Delta_hf/2pi = 1.20702 GHz, which is Langer's clock-point splitting 1.207495843 GHz to 4e-4 relative (giving 0.0153900) and not the zero-field \|A\|(I + 1/2) = 1.250018 GHz (giving 0.0148664); Ozeri's own rounded 1.25 GHz with his 19.6 MHz gives 0.0150199. The test pins 0.0154 to 5e-5 absolute and reports the 4e-4 residual against the printed 0.015396 rather than tuning an input to it. This number existed nowhere in the repository before (M2 audit E9a) | contested |
| `anchor.m2.picture_equivalence_and_step_density` | \|\|psi_S - e^{-i H_0 t} psi_I\|\| | 5.2, 5.3, 9.13 Picture equivalence and Step density | Schroedinger-motion and sideband-decomposed interaction pictures agree to 1.5e-8 for a blue-sideband pulse at n_max = 11; k_max = 2 to 1e-4 with the dropped weight reported; the interaction picture holds the right-hand-side evaluations near 1000 for k_max = 1, 3 and all 21 sidebands (scipy's DOP853 on the same operators: 1058) while the Schroedinger picture needs 27 steps per mode period under QuTiP's dop853 wrapper (12 under scipy's solve_ivp DOP853 at the same tolerances) | tests/test_builder_physics.py; the diagnostic run of 2026-09-05 | the plan's 14 to 45 steps per period (Section 5.2, measured with solve_ivp) brackets the QuTiP figure; the engine reports steps per period of the highest MODE, not of the top Fock component | recomputed here |
| `anchor.m2.rabi_matrix_elements` | Omega_{n',n}/Omega = \|<n'\|D(i eta)\|n>\| | 4.3.1, 9.2 Exact Rabi matrix elements, 9.16 rows 4.3-2 and 4.3-3 | the modulus form matches \|<n'\|expm(i eta (a + a^dag))\|n>\| at N = 400 to rtol 1e-10 over eta in {-0.9, -0.1, 0.05, 0.1, 0.3, 0.5, 0.9, 1.0, 1.5}, n, n' <= 8, and is non-negative; the signed Laguerre form is negative at eta = +0.5 for n = n' >= 6; the drive phase on the k-th sideband is arg (i eta)^k = k pi/2; <p\|D\|0> exceeds the eta^p/p! heuristic by sqrt(p!) e^{-eta^2/2} = 2.4373 (p = 3), 4.8745 (p = 4) | tests/test_hilbert_layer.py | the quartic Debye-Waller residual at n = 1, eta = 0.1 is 0.9850623544 exact against 1 - 3x/2 + 5x^2/8 = 0.9850625000, scaling as eta^6 with coefficient -7/48 | recomputed here |
| `anchor.m2.sideband_two_level_forms` | P = [Omega_{n'n}^2/(Omega_{n'n}^2 + Delta^2)] sin^2((t/2) sqrt(Omega_{n'n}^2 + Delta^2)) | 4.3.1 Two-level propagators; 9.2; 13 Sideband excitation lineshape | in the interaction picture with rwa (one sideband term) the closed form holds to 1e-9 at Delta = 0, +Omega_10, -0.6 Omega_10 with the plan's generalized Rabi frequency; the full model (every sideband, exact D) deviates by the carrier's light shift of the sideband, of order (Omega/(2 eta omega))^2 = 2e-3 at Omega/2pi = 30 kHz, eta = 0.111, omega/2pi = 3 MHz; the transition amplitude carries phi + pi/2 on the first sideband to 2e-3 rad; the blue sideband from \|0> and the red from \|1> flop at Omega eta e^{-eta^2/2} | tests/test_builder_physics.py | a native-gate identity test on a sideband must either use the rwa option or budget the carrier light shift; the plan's 9.6 budget '(Omega/nu)^2' is the carrier-on-carrier figure, the sideband figure is (Omega/(2 eta nu))^2 (M2 finding) | recomputed here |
| `anchor.m2.wineland_eight_term_stark` | Wineland 2003 Eq. 2.11, its Eq. 2.12 limit and its polarization null | 4.3.2 (PLAN.md:447), 9.2 Stark shifts, 12 (PLAN.md:1885); light/stark.py, validation/atomic_closed_forms.py | the eight-term \|2,2> <-> \|1,1> differential shift is NOT transcribed and no numbers for it are invented: PLAN.md:1885 lists Wineland Eqs. 2.5-2.9 and 2.11-2.12 as untranscribed, so the plan carries no coefficient sets pairing the eight scattering channels with their detunings at first power (Eq. 2.14's rationals 2/3, 1/3, 1, 1/6, 1/2, 5/6 are in the plan but not their channel assignment). What IS asserted against the derived AtomicStructure sum on the 9Be+ fixture is the qualitative claim the plan makes [verified]: the \|2,2> <-> \|1,1> differential shift changes sign between pure sigma+ and pure sigma- and therefore HAS a polarization null (root-found, at a genuine monotone crossing), while the clock shift keeps its sign over the same 41-point ellipticity scan and is therefore unnullable. The mechanism common to both is the three-index detuning: the summed line strength out of each member of either pair is identical beam by beam to 1e-12 (PLAN.md:647's sum rule), so a single Delta per intermediate LEVEL would return zero for either | tests/test_m2_stark_null.py; PLAN.md:1885 | light/stark.py's module docstring claimed 'Wineland's eight-term 9Be+ shift ... live[s] in qutip_trap.validation.atomic_closed_forms', where it does not; the docstring now says what is there and what is not, and names this record (M2 audit E9b). REMAINS OPEN: the eight-term shift and the Eq. 2.12 limit need the source, not the plan | contested |

#### Multi-level optical Bloch builder (Sections 4.2.8, 8.1, 9.3; M3a)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m3a.ca40_dark_resonance` | W(Delta_866 - Delta_397) | 4.2.1, 8.1 dark resonances, 12 | 40Ca+ S1/2-P1/2-D3/2 (8 states) at 4 G, 397 nm at -Gamma/2 with s = 0.3 and 866 nm with s = 3, both polarized perpendicular to B: W = 1e-7 s^-1 at equal detunings (the S(-1/2)-D(-1/2) superposition through P(+1/2) is dark at any field because the P energy cancels from the two-photon condition), 9.5e5 s^-1 at +0.5 Gamma, 8.8e4 at -1.5 Gamma, with the Zeeman-shifted two-photon dips at about -0.4 and +0.3 Gamma; a pi-only 866 nm beam at zero field leaves the D3/2 m = +-3/2 states as traps holding more than 99.9 percent | tests/test_bloch.py | the 397 nm beam also addresses S1/2-P3/2 (1 percent away); with the P3/2 level excluded its light shift and scattering are declared dropped | recomputed here |
| `anchor.m3a.dark_states` | null space of Omega_{qp} over the resonant ground manifold | 8.1, 13 Zeeman-degenerate dark-state condition, 13 Dark-state destabilization optimum | 171Yb+ F = 1 -> F' = 0 at the magic angle: 3 ground + 1 excited, ceiling 1/4, two dark states, theta_BE = 54.7356 deg; sigma+ light along B leaves exactly span{\|1,0>, \|1,+1>} dark (the straight pairing of Section 13); F = 1 -> F' = 1 linear light: one dark state at zero field; F = 0 -> F' = 1: none; 40Ca+ S-P-D with two beams: 6 + 2 states, four dark states | tests/test_bloch.py | at a finite field the hyperfine admixture (Zeeman/HFS = 1e-4 at 1 G) makes a Table-I dark state bright at that level, so the Berkeland count is evaluated on the zero-field coupling matrix and the finite-field residual is a singular value, not a null one | recomputed here |
| `anchor.m3a.doppler_limit` | nbar_D = (Gamma/4 nu)(1 + alpha/cos^2 theta_L) at Delta = -Gamma/2 | 4.2.1, 4.2.8 (iv), 9.3 Doppler limit with recoil | two-level fixture, sigma+ along B, nu = Gamma/20, Omega = Gamma/20: level A (W(Delta -+ nu) coefficients) nbar_D = 6.5426 against (Gamma/4 nu)(1 + 2/5) = 7.0 and its zero-point-offset form 6.5; level C on the two populated states with 70 Fock levels 6.531 (boundary population 1.6e-5); the argmin over Delta on a 0.025 Gamma grid is -0.500 Gamma | validation/scripts/check_bloch.py; tests/test_level_c.py | Itano and Wineland's E_K = 7 hbar Gamma/40 for alpha = 2/5 is the (1 + alpha)/4 form; the 1/2 is the zero-point offset the plan's 349.5 against 350 records | recomputed here |
| `anchor.m3a.floquet_fallback` | fixed point of P_T = exp(L T) | 4.2.8, 8.1, 13 Polarization-modulation mappings | 369 nm on F = 1 -> F' = 0 plus its 14.7478 GHz sideband on F = 0 -> F' = 1 (period 67.8 ps): the period-propagator fixed point agrees with the secular static model (each tone in its own hyperfine frame) to 5e-4 in every population and 2e-3 in the photon rate, and repumps F = 0 so the long-time state is more than 90 percent bright; an 'aom' polarization modulation at 3 MHz and B = 0 has period 333 ns and restores fluorescence by more than 10x over the static dark-state-trapped beam | tests/test_bloch.py | the one-period propagator must be integrated with the Section 5.3 ladder (vern9, nsteps 1e7): QuTiP's default multistep integrator returns a non-physical fixed point at the 12.6 GHz hyperfine detuning (M3a implementation finding) | recomputed here |
| `anchor.m3a.mixed_emission_channels` | 2D = sum_channels alpha_q eta_em^2 Gamma_q rho_ee | 4.2.8 (ii), 9.3 V-system diffusion | a beam at 45 degrees to B with pi and sigma components populates \|e, 0> and \|e, +-1> (2.5e-5 and 1.2e-5 each): level C nbar = 8.500721e-5 (minimal and vector recoil alike, mode along B) against the per-channel spectrum path 8.500664e-5, while a single mean alpha of 0.2, 0.4 or 1/3 in the same steady state misses 2D by more than 10 percent; with the mode perpendicular to B the vector form and the per-q scalar patterns differ by more than 1e-3 through Steck's q-coherence tensor | validation/scripts/check_bloch.py; tests/test_level_c.py | the plan's 'one mean alpha is up to 37 percent wrong for mixed pi and sigma channels' is confirmed in mechanism; the azimuthal symmetry about B kills the q-coherence cross terms for a mode along B and not otherwise | recomputed here |
| `anchor.m3a.morigi_eit_figure` | Omega_r = gamma, Omega_g = gamma/20, nu = gamma/10, eta = 0.145, Delta = 2.5 gamma | 4.2.3, 4.2.8, 13 EIT detuning sign, 9.3 EIT | Morigi's Eq. (5) at the caption's parameters gives nbar_S = 0.010833 and Eq. (6) the rate eta^2 (A_- - A_+) = 6157 s^-1; the Lambda level-C solve (sigma+ and sigma- counter-propagating along B, the third ground state renormalized away, 30 Fock levels) gives nbar = 0.01130 with P(0) = 0.989 (her '99 percent occupation') and W = 5938 s^-1 (5964 with recoil off); at the delta = nu tuning Omega_r^2 = 4 nu (nu + Delta) the closed form is (gamma/4 Delta)^2 exactly and level C approaches it from above as the probe weakens (0.00545 at Omega_g = 2, 0.00529 at 0.5 against 0.005103 at Delta = 3.5 gamma), the residual being the O(nu/Delta) the closed form drops | validation/scripts/check_bloch.py; tests/test_level_c.py; Morigi, Eschner and Keitel, PRL 85, 4458 (2000), Fig. 3 caption read from arXiv:quant-ph/0005009 | the plan's 'Morigi Fig. 3' fixture (nu = 2.0068, gamma = 20, Omega_1 = Omega_2 = 17 MHz, Delta = 70 MHz, Omega_r = 24.04 as the quadrature sum) is NOT the caption of that figure, which reads Omega_r = gamma, Omega_g = gamma/20, nu = gamma/10, eta = 0.145, Delta_g = Delta_r = 2.5 gamma for the Ca+ Lambda system; at the plan's numbers level C gives 0.012479, 2.45x the closed form, because equal beams are outside the weak-probe regime (M3a finding) | corrected |
| `anchor.m3a.optical_pumping` | P(F=0)(t), photons scattered | 4.2.6, 8.1 | 171Yb+ pumped on F = 1 -> F' = 1 at s_o = 0.5, 5 G, magic-angle polarization, by the exact exponential of the 64-dimensional Liouvillian: 99 percent in \|0,0> at 2.6 us, 3.00 photons scattered on average (1/3 branching into \|0> per excitation), residual F = 1 population 2e-6 from the 12.6 GHz off-resonant F = 0 -> F' = 1 excitation; at s_o = 0.02 the level-C curve agrees with the weak-drive rate equations built from the Lorentzian excitation rates and the channel branchings to 0.01 | tests/test_bloch.py | the photon count needs the coherent oscillations of the photon rate resolved (5 ns sampling): a 100 ns grid aliases them to 2.83 photons | recomputed here |
| `anchor.m3a.phonon_rate_equation` | <n>(t) = nbar + (n_0 - nbar) exp(-W t) | 4.2.3, 9.3 Phonon rate equation | mesolve on {sqrt(eta^2 A_+) a^dag, sqrt(eta^2 A_-) a} with A_+ = 3e4, A_- = 4e5 s^-1, eta = 0.1, n_0 = 5, 40 Fock levels reproduces the closed form to 3.73e-13 (measured by tests/test_m3_linewidth.py; the first record said 'better than 1e-9') in <n> over 3 ms; the birth-death generator has zero column sums and the geometric stationary distribution (A_+/A_-)^n; A_- <= A_+ raises CoolingError rather than returning a negative occupation | tests/test_level_c.py | none | recomputed here |
| `anchor.m3a.sideband_floor` | nbar_SB = (Gamma/2 nu)^2 [alpha (k_em/k_L)^2 / cos^2 theta_L + 1/4] | 4.2.2, 4.2.8 (iv), 9.3 Sideband floor decomposition, 9.3 Lamb-Dicke rate framework | level C (two-level fixture, sigma+ along B, Delta = -nu, Gamma/nu = 0.02, Omega = Gamma/2, 14 Fock levels): mode along B (alpha = 2/5, weight 0.4) nbar = 6.49987e-5 against 6.5e-5, mode at 45 degrees (alpha = 0.35, cos^2 = 1/2, weight 0.7) 9.4997e-5 against 9.5e-5, identical for the minimal, marginal and vector recoil discretizations (9, 48, 96 operators); recoil off 2.50002e-5 against (Gamma/4 nu)^2 = 2.5e-5; halving Omega leaves the floor, a four times lighter ion leaves it too (eta cancels); relaxation rate from the Liouvillian's slowest eigenvalue 268.544 s^-1 against the spectrum path 268.547 | validation/scripts/check_bloch.py; tests/test_level_c.py | the Stenholm floor is reproduced only with the drive's eta and the emitted photon's eta~ kept distinct (the participation b cancels between them, Section 4.2.2); Roos Eq. 3.20's (eta~/eta)^2 = alpha (k_em/k_L)^2 / cos^2 theta_L is the tested form | recomputed here |
| `anchor.m3a.two_level_lorentzian` | Gamma rho_ee = Gamma (s/2)/(1 + s + (2 Delta/Gamma)^2) | 4.2.1, 9.3 Doppler force | the J = 0 -> J' = 1 fixture driven by pi light (a unit-strength closed two-level line, 2 Omega^2/Gamma^2 = I/I_sat exactly) reproduces RMP Eq. 96 to 1e-9 at (Omega, Delta) = (0.05, -0.5), (0.5, 0), (2, 0.3) Gamma; ceiling 1/2 | tests/test_bloch.py | none | recomputed here |
| `anchor.m3a.yb171_detection_rate` | R_o = (Gamma/18) s_o/[1 + (2/9) s_o + (2 Delta/Gamma)^2] | 8.1, 8.8, 4.5.2, 9.13 | 171Yb+ S1/2 + P1/2 (the 0.501 percent D3/2 branch folded back), magic-angle linear polarization, Gamma_S = 2 pi x 19.62 MHz, I_sat = 50.83 mW/cm^2: the exact four-level conditional-bright-state rate over the closed form, maximized over the destabilizing field at Delta = 0, is 0.99505, 0.99513, 0.99547, 0.99857 at B = 0.299, 0.954, 3.041, 4.476 G for s_o = 0.01, 0.1, 1, 2.45 (at 1 G: 0.984, 0.995, 0.636, 0.240); the (2 Delta/Gamma)^2 bracket holds to 2e-4 at Delta = -Gamma/2; the excited population never exceeds the Gamma/4 ceiling | validation/scripts/check_bloch.py; tests/test_bloch.py | the closed form is the CEILING of the exact rate to 0.15-0.5 percent, not reproduced to nine digits as Section 8.1 states; the optimum field scales with the pumping rate (Gamma s_o/18 = 0.011, 0.11, 1.09, 2.67 MHz against g_F mu_B B/h = 0.42, 1.34, 4.26, 6.27 MHz), so Crain's about 5 G is the optimum for his inferred s_o = 2.45 rather than a factor 2 above it (M3a finding) | recomputed here |
| `anchor.m3a.yb171_leakage_prefactors` | R_d = (2/27)(Gamma/2) s_o (Gamma/2 Delta_HFP)^2, R_b = (2/9)(Gamma/2) s_o (Gamma/(2(Delta_HFP + Delta_HFS)))^2 | 8.1, 8.8, 9.5 | from the slowest Liouvillian eigenvalue and the stationary weights: at s_o = 0.1, B = 1 G, R_d = 9.9734 Hz (Noek's (2/3)(1/3) form with s = s_o/3: 9.9208; Crain's 1/3: 14.881), R_b = 0.60939 Hz (Noek's (2/3) form 0.60634), R_b/R_d = 0.0611 (3/49 = 0.06122); at Crain's s_o = 2.45 and the fluorescence-optimal 4.7 G, R_d = 248.9 Hz (Noek 243.1, Crain 364.6) and R_b = 14.93 Hz (14.86), the slow mode separated from the fast ones by 3e3 to 3e4 | validation/scripts/check_bloch.py; tests/test_bloch.py | the angular algebra settles the Noek-Crain factor 2 in Noek's favour, as Section 8.1 anticipated; R_d is field dependent through the bright-manifold redistribution (346.9 Hz at 1 G, near Crain's measured 341(13) Hz where the fluorescence is only 0.24 of its optimum), so the measured triple constrains the apparatus, not the prefactor | recomputed here |
| `anchor.m3a.yb171_repump_level_gap` | tau(3D[3/2]1/2), branching to S1/2 | 4.2.6, 4.5.6, 8.1 metastable manifold during detection | Olmschenk 2007 prints no lifetime or branching for the 935 nm upper level (its Sec. II says the ion 'quickly returns' to S1/2 and cites Bell et al., PRA 44, R20 (1991)), so the D3/2 -> 3D[3/2]1/2 -> S1/2 repump cycle cannot be closed from cited constants; the 171Yb+ detection model folds the 0.501 percent D3/2 branch back into the S line (leak = renormalize, a 0.5 percent effect on the saturation denominator) or routes it to a sink state that fills at Gamma_D P_e (leak = sink, trace preserved, about 200 photons before the ion is dark without a repump) | Olmschenk et al. 2007 (arXiv:0708.0657) read on 2026-09-05; qutip_trap/species/yb171.py MISSING | Section 8.1's requirement that the Bloch model include the D manifold and the repump beams waits on that constant being cited; the two-tone 935 nm repump (3.07 GHz sideband) is otherwise the Floquet-fallback case the builder already handles | contested |

#### Cooling and state preparation (Section 9.3; M3)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m3.ca40_multilevel_doppler` | 40Ca+ nbar_z, nbar_y under 397 nm at -20 MHz with the 866 nm repumper | 4.2.1; 9.3 Doppler anchors; Roos et al. 2000 | S1/2-P1/2-D3/2 (8 states, 4 G along z) with the 397 nm beam along (1,1,1)/sqrt3 polarized perpendicular to B and the 866 nm repumper in the xy plane, modes 3.3 (z) and 1.6 MHz (y): nbar_z, nbar_y = 3.43, 7.60 at s_397 = 0.3; 3.74, 8.74 at s_397 = 1; 4.92, 12.40 at s_397 = 3 (s_866 = 3, 3, 10), against the S-P two-level model with the D branch renormalized 3.42, 7.53; 3.65, 8.50; 4.58, 11.21 | validation/scripts/check_cooling.py; tests/test_doppler.py | the plan's 'the two-level estimate (n_z ~ 3, n_y ~ 6) fails because the multi-level structure invalidates the two-level assumption' is not reproduced: the level structure changes nbar by less than 10 percent at these parameters and the measured 6.5(1.0) and 16(2) need saturation or geometry factors (M3 finding) | corrected |
| `anchor.m3.ceiling_report_no_manifold` | CeilingReport.ceiling = NaN when no coupling is near resonance | 13 Scattering-rate object | ceiling_report returned a vacuous ceiling of 1.0 for far-detuned light, which reads as a satisfied check; with no coupling within window_gammas linewidths of resonance there is no closed resonant manifold and therefore no equal-population bound, so the ceiling is NaN and CeilingReport.has_manifold says so | qutip_trap/light/bloch.py | none | corrected |
| `anchor.m3.cirac_lambda_repumper` | <n>_min = 1/sqrt(beta) - 1/2 at Omega_phi^2 = 4 Delta nu/sqrt(beta) | 4.2.8 (v); 9.3 Lambda system with repumper | 0.554 at beta = 0.9 (Omega_phi = 2.05) and 2.66 at beta = 0.1 (3.56) for nu = 0.1, Delta = 10; Doppler reference Gamma/(2 nu) - 1/2 = 4.5 | tests/test_cooling_closed_forms.py | none | recomputed here |
| `anchor.m3.doppler_argmin_bounded` | argmin_Delta nbar_D = -(Gamma/2) sqrt(1 + s) as nu/Gamma -> 0 | 9.3 Doppler limit with recoil | bounded minimization (xatol 1e-7) of the level-A nbar over the beam detuning at Omega = 0.05 Gamma (s = 0.005) gives -0.504442 Gamma at nu/Gamma = 0.05, -0.501376 at 0.01 and -0.501253 at 0.002, against -(1/2) sqrt(1 + s) = -0.501248: the residual is first order in nu/Gamma (-3.19e-3, -1.28e-4, -4.37e-6), so the plan's argmin -0.5000 is the nu/Gamma -> 0 limit and is reached from below | tests/test_level_c.py; validation/scripts/check_bloch.py | the 21-point grid of spacing 0.025 Gamma the earlier revision scanned can only ever print -0.500 and cannot see any of that; both the test and check_bloch.py section E now root-find instead | recomputed here |
| `anchor.m3.doppler_rate_framework` | nbar_D = (Gamma/4 nu)(1 + alpha/cos^2 theta_L) - 1/2 | 4.2.1; 9.3 Doppler limit with recoil; 9.12 Doppler-limit fork | two-level fixture, one sigma+ beam along B = (1,1,1)/sqrt3 (cos^2 = 1/3, alpha = 1/3) at Omega = Gamma/20, Delta = -Gamma/2: nbar = 9.5375 at nu = Gamma/20 against 9.5000 (force model 9.5000), 3.490 and 3.342 at nu/Gamma = 0.128 and 0.133 against 3.42 and 3.27 (force model not reported); the detuning optimum for the low mode is -0.503 Gamma against -(1/2) sqrt(1 + s) = -0.501; the force model gives 349.5 against 350 at Gamma/nu = 1e3; the fork 0.480 against 0.336 mK at 20 MHz (1.680 against 1.176 at 70 MHz), 9.5 against 6.5 quanta at 1 MHz | validation/scripts/check_cooling.py; tests/test_doppler.py; tests/test_cooling_closed_forms.py | none | recomputed here |
| `anchor.m3.eit_fixture_and_lechner` | <n>_S = 0.005102 at Delta = +70, Omega_r = 24.04 MHz; R(3.29)/R(1.13) | 4.2.3; 9.3 EIT; 9.12 EIT rate versus mode frequency; 9.13 row 123; 9.17 Morigi EIT fixture | the plan's fixture (nu = 2.0068, gamma = 20, Omega_1 = Omega_2 = 17 MHz) in the plan's sign: 0.005102 against (gamma/4 Delta)^2 = 0.0051020, Delta = -70 raises, a single 17 gives 0.1467 (29x); the RMP tuning Omega_r^2 = 4 nu Delta gives exactly (Gamma^2 + 4 nu^2)/(16 Delta (Delta - nu)) = 0.00361702 (the (Delta + nu) form 0.00320755); Roos's Delta = 70 MHz, Omega_sigma = 21.4 MHz light shift 1.599 MHz; Lechner's Omega_sigma = 30, Omega_pi = 6.2 MHz with a 2.2-2.3 MHz light shift: R(3.29 MHz)/R(1.13 MHz) = 2.66-3.83 (3.19 at 2.25 MHz) with eta^2 ∝ 1/nu, and the measured 19e3 s^-1 needs eta = 0.134 at 3.29 MHz (\|Delta k\| = 1.37 k) | validation/scripts/check_cooling.py; tests/test_eit.py | Section 9.13 row 123 prints A_- = 7.225 and A_+ = 0.036676 MHz, half of Section 4.2.3's own formula at Omega_g = 17 (14.45 and 0.0734; nbar unaffected, W = 5.75 kHz against the row's 2.875 at eta = 0.02); Section 9.12's 'R(1.13)/R(3.29) = 2.9' is inverted, the mode nearer the bright resonance cools faster (M3 findings) | corrected |
| `anchor.m3.eit_level_c_roos` | level C, 40Ca+ S1/2 + P1/2 Zeeman-resolved, Roos geometry | 4.2.3 (level C is the four-level master equation [corrected]); 9.3 EIT | sigma+ beam along B, pi beam along x, Delta = +70 MHz, Omega_sigma/2pi = 21.41 MHz (delta = nu on the 1.6 MHz mode), Omega_pi/2pi = 3 MHz, mode along Delta k (two-photon eta = 0.199), 24 Fock levels: nbar = 0.01026 with P(0) = 0.9907 and W = 7.55e4 s^-1, against the three-level closed form 0.00593 = (gamma/4 Delta)^2 and W = 1.03e5 | validation/scripts/check_cooling.py | the Zeeman-resolved four-level structure (the pi beam also drives S(-1/2) -> P(-1/2)) and the eta = 0.2 Lamb-Dicke corrections raise the floor 1.7x above the idealized Lambda closed form; Roos's measured 0.18 sits far above both and is not a floor of this model | recomputed here |
| `anchor.m3.home2009_mixed_crystal_modes` | 9Be+ - 24Mg+ - 24Mg+ - 9Be+ axial spectrum; the 251 kHz spacing; nbar 15 -> 0.06 | 9.3 Sympathetic; 4.1.7 | PLAN.md gives no trap frequency for the Home 2009 crystal, but Section 4.1.7 carries Home 2013's single-ion frequencies for the SAME apparatus (2 pi x 2.69 MHz axial for 9Be+, 1.65 MHz for 24Mg+ from the mass-independent dc curvature) and the isotope masses 9.0121822 u and 23.985042 u, so the four-ion axial spectrum is computable from the plan: 1.9488, 4.0854, 5.4862 and 5.7373 MHz with mass-weighted amplitudes (0.3222, 0.6294, 0.6294, 0.3222), (0.4664, 0.5315, 0.5315, 0.4664), (0.6294, 0.3222, 0.3222, 0.6294) and (0.5315, 0.4664, 0.4664, 0.5315), Be-Be spacing 10.859 um. Two independent published quantities check it: Home's SOM ('the highest frequency axial modes, which are separated in frequency by 2 pi x 251 kHz', with the gate detuning delta = 2 pi x 83.6 kHz so 3 delta = 250.8 kHz) gives 251.10 kHz here, and Jost's Methods table (2.0, 4.1, 5.5, 5.7 MHz with vectors [0.32, 0.63, 0.63, 0.32], [-0.47, -0.53, 0.53, 0.47], [0.63, -0.32, -0.32, 0.63], [0.53, -0.47, 0.47, -0.53]) agrees to its printed precision and to 0.005 in every amplitude | tests/test_sympathetic.py; tests/test_sideband_cooling.py; Home, Hanneke, Jost, Amini, Leibfried, Wineland, Science 325, 1227 (2009) and its supporting online material; Jost, Home, Amini, Hanneke, Ozeri, Langer, Bollinger, Leibfried, Wineland, Nature 459, 683 (2009) Methods; Barrett et al., Phys. Rev. A 68, 042302 (2003) Sec. II.A | the coolant of Home 2009 is 24Mg+ (23.985042 u), not 25Mg+; the SINGLE-ION axial frequency of that crystal is in no source read (the 2.69 MHz is Home 2013's, for the two-ion Be/Mg crystal of the same apparatus, and it reproduces the four-ion table, so it is used as the derived scale) | recomputed here |
| `anchor.m3.home2009_sympathetic_schedule` | nbar 15 -> 0.005 to 0.013 per axial mode with Home's own 24Mg+ Raman geometry | 9.3 Sympathetic | Home's Doppler stage leaves nbar ~ 15 (SOM, 'Sympathetic cooling') and 'cycles of resolved sideband cooling on both the second and first sidebands of each motional mode' reach '<n> ~ 0.06' (p. 1229; the SOM's per-mode ground-state fidelity 0.94 reads as (1 - 0.94)/0.94 = 0.064). \|Delta k\| = sqrt(2) k at 280.355 nm (Barrett et al. 2003 Sec. II.A: 'The Raman beams propagate at right angles to each other ... Delta k = k_1 - k_2 parallel to the trap axis'; Jost thesis Sec. 6.4.1's worked eta ~ 0.4 for a single 9Be+ at 3 MHz pins the sqrt(2) against 2) reproduces the published 24Mg+ eta of the two-ion pair, 0.282 against Barrett's 0.30 at 2.3 MHz and 0.0769 against his 0.082 at 4.9 MHz, and gives eta = (0.2074, 0.1210, 0.0633, 0.0896) on the four-ion axial modes. Sixty second-order plus 120 first-order cycles inside the plan's own pulse-time window (0.05 to 4 pi times of the first sideband out of \|1>) then reach (0.009798, 0.006724, 0.012112, 0.004530), every mode inside the row's 0.06 | tests/test_sideband_cooling.py | the invented eta = 0.2, Omega_0 = 2 pi x 200 kHz, 40 + 40 pulses and 'nbar < 0.1' of the earlier revision are replaced; Home prints no per-mode cycle count (only 5.1 ms for all four modes), and the greedy per-order optimizer is NOT monotone in the pulse count, so the pulse-time window must be stated in pi times rather than in absolute seconds (the M3 finding) | recomputed here |
| `anchor.m3.itano_three_beam_optimum` | gamma_si/gamma_tot = sqrt(f_si)/sum_j sqrt(f_sj); hbar Gamma/4 per mode | 9.3 Doppler limit with recoil; 4.2.8 (iv) | axis i takes friction from its own beam only (proportional to gamma_si) and the emission recoil from all three (f_si gamma_tot), so E_i is proportional to 1 + f_si gamma_tot/gamma_si and minimizing sum_i E_i at fixed sum_i gamma_si gives gamma_si proportional to sqrt(f_si); at that optimum E_i = (hbar Gamma/4)(1 + sqrt(f_si) sum_j sqrt(f_sj)). For an isotropic pattern (f_si = 1/3, the Cartesian sum rule of Section 4.2.8) the shares are 1/3 each and E_i = hbar Gamma/2 per mode, whose kinetic half is EXACTLY Itano's hbar Gamma/4 per mode, the same number as the single-beam (1 + alpha)/4 form at alpha = 1. For the sigma pattern along z (f = 3/10, 3/10, 2/5) the shares are (0.3169873, 0.3169873, 0.3660254) and the optimum total is 5.9856 hbar Gamma/4 against the uniform split's 6 | qutip_trap/prep/closed_forms.py; tests/test_cooling_closed_forms.py; Itano and Wineland, Phys. Rev. A 25, 35 (1982) Eqs. 20-26 | the row's other clause, 'one beam along x leaves <n_y> growing linearly', is the level-A statement that an unaddressed axis has heating = cooling = 2D exactly, so W_m = 0, no steady state exists (CoolingError) and <n>(t) = n_0 + 2D t; the module's UncooledModeError is that same statement as a refusal | derived |
| `anchor.m3.laguerre_nodes_and_che_schedules` | first zeros of L^{(k)}_{n'}(eta^2); Che's accumulation centres | 4.2.8 Pulsed sideband cooling; 9.12 Laguerre nodes in pulsed cooling | 39.7908 (k = 1) and 71.7720 (k = 2) at eta = 0.3, 112.2895 and 202.0112 at eta = 0.18 as continuous degrees; stranded states round(zero + k) = 41, 74, 113; Che's 25Mg+ (eta = 0.3, nbar = 17, 151 levels, 9 us pulses at 150 kHz): first-order cycles peak at n = 43, 42, 41 after 40, 150, 500 pulses (centres 45.6, 41.9, 41.2) and second-order at 77, 74, 74 (76.8, 74.4, 73.7); his 75-pulse schedule leaves nbar = 0.716 with P(0) = 0.973 | validation/scripts/check_cooling.py; tests/test_sideband_cooling.py | Che's printed 40 and 72 label the degree n' = n - k; his reported centres (43, 41, 40) and (75, 73, 72) are reproduced to within one Fock state with the shifted index | recomputed here |
| `anchor.m3.lechner_eighteen_ion_band` | 18-ion 40Ca+ radial band against 0.01-0.02 in under 1 ms | 9.3 EIT | the 18-ion radial mode set whose band edges are Lechner's 3.29 and 1.13 MHz (which fixes the anisotropy through gamma_p = 1/alpha + 1/2 - mu_p/2 with mu_max = 118.533, giving a single-ion axial 0.4031 MHz and alpha = 0.01501, inside alpha_crit = 0.01702 so the string is linear) evaluated at Omega_sigma = 30 MHz, Omega_pi = 6.2 MHz, a 2.25 MHz light shift (Delta = 97.75 MHz) and W_m = (\|Delta k\| x0,m)^2 (A_- - A_+) for a GLOBAL beam (sum_i c_{i,m}^2 = 1 removes the participation exactly): nbar runs from 0.00334 to 0.13129 (0.04645 on the 3.29 MHz radial centre of mass, 0.13129 on the 1.13 MHz edge) and 1/W from 0.024 to 1.1708 ms. Seventeen of the eighteen modes relax in under 1 ms, but only SEVEN reach nbar <= 0.02 | tests/test_eit.py | the reason is quantitative and is the finding: the 2.16 MHz radial band is 4.45 times the dressed cooling bandwidth Gamma' = 0.485 MHz at that operating point, so a single-tone EIT configuration cannot deliver the row's uniform 0.01-0.02 across an 18-ion string. The numbers above are the regression; the row's headline was uncomputed before | contested |
| `anchor.m3.marzoli_effective_two_level` | Pi = (Omega_aux/2)^2/[delta^2 + ((Gamma_10 + Gamma_12)/2)^2]; gamma'/Gamma' | 4.2.8 (v); 9.3 Effective two-level parameters, V-system diffusion; 13 Effective two-level roles | Gamma_12 = 0.1 Gamma_10, Omega_12 = 0.2, delta_12 = -1: Pi = 0.007678, gamma'/Gamma' = 0.55 (Xi) and 5.5 (V), light shift +0.0077 Gamma_10, optimum delta_20 = -0.0577 (Marzoli Fig. 3's -0.058); the V configuration cools at +nu; the V-system diffusion D = (s_10/4) rho_00 [Gamma_12 (eta_10^2 + alpha eta_12^2) + Gamma_10 eta_10^2 (1 + alpha)] equals the half-rate emission diffusion plus the absorption recoil, and alpha on the absorbed photon or the Gamma_10 branch without its 1 fail | tests/test_cooling_closed_forms.py | none | recomputed here |
| `anchor.m3.monroe_doppler_triple` | 9Be+ nbar = (0.47, 0.30, 0.18) at (11.2, 18.2, 29.8) MHz, Delta = -30 MHz, theory 0.484 | 4.2.1; 9.3 Doppler anchors; 12 (the acceptance test 'not yet computed') | the force model with the isotropic alpha = 1/3, cos^2 = 1 and Delta = -30 MHz gives 0.486 for the 11.2 MHz mode, Monroe's 'theoretical 0.484' to 0.4 percent, although nu/Gamma = 0.58 is outside that model's regime; the rate framework valid there gives 0.573 (sigma pattern along B) or 0.533 (alpha = 1/3) for a weak beam along the mode, 0.211 and 0.065 for the 18.2 and 29.8 MHz modes; one sigma+ beam along (1,1,1)/sqrt3 gives (0.95, 0.38, 0.12) at s = 0.1, (1.02, 0.41, 0.13) at 0.5 and (1.28, 0.56, 0.16) at 2 | validation/scripts/check_cooling.py; tests/test_doppler.py; Monroe et al. 1995 | the Section 4.2.1 acceptance test as designed (three nbar from one Delta with one beam) does not reproduce the measured triple at any saturation (x twice too hot, z 30 percent too cold); it needs the D1-D3 beam geometry, which the plan does not carry (M3 finding) | corrected |
| `anchor.m3.monroe_raman_fifteen_cycles` | (0.033, 0.022, 0.029) after 15 Raman cycles | 9.3 Raman sideband cooling | five first-order cycles per axis on the plan's own eta = (0.21, 0.12, 0.09) fixture, starting from Monroe's Doppler triple (0.47, 0.30, 0.18) with the exact pulsed transfer matrices and the plan's pi-time window, reach (0.006741, 0.002025, 0.000448) with no repump recoil and (0.022561, 0.006901, 0.002965) with three repump photons per cycle: the ideal schedule is 1.5 to 60 times COLDER than the measured triple, and the measurement is not monotone in eta (z is measured hotter than y) while the model necessarily is | tests/test_sideband_cooling.py; Monroe, Meekhof, King, Jefferts, Itano, Wineland, Bergquist, Phys. Rev. Lett. 75, 4011 (1995) | so the row's triple is not a target of the schedule model at all - the experiment's floor comes from heating during the cooling and from the repump's imperfection, neither of which level B carries. Recorded like anchor.m3.monroe_doppler_triple; the computed triples above are the regression. Monroe's Raman beams are COUNTERPROPAGATING (\|Delta k\| = 2 k), unlike the 90-degree geometry of the later Home/Jost apparatus | contested |
| `anchor.m3.optical_pumping_recoil` | 171Yb+ pump: photons, error, recoil quanta per mode; spin-zero Zeeman pump: 3 photons | 4.2.6; 4.2.8 Sizes; 9.3 | F = 1 -> F' = 1 at s_o = 0.5 and 5 G from the uniform F = 1 mixture: 3.0006 photons, 99.99 percent in \|0,0> at 5.25 us, error 2.48e-6 (the steady state), recoil heating 8.55e-3 quanta on a 1 MHz mode along B (eta_em = 0.0925) and 2.85e-3 / 2.95e-3 on the 3.0 / 2.9 MHz modes with the ion's participation and one alpha per emitted polarization; sigma+ light on S1/2 -> P1/2 of a spin-zero ion pumps \|-1/2> into \|+1/2> with exactly 3.0000 photons (pi branching 1/3) and error 2e-12 | validation/scripts/check_cooling.py; tests/test_pumping.py | the two-tone 9Be+ pump into \|2,2> (F = 2 and F = 1 tones on one line) is the periodic-frame case of the builder, not a static solve | recomputed here |
| `anchor.m3.pgc_limits_and_lindblad` | <n_0> = xi + 1/(4 xi) - 1/2; <n> = (3/4) xi + 5/(8 xi) - 1/2; the Lindblad layer | 4.2.4; 9.15 PGC cooling limits, PGC recoil-heating angular factor, PGC operating-point round trips, Three-axis Lamb-Dicke triple; 13 Recoil kernel discretization | fixed-phase minimum 0.5000000000 at xi = 0.5, phase-averaged minimum 0.8693063938 at xi = 0.9128709292, the phase average by quadrature to 1e-6; alpha = 1/3 makes H_carr + H_sb the non-xi^2 part of H exactly and 2/5 fails by 5 percent; s = 0.02098 at xi = 1.35, 210 MHz, 1088 kHz; the 171Yb+ triple (0.05201, 0.05282, 0.09023); the Lindblad layer through the M3a builder (40Ca+ S1/2 + P1/2, two linear beams along B, recoil = minimal, 20 Fock levels) at Delta = +500 MHz and omega = 2 pi x 4 MHz: 9 collapse operators with the sum rule to 1e-16, nbar = 0.7560 against the analytic 0.7500 at xi = 1 (0.5468 against 0.5000 at xi = 0.5, eta = 0.089), relaxation rate 3.88e4 against 4.58e4 s^-1, and nbar = 6.0 at the gradient node phi = pi/4 where the analytic model raises | validation/scripts/check_cooling.py; tests/test_polarization_gradient.py | Joshi's twelve operators are nine in the Wigner-Eckart form (one operator per emitted polarization and recoil class, the two pi decays summed into one) with the same second moments 2/5 and 1/5; the analytic W at the source's operating point is 9.9e4 s^-1 (phase-averaged) against the quoted 6.6e4, the window W < delta < omega_z holding either way (M3 findings) | recomputed here |
| `anchor.m3.rasmusson_pulsed_cooling` | 171Yb+ eta = 0.18: 25 fixed pulses, 50 multiorder pulses, the stranded tail | 4.2.8 Pulsed sideband cooling; 9.3 Thermometry | 25 first-order pulses of one shared duration from nbar = 14.6 reach 3.14 (the pi time of n = 15 gives 3.15) against Rasmusson's nbar_sim = 3.57(58), his measured 0.58 (ratio), 8.0 (SVD) and 4.1 (time-averaged) being estimator biases; from 15.36, 50 multiorder pulses with one duration per order (17 x k = 3, 17 x k = 2, 16 x k = 1, higher first) reach 0.77 and independently optimized durations 0.11-0.12; 50 first-order pulses leave the thermal tail above the node n = 113 (8.0e-4 of the population, 0.10 quanta) | validation/scripts/check_cooling.py; tests/test_sideband_cooling.py | Rasmusson's 0.06 after 50 optimized pulses is not reached by this optimizer (0.11-0.12), and his 'about 0.3 quanta stranded above n = 112' is 0.10 for the thermal tail; the transfer matrices conserve probability to 4e-16 per pulse and the truncated tail at n_max = 150 is 7.3e-5 | corrected |
| `anchor.m3.recoil_energy_identity` | sum_m alpha_m c_{i,m}^2 (k x0_{i,m})^2 hbar omega_m = alpha_axis (hbar k)^2/(2 m_i) | 4.2.8 Angular factors; 9.3 Recoil quadrature identities | for both ions of a two-171Yb+ chain and every family and pattern (pi, sigma, isotropic) the ratio is 1 to 2e-16; for a 171Yb+/40Ca+ pair (rf 60 MHz, c_Yb = 0.961 on the 1.184 MHz mode) it is 1 to 1e-12 for each ion with its OWN mass; the joint kick of one sigma photon over a 6 x 8 direction quadrature on three modes deposits R (the Cartesian sum rule, 1 - 1e-9) with per-mode quanta alpha_m eta_em^2 (3.4196e-3, 8.549e-4, 8.844e-4 at 1.0, 3.0, 2.9 MHz); the Fock kernel of one photon is column stochastic with mean kick alpha eta_em^2 in every column | validation/scripts/check_cooling.py; tests/test_recoil_modes.py | none | recomputed here |
| `anchor.m3.recoil_unit_regression` | v_r, T_r, R/h, R/(hbar gamma) | 9.3 Recoil unit regression; 13 Momentum diffusion and recoil temperature | 133Cs 852 nm 3.52 mm/s, 198.3 nK; 87Rb 780 nm 5.88 mm/s, 362.0 nK with k_B T_r = (hbar k)^2/m; 138Ba+ 493 nm R/h = 5.9 kHz, R/(hbar gamma) = 2.83e-4; 24Mg+ 280 nm 106.4 kHz, 2.47e-3 | validation/scripts/check_cooling.py; tests/test_recoil_modes.py | none | recomputed here |
| `anchor.m3.repump_recoil_per_cycle` | Delta nbar = t s Gamma^3 eta^2/(4 Delta_HF^2) | 4.2.8; 9.15 Repump recoil per cycle and probability guards | 0.031117 for Che's 25Mg+ (t = 10 us, s = 1, Gamma = 2 pi x 41.3 MHz, eta = 0.3, Delta_HF = 2 pi x 1.789 GHz), 0.0049524 with ordinary Hz fed in (low by exactly 2 pi); R_sc = s Gamma^3/(8 Delta^2) = 1.7287e4 s^-1 times t times 2 eta^2 reproduces it | tests/test_cooling_closed_forms.py | none | recomputed here |
| `anchor.m3.roos_eit_operating_point` | (gamma/4 Delta)^2 = 0.00593 against the measured 0.18 | 9.3 EIT | Roos's 40Ca+ operating point Delta = 70 MHz with Omega_sigma = 21.4 MHz puts the narrow dressed state 1.599 MHz up, on his 1.6 MHz radial mode, where the closed form gives (21.57/(4 x 70))^2 = 0.005935 and the exact steady state agrees to 2e-3. His measured nbar_y = 0.18 is 30.4 times that | tests/test_eit.py; validation/scripts/check_cooling.py | 0.18 is therefore not a floor of this model and is an anchor that reports rather than fails; it was previously only printed by check_cooling.py section D and is now asserted in the test | recomputed here |
| `anchor.m3.sisyphus_saturation_exponent` | d log W/d log s_0 = 2 | 9.3 Sisyphus | W = (16/9) eta^2 Gamma s xi cos^2 2phi with xi = Delta s/(3 omega) is proportional to s^2, and s is proportional to the on-resonance s_0 through saturation_bridge, so a log-log fit of W against s_0 over the source's factor of 7.5 (s_0 = 2 to 15 at Gamma = 2 pi x 19.6 MHz, Delta = 2 pi x 310 MHz, omega = 2 pi x 0.79 MHz) returns the slope 2.000000000 to 1e-9, inside both measured error bars 1.98(6) and 1.91(3) | tests/test_polarization_gradient.py | holding xi fixed (a depth independent of the intensity) returns 1.0 exactly and is the negative control; Section 4.2.4 forbids pinning the measured steady state nbar_ss = 1.5-2 against this model, so the 9.3 row's third clause stays unpinnable and conflicts with 4.2.4 | derived |
| `anchor.m3.stenholm_floors_and_sympathetic_list` | (gamma/nu)^2 (alpha + 1/4); nbar_ss = 0.5, 0.1475, 0.039522, 0.0064703, 0.0016231 | 4.2.2; 9.3 Sideband limit, Lamb-Dicke rate framework, Sideband floor decomposition; 9.13 Sympathetic-cooling optimum and steady state | Stenholm's coefficients with the weak-drive Lorentzian give 7/12 and 13/20 in units of (gamma/nu)^2 for alpha = 1/3 and 2/5, alpha -> 0 leaves (Gamma/4 nu)^2 = 6.25e-6 at Gamma/nu = 0.01, the carrier term cancels in A_- - A_+ and not in nbar (ratio 2.6), halving Omega leaves nbar; the Morigi-Walther row 127 list is the same form with alpha = 2/5 and their Omega the trap frequency: A_- - A_+ normalized = 1 - 1/(16 Omega^2/gamma^2 + 1) exactly and nbar = 0.5, 0.1475, 0.039522, 0.0064703, 0.0016231 at Omega/gamma = 0.5, 1, 2, 5, 10, limit 0.1625 (gamma/Omega)^2 | tests/test_cooling_closed_forms.py | none | recomputed here |
| `anchor.m3.thermometry_exactness` | P_rsb/P_bsb = [nbar/(nbar + 1)]^k | 4.2.7; 9.3 Thermometry; 9.12 Sideband-ratio thermometry, exact | at eta = 1.5, nbar = 8 the exact sideband ratio equals (nbar/(nbar + 1))^k to 1.6e-15 for k = 1, 2 and three pulse durations (Turchette 2000; the plan's 6e-15); a double-thermal state shows a duration-dependent ratio; Rasmusson's time-averaged red-sideband signal is (1/2) sum_{n >= m} p(n) and stepping m returns the populations to 1e-14; the blue-sideband flopping inverts by NNLS to 2e-3 | validation/scripts/check_cooling.py; tests/test_sideband_cooling.py | none | recomputed here |
| `anchor.m3.wubbena_sympathetic_optimum` | mu* = 8/11, F_min = 23/16 | 9.13 Sympathetic-cooling optimum and steady state | with (b_1, b_2) the mass-weighted in-phase axial eigenvector of the two-ion mixed crystal (b_1^2 = 0.5 at mu = 1, 0.6839213464 at mu = 0.675, 0.3160786536 at mu = 40/27, the Section 9.12 row), bounded minimization of F(mu) = b_2^2 (b_1 + b_2/sqrt mu)^2/b_1^2 + b_1^2 (b_2 - b_1/sqrt mu)^2/b_2^2 gives mu* = 0.7272727274 and F_min = 1.4375000000, i.e. 8/11 and 23/16 to 1e-9; F(1) = 2 exactly. The two brackets are the modes' uniform-field couplings sum_j c_j/sqrt(m_j) in units of 1/sqrt(m_1), and both are nonzero for mu != 1 (a mixed crystal has no exact centre-of-mass mode); only mu = 1 kills the out-of-phase one | qutip_trap/prep/closed_forms.py; tests/test_cooling_closed_forms.py; Wubbena, Amairi, Mandel, Schmidt, Phys. Rev. A 85, 043412 (2012) Eqs. 34-36 | the plan prints mu* = 0.727272725, which is 8/11 = 0.72727272727... with the last two digits slipped (2.3e-9 off). The negative control the row names holds: the UN-swapped b_1^2 E_i + b_2^2 E_o decreases monotonically over mu in (0.06, 19.9) and has NO interior minimum, its smallest value sitting on the upper boundary | verified |

#### Two-ion entangling gates (Section 9.4; M4)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m4.baldwin_echo` | diag(1, i, i, 1) | 9.17 'Baldwin echo'; 4.4.4 | the printed H = eta Omega sum_j (-1)^j (1 + sigma_z^j)(a e^{i delta t} + h.c.) integrated through R_x(pi) U R_-x(pi) U at 8 pi (eta Omega/delta)^2 = pi/4 gives diag(1, -i, -i, 1) up to a global phase for tones above the mode (the conjugate of the printed diag(1, i, i, 1), whose sign the detuning side sets); off-diagonal elements vanish; Phi_loop = 2 pi (eta Omega/delta)^2 on S^2 per loop | check_two_qubit.py; tests/test_light_shift_gate.py | the printed H fixes only \|Phi\|; the plan's diag(1, i, i, 1) is the other detuning side | recomputed here |
| `anchor.m4.ballance_alpha_k` | alpha_K = (8K + 3)/(16 K^2) | 9.16 row 6-3; 6.2; 4.4.4 | L = a^dag a sqrt(2/tau) on the K-loop force model, one Liouvillian exponential per sigma_x block pair: (1 - F) tau/t_g = 0.687376, 0.296845, 0.136710 at t_g/tau = 1e-4 for K = 1, 2, 4 against 11/16, 19/64, 35/256 | check_two_qubit.py | Ballance's 0.686 is the value at finite t_g/tau = 1e-3, not the coefficient; the \|dd> state infidelity and the entanglement infidelity coincide for this channel | recomputed here |
| `anchor.m4.choi_closure_counts` | 2N + 1, 4N + 1 | 9.4 'Multi-mode closure'; 9.12 'Segment counting with both transverse families'; 4.4.7 (2) | five 171Yb+ ions at 3.045/2.95/0.55 MHz: 11 segments close the five x modes for the pair (1, 3) to \|alpha\| < 1e-15 with chi = pi/4; a Delta k at 30 degrees in the xy plane couples both families (10 modes), 11 segments raise ClosureError and 21 close every mode | check_two_qubit.py | Choi's 190 us with 9 segments for five ions closes four modes, not five | recomputed here |
| `anchor.m4.debye_waller_references` | (pi^2/4) eta^4 <(n - n_ref)^2> | 4.4.7 (1); 9.16 row 4.4-7; 9.12 'eta^4 thermal infidelity unified' | the ENTANGLING-ANGLE error of an n = 0-calibrated gate with the exact Laguerre sidebands, read Fock state by Fock state from P_11/(P_00 + P_11) = sin^2 chi_n, follows chi_n - chi_0 = -(pi/4) 2 eta^2 n within 10% for n = 1, 2, 3 and averages to (pi^2/4) eta^4 nbar (2 nbar + 1); the re-optimized nbar^2 + nbar reference is excluded; \|M_n\|^2 - \|M_{n-1}\|^2 = eta^2 [1 - eta^2 (2n + 1)] to 1.0007/1.0020/1.0039 at eta = 0.123 | tests/test_two_qubit_gates.py; tests/test_gate_calibration.py | the thermal robustness curve of M4 is the n0 reference; a device records which reference its calibration used | verified |
| `anchor.m4.doubled_hamiltonian_control` | -(hbar eta Omega) S_y (a^dag e^{i eps t} + h.c.) | 9.16 row 4.4-4 | integrating the plan's WITHDRAWN doubled force, twice the derived one, for one loop from \|dd>\|0> lands maximally entangling at eta Omega/eps = 1/4 (populations (0.5, 0, 0, 0.5), concurrence 1.0000) and on a product state (0, 0, 0, 1.0) with concurrence below 1e-8 at 1/2, exactly the reverse of the correct -(hbar eta Omega/2) S_y form | check_ms_closure.py; tests/test_m4_rows.py::test_withdrawn_doubled_hamiltonian_is_the_negative_control | the negative control is now the third block of check_ms_closure.py's committed output and a test, so the factor-2 force error the derivation audit caught has a regression guard | recomputed here |
| `anchor.m4.exact_first_order_propagator` | D(alpha S_y) exp[i gamma S_y^2] | 9.4 row 1; 4.4.1 | with the displacement expanded to first order and the resonant sidebands kept the engine's state equals the closed-form propagator at every stored time and the populations follow Kirchmair's envelopes with nbar = 0 and 1 (the first-order force is exact for a thermal mode too) | Kirchmair 2009 Eqs. 3-4, 14; Haljan 2005; tests/test_two_qubit_gates.py | the force axis is phi_s + pi/2 = pi/2 (S_y) for equal tone phases at zero; the plan's -(hbar eta Omega/2) sign gives alpha the opposite sign to Kirchmair's, the moduli and the S^2 phase being identical | verified |
| `anchor.m4.fm_and_fourier_robustness` | d alpha/d delta_1 | 9.4 'FM robustness'; 4.4.3 | two-mode fixture: Leung's nine-vertex FM schedule closes both modes and the robust cost nulls the time-averaged trajectory, so a common 500 Hz drift costs 4e-6 against 9e-4 for the unconstrained schedule; Blumel's first-order stabilized sixteen-function Fourier pulse nulls d alpha/d omega_m and is far less sensitive to a 1 kHz shift than the unstabilized one | tests/test_shaping.py | five and seven vertices leave the robust system underdetermined for two modes (four closure plus four robustness residuals) | recomputed here |
| `anchor.m4.ghz_four_ions` | GHZ at t = pi/(8 chi), N = 4 | 9.4 'GHZ' | H = 4 chi J_x^2 on \|0000> at t = pi/(8 chi) gives populations (0.5, 0.5) on \|0000> and \|1111> with nothing between them and overlap 1.0000000000 with (\|0000> + i\|1111>)/sqrt2 (matrix exponential, 1e-9); the microscopic bichromatic drive at Fig. 4's ratios on 2^4 x 12 = 192 states reproduces it to 5% up to LOCAL Z phases | Molmer-Sorensen 1999; tests/test_m4_rows.py | the microscopic run's off-resonant carrier and ac Stark shift rotate each qubit's phase, which exp(i 4 chi J_x^2) does not, so the overlap must be maximized over the relative phase; the entanglement structure, not a phase convention, is what the row asserts | recomputed here |
| `anchor.m4.gradient_drive_ca40` | Omega_g J_2(4 Omega_mu/delta)(sigma_z1 - sigma_z2) | 4.4.5; 9.4 'Microwave gradient' | two 40Ca+ Zeeman qubits (S1/2 mJ = -1/2 <-> +1/2 at 5 G, d f_0/dB = 2.8025 MHz/G) in the Section 11.1 trap with 152 T/m gradient electrodes at omega_m - 2 delta and delta/2pi = 200 kHz: Omega_g/2pi = 5035.3 Hz on the antisymmetric x mode, w_{i,m} = +-2 Omega_g exactly, and at the decoupling point Omega_mu/delta = 0.6012 the dressed coupling is Omega_g J_2(2.4048)/2pi = 2174.0 Hz. The dressed build's H(0) equals Omega_g J_2 (sigma_z1 - sigma_z0)(a + a^dag) plus the free motion to round-off | tests/test_m4_gradient.py | the intrinsic dynamical decoupling is demonstrated from the exact dynamics and not from a Bessel-zero lookup: a 2 kHz static qubit-frequency offset on both ions over 100 us costs 6.9e-5 in state fidelity at Omega_mu/delta = 0.6012 and 3.06e-1 at 0.3, a suppression of 4400 | derived |
| `anchor.m4.hughes_gate_angle` | theta_g | 4.4.6; 9.4 'Smooth gate' | for H_g = hbar delta a^dag a + (hbar Omega_g/2) S (a^dag + a) a closed square pulse has theta_g = pi K (Omega_g/delta)^2 = int Omega_g^2/(2 delta) dt, maximal at pi/4 as the plan corrects | Hughes 2025 Eq. 4 and the theta_g integral as the plan prints them | the printed theta_g ~ int (Omega_g^2 + alpha_dot^2)/delta dt is twice the displayed Hamiltonian's angle; one of the two carries a factor-2 convention the plan does not resolve | contested |
| `anchor.m4.kirchmair_ca40` | F = 0.993(1), 0.971(2), parity 0.964 | 9.4 'Thermal populations'; 9.12 | at nu/2pi = 1.232 MHz and eta = 0.044 the closure algebra fixes the apparatus: t_g = 50 us is eps/2pi = 20 kHz at K = 1 and needs Omega/2pi = 227.3 kHz, t_g = 25 us is eps/2pi = 40 kHz and needs 454.5 kHz, i.e. Omega/nu = 0.369. The one closed-form prediction among the row's numbers is the nbar = 20(2) parity contrast, \|sum_n P_n e^{-i 4 chi_0 eta^2 n}\| = 0.9923 against the paper's 0.964, agreeing to 2.9% | Kirchmair et al., NJP 11, 023002 (2009); tests/test_gate_calibration.py::test_kirchmair_forty_calcium_consistency_anchors | the audit supposed the 25 us point was eps/2pi = 10 kHz; it is 40 kHz (tau = K/eps). F = 0.993(1) and 0.971(2) are NOT reproduced from eta and nu: the intrinsic residual-displacement and Debye-Waller terms at nbar = 0 are below 1e-5, three orders under the measurement, so both are dominated by the technical terms Section 4.4.7 lists for this paper (2e-3 from incoherent carrier excitation by laser frequency noise, heating Gamma_h t_g/2, delta Omega/Omega = 1.4e-2 whose angle error costs 4.8e-4), which do fit inside the paper's 1 - F = 7e-3 at 50 us | recomputed here |
| `anchor.m4.leung_five_ion` | Leung 2018's five-ion design | 9.4 'FM robustness'; 4.4.3 | a 90 us robust FM design on the five-ion 171Yb+ chain at omega_x/2pi = 3.045 MHz closes all five transverse-x modes to \|alpha\| = 3e-13 with 19 cosine-interpolated vertices and needs Omega/2pi = 292 kHz; 13 vertices (Leung's 13 oscillations) leave the loops open at \|alpha\| = 0.13, because seven free time-symmetric vertices cannot satisfy ten closure plus ten robustness conditions | Leung and Brown 2018; tests/test_shaping.py::test_fm_solver_closes_the_five_ion_leung_design | the required Omega brackets the two numbers Section 4.4.3 says the calibration layer must reconcile (Leung's printed 2 pi x 600 kHz against 151 kHz) but reproduces neither: the fixture's eta (0.05 to 0.08 on the x family) is this crystal's and not Leung's apparatus's, so the discrepancy is reported and not asserted | recomputed here |
| `anchor.m4.light_shift_gate_ca40` | ZZ from the atomic layer | 4.4.4; 9.4 'Light-shift gate' (M4) | two 40Ca+ ions, counter-propagating 398.5 nm circular beams (3 THz below S-P1/2) with a 729 nm E2 beam for the echo pulses: essentially only the S1/2 level of the optical qubit is shifted (weights (-1.99740, 0.00260), the D5/2 level's own two-photon shift being 1.3e-3 of the S level's), the symmetric light-shift pulse on the x-COM at chi = pi/8 per loop in the spin-echo pair gives ZZ(pi/2) with fidelity 0.98 before and above 0.99 after the exact calibration of the AM-closed pulse; the 'inside' detuning side gives the opposite sign | tests/test_light_shift_gate.py | the exact per-pulse angle is 11% below the surrogate at eta = 0.145 (larger loops, larger Debye-Waller corrections). Every number the table carries is now DERIVED: the 729 nm beam was rotated to k_hat = (1,1,0)/sqrt2 with pol = (-1,1,0)/sqrt2 (tests/m4_fixtures.CA40_729_E2_BEAM), where the E2 Rabi frequency derives to 200144 Hz at 166 mW in a 200 um waist (34735.4 Hz at the 5 mW of the first fixtures, Omega growing as sqrt(P)), because the first fixture's k_hat = y_lab with pol = x_lab put both eps_z and n_z of the atomic frame in the plane perpendicular to B and derived EXACTLY zero, so a supplied 200 kHz was being played (conv.played_zero_truth). The same beams' S-D two-photon matrix element gives a spin-flip weight of -0.17366 whose off-resonant excitation is ~1e-23 (411 THz off resonance), and the builder drops it and records the drop | derived |
| `anchor.m4.ms_closure_through_the_builder` | concurrence at eta Omega/eps = 1/2, 1/4 | 9.4 row 2; 9.16 rows 4.4-4, 13-8 | eta = 0.05, nu = 1 MHz, eps = 10 kHz, d = 16, one loop from \|dd> through Waveform.symmetric and the builder: concurrence 0.9999 with populations (0.5074, 0, 0, 0.4926) at 1/2 and 0.3825 with (0.962, 0, 0, 0.038) at 1/4, the check script's numbers | check_two_qubit.py; check_ms_closure.py | the anchor's fixture has one mode, so the synthetic device freezes the rocking mode with its Debye-Waller factor off | recomputed here |
| `anchor.m4.n_independence` | Omega~ = -(Omega eta)^2/(nu - delta) | 9.4 'n-independence' | mesolve of the two-ion one-mode bichromatic model at Fig. 4's parameters (delta = 0.9 nu, Omega = 0.1 nu, eta = 0.1, Gamma = 2e-4 nu with the thermal pair sqrt(Gamma(n_th+1)) a and sqrt(Gamma n_th) a^dag, Lamb-Dicke kept to second order as the source's own model is): P_ee = sin^2(Omega~ t/2), so the populations cross at t = pi/(2\|Omega~\|) = 1570.8/nu; measured 1520 at n_th = 0 and 1525 at n_th = 2 | Sorensen-Molmer 1999 Fig. 4; tests/test_m4_rows.py::test_bichromatic_coupling_is_n_independent | the crossing sits 3.2% below the closed form (the ac Stark correction of a drive at Omega/nu = 0.1 plus the second-order truncation) and the two temperatures agree to 0.34%: the n-INDEPENDENCE is the row's claim and it holds an order of magnitude better than the closed form's own accuracy | recomputed here |
| `anchor.m4.residual_displacement_conversions` | eps_ent | 9.4 'Residual displacement'; 4.4.7 (8) | an open loop (1.3 loops) with the first-order force: the final mode energy equals sum_j \|alpha_j\|^2, 1 - F_ent = eps_ent to first order, the average gate infidelity is (4/5) eps_ent, and the \|dd> state infidelity equals eps_ent | Landsman 2019; Leung 2018; Zhu 2006; tests/test_two_qubit_gates.py | Zhu's printed sum beta (\|alpha_j\|^2 + \|alpha_n\|^2)/4 is one quarter of what direct integration gives for the \|dd> input with Choi's alpha; the plan's 'a factor 1/4 below eps_ent for the same displacements' holds only if Zhu's alpha is twice Choi's (his Eq. 8 is inconsistent with his Eq. 7 by that factor); recorded, not reproduced as a distinct fidelity measure | verified |
| `anchor.m4.solvers_verified_by_exact_integration` | exact chi/surrogate chi | 4.4.3 (closing paragraph); 9.4 | Section 4.4.3 requires every solution to be verified by exact integration of the full Hamiltonian through the injected PulseEngine; until now only symmetric_pulse and the segmented AM solver were ever played. On the two-ion fixture: FM (dims [2,2,11,10]) exact chi/surrogate = 0.9847 with leakage 4.7e-4, stabilized Fourier AM (dims [2,2,10,10]) 0.9831 with leakage 4.7e-7, PM (dims [2,2,11,12]) 0.9528 with leakage 1.8e-2; boundary populations below 1e-15 throughout | tests/test_gate_calibration.py::test_the_fm_fourier_and_pm_solutions_are_verified_by_exact_integration | the PM family's leakage is an order of magnitude above the others' because its motion phase is its modulation parameter and Roos's spin-axis tilt cannot be tuned away (conv.pm_solver) | recomputed here |
| `anchor.m4.spectator_loop_and_am_closure` | sum_j \|alpha_jm\|^2 | 4.4.7; 9.4 'Multi-mode closure'; M4 exit criterion | two 171Yb+ ions (x modes 2.828/3.000 MHz, eta 0.081/0.079): the symmetric COM pulse leaves 7.1e-3 quanta in the rocking mode against the closed form 2 \|alpha\|^2 = 7.2e-3; the five-segment AM pulse closes both modes (leakage 8.7e-5, residual quanta < 2e-4) and the exact chi is 0.7720 against the surrogate 0.7854; the exact checks correct the amplitude by 0.87% to chi = 0.785398, Bell fidelity 0.99977 | check_two_qubit.py | the surrogate over-predicts chi by the Debye-Waller factor of the sideband couplings and the carrier's second-order terms (1.7% here); Section 7.5's spot check is what absorbs it | recomputed here |
| `anchor.m4.srinivas_gradient` | Srinivas 2021's own parameters | 9.4 'Microwave gradient'; 9.13 '25Mg+ clock point' | the paper's qubit is \|F=3, mF=3> <-> \|F=2, mF=2> of the 25Mg+ S1/2 manifold at 21.3 mT, gradient 152(15) T/m at omega_g = 2 pi x 5 MHz on a mode at omega_r ~ 2 pi x 6.9 MHz, eight Walsh-modulated pulses over 740 us. The hyperfine-Zeeman diagonalization of the cited 25Mg+ constants puts that transition at 1.326467253 GHz with d f_0/dB = 1.97333 MHz/G at 212.78 G against the paper's printed omega~_0 ~ 2 pi x 1.326 GHz (four digits), while the 9.13 row's clock pair \|3,+1> <-> \|2,+1> sits at 1.686462 GHz with 21 Hz/G at the same field. With the TOTAL two-ion mass r_0 = 3.82844 nm and Omega_g/2pi = 2870.8 Hz, the dressed coupling at the decoupling point is Omega_g J_2(2.4048)/2pi = 1239.5 Hz and Omega_mu/2pi = 571.1 kHz | Srinivas et al., Nature 597, 209 (2021); tests/test_m4_gradient.py::test_srinivas_own_parameters | the paper's delta ~ (omega_r - omega_g)/2 = 2 pi x 950 kHz IS the 2 delta = omega_r - omega_g resonance the dressed force needs, which the plan does not say; one maximally entangling closed loop at Omega_eff/Delta = 1/4 would take 202 us, so the 740 us eight-segment Walsh sequence is the right scale (reported, not asserted: the Walsh sequence's phase efficiency is no plan number). The plan's 9.13 row cites Srinivas for the CLOCK point, which is not the transition the gradient gate drives | recomputed here |
| `anchor.m4.symmetrized_kernel` | chi_ij = K_ij + K_ji | 9.16 row 4.4-5; 4.4.3 | with Omega_1 ~ sin^2(pi t/tau) and Omega_2 ~ (t/tau) sin^2(pi t/tau) the block-diagonal exact phases give chi equal to the symmetrized kernel while the two readings of Choi's printed 2 Omega_i(t) Omega_j(t') differ from it by more than 10% with opposite signs | derivation audit 2026-09-04; tests/test_two_qubit_gates.py, tests/test_shaping.py | the analytic segment kernels (F_k, the triangle T_k and Im(F_l F_k^*)) match scipy quadrature to 1e-9 for both the exact first-order and the rwa form at any phi_m | verified |
| `anchor.m4.symmetrized_kernel_deviation` | 2 K_ab, 2 K_ba against chi | 9.16 row 4.4-5 | on the committed two-ion one-mode fixture with non-proportional envelopes (Omega_a ~ sin^2, Omega_b ~ (t/tau) sin^2) the symmetrized kernel K_ab + K_ba equals the block-diagonal exact chi to 1e-8 (measured 2.70e-9 at 4001 Simpson points, 2.4e-11 at 20001), chi_ij = chi_ji to 1e-12, and the full-square integral of the printed integrand equals the ANTISYMMETRIC combination K_ab - K_ba to 1e-8; the two printed readings 2 K_ab and 2 K_ba miss chi by -26.81% and +26.81% | tests/test_two_qubit_gates.py::test_symmetrized_kernel_is_exact_by_block_diagonal_integration | the row's -18.20%/+18.20% (2 ions) and +-29.6%/+-21.4% (3 ions) came from the audit's own two-mode fixture, whose parameters are not committed, so the PERCENTAGE is fixture-dependent; what is universal, and is now pinned, is that the two readings are equal and opposite about the symmetrized kernel | recomputed here |
| `anchor.m4.thermal_residual_entanglement` | eps(nbar) exact | 4.4.7 (1); 9.4 'Thermal fidelity' (M4 finding) | the exact Bell-state infidelity of an n = 0-calibrated square MS gate for a Fock input n is about (pi^2/4) eta^4 n(n + 1): Ballance's angle error (pi^2/4) eta^4 n^2 plus residual spin-motion entanglement from the n-dependent sideband coupling (leakage into \|du>, \|ud> at the same order in eta^4, linear in n: loss/[(pi^2/4) eta^4 n^2] = 1.96, 1.45, 1.28 at n = 1, 2, 3 for eta = 0.05 and 0.079 alike); thermally averaged the total lies between 1.1 and 1.6 times eps_nbar at nbar <= 2 | tests/test_gate_calibration.py; the Fock-resolved diagnostic of 2026-09-05 (two devices, the rwa and the full builder agree) | none of the three published thermal forms carries the residual-entanglement term; Ballance's eps_nbar is the angle error alone (the plan's Section 4.4.7 (1) reconciliation concerns only that part) | recomputed here |

#### Readout (Section 9.5; M5)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m5.acton_factors_and_ceiling` | M_1, M_2pi, M_2-, F_max | 9.5 rows 'Clebsch-Gordan factors', '111Cd+ ceiling', 'Corrected I_sat' | M_1 = 2/9, 20/81, 1/4, 56/225, 20/81, 12/49 for I = 1/2..3; (2/9, 1/9, 1/9) at I = 1/2 pins the normalization; F_max = 99.9375 % at gamma/2pi = 60 MHz, omega_HFP/2pi = 800 MHz; 87Rb D2 I_sat 1.669 mW/cm^2; Cd+ I_ion/I_sat = 1.09e-4 at 4 um (the printed 7e-4 is 2 pi too large) | Acton 2006 Eqs. 9, 12, 13, 20, 22; check_readout.py B | Acton's Eqs. 5-6 are the single-jump Poisson-exponential mixtures: normalized to 1e-15 and equal to the Gauss-Legendre quadrature of Crain's corrected Eq. 1 to 1e-15 (the plan's 'agree with quadrature to 2e-16' is machine precision) | verified |
| `anchor.m5.acton_quadrature_and_snspd_window` | \|Acton - quadrature\|; argmin of the SNSPD error | 9.5 rows 'Acton distributions', 'SNSPD operating point' | Acton's Poisson-exponential mixtures agree with the Gauss-Legendre quadrature of Crain's corrected Eq. 1 to 1.11e-16 (dark) and 9.71e-17 (bright) at 64 nodes, which meets the row's own 2e-16, and to 4.44e-16 / 3.47e-16 at the default 96 nodes; the zero-threshold average error has its minimum at 21.26 us on a 10 ns grid and 21.5 us on the test's 0.5 us grid, inside the row's 20-25 us band | tests/test_photon_records.py | the residual is pure double-precision round-off (2e-16 is one ulp at these magnitudes) and grows slowly with the node count as the quadrature sums more terms, so the row's 2e-16 is a node-count statement and not a tighter identity; the earlier test bounds (1e-14 and an 18-26 us band) were 50x and 1.6x looser than the plan's rows | recomputed here |
| `anchor.m5.budgets` | eps = (eps_B + eps_D)/2 | 9.5 row 'Budgets'; 8.4 | Harty 1.8 + 1.8 + 1.7 + 1.5 = 6.8e-4 closes; Christensen 0.1 + 0.5 + 0.7 + 1.0 + 1.0 + 0.1 = 3.4e-4 against 2.9(6)e-4 with the decay row 1 - exp(-4.5 ms/30 s) = 1.5e-4 per \|1> shot; Egan 0.71 %/0.22 % with the 0.55, 0.13 and 0.07 % line items; N_q < ln 2/eps = 2390 at 2.9e-4 | Harty 2014 Table I; Christensen 2020 Table I; Egan 2021 (via PLAN.md 6.7); check_readout.py G | Christensen prints the decay exponent with the wrong sign; the arXiv text of Egan carries 0.46(2) % for the single-qubit Z-basis SPAM error and not the 0.71/0.22 % split, which the plan quotes from the published supplement | verified |
| `anchor.m5.burrell_camera` | eps_D floor, PSF leakage | 9.5 row 'Camera'; 8.3, 8.5 | eps_D -> t_exp/(2 tau) = 1.71e-4 at 400 us (1.667e-4, 1.704e-4, 1.710e-4 as the bright count grows); the diffraction-limited NA 0.25 Airy pattern leaks 0.20 % into a one-spacing ROI at 14 um; a Gaussian PSF of sigma 4.1 um reproduces the measured 4.0 % nearest neighbour; the neighbour-conditioned ICM decode and sum_k e^{-R_k} | Burrell 2010; check_readout.py E; tests/test_photon_records.py, test_discriminators.py | the measured 0.9 % next-nearest leakage is a wing no Gaussian has and no diffraction-limited Airy pattern reaches at this spacing: the aberrated PSF of the apparatus is not derivable from its NA (Burrell's Airy scale 1.22 lambda/tan alpha is read as 1.22 lambda/NA here, contested) | recomputed here |
| `anchor.m5.cpt_negative_control` | eps_sys R_o against (eps_sys/3) R_o | 9.5 row 'CPT'; 8.1 | at Noek's s = 0.815 (s_o = 2.45) the corrected R_o = 0.0880 Gamma gives eps_sys R_o = 472 kcps at eps_sys = 4.356 %, against 157 kcps for the lumped F_CPT(eta) = F_no-CPT(eta/3) model: a factor 3 exactly. No code path applies eta/3 - the detected rate is linear in Detector.efficiency with slope R_o - and the ceiling asserted is the F = 1 -> F' = 0 manifold's n_e/(n_e + n_g) = 1/4, never the three-level Lambda cycle's 1/3 | Olmschenk 2007 (the Lambda ceiling 1/3); check_readout.py J; tests/test_m5_cpt_negative_control.py | the second clause of the 9.5 CPT row ('the lumped model is illegal beside the Bloch solve') had no test anywhere before the 2026-09-07 audit; the first clause (R_o <= Gamma/4) was already pinned | verified |
| `anchor.m5.crain_corrections_and_operating_point` | eps_B(11 us), eps at t* | 9.5 rows 'Crain corrections', 'SNSPD operating point' | Eq. 1 sums to 1 only with the no-jump branch (0.0068 without at 20 us); Eq. 3 at 11 us: 6.26e-3 corrected against 7.18e-4 printed; the zero-threshold average error has its minimum 5.84e-4 at 21.5 us (5.39e-4 without background) against the measured 6.9e-4 at 11 us average detection time | Crain 2019 Eqs. 1-3, 5-8; check_readout.py C | Crain's 11 us is the AVERAGE detection time of the stop-on-first-photon protocol (a bright ion stops at its first photon after 2 us), not the record window; the closed-form optimum window is 20 to 25 us | recomputed here |
| `anchor.m5.crosstalk_depumping` | Delta R_d, Delta R_b per bright neighbour | 8.5 'added counts on a dark neighbour plus depumping-reduced N' | the leaked resonant light of a bright neighbour at spacing x has s_nb = 3 lambda^2/(8 pi^2 x^2) (Acton's bound with the corrected I_sat = pi h c Gamma/(3 lambda^3)), and R_d, R_b are linear in intensity with no saturation denominator (Section 8.1), so one bright neighbour adds (R_d, R_b) x s_nb/s_beam to the ion's chain, several neighbours adding linearly; at 369.5 nm and 14 um s_nb = 2.65e-5, so at Crain's s_o = 2.45 one neighbour adds 3.7 mHz to R_d = 341 Hz and 0.18 mHz to R_b = 16.4 Hz. R_o is untouched: Wineland's mechanism degrades DISCRIMINATION, never eta_d | qutip_trap/readout/detection.py depumped_model; qutip_trap/readout/fluorescence.py neighbour_pumping_rates; tests/test_m5_crosstalk_depumping.py | the first M5 implementation carried only the added-counts half and neighbour_intensity_ratio had no consumer, an undocumented deviation (conv.readout_crosstalk_added_counts stated only the implemented half). The pumping is frozen at the neighbours' START classes, the same first-order form the factored confusion tensor uses, because one ion's chain is sampled before its neighbours' paths are known; the polarization share of the leaked light that reaches the pumping channel is a device parameter defaulting to the plan's BOUND of 1 | derived |
| `anchor.m5.detected_line_and_micromotion` | R_o(one line)/R_o(all lines); R_o(beta)/R_o(0) | 8.1; 8.8; 13 'J_n(beta) on every drive' | 40Ca+ under 397 nm (s_o = 1) + 866 nm (s_o = 5) with levels (S1/2, P1/2, D3/2): 1.833e6 photons/s on S1/2<-P1/2 against 1.950e6 summed over both lines, ratio 1.0638 = 1 + Gamma_D/Gamma_S exactly (a closed steady-state identity, pinned at 1e-9). 171Yb+ at s_o = 0.5, B = 5 G, beta = 0.4, Omega_rf/2pi = 30 MHz: J_0^2 = 0.92237, J_1^2 = 0.03843, R_o(beta)/R_o(0) = 0.930 (the two sidebands give back 0.0074, each carrying about 0.097 of the resonant rate 30 MHz off a 19.62 MHz line) | check_readout.py J; tests/test_m5_detection_line.py | the sidebands' own R_d is about 7 % ABOVE the resonant one, so the J-weighted R_d rises by 0.5 % while R_o falls by 7 %: the leakage ratio R_d/R_o grows under micromotion (derived here; no readout source applies the factor at all) | recomputed here |
| `anchor.m5.doppler_widths` | k v_rms/2 pi | 8.8 'Doppler broadening from residual motion' | 171Yb+ with modes at 3.0, 2.9 and 1.0 MHz all at nbar: 0.266, 0.421, 1.555, 3.443 MHz at nbar = 0.1, 1, 20, 100 against the plan's 0.257, 0.425, 1.57, 3.48 (unstated mode set) and the 19.72 MHz linewidth | check_readout.py I | the plan's four numbers do not follow sqrt(2 nbar + 1) exactly among themselves (ratios 1.65 and 13.5 against sqrt(3/1.2) = 1.58 and sqrt(201/1.2) = 12.9), so they were computed with different mode sets or occupations per row; the formula is the plan's and the conclusion (negligible until nbar ~ 100) stands | recomputed here |
| `anchor.m5.fast_path_imperfect_transfer` | P(declared bright \| shelved level) | 9.5 row 'Budgets'; 9.17 'POVM fast-path domain' | 40Ca+ shelving with transfer_probability = 0.9 at Myerson's (n_c = 5.5, t_b = 420 us): the exact POVM row is 0.10024 = 0.9 x P(bright \| shelf start) + 0.1 x P(bright \| bright start); 20 000 shots give 0.1017 on the full record path and 0.1027 on the fast path, both within 1 sigma of the row | check_readout.py F; tests/test_m5_fast_path_levels.py | the pre-fix fast path gave 0.1925 on the same seeds (the transfer channel applied twice); the register-wide form carries the same fix and the max per-ion discrepancy against the product form at 4 % PSF leakage is reported by max_confusion_discrepancy (0.338 on Crain's zero-threshold protocol, the same 0.34 the dense tensor shows) | recomputed here |
| `anchor.m5.myerson_optimum_and_recursion` | eps(n_c, t_b) | 9.5 rows 'Myerson recursion', '40Ca+ optimum' | recursion equals brute force to 1e-14 in log; with ideal Poisson statistics the exact chain's optimum is eps = 1.24e-4 at n_c = 3.5, t_b = 320 us and 1.37e-4 at the paper's (5.5, 420 us) against the measured 1.8(1)e-4; time-resolved ML Monte Carlo eps_D ~ 1.0e-4 to 1.5e-4 with eps_B <= 5e-5 (paper 1.5(2)e-4, asymptote 0.87e-4); adaptive 60/177 us average (paper 72/219 us) | Myerson 2008; check_readout.py D; tests/test_discriminators.py | the measured optimum sits at a higher threshold and longer window than the ideal-Poisson one because the PMT dark counts are non-Poissonian (the paper attributes about 20 % of eps_D to cosmic rays), which the model does not carry (M5 finding) | recomputed here |
| `anchor.m5.povm_domains` | product POVM vs register confusion | 9.5 row 'Budgets'; 5.7; 9.17 'POVM fast-path domain' | at zero crosstalk the full record path and the product POVM agree in per-ion confusion within statistics and both keep the Bell state's bit correlations (P(equal) > 0.995 at Crain's 1.2e-3 per-shot error); at 4 % leakage the register form differs from the product form by 0.34 in one entry and the full path follows the register form | check_readout.py F; tests/test_readout_povm.py | the readout error is applied once: the fast path replaces the record layer; the projective outcome is keyed by (sample, trajectory, shot, ion = 0, 'register_outcome') and the records by (.., ion, 'photon_record'), so two shots on one (sample, trajectory) draw different records | recomputed here |
| `anchor.m5.yb171_rate_object` | R_o, R_d, R_b | 9.5 rows '171Yb+ rates', 'CPT'; 8.1, 8.8 | the M3a Bloch solve through rates_from_bloch: R_o/closed = 0.9948 (s_o = 0.1, 1 G) and 0.9969 (2.45, 4.7 G) with the ceiling 1/4 asserted; R_d and R_b within 0.5 to 2.4 % of Noek's prefactors; R_b/R_d = 0.0611 and 0.0600 against 3/49 = 0.0612 and the measured 0.048 (30 % band) | check_readout.py A; tests/test_readout_rates.py | the Section 9.5 CPT row prints 's_o = 0.815' for the point R_o = 0.0880 Gamma: that is Noek's s = 0.815, i.e. s_o = 2.45 (the two saturation parameters Section 8.8 warns against aliasing); at s_o = 0.815 the form gives 0.0383 Gamma (M5 finding) | recomputed here |

#### End-to-end circuits (Section 9.6; M6)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m6.am_beat_note_midpoint_sensitivity` | residual displacement of the 2N + 1 segment AM solution against mu | 4.4.3; 7.5; 7.8 | at the exact mode midpoint the closure null space is one-dimensional and its power-optimal direction flips within +-20 Hz: leakage 1.1e-2 and 3.3e-3 residual quanta on the COM at the midpoint, 8.7e-5 and 1.8e-4 at 213.6 Hz below it (the M4 fixture's mu = 2.914 MHz), 1.3e-2 at 20 Hz below; above the COM (mu = omega_top + 0.35 gap = 3.060 MHz) leakage 3.6e-5, residual 1.9e-4, fidelity 0.99969 | M6 scan on the two-ion 171Yb+ fixture (modes 2.8284 and 3.0000 MHz, 100 us, 5 segments), exact JOINT_EXACT checks | the surrogate places the beat note 0.35 of the smallest mode gap above the highest coupled mode (Landsman's and Chen's placement above the spectrum) and never at a midpoint; the first-order closed forms close every loop in all cases, the differences are second order and appear only in the exact check | recomputed here |
| `anchor.m6.beat_phase_tilt` | MS(pi, 0) from \|10> against the gate's start phase | 7.10; 4.4.1 (Roos); 9.6 | phase_continuous = False (the beat note reset per gate through the leg phases): fidelity 0.99995 at gate starts of 0, 21.25 and 21.5 beat cycles; phase_continuous = True: 0.99995 at 0 and 21.5 cycles, 0.98886 at 21.25 cycles (zeta = pi/2, Roos's psi = (4 Omega-bar/mu) sin zeta) | check_circuits.py 2b on the two-ion fixture (calibrated 5-segment AM waveform, mu = 3.0601 MHz) | the exact spot check at t = 0 cannot see the tilt, so a run under continuous tones would report a gate the calibration never measured; the intrinsic budget carries sin^2 psi for that hardware | recomputed here |
| `anchor.m6.bell_state` | two-ion Bell circuit through run() (Section 9.6 row 1) | 9.6; 3.4; 8.6 | 4000-shot histogram 00: 0.5015, 11: 0.4965, 01: 0.0013, 10: 0.0008 against the ideal 0.5/0.5 (error bar on P00 0.0079); register 1 - F = 2.336e-3 (2.353e-3 in M6, before the compensated light shift and its frame, M8) against the compiled circuit's state inside the intrinsic budget (8.2e-3 in M6; 3.0e-2 once the Bessel force-saturation term the Section 9.6 row names was added: carrier scale 6.6e-3, Bessel saturation 2.1e-2, five gpi2 crosstalk terms of 3.0e-4, Debye-Waller 4.1e-6); detection threshold 0.5 at a 20 us window with (eps_B, eps_D) = (6.05e-4, 4.18e-4) from the record model, SPAM per qubit (6.1e-4, 3.5e-4) at zero crosstalk, preparation error 2.48e-6; Doppler nbar 3.5 to 11.6 falls to 0.0185/0.0154 on the gate modes after 40 sideband pulses; six branches carry all but 3.4e-5; parity contrast 0.99712, Bell bound (P00 + P11 + C)/2 = 0.99768 (0.99699 / 0.99763 in M6) | check_circuits.py 2 on the two-ion 171Yb+ fixture (global 355 nm pair at 100.93 kHz, 2.5 um addressing pairs with eps = 0.0220, oblique 369.5 nm cooling/detection beam, Crain SNSPD) | the row asks for agreement with the ideal statevector within the readout errors: \|P00 - 0.5\| = 1.5e-3 at a 7.9e-3 error bar; the exact spot check of the gate leaves 1.3e-4 while the (Omega/nu)^2 carrier scale bounds it at 6.6e-3 | recomputed here |
| `anchor.m6.bessel_force_saturation` | 1 - F = sin^2(pi f/2), f = 1 - (J_0 + J_2)(4 Omega/mu); and the reported deficit 1 - Omega_{n+1,n}/(Omega eta sqrt(n + 1)) | 9.6 row 3 ('the intrinsic budget lists off-resonant carrier, Bessel force saturation, residual displacement, Debye-Waller'); 4.4.1 (Roos Eq. 17); 4.3.1 | Section 9.6's 'Bessel force saturation' is Roos's carrier correction: the spin-dependent force is scaled by (J_0 + J_2)(x) at x = 4 Omega/mu (Omega the tone Rabi frequency, mu the tone-to-carrier detuning), so an uncalibrated pulse solved with the linear force reaches chi (1 - f)^2 with f = 1 - (J_0 + J_2)(x) = x^2/8 + O(x^4), an angle error pi f/2 at chi = pi/4 and a Bell-state infidelity sin^2(pi f/2); intrinsic_budget reports it per played MS gate as '<gate>.bessel_saturation' at the waveform's largest amplitude and smallest \|mu\| and sums it into 'total'. On the Section 11.1 pulse (Omega/2pi = 64.148 kHz, mu/2pi = 3.01 MHz) x = 0.0852, f = 9.1e-4 and the term is 2.0e-6, far inside the row's 1.1e-4 as the row's sum requires. The sideband element's Lamb-Dicke deficit f_LD = 1 - Omega_{n+1,n}/(Omega eta sqrt(n + 1)), with Omega_{n+1,n}/Omega = e^{-eta^2/2} (eta/sqrt(n+1)) \|L^{(1)}_n(eta^2)\|, is reported as '<gate>.sideband_lamb_dicke_deficit' (worst case over the gate's ions and carried modes at the cap's top Fock index) and NOT summed | Roos 2008, New J. Phys. 10, 013002, Eq. 17 (qutip_trap/validation/two_qubit_closed_forms.roos_force_saturation, run/job.roos_bessel_saturation); Wineland et al. 1998, NIST J. Res. 103, 259, Eq. 18 with the phase of Eq. 21 (hilbert/operators.rabi_matrix_element, run/job.sideband_lamb_dicke_deficit) | the row names four contributions and the M6 budget carried three; the first M6 fix added the Wineland Lamb-Dicke deficit under the row's name and summed it (3.06e-2 at the Section 11.1 pulse's cap top n = 11, which dominated every budget total although that Fock state is unpopulated), while the row's term is Roos Eq. 17 (PLAN.md 4.4.1: 'the force is saturated by (J_0 + J_2)' at argument 4 Omega/delta). The 2026-09-08 consolidation entered the Roos term under the row's key as an infidelity scale and moved the Lamb-Dicke deficit to its own reported key outside the sum: its thermal mean is exactly the force rescaling the s^2 calibration of Section 4.4.7 (7) absorbs and its thermal spread is the Debye-Waller term (ballance_thermal_error) the total already carries, so summing it double counted. Both are upper bounds on what survives calibration, not estimates of it | corrected |
| `anchor.m6.bv_crosstalk` | Bernstein-Vazirani with the secret 01 under addressing crosstalk (Section 9.6 row 6; Wright's minimal model) | 9.6; 3.3; 6.6 | exact register data-string populations 00: 1.82e-3, 01: 0.998, 10: 7.2e-7, 11: 4.02e-4; declared through the POVM 00: 2.43e-3, 11: 7.48e-4, so the 1 -> 0 flip of the secret exceeds the 0 -> 1 flip by 3.25 (M6 without the light-shift physics: 1.81e-3 / 3.87e-4, 2.41e-3 / 7.32e-4, ratio 3.30) with per-ion eps_B = 6.11e-4 against eps_D = 3.47e-4; the coherent-only matrix model (compiled circuit, eps theta rotations on the neighbours, ideal MS) gives 00: 2.22e-3 and 11: 4.11e-4, a ratio 5.39 before any readout; register 1 - F = 4.413e-3 (4.344e-3 in M6) inside the budget; 4000-shot histogram 011: 0.9942, 001: 0.0035, 010: 0.0013 | check_circuits.py 4 on the three-ion fixture (data ions 0 and 2, the middle ion the ancilla, eps = 0.0128 onto nearest neighbours) | the 1 -> 0 dominance the row predicts emerges, and mostly from the coherent circuit: the oracle CNOT maps the ancilla's crosstalk rotations (from the data ions' Hadamard pulses) onto its control, the secret's 1 bit, while the 0 bit sees only the ancilla pulses' direct rotations, and the readout asymmetry (eps_B > eps_D) then lowers the ratio from 5.4 to 3.3 (3.25 with the compensated light shift, M8); the magnitude (3.1e-3 total flip weight at eps = 0.0128) is that of this fixture, not Wright's 3 % applied twice, which the plan's 'stated magnitude' cannot fix without his beam profile | recomputed here |
| `anchor.m6.ca40_optical_preset` | device.presets.ca40_optical | 3.2; 3.4; 4.2.6; 8.4 | a 40Ca+ S1/2 mJ = -1/2 <-> D5/2 mJ = -1/2 optical qubit at B = 5 G along x, secular frequencies (3.0, 2.9, 2.0) MHz, one 729 nm quadrupole beam (5 mW, 200 um waist, k = (1,1,0)/sqrt 2, polarization at 135 degrees) whose DERIVED Delta m = 0 E2 carrier Rabi frequency is 34742.3 Hz, the 397 nm S1/2-P1/2 detection/Doppler light and the 866 nm D3/2-P1/2 repump along (1,1,1)/sqrt 3 at I/I_sat = 2 and 10, and Myerson's PMT chain (eps_sys = 0.19 %, 442 cps) at a 2 ms window. run() on GPi2(0): preparation error 6.11e-05, Doppler occupations nbar = 15.1 (axial 2.0 MHz), 9.6 and 10.0 (radial), every mode frozen or dropped so the space is [2], register 1 - F = 6.86e-04 (the two frozen radial modes' Debye-Waller loss at those occupations), 1102 Fock branches carrying all but 5.9e-02, SPAM (eps_B, eps_D) = (2.5e-04, 1.8e-03) at a 3 ms window and threshold 7.5 | tests/test_m6_ca40_optical.py; readout.presets.MYERSON_CA40_PMT (Myerson et al. 2008) | three preset choices are forced by limits of the first release and are not laboratory practice. (1) The axial mode is 2.0 MHz, not the 171Yb+ preset's 1.0 MHz: with no sideband-cooling stage the Doppler-limited eta^2 (2 nbar + 1) = 0.60 at 1.0 MHz, which the Section 4.2.8 (vii) Lamb-Dicke guard of the level-A rate model refuses above 0.25, and the quantity scales as 1/omega^2. (2) There is no sideband-cooling stage at all: Section 4.2.2's stage is pulsed RAMAN cooling on a two-beam pair (SidebandCoolingSpec.beams), and 729 nm resolved-sideband cooling on the quadrupole line is not scheduled. (3) The 854 nm shelf-reset beam is opt-in (reset_beam=True), because a Device's beams are always on and readout takes every resonant beam of the cycling cycle as detection light, so an ungated 854 nm beam depumps the shelf DURING detection: measured SPAM (0.573, 0.427) with it against (2.5e-04, 1.8e-03) without. Separately, the derived bright scatter rate peaks at R_o = 3.57e6/s at I/I_sat = (2, 10) and FALLS at higher intensity (1.99e6/s at (10, 30), 3.06e5/s at (50, 100)) because the magic-angle LINEAR polarization of the 397/866 cycle traps population in coherent dark states; that is 8 times below Myerson's 2.9e7/s, so the preset buys the readout back with a 2 ms window instead of his 420 us | recomputed here |
| `anchor.m6.check_circuits_oracle_regenerated` | validation/scripts/outputs/check_circuits.out after the M6 fixes | 9.6; 5.2; 11.3 item 2 | the lines the M6 FIXES moved, all in Section 5.2's dropped class and the Section 9.6 budget: pairs (0,1) and (1,2) of the three-ion fixture now report mode 4 'dropped' with an empty frozen-chi map; the GHZ run() line reports 4: 'dropped', 3 Fock branches (was 4) carrying all but 1.5e-3 (was 2.8e-3), boundary {3: 4.9e-15, 5: 8.6e-14} (was 2.7e-15, 7.8e-14), frozen contribution {} and the new clause 'dropped modes (0, 1, 2, 4, 6, 7, 8) contributing (2.4e-33, 4.9e-17) that nothing absorbs'; register 1 - F = 1.395e-2 (was 1.480e-2, the tilt's Debye-Waller factor no longer rescaling the drive, absorbed by the same spot check either way); the 'branches' field now prints Diagnostics.branches (the Fock sum) rather than Diagnostics.trajectories (branches x trajectories); and every budget total grew by the new Bessel force-saturation term - section 2 from 8.4e-3 to 3.0e-2 (bessel_saturation 2.1e-2), section 3 from 3.0e-2 to 7.8e-2, section 4 from 1.7e-2 to 4.1e-2 | uv run python validation/scripts/run_checks.py --only check_circuits --update (2824 s, 54 lines, max_rel_dev 0.0) | the same regeneration also picked up numbers that no M6 change touched, from other milestones' concurrent edits, and they are recorded here so a later reader does not attribute them to the dropped-mode fix: the derived carrier Rabi frequency of the two-ion fixture moved from 100.93 to 147.12 kHz (a change in the Raman derivation), which shortened T_rep from 7.222 to 5.566 ms and moved section 2's register 1 - F from 2.336e-3 to 2.381e-3, its gpi2 sideband scale from 8.3e-6 to 1.8e-5, its parity contrast from 0.99712 to 0.99715 and its Bell bound from 0.99768 to 0.99769; and the per-pulse photon-scattering estimates were restructured (P_raman 4.0e-6 -> 1.6e-5 per MS segment, P_rayleigh 4.0e-6 -> 5.3e-8), which moved section 4's Bernstein-Vazirani declared distribution from 2.43e-3 / 7.48e-4 (ratio 3.25) to 2.26e-3 / 7.58e-4 (ratio 2.98) and its register 1 - F from 4.413e-3 to 4.212e-3. The 1 -> 0 dominance the Section 9.6 row predicts survives at ratio 2.98. anchor.m6.bell_state and anchor.m6.bv_crosstalk still print the pre-change Rabi-dependent values; whoever owns that derivation should re-anchor them once it settles | recomputed here |
| `anchor.m6.compiler_templates` | ZXZXZ residual; e^{i pi v s/4}; 0.854 / 0.691 | 7.2 item 1; 7.7; 9.6 rows 'Compiled CNOT and CP', 'Native-gate identities' | 20 random SU(2) targets decompose to two GPi2 pulses plus virtual rz with max residual 1.5e-15; Maslov's CNOT equals CNOT up to e^{i pi v s/4} for all four (s, v) (+0.25 pi, -0.25 pi, -0.25 pi, +0.25 pi); CP(theta) = RZ(theta/2)^x2 ZZ(-theta/2) is exact for every theta, Debnath's fixed-RZ(pi/2) template has \|Tr\|/4 = 0.8536 at pi/2 and 0.6913 at pi/4; the whole compiled Bell circuit reproduces its target up to the residual frame to 5e-16 with 6 pulses (one entangling) | check_circuits.py 1; tests/test_compiler.py | the Section 9.6 virtual-Z pin (RZ(0.1) then GPi2(0) equals GPi2(-0.1) followed by RZ(0.1)) is the frame-propagation rule of the compiler, tested as matrices and through the pipeline | recomputed here |
| `anchor.m6.dropped_mode_debye_waller` | 1 - DW(0, eta)^2 of a dropped mode | 5.2; 9.8 row 3; 4.3.1 | the three-ion fixture's tilt mode 4 has eta = 0.0809 on one ion of each adjacent pair and 0 on the other, so its carrier Debye-Waller factor at n = 0 is 0.996731 and 1 - DW^2 = 6.53e-03, while its loop contribution is \|alpha\|^2 (2 n + 1) = 3.5e-33 and \|chi\| = 0. The other two x modes: mode 3 eta = 0.049/0.098, 1 - DW^2 = 9.57e-03; mode 5 eta = 0.0642 on both, 4.11e-03 | M6 measurement on tests/m6_fixtures.circuit_fixture(3, address_waist_m=2.0e-6), adjacent pairs (0,1) and (1,2) | Section 5.2's drop criterion reads the LOOP pair (\|alpha\|^2 (2 n + 1), \|chi\|) and never eta or nbar, so it can drop a mode whose Debye-Waller factor is 6.5e-03 - four thirty-odd orders above the summed dropped contribution 9.8 row 3 says bounds the fidelity change. The row still holds for a CALIBRATED gate, because the exact spot check and the run drop the same mode and the s^2 rescaling of Section 4.4.7 (7) absorbs the mode-independent part of the factor; what is not absorbed is the shot-to-shot variance of DW(n_m, eta_m) over the thermal distribution of n_m, which is negligible only because the dropped modes of these fixtures are sideband cooled to nbar < 0.05. A HOT dropped mode with a large eta would break the row, and the criterion would not notice: the honest guard is a drop test on eta^2 (2 nbar + 1) beside the loop pair, which this milestone did not add | recomputed here |
| `anchor.m6.ghz_fixtures` | three- and four-ion Molmer-Sorensen GHZ fixtures (Section 9.6 row 2) | 9.6; 5.2; 5.1.1 | three ions, global symmetric pulse (Omega/2pi = 151.67 kHz, 50 us) at t = pi/(8 chi): pairwise closed-form chi 0.7854/0.8801/0.7854; exact fidelity 0.98230 against the pairwise target and 0.97346 against the ideal exp(-i pi/4 sum XX) with the top two modes resolved at d_m = 12 (dimension 1152), P(000) = 0.2371, boundary 2.9e-9, frozen zigzag \|alpha\|^2 (2n+1) = 1.1e-3 and chi = 0.0196 rad; four ions (dimension 2304): 0.96718 / 0.93734, P(0000) = 0.4654, P(1111) = 0.4720, boundary 8.2e-10; H, CNOT(0,1), CNOT(1,2) through run(): space [2, 2, 2, 11, 13] with the tilt DROPPED (its contribution pair 3.5e-33 / 0 sits below Section 5.2's (1e-6, 1e-4), so after the M6 fix it gets neither a Debye-Waller factor nor a Fock branch: 3 branches carrying all but 1.5e-3, against 4 and 2.8e-3 while it was frozen), adjacent-pair spot checks 0.99530 with 4.4e-3 leakage, register 1 - F = 1.395e-2 (1.480e-2 with the tilt frozen, 1.483e-2 in M6) inside the budget, 2000-shot histogram 000: 0.5120, 111: 0.4825; summed dropped contribution (2.4e-33, 4.9e-17) over modes (0, 1, 2, 4, 6, 7, 8); a displaced \|alpha\| = 1 state at d_m = 12, 8, 6 holds 1.1e-7, 5.9e-4, 1.9e-2 in its top two levels | check_circuits.py 3, 3b and 5 on the three- and four-ion fixtures (x modes 2.569/2.8284/3.0 MHz and 2.2013/2.5681/2.8284/3.0 MHz; 2.0 um addressing waist, eps = 0.0128) | the plan's 'two resolved modes at d_m = 12' holds only with the zigzag frozen and its residual reported (the symmetric pulse closes the COM loop only); the outer pair (0, 2) couples three modes (\|chi\| = 0.39 on the tilt, dimension 8000 > 4096) and waits for GATE_LOCAL (M9a); the seven-segment adjacent-pair gate's 4.4e-3 leakage sits inside its (Omega/nu_min)^2 carrier scale | recomputed here |
| `anchor.m6.native_gate_identities` | the six Section 9.10 native-gate identities over a phase sweep | 9.10 rows 'Native-gate identities' and 'Native-gate axis mapping' | max deviations over the sweep: GPi(phi)^2 = I exactly (0.0); GPi2(phi)^2 = -i GPi(phi) 2.2e-16; GPi2(phi + 0.5) = GPi2(phi)^dag 7.1e-16; GPi2(phi) = R(pi/2, 2 pi phi) 1.1e-16; GPi(phi) = i R(pi, 2 pi phi) 6.1e-17; VZ(t) G(p) VZ(t)^dag = G(p + t) 4.0e-16 for both natives; the Hilbert-Schmidt overlap \|Tr[GPi2(0.5)^dag RX(+pi/2)]\| = 2.2e-16, the row's 'exactly 0' (GPi2(0.5) = RX(-pi/2) and the two differ by Z, traceless against I) | tests/test_native_gates_and_ionq.py (eight phases over a full turn, three frame shifts, atol 1e-12) | the M6 tests asserted only GPi2(phi) = R(pi/2, .) at three turn values and GPi = cos X + sin Y (equivalent to i R(pi, .)); four of the six identities and the 'exactly 0' overlap had no assertion at all, and none was checked over a phase sweep | recomputed here |
| `anchor.m6.section_11_1_native_identity` | the Section 11.1 pulse against MS(0, 0, pi/2) | 9.6 row 'Native-gate identities'; 11.1 | the exact play (no Lamb-Dicke expansion, no RWA), calibrated to chi = pi/4 by the exact spot check at Omega_tone/2pi = 64.148 kHz, reproduces the native matrix from \|00>\|0> with 1 - F = 4.659e-05, inside the off-resonant carrier excitation (Omega_tone/(2 nu))^2 = 1.1430e-04 which is the row's 1.1e-4 (1.1228e-04 at the closed-form amplitude, before the 0.9 % spot-check correction); the same pulse with lamb_dicke_order = 1 and rwa = True in the interaction frame gives 1 - F = 2.32e-10, three orders inside the row's 1e-6, which is what the clause 'to 1e-6 only with lamb_dicke_order and rwa on' asserts. The closed-form closure eta Omega/eps lands at 0.499583 rather than the plan's 1/2 (a 8.3e-4 correction from the \|alpha\|^2 term of the Section 4.4.3 kernel); the Bessel force saturation at the top of the carried range (n = 11, eta = 0.0786) is 3.055e-02 | tests/test_m6_native_budget.py on the M4 two-ion 171Yb+ fixture (x-COM 3.0000 MHz, eta = 0.078577 on both ions, tones inside the sideband at eps/2pi = 10 kHz, one 100 us loop, d_m = 12, joint dimension 48, no noise and no light shift) | the row states the budget's off-resonant carrier term as 'approx (Omega/nu)^2' AND its value as 1.1e-4, and the two disagree by a factor 4: (Omega_tone/nu)^2 = 4.49e-04 while (Omega_tone/(2 nu))^2 = 1.14e-04. The VALUE is the physically correct one (the off-resonant excitation probability of a tone detuned by nu is (Omega/2 nu)^2, and the measured infidelity 4.66e-05 is about 2 x 1.14e-04 / 4 for the two ions), so run.job.intrinsic_budget's carrier_scale key - which computes (2 pi peak/nu_min)^2 = 5.144e-04 on this fixture, using the smallest mode frequency - overstates that term by 4.5 and every reported budget total with it (the two-ion Bell fixture's carrier scale 6.6e-3 included). Left as it stands rather than changed here, because the change tightens the budget bound that anchor.m6.bell_state and tests/test_run_circuits.py assert against and belongs with a re-derivation of the whole 4.4.7 budget | recomputed here |

#### Noise and error channels (Section 9.7; M7)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m7.decoupling_high_precision` | Section 9.15's mpmath rows, in the package | 9.15 rows 'Bang-bang suppression orders (mpmath >= 200 dps)', 'IR convergence on a 1/omega^4 spectrum', 'Filter-function limits and parities', 'END-TO-END dephasing normalization'; 6.9 | all reproduced at mp.dps = 220-250. Carr-Purcell: amplitude Taylor order 3 at every even n with c_3 = i/(8 n^2) = 0.03125i, 0.0078125i, 0.003472222222i, 0.001953125i at n = 2, 4, 6, 8 (the plan's four printed values), and order 2 at odd n with c_2 = -1/(4 n^2); \|c_2\|^2 = 1/(16 n^4) and \|c_3\|^2 = 1/(64 n^4) ARE the plan's two printed CPMG coefficient laws. UDD: order n + 1 for n = 2, 3, 4, 5, 6, 8, 10, 12 with every lower coefficient below 10^{-110}, and F/(omega tau)^{2n+2} = 1/(16^n (n!)^2) to 12 digits for n = 1..8. IR convergence at tau = 1, tau_pi = 0.05, omega_max = 50: chi(1e-7) -> chi(1e-8) = 2.48719319 -> 24.8683571 (ratio 9.9985627) for UDD 3 and 2.48684517 -> 24.868009 (9.999822) for UDD 5, against 1.20925691e-4 and 2.39513137e-5 at ratio exactly 1.0 for n = 4 and 6. Green Eq. 45: 4.75678006346 against Biercuk's 0.0396431177199 at n = 1, identical at even n, \|R_zz(0)\|^2 = 4 exactly at n = 1, 3, 5. <a_1^2> = 0.06875600894468 from the double integral of C(t1 - t2), independently of the spectral integral | M7 audit E-14 (noise/decoupling.biercuk_taylor_coefficients_mp / leading_taylor_order_mp / biercuk_filter_function_mp / chi_integral_mp; tests/test_decoupling_mpmath.py) | the even-n IR rows CANNOT be reproduced in float64 and this is now asserted as a negative control: the even-n finite-pulse F is (omega tau)^2 (omega tau_pi)^4/64, which at omega = 1e-6 is 1e-43 of an O(1) sum, so double precision returns omega-independent cancellation noise of order 1e-32, which turns the omega^-4 integrand into omega^-6 and makes the package report chi(1e-7) = 128.56 for UDD-4 and 92.83 for UDD-6 (ratios 107 and 19) against true values of 1.2e-4 and 2.4e-5. The retired claim in conv.filter_function_machinery that check_composite.py's sections 9-15 are 'reproduced by noise/decoupling.py' was true for the finite-pulse collapse, the gated Biercuk values and the dc floors, and false for these rows | recomputed here |
| `anchor.m7.dephasing_correlation` | N = 3 local against global sigma_z noise under the derived bounds | 9.7 row 4; 4.4.7 | the losses sit below N t_g/(2 T2) = 3e-3 (local) and N^2 t_g/(4 T2) = 4.5e-3 (global), with global > local; Bermudez's printed forms are 4 and 8 times the bounds | check_noise.py 3 (force model, one loop, T2 = t_g/2e-3) | the derived forms are upper bounds (Cov_t <= 1), reproduced as bounds and ordering, not as equalities | recomputed here |
| `anchor.m7.gate_budgets` | eps_h = ndot t_g/(2K), eps_d = alpha_K t_g/tau through the exact engine | 9.7 rows 1-2; 6.2 | K = 1, 2 at 400 quanta/s: loss/(ndot t_g/(2K)) = 0.96-0.97; motional dephasing at tau = t_g/5e-3: loss/(alpha_K t_g/tau) = 0.96; the two-mode closure played with the rocking mode frozen over-rotates the gate and returns 0.84 and 0.80 | check_noise.py 1 on the two-ion 171Yb+ fixture (COM gate closed on the COM alone, rocking frozen, d_m = 12, mesolve) | Ballance's forms hold for the gate as calibrated; a fixture that closes two modes and resolves one is not that gate | recomputed here |
| `anchor.m7.hardware_chain_realistic` | 0.999923 (realistic 50 ns chain) against 0.999868 (ideal modulator) | 7.10; 9.6; conv.response_phase_reference | with the 50 ns AOM rise time and the Roos arctan(2 pi mu tau_r) beat-phase reference the exact spot check gives fidelity 0.999923 and leakage 3.315e-5, against 0.999868 and 4.485e-5 for an ideal modulator - the plan's 0.99992 against 0.99987. An ideal modulator's response is the identity, so SolverOptions(hardware_chain=False) is bit-identical there | M7 audit E-24 (tests/test_hardware_chain.py on circuit_fixture(2) with make_hardware(realistic=True)) | the test used to run circuit_fixture(2), whose hardware is make_hardware() with aom_rise_s = 0.0, so response_phase_rad returned 0 and no modulator response was applied at all: it exercised DDS word rounding only and the M7 finding its own docstring quoted lived solely in check_noise.py section 7. The 0.99700 uncompensated leg is still not reachable from a test, because the scheduler applies the arctan reference unconditionally and no option disables it (audit E-23, control/schedule.py:613, left for the coordinator) | recomputed here |
| `anchor.m7.hughes_two_term` | I = (2 nbar + 1)\|alpha_t\|^2 Var(S_alpha) + (delta_theta^2/4) Var(S_alpha^2) | 9.7 row 'Mode-frequency fluctuation'; 6.2 | alpha_t = int_0^{t_g} eps(t) gamma(t) dt and delta_theta = 2 int_0^{t_g} eps(t)\|gamma(t)\|^2 dt with H_e = hbar eps(t) a^dag a (Eq. 29) and U_g = exp{(gamma a^dag - gamma* a) S_alpha} exp{-i (theta/2) S_alpha^2} (Eq. 27). For a CONSTANT fractional offset on a symmetric K-loop gate \|alpha_t\| = (pi sqrt K/2)(eps/delta) and delta_theta = (pi/2)(eps/delta), the second being K-INDEPENDENT. Verified against exact two- and three-ion propagation of the plan's own force model: the exact-to-closed-form ratio extrapolates to 1.0000 with a linear-in-eps remainder (0.990568, 0.995311, 0.997671, 0.998873 at eps/delta = 4e-3, 2e-3, 1e-3, 5e-4 for N = 2, K = 1, nbar = 0 -> 1.000076), at every (N, K, nbar) in {(2,1,0), (2,1,1), (2,2,0), (3,1,0), (3,1,2), (2,3,0)} | Hughes, Srinivas, Loeschnauer, Knaack, Matt, Ballance, Malinowski, Harty and Sutherland, 'Trapped-ion two-qubit gates with >99.99% fidelity without ground-state cooling', arXiv:2510.17286 (2025), Appendix B 'Infidelity Due to Mode Frequency Fluctuations', Eq. 33; read from the source because the plan transcribes only the first term | the source's lambda^2_{S_alpha} and lambda^2_{S_alpha^2} are the VARIANCES of the two spin operators over the input state, not the bare moments <S^2> and <S^4>: the first coincides with <S_alpha^2> = N because <S_alpha> = 0 on \|0...0> (which is why the plan prints it that way), but the second is <S^4> - <S^2>^2 = 2N(N - 1) and reading it as <S^4> = 3N^2 - 2N overshoots by a factor 2 at N = 2 and 1.75 at N = 3. The exact propagation discriminates them | extracted |
| `anchor.m7.intensity_noise_factor_two` | RETIRED: superseded by anchor.m7.intensity_noise_n_ions | 9.16 row 4.4-8; 4.4.7; 6.4 | this record read the N = 2 measurement A/(Gamma_I t_g eta^2/2) = 2.00 as a factor 2 and inferred an open Gamma_I convention. Extending the fit to all five (N, K) the row asks for shows the factor is N (1.00, 2.00, 3.01, 2.00, 2.00, 3.00 at (1,1), (2,1), (3,1), (2,2), (2,3), (3,2)), so the plan's derived A is EXACT at N = 1 with the carrier-contrast Gamma_I and the convention is settled | M7 audit E-2 | see anchor.m7.intensity_noise_n_ions for the derivation, the six measurements, the global-versus-per-beam discriminator on B/A, and the reasons 9.16 row 4.4-8's own ratios are not reproducible | corrected |
| `anchor.m7.intensity_noise_n_ions` | A = N Gamma_I t_g eta^2/2, B = A x 3(N - 1)/(4K) | 9.16 row 4.4-8; 4.4.7; 6.4 | in the sigma_alpha eigenbasis block s closes with a residual displacement s x delta_alpha, <\|delta_alpha\|^2> = f^2 (2 Gamma_I/Omega^2) t_g with f = eta Omega/2; the pair (s, s') decoheres by exp(-\|s - s'\|^2 <\|delta_alpha\|^2>(2 nbar + 1)/2), so 1 - F = Var(sum_i s_i)(2 nbar + 1)(eta^2/2) Gamma_I t_g and Var = N for \|0...0>. Measured A/(Gamma_I t_g eta^2/2) = 1.00002, 2.00035, 3.00622, 2.00003, 1.99998, 3.00038 at (N, K) = (1,1), (2,1), (3,1), (2,2), (2,3), (3,2), and B/plan_B = 1.99939, 2.99441, 1.99989, 2.00005, 2.99922 - a factor N in BOTH terms. B/A = 3(N-1)/(4K) exactly (0.74964, 1.49411, 0.37497, 0.25001, 0.74971) | M7 audit E-2, re-derived and re-measured independently of check_noise.py (force model, d = 40, k = 2e-6, eta = 0.1, mesolve atol 1e-12; tests/test_noise_budgets.py) | the Gamma_I convention is SETTLED, not open: the plan's derived A = Gamma_I t_g eta^2/2 is exact at N = 1 with Gamma_I the carrier-contrast rate k Omega^2, so the retired reading of this record (that the plan's A pairs with half the contrast rate or another fidelity measure) was a wrong inference from the N = 2 case. What the plan's eps_I first term is missing is a factor N for an N-ion drive; the same Var(sum s_i) = N is Hughes's lambda^2_{S_alpha} (anchor.m7.hughes_two_term), which is the cross-check that fixes it. 9.16 row 4.4-8's A/(eta^2 Gamma_I t_g/2) = 0.997-0.999 and B/[3(N-1)/(8K)] ~ 1.00 across all five (N, K) are therefore NOT reproducible in this model - both are N times larger - and the row's model must divide the collective operator's variance by N (equivalently define Gamma_I as N times the single-ion contrast rate). New: only a GLOBAL intensity-noise operator gives B/A = 3(N-1)/(4K); N independent per-beam operators leave A(N) unchanged but halve the ratio to 3(N-1)/(8K) (measured 0.37469, 0.74563, 0.18746 at (2,1), (3,1), (2,2)), so the plan's B term is itself evidence that its eps_I describes one laser shared by the register | contested |
| `anchor.m7.landsman_parallel_channel` | (1/2)\|\|E\|\|_diamond <= sum_rs \|Theta_rs\|, against the exact channel | 9.7 row 'Parallel gates'; 6.6 | the diamond norm is by definition the supremum of \|\|(E (x) I)(rho)\|\|_1/2 over the DOUBLED space, so a sampled input gives a lower bound and the inequality is testable in the direction that can falsify it. Lower-bound-to-bound ratios: 0.6686 at (0.1, -0.05, 0.02, 0.01), 0.5533 at four equal 0.01, 0.5588 at (0.6, 0.5, 0.1, 0.1), 0.4947 at four equal 0.3, 0.2880 at four equal 0.8, and 0.9996 with a SINGLE non-zero phase - the sum is a triangle inequality and is saturated only there | M7 audit E-15 (tests/test_noise_budgets.py, on the four-qubit commuting unitary exp[-i sum Theta_rs Z_r Z_s] built directly, NOT through schedule(parallel=True), which serializes MS gates) | Section 6.6's fallback is confirmed numerically rather than asserted: wherever the bound exceeds 1 (Theta sums of 1.2, 1.3, 3.2) the exact channel's (1/2)\|\|E\|\|_diamond is still below 1, so the bound says nothing and the simulated channel is the answer. What is NOT done is an exact diamond norm (an SDP): the test brackets the true value from below and the bound from above, which is enough to falsify the inequality but not to quote (1/2)\|\|E\|\|_diamond itself | recomputed here |
| `anchor.m7.ou_field_heating` | OU field noise: slope ratio and MC | 9.16 row 4.1-3 | e^2 S_E^(1)/(4 m hbar omega) = 9.97506e-4 reproduced; the exact double integral <n>(200)/200 gives 0.99975 of it (the row's 0.99991 belongs to an estimator the audit did not specify); a symplectic Monte Carlo of 4000 classical trajectories agrees within its error | check_noise.py 6 (sigma^2 = 0.02, tau_c = 0.05, omega = 1, e = m = hbar = 1) | rival prefactors excluded: (1/2) S^(1) gives 2.0, (1/4) S^(2) gives 0.4997 | recomputed here |
| `anchor.m7.xy_n_t2_anchors` | (XY)^N T_2 as the tau where chi = 1, against Egan 2.84(16) s and Harty 50(10) s | 9.7; 6.3; 6.9 'evaluated as W = e^{-chi} rather than by an exponential-decay fit' | T_2 is obtained by bisection on chi(tau) = 1 through the package's own XY4 machinery on a 1/f background carrying Lorentzian lines at the mains fundamental and its first four harmonics. Consistency-anchor semantics: no source supplies its S_b, so the test inverts the anchors and reports the background each implies, asserting only that chi is monotone in tau and in the level, that Harty's 50 s needs a quieter spectrum than Egan's 2.84 s, that the implied levels are physically plausible, and that XY4 buys more than 20x over free induction on the same spectrum | M7 audit E-8 (tests/test_m7_closed_forms.py, through filter_function on a mains-carrying 1/f S_b) | the mains lines are load-bearing, not decoration: a Hahn echo whose filter maximum sits on the 60 Hz line sees more than twice the chi of the same 1/f background without lines, and deep in the echo's omega^4 asymptote (tau = 1 us) the lines still carry 3.367x the background's integrated power. The line's contribution falls as tau^4, measured 9975.4 per decade against the asymptotic 1e4 | background |

#### Calibration emulation (Sections 7.5, 9.17; M8)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m8.carrier_debye_waller_every_mode` | Omega_fit = Omega prod_p <e^{-eta_p^2/2} L_n(eta_p^2)> | 7.5 item 1; 4.2.7 iii | the carrier Rabi curve's Debye-Waller reduction is the product over EVERY mode the drive couples to (Wineland 1998 Eqs. 122-124), spectators included: with the fit carrying the driven mode only, the two-ion fixture's fitted Omega came out 400 Hz (0.4 %, nine standard errors) below the derived 100.93 kHz, the frozen COM mode's e^{-eta^2/2} = 0.9969; with both modes at the thermometry's occupations it agrees within 1.7 standard errors | check_calibration.py 5; experiments/fitting.py multimode_rabi_model | - | recomputed here |
| `anchor.m8.compensation_phase_reference` | 1 - F of a compensated GPi2 at t_start = 0, 1 ms, 2 ms | 7.5 item 7; 7.10 | with the tone phase referenced to t = 0: 2.9e-5, 1.45e-2, 5.7e-2 for delta_s = -38.4 Hz, i.e. sin^2(pi delta_s t_start) / 2 = (2 pi delta_s t_start)^2/4 for small angles (0.24 and 0.48 rad); with compensation_phase_rad: 2.9e-5 at every start. A five-segment MS gate 500 us into a schedule with the per-gate beat reset the fixture's hardware uses: 1.3e-4 as at t = 0 (4.8e-4 with the beat note left running, Roos's tilt, Section 7.10) | tests/test_calibration_layer.py (phase reference); scratch probe on the two-ion 171Yb+ circuit fixture, JOINT_EXACT | the first M8 build carried the compensation detuning without the phase reference; the Bell circuit through run() read 4.196e-3 (2.353e-3 in M6, 2.333e-3 with the reference) and the check_circuits three-ion GHZ 5.25e-2 (1.48e-2 in M6) | recomputed here |
| `anchor.m8.crosstalk_phase_protocol` | pi/2 - pi(crosstalk) - pi/2(phi): phi_0 = 2 theta_x + const | 7.5 item 8 | a pi rotation about the crosstalk axis reflects the neighbour's equatorial Bloch vector across the axis, so the analysis fringe phase carries twice the axis angle with full contrast; the two-ion fixture's axis is at 0.00 +- 0.02 rad relative to the neighbour's frame (the addressing beams' Delta k is perpendicular to the chain, so the geometric phase is zero) | check_calibration.py 3; experiments/light.py crosstalk_scan | a pi/2 crosstalk rotation (the first draft) about an axis parallel to the preparation pulse's maps the equatorial state onto the pole and gives no fringe at all | recomputed here |
| `anchor.m8.entangling_scan_frame_error` | d correction_rad[i] under a frame error delta_f | 7.5 step 3; 7.3 | with qubit_shifts_hz = {0: delta_f} the phase scan's per-ion corrections move by +0.384025 rad on ion 0 at delta_f = 1 kHz and by -0.000362 rad on ion 1 (the shift lands on the shifted ion alone, because the corrections are the half-sum and half-difference of the two inputs' offsets), against 2 pi delta_f t_gate = 0.628319 rad over the 100 us gate: a factor 0.611, because the reported offset is in SPIN-PHASE units (the constant spin-phase offset that would give the same parity fringe shift) and a drift growing across the pulse shifts the fringe by less than its final size; the amplitude-weighted time of the five AM segments and the analysis pulse's own timing set the factor, which is not derived here. The dependence is linear in delta_f (the test asserts the 2 kHz shift is twice the 1 kHz one to 5 %) | tests/test_m8_calibration_fixes.py; scratch probe on the two-ion 171Yb+ circuit fixture, exact populations, four spin phases, inputs 00 and 01 | with qubit_shifts_hz discarded the corrections were identical for any table frequency (+0.000160 and +0.000089 rad, the exact-population noise floor): the fixture's own 6.75 Hz fitted uncertainty is only about 4e-3 rad over the gate, but a stale table's error is unbounded and the phase scan wrote corrections that omitted exactly the error run() then applied | recomputed here |
| `anchor.m8.full_calibration_two_ion` | calibrate(surrogate=False) on the two-ion 171Yb+ fixture | 7.5; 10 M8 exit criteria | every entry the experiments fit (field, qubit frequencies, mode frequencies, occupations, Rabi frequencies, light shifts, crosstalk ratios and phases, the entangling waveform's closure and phase alignment, detection) agrees with the device's derived truth within TWO of its reported standard errors, which is what tests/test_full_calibration.py enforces (SIGMA = 2.0); the realized deviations printed in outputs/check_calibration.out section 5 are all at or below 1.02 sigma, so the plan's 'within the uncertainty the fits report' holds in fact and 2 sigma is the reproducible bound the suite asserts. A Bell circuit run from the fitted table reaches a register infidelity inside the intrinsic budget PLUS the calibration's own contribution (the per-pulse over-rotation (sigma_Omega/Omega x pi/2)^2 and the residual frame offset (2 pi sigma_f t_gate)^2, with no slack factor) and strictly ABOVE the surrogate-table run's, whose seeds are the derived values (numbers in outputs/check_calibration.out) | check_calibration.py 5 (reduced scans: 400 shots per point) | the first M8 build asserted 4 sigma per entry and, for the fidelity, budget + 3 x cal + 2e-3 with no lower bound, which would have passed with a substantially worse calibration and equally with a calibration whose errors had disappeared (a table played as the physics, the played chain switched off, or the frame taken at the true transition); the claim 'within its reported uncertainty' described neither | recomputed here |
| `anchor.m8.micromotion_nulls` | shim null, residual beta | 7.5; 4.1.1; 4.3.6 | a 30 V/m stray field on a single 171Yb+ ion at Omega_rf = 2 pi x 30 MHz gives beta = 0.245 along the Raman Delta k; the sideband-ratio scan through the exact e^{i beta cos Omega t} modulation nulls it at -30.00 +- 0.46 V/m with a residual \|beta\| of 2.5e-4, and the rf-photon-correlation periodic steady state of the 369.5 nm detection beam, retuned to -Gamma/2 and projected on the atom's response phase (odd in the signed index, linear to 15 % at beta = 0.2), nulls a 20 V/m field at -20.0000 +- 2.8 V/m from the exact signal (the photon budget of a 1 s acquisition at 3.8e5 photons/s and 4.4 % detection efficiency) and at -19.27 +- 0.83 V/m (0.87 sigma) with photon shot noise drawn over 10 s, residual beta -7.9e-5 +- 4.5e-3, on the two-ion fixture | check_calibration.py 4 | the first M8 build correlated the photon rate at the fixture's detection frequency (on resonance) and kept only the sin projection: there the first-order response vanishes (the rate is even in the detuning) and the first harmonic was \|S_1\| = 3.2e-5 at beta = 0.045 against 3.4e-2 half a linewidth to the red, so the null was found exactly but its declared photon-budget uncertainty was 3.3e3 V/m; the response lags the rf by 141 degrees at Omega_rf = 1.5 Gamma, and a fixed sin projection kept 63 % of the amplitude | recomputed here |
| `anchor.m8.run_level_over_rotation` | 1 - P(excited) after a GPi from a table 5 % high in Omega | 7.10; 7.5; 10 M8 bullet 2 | a table whose Rabi belief for ion 0 is 5 % high schedules the GPi 1/1.05 short (3.398572 us -> 3.236736 us, ratio 0.952381) and the played chain gives the ion Omega_req x Omega_derived/Omega_table = Omega_req/1.05, so the achieved rotation is pi/1.05 and 1 - P(ion 0 excited) rises from the 1.104e-04 baseline to 7.150e-03 against the analytic under-rotation cos^2(pi/2/1.05) = 5.585e-03 (a factor 1.28, the remainder being the baseline and the second-order light-shift term the shortened pulse leaves) | tests/test_m8_calibration_fixes.py; scratch probe on the two-ion 171Yb+ circuit fixture, JOINT_EXACT, branch_weight_min = 1e-3 | the M8 suite pinned the PLAYED envelope of a miscalibrated Rabi entry (envelope = requested/0.99) but nothing at the run level, where a played chain that silently played the request would have shown no error at all | recomputed here |
| `anchor.m8.sideband_light_shift` | delta_sb = Omega^2/(2 omega_m) | 7.5 item 2; 4.3.2 | a resonant sideband pulse light-shifts the qubit through its own off-resonant CARRIER coupling by Omega^2/(2 omega_m) (1.70 kHz at Omega/2pi = 100.9 kHz, omega_m/2pi = 3 MHz), pulling both the blue and the red sideband resonance toward the carrier, so blue - carrier and (blue - red)/2 both read the mode frequency low by it; the scan fits the blue centre at two rf amplitudes and extrapolates to zero power (the shift scales as the intensity), which recovers the mode frequency within its uncertainty | check_calibration.py 5; experiments/motion.py mode_spectroscopy | Section 7.5's fine scan of a single resonance at full power would have written the mode frequency low by 1.5 kHz into the table, above the FM solvers' sub-kilohertz need | recomputed here |
| `anchor.m8.stark_scan_per_beam` | delta_St = sum_beams delta_St,b | 7.5 item 7 | with one beam of the Raman pair on during the Ramsey delay there is no two-photon coupling and the fringe shift is that beam's differential light shift; the drive's shift is their sum (-19.2 - 19.2 = -38.4 Hz on the two-ion fixture, recovered within its uncertainty). The Ramsey fit returns \|probe - delta_St\|, so each beam's Ramsey is run at BOTH probe signs and the shift is the half-difference delta = (f_minus - f_plus)/2 with sigma = hypot(sigma_plus, sigma_minus)/2, the estimator ramsey_frequency already used for a qubit offset: exact while \|delta\| < probe, with f_plus + f_minus = 2 probe as the out-of-range detector (tolerance 4 sigma or 1e-3 probe, whichever is larger) and 1/(2 dt) of the delay grid as the Nyquist guard on probe + \|delta\| | check_calibration.py 3; experiments/light.py stark_scan | the both-beams-on measurement with the beat note detuned by Delta carries the odd coupling shift Omega^2/(2 Delta) (1.27 kHz at Delta = 4 MHz), whose fringe at probe minus shift aliases with the plan's Ramsey delays (Nyquist 2.5 kHz for six points over 1 ms); kept as mode='beat_note' with that caveat. The DEFAULT per-beam mode had the same aliasing unrecorded: it took the modulus of one fringe fit and reported probe - \|f\|, losing the sign of (delta_St - probe), so with probe = 1 kHz a true -38.4 Hz read -38.40, 300 Hz read 300.00, but 1500 Hz read +500.00 and 2500 Hz read -500.00, all four with converged=True and the \|shift\| < probe guard satisfied; kilohertz differential shifts are ordinary for Raman gates and CalibrationScans.stark_probe_hz defaults to 1 kHz. Fixed by the two-probe estimator above (anchor.m8.stark_scan_two_probe_branch). The mode switch also reached _setup, which reads a 'mode' keyword as a driven-mode INDEX, so stark_scan(mode='beat_note') raised int('beat_note') | recomputed here |
| `anchor.m8.stark_scan_two_probe_branch` | delta_St = (f_minus - f_plus)/2, f_plus + f_minus = 2 probe | 7.5 item 7 | the two-probe estimator on the exact fringe: delta_St = -38.4 Hz gives f_plus = 1038.40 and f_minus = 961.60, half-difference -38.400 exactly; 300 Hz gives 700.00 and 1300.00 -> 300.000; -900 Hz gives 1900.00 and 100.00 -> -900.000. Above the probe the estimator saturates at the probe: 1500 Hz gives 500.00 and 2500.00 and 2500 Hz gives 1500.00 and 3500.00, both refused by \|delta\| < probe AND by the sums 3000 and 5000 against 2 probe = 2000. The delay grid's Nyquist frequency 1/(2 dt) bounds probe + \|delta\|: five delays over 2 ms resolve 1000 Hz and refuse a 1 kHz probe with any non-zero shift, nine resolve 2000 Hz and accept it | tests/test_m8_calibration_fixes.py (the exact fringe \|detuning - delta\| at probe = 1 kHz, nine delays over 2 ms) | with one probe sign the fit's \|probe - delta\| loses the sign of (delta - probe): the same four shifts read -38.40, 300.00, +500.00 and -500.00, the last two silently wrong and marked calibrated with the \|shift\| < probe guard satisfied (see anchor.m8.stark_scan_per_beam). The two-ion fixture's per-beam shift is far inside the probe (-27.07 Hz each after the M0a P3/2 correction, -19.2 Hz before it), so no test or committed number caught it | recomputed here |

#### Scaling I: mode selection, frozen spectators, ENR, GATE_LOCAL (Sections 9.8, 9.9; M9a)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m9a.cap_rule_populated_range` | n_hi(max_t sum_i \|alpha_im(t)\|, nbar) at the 1e-6 tail | 5.1.1; 5.5; 11.3 item 2 | the cap of a resolved mode is the highest Fock index its thermal state displaced by the pulse's coherent excursion, max over the pulse of sum_i \|alpha_im(t)\| from the closed-form trajectories (the S = +-N spin branch), populates above the boundary threshold (sum_k P_th(k) \|<n\|D(i\|alpha\|)\|k>\|^2, the analytic elements) plus the Section 5.1.1 margin; the engine's margin check after every pulse reads the same definition with the threshold taken as a fraction of the MIXTURE's population (a Fock branch of weight w, evolved on its own by run() or the tomography, is measured at tail/w through the sample key branch_weight), so a first attempt does not trip: the two-ion Bell circuit at the default 1e-6 branch cutoff runs its twelve branches on [2, 2, 11, 13] with no growth, populated ranges 3 and 5 and margins of 7 levels | hilbert/operators.py populated_range; control/shaping.py excursion_by_mode; check_scaling.py 5; tests/test_run_circuits.py | the M6 rule sized the cap from the single-loop radius eta Omega_peak/eps through ceil((\|alpha\| + sqrt nbar)^2 + 2 sqrt(\|alpha\|^2 + nbar + 1/4) + 1): on the two-ion fixture's five-segment AM pulse the joint run populates n = 6 on the COM (n = 4 on the rocking mode) at the 1e-6 tail against the rule's 4 (3), so once the Section 5.5 policy was enforced every entangling pulse tripped, grew both caps by four levels and ran three times slower (80 s against 26 s for the Bell circuit); check_circuits.out and check_two_qubit.out regenerated with the changed caps (check_calibration.out re-run and unchanged to every printed digit) | recomputed here |
| `anchor.m9a.carrier_frozen_bound` | sum (eta Omega sqrt(n + 1)/(mu - l omega_m))^2 | 5.2 | a 100.9 kHz carrier pi/2 pulse on the two-ion fixture freezes the x modes (eta 0.078 to 0.081) with an off-resonant excitation bound of 3.1e-5 over the nearest sidebands of both modes, the (eta Omega/omega_m)^2 sideband scale of the intrinsic budget summed over sidebands and modes; the detuning guard \|mu - l omega\| > 20 eta Omega sqrt(n + 1) is a note, never a refusal; a tone exactly on a sideband reports an unbounded excitation | run/space.py frozen_excitation_bounds; tests/test_gate_local.py; tests/test_scaling_modes.py | - | recomputed here |
| `anchor.m9a.gate_local_bell` | GATE_LOCAL against JOINT_EXACT, two-ion Bell circuit | 5.4; 9.8; 6.8 | register populations (00, 01, 10, 11) JOINT_EXACT (0.508139, 0.000489, 0.001267, 0.490104) against GATE_LOCAL (0.508170, 0.000484, 0.001241, 0.490105), max difference 3.0e-5 against the reported bound 2.5e-4 (residual displacement 9.3e-5, the carrier steps' frozen-mode excitation 1.6e-4); register 1 - F 2.333e-3 against 2.287e-3; the MS step on the 572-dimensional gate-local space [2, 2, 11, 13]: average gate infidelity 1.14e-4, depolarizing rate 1.42e-4, twirl p_II 0.99986, residual \|alpha\| per spin eigenstate 2.7e-3 (rocking) and 9.1e-3 (COM), purity deficit of the reduced motional state 4.0e-2 and 3.4e-2, tracked nbar 0.02027 and 0.01726 against the joint run's branch-weighted reduced state; 344 engine runs (16 inputs x 3 branches per gate step) | check_scaling.py 6; tests/test_gate_local.py | the first M9a build compared the tracked occupation with the joint run's FIRST branch (the vacuum Fock branch, nbar 4e-5 after the gate) instead of the branch-weighted reduced state; the Section 9.8 row is a comparison of reduced states, so the weights of the initial mixture belong on the joint side | recomputed here |
| `anchor.m9a.gate_local_steps` | one step per GateTarget | 5.4 | a GATE_LOCAL step is every pulse of one gate piece as the scheduler recorded it (Schedule.targets), merged with whatever overlaps it in time; the Bell circuit splits into six gate steps and six dead-time idles | run/gate_local.py gate_steps; tests/test_gate_local.py | the first M9a build grouped pulses by time overlap alone, which split a five-segment Moelmer-Soerensen waveform into five steps with the motion traced out between them, discarding exactly the spin-motion correlations the gate builds across its segments | corrected |
| `anchor.m9a.register_cutoff` | GATE_LOCAL register against the JOINT_EXACT branches | 5.3; 5.4; 9.8 | on the two-ion fixture's GPi2 circuit the GATE_LOCAL register (the pumped density matrix carried whole) and the JOINT_EXACT register (the pumped mixture enumerated into branches) agree to 2.6e-9 when every branch is kept (branch_weight_min = 1e-9) and to 3.7e-6 at the default cutoff, the weight of the 2.5e-6 preparation-error branch JOINT_EXACT drops and reports (dropped_branch_weight); the Section 9.8 comparison is held to the reported bound plus that weight | tests/test_gate_local.py (single-qubit gate) | - | recomputed here |
| `anchor.m9a.tilted_frozen_spectator` | y-COM of the tilted-beam fixture | 5.2; 9.9; 11.3 item 1 | the two-ion 171Yb+ chain with the global 355 nm pair rotated by 6 degrees in the xy-plane couples the drive to the y modes with eta_y = -/+ 0.0085 against 0.078 to 0.081 on the x modes; a symmetric x-COM pulse (eps = 20 kHz, 50 us, 119.9 kHz per tone) leaves the y-COM (2.900 MHz, 120 kHz below the tone) with \|chi_m\| = 1.94e-3 rad and \|alpha\|^2 (2 nbar + 1) = 4e-32 (six closed loops) and the y-rocking mode (2.722 MHz) with 6.3e-4 rad and 1.6e-6: both frozen by the Section 5.2 classes; at eps = 30 kHz the y-COM's 3.1e-4 residual makes it resolved | tests/m9_fixtures.py; tests/test_scaling_modes.py | the fixture exists because the M4 to M8 fixtures drive along x exactly, where the y and z modes have eta = 0 and are dropped, so no frozen spectator with a measurable chi_m loss occurred anywhere in the earlier suites | recomputed here |
| `anchor.m9a.tomography_projection` | Dykstra onto CP and TP | 5.4; 6.8; 9.17 'GATE_LOCAL tomography' | the Choi matrix of MS(0.3, -0.7, pi/2) from its sixteen product inputs equals choi_from_unitary to 5e-16 with one Kraus operator; a depolarizing channel (eps = 0.05) reconstructed from outputs carrying 3e-4 of noise violates trace preservation at the noise level and the alternating projection returns \|\|Tr_out(Choi) - 1\|\| and the distance to the cone both below 1e-10, the map within 5e-3 of the truth, the twirl p_II = 0.95 and the depolarizing rate equal to the entanglement infidelity (Chen 2023's normalization) | dynamics/tomography.py; check_scaling.py 1; tests/test_tomography.py | a positive-semidefinite projection alone leaves Tr_out(Choi) != 1 and a map that changes the register's normalization on every application (the 2026-09-04 numerics critique); the projection ends on the trace-preserving set and reports what is left against the cone | recomputed here |

#### Scaling II: the matrix-free kernel and parallelism (Sections 9.9, 11.1; M9b)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m9b.cost_model_constants` | cost per drive term (Section 11.2) | 11.1; 11.2; 11.3 item 4 | one right-hand side through QobjEvo.matmul (dispatch included), least-squares fit over eleven spaces from dimension 48 to 8192: a factorized term costs about 2.9 us per factor step plus 0.58 ns per complex multiply-add on the D/d_ion amplitudes of the source level, an assembled CSR term 0.57 ns per non-zero (2^{N-1} prod_m d_m^2 for two-level ions); the kernel carries 2.0 us + 2.5 us per step + 0.6 ns per multiply-add against 0.5 us + 0.57 ns per non-zero, which reproduces the measured choice on every space: assembled at dimensions 48 (0.7 against 4.7 us) and 256 (5.2 against 8.2), factorized from the 440-dimensional Bell space (14.3 against 9.8) upward, 299 against 24 us at 2048 and 1196 against 69 us at 8192 (four ions, three modes at d_m = 8) | bench_ms_timing_v5.py part 1; dynamics/kernels.py kernel_costs_us; check_kernel.py 2 | Section 11.3's '24 against 512 amplitudes' counts multiply-adds; the measured gain is smaller and set by the fixed cost of each NumPy call (about 3 us per factor), so the auto rule is a measured crossover and not the amplitude ratio, and the assembled operator stays the right choice below dimension 440 | recomputed here |
| `anchor.m9b.factorized_kernel_exactness` | sigma_+ (x) prod_m D_m as factors | 11.3 item 4; 5.1.1; 5.2 | the drive operator held as its tensor factors (sigma_+ on the ion's factor, the oracle-checked per-mode exponentials D_m(i eta_m) on the resolved modes) and applied axis by axis (the ion factor a slice of the source level into the target level, every mode factor one batched matrix product) is the assembled operator to round-off: \|F psi - A psi\| = 2.4e-16 on the Bell space [2, 2, 10, 11] and 5.0e-16 at dimension 2048, max\|F - A\| = 0 element by element, the adjoint, the factor-wise F^dag F (9e-15) and the trace exact; registered as a QuTiP data-layer type so that QobjEvo, sesolve, mcsolve and the dop853/vern9 ladder run unchanged on it; the 20 us single-loop pulse through the engine with the kernel forced each way gives \|psi_assembled - psi_factorized\| = 2.2e-13 with identical populations (0.491426, 0.0252973, 0.0252973, 0.4579794) | dynamics/kernels.py; hilbert/space.py drive_operator_factorized; check_kernel.py 1; tests/test_kernel.py | an ENR group's sum-generator exponential is not a product of per-mode factors (Section 5.1.1), so a drive coupling to an ENR mode is assembled whatever the option says; a sum of two factorized operators has no factorized form and converts to Dense (the reason QobjEvo.compress must never merge the drive terms: every term carries its own coefficient object), and every mesolve segment builds the assembled CSR operator because the Liouvillian is formed from the matrix (Section 5.3) | recomputed here |
| `anchor.m9b.propagator_cache` | U(t, t_0) on internal-state-only spaces | 5.3; 11.3 item 5 | on a space with every mode frozen the segment propagator is integrated once as a D x D operator through the Section 5.3 ladder (an operator-valued state under sesolve) and applied to every initial state, cached per engine on the built Hamiltonian's value fingerprint (space, frequencies, the pulses' sampled tones, the sample's offsets and trajectories, the shifts); four inputs through one GPi2 segment cost one integration and three matrix products with \|psi_cache - psi_ode\| = 0; the tomography of a carrier step (3 frozen-mode Fock branches x 16 inputs) costs 3 integrations and 45 cache hits, one integration per distinct Debye-Waller factor | dynamics/engine.py _segment_propagator; check_kernel.py 5; tests/test_parallel.py | the plan's 'propagator with piecewise_t' route (a piecewise-constant generator with a reported segmentation error) is not needed: QuTiP 5 integrates the time-dependent propagator exactly, so no segmentation approximation is declared; the sample key branch_weight is left out of the fingerprint because it steers the margin check and no term of H | recomputed here |
| `anchor.m9b.section_11_1_kernel_rows` | Section 11.1 table, factorized column | 11.1; 11.3 item 4; 10 (M9b acceptance) | the 20 us pulse of the Section 11.1 fixture with one coefficient per drive term, dop853 at atol 1e-10, rtol 1e-8: factorized 0.25 / 0.45 / 1.11 / 3.15 s against CSR 0.04 / 0.35 / 4.76 / 42.49 s, unmerged dense 0.09 / 0.34 / 11.61 / 109.50 s and the merged dense operator of the plan's table 0.03 / 0.12 / 1.01 / 23.52 s at dimensions 48 / 256 / 864 / 2048 (vern9 within 25 %); every construction's final state agrees with the CSR one to 4e-13 under dop853 and to 6e-7 between the integrators; per right-hand side the factorized terms cost 13, 22, 45 and 90 us against 2, 17, 195 and 1208 us CSR | bench_ms_timing_v5.py part 2; tests/test_kernel.py (slow) | the plan's column for Sections 11.2 to 11.4 was the merged dense operator, which a shared coefficient function produces and the real builder cannot (every drive term carries its own scale factors); against it the kernel is 7x faster at dimension 2048 and slower below 864, so the 'twentyfold' amplitude ratio of Section 11.3 is a measured 14x against CSR at 2048 and a loss at 48 and 256 that the auto rule avoids | recomputed here |
| `anchor.m9b.workers_reproducibility` | 1 against N workers | 3.4; 9.9; 9.17 'Seeds and reproducibility'; 11.3 item 9 | the trajectories of a segment run as one MCSolver.run over the ensemble of their kets (mixed initial conditions, one trajectory per ket, the keyed seed list of Section 3.4) through QuTiP's map, matched back to their kets by seed: six keyed trajectories of the 20 us single-loop pulse on the Bell space heated at 2.4e4 quanta/s (ten jumps among them) over 1 and 6 workers give \|rho_serial - rho_parallel\| = 0, per-trajectory \|psi_s - psi_p\| = 0 and identical jump records (36 s in-process against 11 s over six workers); the serial and the parallel run() of the GPi2 and Bell circuits agree shot by shot and to 1e-12 in the register | dynamics/engine.py (MCSolver.run over the ensemble); dynamics/parallel.py; check_kernel.py 4; tests/test_parallel.py | the plan's 4.4e-16 completion-order discrepancy of MultiTrajResult does not arise because the engine averages the trajectories itself in keyed order; SolverOptions.map defaulted to 'parallel' since M0 while the engine ran every trajectory serially until M9b; a cap the truncation monitor grows on one branch is reported and no longer carried into the later branches, so the result is independent of the worker count; the coefficient counter cannot see the workers' calls, so a segment integrated over workers reports no right-hand-side count | recomputed here |

#### Benchmark emulation (Section 10 M10)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m10.clifford_groups` | \|C1\| = 24, \|C2\| = 11520 | 7.9; 13 RB error rate; M10 | the closure of {H, S} has 24 elements and the closure of {H (x) 1, 1 (x) H, S (x) 1, 1 (x) S, CNOT} 11520; the stabilizers \|L n g L g^-1\| of the cores 1, CNOT, iSWAP, SWAP are 576, 64, 64, 576, so the classes have 576, 5184, 5184, 576 elements, which the symplectic invariant counts over the whole group; 1.5 entangling gates per Clifford on average; 200 of 200 random elements are recognized with their class and 50 of 50 random six-Clifford sequences closed by the inverse of their product return the identity | benchmarks/clifford.py; check_benchmarks.py 1; tests/test_clifford.py | a phase key built from rounded floats distinguished -0.0 from 0.0 and closed C1 to 311 'elements'; the key quantizes to integers | recomputed here |
| `anchor.m10.example_device_preset` | yb171_chain(n).device.hash() | 3.2 device/presets.py; M10 | the package-level example device reproduces the M6 fixture tests/m6_fixtures.circuit_fixture(n) hash for hash (Appendix E's canonical digest) for n = 2 and for n = 3 at the 2.0 um addressing waist, with the same drive maps and detection beam index, so every number the M6 to M10 suites established on the fixture holds for the preset the documentation examples and the benchmarks run on | device/presets.py; tests/test_presets.py | - | recomputed here |
| `anchor.m10.ghz_fidelity` | (P_0 + P_1 + C)/2 against <GHZ\| rho \|GHZ> | 7.9 Entangling gate; 9.6 rows 1 and 2; M10 | two ions, 1000 shots: P00 0.543(16), P11 0.454(16), parity contrast 0.9981(19) from eight phases (chi2/dof 0.19), bound 0.9975(112) against the two exact fidelities the simulator reads off the same register state -- max_theta <GHZ_theta\| rho \|GHZ_theta> = 0.99808, which the bound ESTIMATES (they agree to 0.0006, one twentieth of the bound's sigma), and the fixed-phase <GHZ\| rho \|GHZ> = 0.99762, which the bound is an upper bound on -- with F_gates 0.99860 and the predicted bound 0.99716 from the channels and Section 9.6 scales 8.85e-3; three ions on the three-ion example device (2.0 um addressing waist), five phases, 400 shots, dimension 1144 = [2, 2, 2, 11, 13] with the zigzag frozen: P000 0.545(25), P111 0.4475(25), contrast 0.9952(116), bound 0.9939(185) against max_theta 0.99227 (agreeing to 0.0016, a tenth of sigma) and the fixed-phase 0.98508, which the bound exceeds by 0.0088, Section 9.6 scales 3.23e-2 | check_benchmarks.py 5; tests/test_benchmarks.py | the first version of this record reported the bound beside the FIXED-PHASE register fidelity alone and called it a lower bound; the three-ion row was already the counterexample (bound 0.9935 against 0.98520) and the two numbers were never the same quantity. (P_0 + P_1 + C)/2 is exactly max_theta <GHZ_theta\| rho \|GHZ_theta> (conv.ghz_parity_bound), which both rows now confirm to well inside one sigma, and it is an UPPER bound on the fixed-phase fidelity. Unchanged and still supported: the exact two-ion register infidelity 2.38e-3 exceeds 1 - F_gates = 1.40e-3 because the five carrier pulses' crosstalk rotations on the same neighbour add coherently (M6's finding in channel language) -- this, and not the simultaneous-RB comparison, is the evidence that coherent crosstalk adds faster than the first-order product; and the parity at phi = 0 is -1.0 and not +1 because GPi2's rotation axis and the compiled frames put N pi/2 plus the frame angles into phi_0, so the bound reads the contrast and never the value at one phase | recomputed here |
| `anchor.m10.kak_decomposition` | (a, b, c) in the Weyl chamber | 7.2; M10 | CNOT and CZ decompose to (pi/4, 0, 0) at one entangling gate, iSWAP to (pi/4, pi/4, 0) at two, SWAP to (pi/4, pi/4, -pi/4) at three, CP(0.3) to (0.075, 0, 0) at one, residuals below 1e-15; 200 Haar-random SU(4) all cost three entangling gates with a worst residual of 1.3e-14, every Moelmer-Soerensen angle in [0, pi/2]; the ideal heavy-output probability of random width-2 depth-2 circuits is 0.796 +- 0.092 (one standard deviation over 200 circuits) against the large-width asymptote (1 + ln 2)/2 = 0.8466 | control/two_qubit.py; check_benchmarks.py 2; tests/test_two_qubit_kak.py | - | recomputed here |
| `anchor.m10.knill_style_rb` | B p^L + 1/2 on the two-ion example device | 7.9 Randomized benchmarking variants (first bullet); 9.10 Wright reproduction; M10 | the Knill-style variant of Section 7.9 runs through run() as a protocol, not as an analytic model: per computational gate a random Pauli (a pi rotation about +-x or +-y as one GPi pulse, about +-z as a virtual RZ, or an identity) then a random Clifford (one GPi2 about +-x or +-y), closed by ONE final pi/2 into the computational basis, the ideal state never leaving the six Pauli eigenstates so that the survival is the probability of the sequence's own target bitstring. On ion 0 of the two-ion example device (L = 1, 128, 512, two sequences, 2000 shots) the survival is 0.9995, 0.9985, 0.985, fitting F(L) = 0.4995 x 0.99998^L + 0.5000 with p = 0.999983(9) and r = (1 - p)/2 = 8.50(44)e-6 per computational gate at 1.561 pulses per gate. That is BELOW the Clifford-RB r = 1.97e-5 per Clifford at 0.829 pulses, although a computational gate costs 1.9x the pulses: the Pauli randomization twirls the pulses' coherent errors toward a depolarizing channel, so the partial cancellation Clifford RB relies on is replaced by an average that does not accumulate | check_benchmarks.py 3; benchmarks/rb.py (variant='knill'); tests/test_m10_benchmarks.py; Knill et al., PRA 77, 012307 (2008); Harty et al. 2014; Wright et al. 2019 | Section 9.10's 'Wright reproduction' row (single-qubit RB fits B p^L + 1/2 with p ~ 0.995 and B + 1/2 ~ 0.993) is NOT reproduced and is deferred. Those numbers are a per-pi/2-gate error of 5e-3 and a SPAM offset of 7e-3, a hundred times this device's (p = 0.999983, B + 1/2 = 0.9995), and Wright et al. 2019 reports the aggregate fidelities without the beam profile, the intensity and laser-phase noise spectra or the SPAM decomposition that configuring a device to his operating point needs -- the same obstacle the M6 ledger records for that row's Bernstein-Vazirani clause ('the plan's stated magnitude cannot fix [it] without his beam profile'). Turning a noise knob until p = 0.995 would fit a number, not reproduce a measurement, so the protocol is provided and the row's numbers are left open; anchor.m10.single_qubit_rb no longer cites 9.12 Wright reproduction as its plan section. Section 7.9's other two variants, direct RB with random XX layers at probability p_2Q and its two-instance (r_1Q, r_2Q) extraction, still do not exist, so the corrected direct-RB right-hand side r = (4^n - 1)(1 - p)/4^n is a conversion in noise/summary.py with no protocol behind it (docs/limits.md) | recomputed here |
| `anchor.m10.quantum_volume_width_two` | h_U at width two | 10 M10; 4.6 (Pino 2021's quantum-volume circuit) | four random square circuits of depth two (six entangling gates and 26 pulses each after merging the local unitaries), 400 shots: heavy-output probabilities 0.795(20), 0.648(24), 0.820(19), 0.603(25) against the ideal 0.7429, 0.6176, 0.8657, 0.5858; mean 0.7163 against the ideal mean 0.7030 and the depolarizing prediction 0.7016 (eps_gates 5.83e-3 from the full-angle channels, eps_readout 9.6e-4); exact register fidelities 0.990, 0.996, 0.995, 0.991; Section 9.6 scales 2.89e-2 per circuit. Cross et al. Appendix C Eq. (32) gives sigma = sqrt(h(1 - h)/n_c) = 0.2254, so mean - 2 sigma = 0.2654 and threshold_cleared is False; the standard error of the mean over the circuits is 0.0537, 4.20 times smaller, and is reported as a diagnostic and never as the criterion; the run also fails the protocol's n_c >= 100, so passed is False and log2_quantum_volume is None | check_benchmarks.py 6; tests/test_benchmarks.py | the first version used the standard error of the mean as the confidence and let a run below 100 circuits clear a quantum volume; both are corrected (conv.heavy_output_criterion). The SEM is 4.20 times smaller than Eq. (32)'s sigma here, and the difference is decision-changing at the protocol's own circuit count: extrapolating this run's spread to n_c = 100 the SEM clears 2/3 at mean - 2 sigma = 0.6943 while Eq. (32) fails it at 0.626087, so a run Cross et al. would reject was being reported as a pass (tests/test_m10_benchmarks.py pins both). At width two the ideal heavy-output probability itself spreads from 0.59 to 0.87 between circuits (0.796 +- 0.092 over 200), so the criterion needs the protocol's hundred circuits even on a machine at 0.99 register fidelity; the result carries the circuit count and says so | recomputed here |
| `anchor.m10.release_checks` | version 0.1.0, uv build, the import without the dev group | 10 M10 (release); 14.5 (the provenance ledger's location); Appendix E | the first release is 0.1.0 in pyproject.toml and in qutip_trap.__version__ and in the installed dist metadata; 'uv build' produces qutip_trap-0.1.0-py3-none-any.whl (681 KB, 143 files) and the sdist; 'uv run --no-dev python -c "import qutip_trap.api"' succeeds with 235 public names in api.__all__, the ten M10 names among them (randomized_benchmarking, ghz_fidelity, quantum_volume, gate_channel, yb171_chain, DevicePreset, BenchmarkBudget, RBResult, GHZResult, QVResult); a CI job runs both so neither is checked by hand again | pyproject.toml; qutip_trap/__init__.py; .github/workflows/ci.yml job 'release'; qutip_trap/provenance.py | the wheel ships only qutip_trap/**, so qutip_trap.provenance.load_ledger() raises FileNotFoundError from an installed wheel (repository_root() resolves into site-packages). That is consistent with Section 14.5's 'a YAML file kept beside the plan' and nothing in the public API breaks -- Device.derived() stores record ids, never the records, and no runtime module calls load_ledger -- but the milestone-M11 application is specified to consume the ledger through the released core, so the app must locate docs/provenance/ledger.yaml itself and pass its path to load_ledger (docs/limits.md). The plan asks for no LICENSE, changelog or PyPI release and the repository states no license, so none is invented here | recomputed here |
| `anchor.m10.simultaneous_rb_crosstalk` | marginal r_q and the joint layer decay under independent sequences on both ions | 6.6; 7.9; 13 RB error rate; M10 | both ions driven by independent Clifford sequences (2000 shots, two sequences, m = 1, 128, 512). Per-qubit MARGINAL survival (Gambetta et al. 2012) 0.9995, 0.9495, 0.938 on ion 0 and 0.998, 0.96375, 0.79925 on ion 1, giving marginal p 0.999738 and 0.999227 and r_q = (1 - p_q)/2 = 1.31e-4 and 3.87e-4, mean 2.588(94)e-4 against 1.97e-5 for the ion benchmarked alone -- a factor 13 that single-ion RB cannot see. The per-qubit composition (each kind's GATE_LOCAL channel reduced to THAT ONE qubit, so the neighbour's crosstalk rotation counts) is r_channel.q0 = 2.853e-4 and r_channel.q1 = 2.858e-4, mean 2.855e-4, which the measured mean matches to 9 %. The same run's JOINT survival P(00) = 0.9975, 0.91425, 0.75225 fits p = 0.999078(86), so (1 - p)(2^n - 1)/2^n = 6.91(7)e-4 per LAYER of two Cliffords -- 35x the isolated r -- against the layer composition r_channel_joint_layer = 6.853e-4, which it matches to 0.9 % | check_benchmarks.py 3; benchmarks/rb.py (pair=False); tests/test_m10_benchmarks.py | this record said 'the measured joint decay is twice the first-order composition: consecutive carrier pulses rotate the same neighbour about nearby axes and their 2.2 % crosstalk errors add in amplitude, not in probability'. That factor 2 was a UNIT MISMATCH, not physics: the joint decay's (1 - p)(2^n - 1)/2^n is the error per LAYER of n Cliffords while the budget's r_channel was per SINGLE Clifford (the check script divided it by 2 to print them side by side), so the ratio 6.90e-4/3.426e-4 = 2.01 was the factor n and nothing else. With the units matched (conv.simultaneous_rb_units) the first-order composition predicts the joint decay to 0.9 % and the marginals to 9 %, so simultaneous RB is no evidence of errors adding in amplitude and the 'add in amplitude' mechanism is withdrawn here (the GHZ circuit's coherent crosstalk, anchor.m10.ghz_fidelity, is a separate and still-supported observation). What survives: the ions' gates cost 13x more under simultaneous operation than alone, 35x per layer, and single-ion RB is blind to it -- which is why the benchmark offers the variant. New here: the two ions' marginals differ by a factor 3 (1.31e-4 against 3.87e-4) although their compositions are equal to 0.2 %, so the coherent cancellation within a sequence is ion-dependent on this chain | recomputed here |
| `anchor.m10.single_qubit_rb` | r = (1 - p)/2 on the two-ion example device | 7.9; 13 RB error rate; M10 | lengths (1, 128, 512, 2048), three sequences, 4000 shots: mean survival 0.99967, 0.9975, 0.987, 0.96358 +- 0.00022, 0.0008, 0.00393, 0.01411; with B fixed at 1/2 the fit gives A = 0.49970(22), p = 0.999961(8), F(L) = 0.4997 x 0.99996^L + 0.5000, chi2/dof 0.335 and r = (1 - p)/2 = 1.97(42)e-5 (the depolarizing entanglement infidelity (3/4)(1 - p) = 2.96e-5 is reported under that name); 0.829 pulses per Clifford (0.66 gpi2, 0.169 gpi in this sample, against the group averages 0.8333, 0.6667 and 0.1667; a SIXTH of the Cliffords -- four of twenty-four, the Z-rotation subgroup -- cost no pulse); the Section 6.8 channels reduced to the benchmarked ion are 1.840e-5 (gpi2) and 7.358e-5 (gpi), the full two-ion steps 2.582e-4 and 1.032e-3 with the neighbour's crosstalk rotation counted; r_channel = 2.455e-5, r_intrinsic = 4.148e-4 (the Section 9.6 scales, which include the crosstalk rotation inflicted on the unbenchmarked neighbour and so bound a larger error than r_channel measures: 17x is partly a scope difference and not only the looseness of a bound), F(0) from SPAM 0.99965 against the measured 0.99967(29) | check_benchmarks.py 3; tests/test_benchmarks.py | the three-parameter fit is degenerate at this decay (A = 0.19 +- 0.93 and B = 0.81 +- 0.93 on the same data), so B is pinned at 1/2^n (fix_offset); the first-order composition of the channels over-predicts r by 20 %: a Clifford's two pulses carry coherent errors that partly cancel. This record cited '9.12 Wright reproduction' as a plan section while reporting the example device's own p = 0.999960, a hundred times better than Wright's p ~ 0.995: the citation is dropped and the row's status is recorded in anchor.m10.knill_style_rb. Its 'a third of the Cliffords' pulse-free count was wrong (four of twenty-four, see conv.two_qubit_clifford_classes), and the quoted +-4.5e-6 on r came from an error bar that added the within-sequence shot noise to a between-sequence variance already containing it, up to sqrt 2 too large at three sequences (benchmarks/rb.py mean_survival_sigma) | recomputed here |
| `anchor.m10.two_qubit_rb` | r = (3/4)(1 - p) on the pair | 7.9; 13 RB error rate; 6.8; M10 | lengths (1, 6, 16), two sequences, 400 shots: survival 0.9963(45), 0.9825(53), 0.9563(178); with B fixed at 1/4 the fit gives p = 0.99631(136) and r = (3/4)(1 - p) = 2.77(100)e-3 (entanglement infidelity (15/16)(1 - p) = 3.46e-3); 7.0 pulses and 1.442 entangling gates per Clifford in this sample; the channels on the pair are gpi2 2.582e-4, gpi 1.032e-3 and ms 1.137e-4 (depolarizing rate 1.421e-4, twirl p_II 0.99986, 48 engine runs on the 572-dimensional gate-local space, the M9a numbers); r_channel = 2.12e-3, r_intrinsic = 1.23e-2, F(0) from SPAM 0.99930 | check_benchmarks.py 4; tests/test_benchmarks.py | the two-qubit Clifford error of this device is the carrier pulses' crosstalk, not the entangling gate: 4.9 gpi2 and 0.67 gpi per Clifford at 2.6e-4 and 1.0e-3 on the pair against 1.44 entangling gates at 1.1e-4; the swap class emits one CNOT on the reversed pair, so ms[1,0] is a kind of its own (0.058 per Clifford) | recomputed here |

#### Performance pass of 2026-09-09: the exact rotating frame, the kernel's products, the shared atomic algebra, the direct steady state (Sections 5.2, 5.3, 11.1, 11.2)

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.perf.direct_steady_state_and_records` | steady_state_direct; spectrum_es; sample_record | 4.2.8; 8.1; 8.2; 8.5; 7.5 | qutip.steadystate(method='direct') and qutip.spectrum(solver='es') wrap their solves in a CoreOptions context whose entry and exit rebuild every data-layer dispatch table (20 ms with QuTiP's types, 40 ms with the two this package registers, 80 ms under load): 5 s of a 19 s surrogate calibration for 32 steady states whose solves take milliseconds; steady_state_direct performs QuTiP's steps around the same solver call without the context when the setting already is 'creation' (its default) and spectrum_es takes the steady state the caller has, both bitwise identical to QuTiP's results; the photon-record sampler classifies its rate cells in one pass over the path and draws the cells' Poisson counts as one vector (NumPy's Generator draws array elements in order, so the counts and the generator state are the scalar loop's, 6000 records identical); the surrogate table of the two-ion example device 19.1 -> 6.4 s, the Bell circuit with 2000 shots 16.1 -> 3.5 s, preparation_occupations 5.9 -> 0.7 s, calibrate_detection 3.8 -> 1.25 s (2026-09-09, under the load of parallel test runs) | dynamics/steady.py; light/bloch.py; prep/level_c.py; readout/detection.py _rate_cells, sample_record; calibration/readout.py | the arrivals branch of sample_record (dead time, afterpulsing, kept arrival times) interleaves Poisson and uniform draws per cell and is left as it was, since vectorizing it would reorder the stream; SegmentReport.rhs_evaluations on a mesolve segment used to count twice per evaluation (the Liouvillian's spre and spost elements each evaluate the coefficient) and now divides by the Liouvillian's element ratio (446 evaluations counted as 446) | recomputed here |
| `anchor.perf.gate_local_memos` | Device.hash, derive_raman_drive memoized per device | 5.4; 7.3; 11.3 item 5 | a GATE_LOCAL tomography makes tens of thousands of engine runs on one device (24579 for the four-qubit GHZ circuit, 16 gate steps), and every run re-derived the played chain's Raman drives (159 s) and re-hashed the device for each segment's fingerprint (80 s); both are pure functions of an immutable device and are memoized per device instance (the instance kept alive so its id cannot be reused; a changed device is a new instance through dataclasses.replace), and the builder's QobjEvo skips QuTiP's merge pass (its k^2 operator equalities were a third of a 2.7 ms build; the constant part is one Qobj and every drive term carries its own coefficient, so nothing was ever merged): a segment build 2.7 -> 0.7 ms, the four-qubit GHZ run through GATE_LOCAL 1137 -> 856 s with its in-process part 304 -> 67 s, the rest the three entangling-gate tomographies in the worker pool (64 to 256 exact inputs x 3 branches each) | device/model.py Device.hash; light/raman.py derive_raman_drive, derive_optical_drive; dynamics/hamiltonian.py (compress=False) | the remaining GATE_LOCAL cost is the tomography itself (Section 5.4: prod_i d_i^2 inputs over the addressed ions plus every crosstalk neighbour above SolverOptions.crosstalk_threshold, times the motional branches), about four to five minutes per interior entangling gate on 18 workers; it is the design, not overhead - superseded the same day by anchor.perf.tomography_isometry, which propagates the prod_i d_i basis columns instead (about one minute per entangling gate on 18 workers) | recomputed here |
| `anchor.perf.kernel_products` | (x)_f A_f applied axis by axis | 11.2; 11.3 item 4 | the trailing factor of the block is applied as ONE (lead, d) @ A^T product instead of a batch of lead (d, d) @ (d, 1) products (9.0 -> 2.1 us at d_m = 8, lead = 128), a leading factor as one A @ (d, trail) product, only a middle factor batched, and the operator's scalar prefactor is folded into the first factor matrix once; one right-hand side through QobjEvo.matmul on the Section 11.1 fixture falls 78 -> 56 us at dimension 2048 and 40.6 -> 29.5 us at 864, the application equal to the assembled matrix to 1.4e-15; apply_into accumulates alpha (A psi) into a shared output without a zero fill of its own, which is what the rotating-frame sum uses | dynamics/kernels.py _ApplicationPlan; bench_ms_timing_v5.py part 1 (the constants); tests/test_kernel.py; tests/test_rotating.py | the cost-model constants of anchor.m9b.cost_model_constants are the 2026-09-07 measurements and still decide the auto kernel choice conservatively (the factorized term got cheaper, so the crossover moved toward smaller spaces, not away); the assembled path is untouched | recomputed here |
| `anchor.perf.rotating_frame_exactness` | psi = e^{-i H_0 t} phi | 5.2; 5.3; 11.1; 11.2 | every ket segment (sesolve, mcsolve) integrates i phi' = Theta^dag V(t) Theta phi with Theta = e^{-i H_0 t} the diagonal of H_mot + H_int (+ a constant Stark shift) held as its Kronecker-sum factors, the phases applied to the STATE around the unchanged drive operators (the factorized kernel or the assembled CSR matrix, one function element per segment), collapse operators that are eigenoperators of ad_{H_0} carrying e^{i lambda t}, expectation values of eigenoperators phased back (<a_m> by e^{-i omega_m t}), stored states and finals rotated back; on the four Section 11.1 rows (factorized kernel, dop853, 20 us) the evaluations fall 19283 -> 6029, 21067 -> 5116, 24366 -> 4852, 35177 -> 4599 (x3.20, 4.12, 5.02, 7.65) and the wall times 0.32 -> 0.14, 0.50 -> 0.16, 0.98 -> 0.24, 2.41 -> 0.38 s, with \|\|psi_rot - psi_schr\|\| = 1.35e-7, 2.92e-7, 4.38e-7, 5.78e-7 (the dop853-vern9 figure of Section 11.1); the real two-ion 100 us single-loop pulse on [2, 2, 9, 15] runs 5.12 -> 1.00 s at 43.3 -> 6.7 steps per period with the populations equal to 3e-8, \|\|dpsi\|\| = 5.1e-7 and the <a_m> traces to 3.6e-10; on the trajectory path every trajectory ends in the same state (1e-6) with the same jump record | dynamics/rotating.py; dynamics/engine.py (SolverOptions.rotating_frame, SegmentReport.frame); bench_rotating.py; tests/test_rotating.py | an exact change of integration variable, not a frame of BuilderOptions (those change H(t)) and not an approximation: nothing is expanded, truncated or dropped, and the builder's H(t) is unchanged; mesolve segments form the Liouvillian from the Schroedinger-picture matrices as before (Section 5.3), the closed forms and the propagator path need no frame, and a segment the frame does not cover (a non-diagonal static part such as an anharmonic term, a function-element term, an interaction-picture build with a zero static part, a collapse operator that is not an eigenoperator of ad_{H_0} such as a recoil kick or the intensity-noise channel, since mcsolve forms c^dag c from the operators it is given and a rotated non-eigenoperator makes every such product a time-dependent pair of applications per right-hand side) integrates in the Schroedinger picture and says so in SegmentReport.frame; the Section 5.3 step-density band (27 to 52 per period) is a Schroedinger-picture statement and is checked with rotating_frame=False | recomputed here |
| `anchor.perf.shared_atomic_algebra` | structure_at(species, B, b_hat) | 4.5.4; 4.5.5; 7.5; 9.6 | one AtomicStructure per (species, B_gauss, b_hat), read-only after construction, memoizing the reduced elements, the <a\|T_q\|b> elements between two dressed sublevels, the decay amplitudes, total rates and branching totals per instance (keyed by the dressed states' labels, bypassed for a state of another structure); the exact-fraction Wigner algebra that the intrinsic budget, the played chain, the calibration seeds and the scattering channels recomputed per call (6.6 s of a 26.8 s serial two-ion Bell run) drops below the profiler's resolution, every rate unchanged | species/raman.py structure_at, AtomicStructure; species/model.py level_j; species/wigner.py as_half_integer; species/dipole.py reduced_element_from_partial_rate; noise/scattering.py; light/raman.py; control/played.py | the pure functions level_j, as_half_integer and reduced_element_from_partial_rate are lru-cached on their arguments; a structure obtained by constructing AtomicStructure directly still works and caches per instance, it just is not shared | recomputed here |
| `anchor.perf.tomography_isometry` | K_{b,a} = sqrt(w_b) (<a\|_mot (x) 1) V_b | 5.4; 5.5; 11.2; 11.3 items 5 and 8 | a unitary step's final state is linear in its initial ket, so the GATE_LOCAL tomography propagates the prod_i d_i internal basis kets \|j> (x) \|m_b> of every motional branch b instead of the prod_i d_i^2 input states and reads the channel off the Stinespring isometry V_b their finals form: Kraus operators K_{b,a} = sqrt(w_b) (<a\|_mot (x) 1) V_b (the slices by motional output index a), the trace-1 Choi matrix (1/d) sum_{b,a} \|phi_{b,a}><phi_{b,a}\| formed as one (d^2 x d_mot)(d_mot x d^2) product per branch without materializing the Kraus operators, and the input states' outputs, reduced motional states and <a_m> as the same functions of V_b psi_k; on an internal-state-only space the cached segment propagator IS the branch's Kraus operator (JointExactEngine.propagator: one engine call per branch, no state propagated). Against the state route on the same space (bench_tomography.py: a 10 us single-loop pulse on the two-ion chain, the x-COM resolved at d = 15, three branches) the Choi matrices agree to 3.2e-9, the outputs to 5.3e-9, the reduced motional outputs to 8.7e-9 and <a_m> to 6.1e-9 with 12 instead of 48 engine runs (0.52 against 1.95 s); the raw Choi matrix is completely positive to 7e-17 (1.7e-9 for the least-squares fit) and trace preserving to 4.3e-8, the norm the columns lost, and Dykstra converges in 2 iterations instead of 12; the carrier step agrees to 3.4e-16 with 3 instead of 48 runs. The four-qubit GHZ circuit through GATE_LOCAL (16 gate steps, 41 motional branches, 18 workers) runs in 205 s instead of 856 s with the same register fidelity (0.8437147 against 0.8437146) and the same bound (0.0457707): its entangling steps 164 instead of 656 runs each, its in-process part 2 s instead of 67 s (13 carrier steps of 41 propagators each; 60 of the 64 one-ion idle channels from the idle cache). The example device's Bell circuit through GATE_LOCAL (2000 shots) 17.1 -> 5.1 s, the entangling step 40 columns over 10 branches instead of 160 runs, the final states equal to 4.3e-10. The register update sum_a K_a rho K_a^dag as one superoperator product (sum_a K_a (x) conj K_a on the (local, local) x (rest, rest) reshape) 0.148 -> 0.010 s at ten qubits, difference 1.4e-18, and Register.marginal on the strided view 1.05 -> 0.08 ms, identical | dynamics/tomography.py (choi_from_isometry, kraus_superoperator, TomographyRecord.route); dynamics/engine.py (JointExactEngine.propagator, is_unitary, SolverOptions.tomography_isometry); run/gate_local.py (Register.marginal, the idle-channel cache, one engine per walk); bench_tomography.py; tests/test_tomography.py; tests/test_parallel.py; tests/test_gate_local.py | an exact use of linearity, not an approximation: nothing is dropped and the same engine integrates the same H(t) at the same tolerances; a dissipative step (any collapse operator on any segment: a trajectory is not linear in its initial ket and a mesolve segment evolves a density matrix) keeps the state route whatever the switch says, and SolverOptions(tomography_isometry=False) forces it everywhere as the M9a reference; the one thing that does change is the Section 5.5 truncation monitor, which now runs on the propagated basis kets, so a superposition input's boundary population is bounded by d_int times the reported maximum (Cauchy-Schwarz) and the record's notes say so - on the bench fixture at d = 13 the sixteen-input route's superpositions tripped the margin check one level above the basis kets and the two routes ended on caps 13 and 14, which is why the comparison is made at d = 15; the CP/TP projection is kept so that the register's normalization stays exact and tp_residual(choi_raw) is the reported norm loss; Section 5.4's cost statement gains the branch multiplier it omitted (41 on the GHZ circuit); PLAN Section 11.3 item 5's 'never on the joint space' is respected, since the tomography propagates D x d_int columns of a joint space, never its D x D propagator; the one-ion idle channels are cached by (device, ion, duration, engine setup, sample, options), the absolute window joining the key only when the sample carries a fast trajectory, because an idle has no pulse and nothing motional enters its Hamiltonian; the gate-step cache key still carries the absolute time (the beat phase and the trajectories depend on it), so identical gates at different times are still extracted twice | recomputed here |

#### composite

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.composite.bb1` | BB1, NB1 | 4.3.5 verified library; 9.10 composite pulses; 9.15 | phi_1 = arccos(-theta/(4 pi)); segments (pi, phi + phi_1), (2 pi, phi + 3 phi_1), (pi, phi + phi_1) after the target; leading coefficient 9.388566 eps_a^6 at theta = pi; order-ladder slopes 2.998 (primitive) and 5.996 (BB1) on (1e-2, 1e-1); NB1 is the narrowband sibling with the same phase set | Wimperis 1994; Kabytayev 2014; check_composite.py; tests/test_composite_pulses.py | none | recomputed here |
| `anchor.composite.concatenated_durations` | CinSK, CinBB | 4.3.5 concatenated pulses; 9.17 composite-pulse durations | reduced CinSK: the CORPSE block then (2 pi, -phi_1), (2 pi, +phi_1); reduced CinBB: the CORPSE block then (pi, phi_1), (2 pi, 3 phi_1), (pi, phi_1); durations CORPSE 4.3333 pi, CinSK and CinBB 8.3333 pi, SK1 and BB1 5 pi; slope 4 under simultaneous eps_a = eps_d where SK1, BB1 and CORPSE fall to 2; CinBB keeps BB1's 9.388566 amplitude coefficient | Kabytayev 2014 Table I; check_composite.py | none | verified |
| `anchor.composite.corpse` | CORPSE, short CORPSE | 4.3.5 verified library; 9.15 CORPSE angles | winding n = (1, 1, 0): angles (367.5645, 345.1291, 7.564528) deg at theta = 30, (371.4689, 337.9378, 11.4689) at 45, (384.2952, 318.5904, 24.29519) at 90, (420, 300, 60) at 180; leading coefficient 6.50075189242e-3 eps_d^4 at theta = pi; total duration 4.3333 pi | Cummins, Llewellyn, Jones 2003 Table I; Kabytayev 2014; check_composite.py | short CORPSE (winding (0, 1, 0)) folds to -R, a global phase, so a distance that does not quotient the phase misreports its order (M2 audit, 2026-09-07) | recomputed here |
| `anchor.composite.mount_pd6` | PDn (n = 2, 6), B2 | 4.3.5 Mount's tables; 9.2 Composite pulses; 9.10 | PD6 phase tables at theta_t = pi and pi/2 indexed by theta_t; B2 identity with phi_B2 = arccos(-theta_t/(4 pi)) = 1.696124158 / 1.823476582 / 1.955193101 at the plan's three angles; error below 1 % for \|eps\| < 0.4 (B2) and 0.6 (PD6) | Mount et al. 2015; check_composite.py; tests/test_composite_pulses.py | PD6 at arbitrary theta_t needs the numerical continuation the plan lists as open (Sections 4.3.5, 12); the 9.10 target-first/target-last pair 4.33e-3 / 1.84e-3 is not reproduced (anchor.m2.mount_pd6 records the measured values) | recomputed here |
| `anchor.composite.pb1_five_pulses` | PB1 | 4.3.5 verified library; 9.15 composite block | the five-pulse passband sequence; leading coefficient 118.295935928 eps_a^4 at theta = pi; order-ladder slope 6.000 in amplitude and 2.000 in detuning | Wimperis 1994; Kabytayev 2014 Table I; check_composite.py | none | recomputed here |
| `anchor.composite.scrofulous` | SCROFULOUS 180_60 180_300 180_60 | 4.3.5 verified library; 9.15 composite block | at theta = pi the three segments 180_60, 180_300, 180_60; leading coefficients 4.566051 eps_a^4 and 4.000000 eps_d^2 (four times the primitive detuning sensitivity) | Cummins, Llewellyn, Jones 2003; Kabytayev 2014; check_composite.py | non-uniform areas, so the LYC certificate of equal-area families is refused for it | recomputed here |
| `anchor.composite.sk1_sign` | phi_SK1 = arccos(-theta/(4 pi)) | 4.3.5 verified library, SK1 and APn; 9.15 composite block | target (theta, phi) followed by the correctors (2 pi, phi + phi_SK1), (2 pi, phi - phi_SK1); leading coefficient 22.830256 eps_a^4 = (pi^2 sin 2 phi_1)^2 at theta = pi; APn at n = 1 is SK1 | Brown, Harrow, Chuang 2004; Cummins Eq. 24 (sign corrected); Kabytayev Sec. II B; check_composite.py | the printed SK1 corrector sign is a negative control in check_composite.py; a primitive followed by the printed order is not an amplitude-correcting pulse | corrected |
| `anchor.composite.suzuki_factor` | f_j = (2^{2j-1} - 2) f_{j-1} | 4.3.5 P2j/N2j/B2j ladders; 9.15 composite block | the Trotter-Suzuki factor of the cached T_{2j-2} block, f_2 = 24 (P4 at 37 pulses gives 1.61629e-10); the phases arccos(-theta/(8 pi)) and arccos(-theta/(48 pi)) follow | Low, Yoder, Chuang 2014 (LYC); Cummins Eq. 7; Kabytayev Sec. II B; check_composite.py | the printed f_2 = 28 and f_2 = 14 variants (the source's -1 defect) are negative controls; the plan asks that every Trotter-Suzuki phase be root-found on the leading eps coefficient rather than trusted from the closed form | corrected |
| `anchor.composite.toggled_pd2` | BBn (n = 2) = toggled PD2 | 4.3.5 verified library; 9.15 composite block | the toggled PD2 sequence equals BB1 at theta = pi; PD2 phases from the closed form | Mount et al. 2015; Kabytayev 2014; check_composite.py | none | recomputed here |

#### m0a

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m0a.ozeri_table_ii` | eps_S x P_0 = 2 pi \|Omega_R\| hbar omega_{3/2}^3 w_0^2/(3 c^2) | 9.13 Ozeri scattering optimum and power regression; 4.3.2 | the invariant of Eq. 17 is eps_S x P = 3.365441e-7 W for 9Be+ at w_0 = 20 um, Omega_R/2pi = 0.25 MHz and lambda_{3/2} = 313.0 nm, so eps_S = 0.337e-4 needs P_0 = 9.986 mW | Ozeri 2007 Eq. 17; qutip_trap/validation/ozeri_closed_forms.py; tests/test_m0a_ozeri_closed_forms.py | PLAN.md 9.13 pairs eps_S = 0.337e-4 with P_0 = 3.37 mW, which violates its own Eq. 17 by a factor 2.9633 (the prefactor would have to be 9, not 3, and even then reproduces the pair only to 1.2%). Its negative control 'the printed lambda = c/omega gives 133 mW' is 133/3.37 = 39.47 = (2 pi)^2, while the two lambda readings of Eq. 17 differ by (2 pi)^3 = 248.05 - so that ratio follows from neither reading. The 25Mg+, 43Ca+, 67Zn+ and 111Cd+ rows cannot be checked at all: their lambda_{3/2}, gamma and omega_f are not in PLAN.md. The Yb+ row is excluded as the plan instructs. eps_S = P_Raman = 2 pi \|Omega_R\| (gamma/g^2) was RE-DERIVED here from gamma/g^2 = 4 hbar omega^3/(3 pi eps0 c^3 E^2) and the Gaussian peak E^2 = 4P/(pi eps0 c w_0^2), reproducing Eq. 17 exactly, so the form is right and the plan's pair is not (audit item E11) | contested |
| `anchor.m0a.uys_4_5_tesla` | Gamma_el versus (Gamma_uu - Gamma_dd)^2/(Gamma_uu + Gamma_dd) | 9.13 Uys bounds and Lindblad prefactor; 9.16 Signed fine-structure detunings; 4.5.5 | the 9Be+ electron-spin qubit at 4.5 T: Omega_z/2pi = 124.1 GHz, 313 nm, P3/2 resonances at -79.4 and -37.7 GHz from the cycling transition, equal elastic rates near -56 GHz (which lies between the two resonances, as an amplitude crossing must). The plan's own sanity check 2 x 13.996 GHz/T x 4.5 T = 125.96 GHz is 1.5% above the printed Omega_z | Uys et al. 2010 Eqs. 4-8, Fig. 3; qutip_trap/validation/ozeri_closed_forms.py; tests/test_m0a_ozeri_closed_forms.py | the row's 'about 5x the rate-difference estimate' is a RATIO TO ZERO and no finite factor may be pinned: at the detuning where the two elastic rates coincide the Ozeri estimator (Gamma_uu - Gamma_dd)^2/(Gamma_uu + Gamma_dd) is EXACTLY 0 while Uys's Gamma_el - the square of the amplitude DIFFERENCE - saturates its upper bound 2(Gamma_dd + Gamma_uu). That structure is what the test pins. The crossing detuning has the closed form D = u D_f/(u + v) with u, v the summed a^(1/2) and a^(3/2) amplitudes | corrected |

#### m9

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.m9.integrator_ladder_stiffness_not_reproduced` | dop853 'probably stiff' at d_m = 121 | 5.3; 11.1 | PLAN.md 5.3: 'with atol 1e-10 dop853 aborts ("problem is probably stiff") at d_m = 121 on the Section 11.1 pulse while succeeding at 101, and succeeds at 121 with atol 1e-8 or with vern9' | bench_ms_timing_v5.py fixture through qt.sesolve, measured 2026-09-07 on QuTiP 5.3.1, Python 3.13.14; tests/test_m9_scaling.py, tests/test_m2_convergence.py | the abort does not reproduce here. On the Section 11.1 one-mode fixture (2 ions x 1 mode, 20 us, nsteps 1e7) dop853 succeeds at d_m = 101, 121, 151 and 201 at BOTH atol 1e-10 and 1e-8 (17.8 s and 19.8 s at 121, coefficient calls 243815 and 291820; vern9 18.0 s and 17.9 s, 257510 and 257286), and atol as tight as 1e-30 at d_m = 121 still succeeds, so atol is not the binding constraint at rtol 1e-8. The failures reachable on this machine are nsteps limits, which every rung shares: at nsteps = 2500 both integrators fail at d_m = 121, 151 and 201 at both atol values. The atol keying of Section 5.3 is therefore pinned as a keying (1e-10 at d_m <= 100, 1e-8 above, at 121, 151 and 201) and the escalation is exercised through a step budget between the two integrators' requirements, with no failure manufactured to make the ladder look tested. The plan's number is left standing as a measurement on its own machine | contested |
| `anchor.m9.section_11_1_acceptance_bounds` | the Section 11.1 acceptance test | 11.1; 11.2; 11.3 item 4; 10 (M9b acceptance) | the acceptance test now pins the table: the right-hand-side evaluation counts 19283 / 21067 / 24366 / 35177 at dimensions 48 / 256 / 864 / 2048 to 20 % (the plan's Section 11.2 numbers 1.9 / 2.1 / 2.4 / 3.5 x 10^4, set by the integrator's arithmetic and machine independent), the two constructions' counts equal, the per-right-hand-side cost RATIO factorized/CSR (13.2/1.9 = 6.9, 21.5/16.7 = 1.29, 45.4/195.2 = 0.233, 89.5/1207.9 = 0.074) within a factor of two, and the factorized final state equal to the CSR one to 2e-7 | tests/test_kernel.py::test_section_11_1_rows_factorized_against_assembled_final_states_and_wall_time (slow, runs in CI); outputs/bench_ms_timing_v5.out | M9b's version compared only final states and two relative wall times (walls[3,8,factorized] < 0.5 walls[3,8,csr] and walls[1,12,csr] < walls[1,12,factorized]) and never touched the table's numbers. The absolute factorized wall times 0.25 / 0.35 / 1.11 / 3.15 s are asserted only within a factor of four rather than the two the audit suggested, because the whole suite runs many pytest processes at once (load average 113 on 18 CPUs while this was measured) and the plan's numbers come from an idle machine; the cost RATIO is the load-robust half and carries the two-fold bound. bench_ms_timing_v5.py is NOT added to CI: run_checks.py --bench runs every bench_*.py, the v5 script alone is about four minutes on the plan's idle machine and its 2048 rows total about 266 s of integration, which on a two-core GitHub runner without Accelerate BLAS is tens of minutes; the slow acceptance test above is what re-verifies the table in CI instead (.github/workflows/ci.yml runs a bare `uv run pytest`) | recomputed here |

#### raman

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.raman.wineland_2_3` | Omega e^{i phi} (Wineland 2003 Eq. 2.3) | 4.3.2 Stimulated-Raman drives; 9.2 Raman coupling | the two-photon coupling Omega e^{i phi} with the effective wavevector Delta k = k_1 - k_2 substituted for k (\|Delta k\| = 2 k sin(theta/2) at crossing angle theta; 2k counter-propagating; 0 co-propagating, where every eta vanishes) | Wineland et al. 2003 Eq. 2.3; light/raman.py; tests/test_light_layer.py | the complex phase is recorded and may be set to zero once per ion (Section 4.3.4) | verified |

#### species

| ledger id | quantity | plan section | what the check established | where | corrected | tag |
|---|---|---|---|---|---|---|
| `anchor.species.ba_g_factors` | g_J(6s 2S1/2) = 2.00249192(3); g_J(5d 2D3/2) = 0.7993278(3); g_J(5d 2D5/2) = 1.20036739(24) | 4.5.6 species tables 137Ba+ and 133Ba+; 9.13 (no Ba+ anchor); M0 | all three Ba+ Lande g factors of the S and D levels are MEASURED and were missing or wrong. (1) g_J(6s 2S1/2) = 2.00249192(3), Marx et al. 1998, measured in a Penning trap on 138Ba+ and 135Ba+ (the companion AIP Conf. Proc. 457, 57 (1999) states both isotopes and a fractional accuracy of 2e-8); the absence of this one constant is what made 137Ba+ refuse to build, and it is stored in BOTH isotope tables because g_J is isotope-independent to the quoted 3e-8 -- an expectation the field states (Hanley et al., arXiv:2105.10352) and not a measurement on 133Ba+ or 137Ba+. Tagged extracted, not verified: Marx et al. 1998 could not be read (Springer redirects to an identity provider, no open-access copy, no abstract in OpenAlex or Semantic Scholar), and the digits are read verbatim in the full text of Arnold et al. 2020, which uses g_S = 2.002 491 92(3) twice and calls it known to 1e-8. It supersedes Hubrich, Knab, Knoll, Werth, Z. Phys. D 18, 113 (1991)'s 2.0024906 (quoted with uncertainty (12) in the Lindroth and Ynnerman 1993 abstract and as (11) elsewhere; neither read in Hubrich itself) by about 40x, and theory brackets it at 2.0024911(30) and 2.0024905(16). (2) g_J(5d 2D3/2) = 0.7993278(3), Knoell et al. 1996, measured; extracted, read in the Chin. Phys. Lett. 42, 063101 (2025) abstract because APS returns 403. (3) g_J(5d 2D5/2) = 1.20036739(24), Arnold et al. 2020, read DIRECTLY: it is the measured Zeeman-splitting ratio r = 0.59943681(12) times Marx's g_S, and the product reproduces the printed value to 2.1e-9, the rounding of the printed ratio. Chin. Phys. Lett. 42, 063101 (2025) misquotes that uncertainty as (14); the PRL says (24). 137Ba+ now BUILDS, and its ground-state clock qubit reproduces Section 9.13's closed form taylor_c2 = (g_J - g_I)^2 mu_B^2/(2 h^2 nu_0) = 488.81912 Hz/G^2 at nu_0 = 8 037 741 667.69 Hz to 1e-9 relative; PLAN.md prints NO field-independent point, frequency or curvature for 137Ba+, so nothing published pins that result and the qubit pair, cycling line, repumps and shelving line in ba137.species() are this package's declared choice | Marx, Tommaseo, Werth, Eur. Phys. J. D 4, 279 (1998), doi:10.1007/s100530050210; Knoell, Marx, Huebner, Schweikert, Stahl, Weber, Werth, Phys. Rev. A 54, 1199 (1996), doi:10.1103/PhysRevA.54.1199; Arnold, Kaewuam, Chanu, Tan, Zhang, Barrett, Phys. Rev. Lett. 124, 193001 (2020), arXiv:1906.09150v2; NIST ASD ver. 5.12 Ba II Lande column; species/ba137.py, species/ba133.py, tests/test_species_gaps.py | both Ba tables previously carried D32.g_J = 0.79 and D52.g_J = 1.12 read off the NIST ASD Lande column, with the 1.12 flagged contested as a suspected transcription slip for 1.20. A direct re-query of the ASD CGI settles which side the error is on: ASD's own Lande column for Ba II returns literally 0.79, 1.12 and 1.32 and BLANK for 6s 2S1/2, so the transcription was faithful and the 1.12 is ASD'S OWN ERROR -- 6.7 % from Arnold's measured 1.20036739(24), from Hoffman et al., Phys. Rev. A 88, 025401 (2013)'s earlier measured 1.200372(4)(7), from the LS Lande 1.2005 and from the isoelectronic measured 1.2003340 of Ca+ 3d 2D5/2. Both ASD readings are kept as D32.g_J_asd and D52.g_J_asd tagged contested rather than deleted, so the defect stays documented, and nothing consumes them: species() reads the measured values, and the 6p levels take the LS Lande 0.665894 and 1.334106 tagged [background] because no measured Ba+ 6p g_J was located (the ASD column's 1.32 is a cross-check for the same reason its 1.12 is). The blank 6s row is what the retired _CONSULT note meant by 'NIST ASD lists none'. Not fixed here, and left for the coordinator because the file is outside this pass's scope: validation/scripts/check_atomic.py:144-146 computes the 133Ba+ quadratic clock shift with 171Yb+'s g_J = 2.002615 and says in a comment that 'no measured 133Ba+ g_J is cited anywhere in the tree', which is now false -- ba133.S12.g_J is Marx's 2.00249192(3), and using it changes that printed shift by 0.01 % | extracted |

<!-- END generated: anchors -->
