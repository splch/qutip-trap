# Conventions the simulator fixes

Every physical quantity qutip-trap handles has at least two conventions in the primary literature, and the research runs
behind [`PLAN.md`](../PLAN.md) found published factor-of-2 (and 2π, and √N) errors that trace to mixing them. Section 13 of the
plan adopts one convention per quantity, names the source it follows, and lists the alternatives; every module that touches
a quantity restates the row it implements in its docstring, and the tests pin the conversions. This page is the code's view
of that table: the rows the implementation encodes, generated from the provenance ledger
[`docs/provenance/ledger.yaml`](provenance/ledger.yaml) so that it cannot drift from what the tests assert (the table below is
rewritten by `tools/docs_from_ledger.py`, and CI fails when it is stale).

## Vocabulary

Settled on 2026-09-11 for the API work of [`api_implementation_plan.md`](api_implementation_plan.md) (Phase 0, item 0.6). Every
later phase uses these names; a name here changes only through a deprecation cycle.

- **Machine.** The executor that 0.2.0 adds: a `Device`, the roles its beams play, the calibration table and the policy that
  turns a circuit into a `Result`, with `run`, `compile`, `schedule` and `calibrated`. PLAN.md Section 14.2 already names
  Level 0 "Machine" and the app's device card uses the word. Rejected: `Emulator` (Pulser, Quantinuum), because the object is
  the machine as the physics describes it, not an imitation of one; `Backend` (Qiskit, pytket), because a backend is an
  execution service, and this is a record with behaviour and no service behind it. `Device` stays the physical record, the
  split pytket made between `BackendInfo` (data) and `Backend` (behaviour).
- **`trap`.** The recommended import alias is `import qutip_trap as trap`; the documentation's examples use it from 0.2.0 on.
  `qutip_trap.api` stays the spelled-out Appendix E import and is never aliased.
- **Rungs.** The five levels of PLAN.md Section 14.2 are five modules: `qutip_trap` (rung 0, the machine), `qutip_trap.circuit`,
  `qutip_trap.schedule`, `qutip_trap.dynamics` and `qutip_trap.physics`; `qutip_trap.interop` holds the adapters to other SDKs
  (`qutip_trap.interop.qiskit` today) and `qutip_trap.experimental` the names outside the stability guarantee. The experiments,
  calibration and benchmarks packages are "the laboratory". In API prose a step of the ladder is a **rung**, so that "level"
  keeps its two other meanings; the app's "Level 0" to "Level 4" labels are the same five rungs.
- **Fidelity levels.** `JOINT_EXACT` and `GATE_LOCAL` are the two levels a run can integrate at (Section 5.4), and `"auto"` the
  policy that picks one against the Section 11.5 guards. The enum that replaces the strings in 0.2.0 is named `FidelityLevel`,
  with members `AUTO = "auto"`, `JOINT_EXACT` and `GATE_LOCAL` equal to today's strings (so `Diagnostics.level == "GATE_LOCAL"`
  stays true); it widens the `Literal` alias of that name in `run/levels.py`. The proposal's `Level` is rejected because the
  word is taken twice: `qutip_trap.api.Level` is an atomic level (Appendix E, frozen), and "Level n" is a rung in the app.
- **Error-model names** (`error_model()`, 0.3.0): `p_*` a probability per operation, `*_rate` per second, `*_ratio` a fraction
  of another probability, `*_scale` a dimensionless multiplier. They join the unit suffixes every public float carries: `_hz`,
  `_s`, `_m`, `_w`, `_gauss`, `_rad`, `_v`, `_pa`, `_cps`, and `_turns` only at the IonQ boundary.

## The rules that reach the public API

- **Frequencies.** Every public number is an ordinary frequency in Hz (`qutip_trap.units.Hz`); every internal frequency is
  angular in rad/s (`RadPerS`); the conversion is one explicit 2π at the boundary (`rad_s_from_hz`, `hz_from_rad_s`). A
  linewidth `Transition.gamma_hz` is the upper level's total decay rate divided by 2π; the saturation intensity uses the
  angular partial rate of the cycling branch.
- **Rabi frequency.** H = (ħΩ/2) σ₊ e^{…} + h.c., so a carrier π pulse takes Ωt = π and a π/2 pulse Ωt = π/2. Sources that
  write P = sin²(Ωt) quote half this Ω; the ingest maps in `light/` double them.
- **Detuning.** δ = ω_drive − ω_transition, red negative. The beat-note detuning of a bichromatic gate is μ, measured from the
  carrier, and δ_{i,m} = μ_i − ω_m is always two-indexed.
- **Lamb-Dicke parameter.** η_{i,m} = (Δk · ê_m) c_{i,m} √(ħ/(2 m_i ω_m)) with the ion's own mass and the mass-weighted
  unit-norm eigenvector component; the micromotion factor C₀ = 1 + 3q²/16 + O(q⁴) is applied once, in `Crystal.lamb_dicke`.
- **Gates.** Parameters are radians internally and IonQ turns at the API boundary (1 turn = 2π). GPi(φ), GPi2(φ), MS(φ₀, φ₁,
  θ) with θ = π/2 fully entangling and ZZ(θ) are the matrices of `control/native.py`; R(θ, φ) = exp[−iθ σ_φ/2] carries θ/2 in
  the exponent and XX(χ) = exp(−iχ σ_x σ_x) carries χ with no ½, maximally entangling at χ = π/4. A virtual RZ(θ) shifts
  every later pulse phase by φ → φ − θ, gates read in time order.
- **Result bit order.** In every bitstring key of a `Result`, qubit 0 is the least-significant bit, the rightmost character:
  "101" on three qubits is qubit 0 = 1, qubit 1 = 0, qubit 2 = 1, and the IonQ v1 decimal key "5". This sentence is stated
  once, here; `tests/test_docs.py` checks that no other page restates it and that no docstring contradicts it. IonQ's v2
  result strings run the other way, `q[0]` first (`io/ionq.py` records the source). `Result.final_state` is in QuTiP's tensor
  order (ion 0 the first factor); `run.job.to_register_order` converts.
- **Tensor order of the native gate matrices.** Stated once, in the module docstring of `control/native.py`, with the check
  that `ms` is the SWAP conjugate of qiskit-ionq's matrix (`tests/test_docs.py`, when qiskit-ionq is installed).
- **Fidelity measures, three of them, kept apart.** The compiler and the benchmarks compare unitaries up to a global phase.
  (i) A channel's *average gate infidelity* is 1 − F_avg = d/(d + 1) (1 − F_e) with F_e the entanglement fidelity, and the
  *depolarizing rate* ε of Section 6.8 equals the entanglement infidelity in Chen et al. 2023's normalization
  Λ_ε(ρ) = (1 − ε)ρ + ε/(4ⁿ − 1) Σ_{P≠I} PρP — the same channel as Trout 2018's uniform p/3 and p/15 Kraus weights, with
  Qiskit's λ = p 4ⁿ/(4ⁿ − 1) as a conversion and never as the same number (`conv.depolarizing_normalization`).
  (ii) Randomized benchmarking reports r = (1 − p)(2ⁿ − 1)/2ⁿ, the average error per Clifford (r = 1 − F_avg of the Clifford
  channel), beside the entanglement infidelity (4ⁿ − 1)(1 − p)/4ⁿ of the depolarizing channel with the same p, which is kept
  under that name and never called the RB error rate; simultaneous RB fits each qubit's own marginal decay rather than
  applying the n-qubit formula to the joint one (`conv.simultaneous_rb_units`).
  (iii) For a single-qubit *unitary* error the composite-pulse and filter-function layers use F_K = ¼|Tr(U_id†U)|², with F_C
  and F_avg as conversions (`conv.gate_fidelity_measure`). A decay base, a channel rate and a unitary overlap are three
  different quantities and the plan's Section 13 asks for three rows; so does this page.
- **Readout figure of merit.** ε = ½(ε_B + ε_D), both reported: ε_B the probability that a bright ion reads dark, ε_D that a
  dark ion reads bright. For 171Yb+ the qubit |0⟩ (F = 0) is dark and |1⟩ (F = 1) bright.
- **Noise spectra.** Every spectrum the noise layer stores is two-sided in angular frequency with the e^{−iωt} kernel; the
  single-sided S_E of the heating literature (Γ_h = e² S_E/(4mħω)) is converted at ingest. Qubit dephasing enters as
  L = √(γ_φ/2) σ_z, under which the coherence decays at γ_φ, so γ_φ = 1/T₂ for the white component.
- **Seeds.** One root `SeedSequence` per run, spawned by (sample, trajectory, shot, ion, channel), so every random variate is
  independent of execution order, of the worker count and of truncation retries; the 1-versus-18-worker agreement is a
  tolerance test at 10⁻¹² (Section 3.4).
- **Identity.** `Device.hash()` is a canonical serialization (declaration-order fields, floats rounded to 12 significant
  digits, dicts by sorted key, `Qobj` fields excluded), the key of the calibration cache and of every cached tomography.

## The full convention table

<!-- BEGIN generated from docs/provenance/ledger.yaml by tools/docs_from_ledger.py: conventions; do not edit by hand -->

216 convention records. The **convention** column is the rule the code implements, **follows** the source it adopts, **alternatives** what other sources do and what the plan corrected; the tag is Appendix D's.

| ledger id | quantity | plan section | convention | follows | alternatives and corrections | tag |
|---|---|---|---|---|---|---|
| `conv.appendix_e_signatures` | Appendix E deviations | Appendix E; M0 (Section 10); 9.17 | Drive.delta_k is a method taking the device's beams (a Drive stores beam indices); MetastableChannels.bbr_rate_hz takes the Species that owns the line; DecouplingSequence.rounded_to_clock returns (sequence, residual moments) as its own comment requires; Drive.kind gains light_shift (M4), Diagnostics.mode_class gains enr (M9a), Tone.envelope_hz accepts a float (M2), CollapseOp.op accepts a QobjEvo (M7), CombSpec.tau_convention accepts the two FWHM conventions beside the three sech ones and CombSpec.pair_order_max accepts None, its default, to derive the sum depth from the envelope in place of the literal 1200 (M2, conv.comb_sum_depth); every other field, type, default and parameter matches Appendix E, and a required parameter Appendix E does not declare fails the freeze test | tests/test_api_freeze.py (ALLOWED_TYPE_DIVERGENCES, ALLOWED_DEFAULT_DIVERGENCES, EXTRA_REQUIRED_PARAMETERS, PROPERTY_AS_METHOD) | the freeze test compares annotations (aliases resolved to their Literal members), defaults and parameter sets, and refuses an Appendix E method outside milestone M12 whose whole body is a NotImplementedError | corrected |
| `conv.ba133_inverted_ground_state` | A(6s 2S1/2) < 0 for 133Ba+ | 4.5.6; 8.1; 13 Sign of A_hfs | A = -9925.45355459(10) MHz: mu_I(133Ba) = -0.77167(2) mu_N is NEGATIVE, so the ground-state multiplet is INVERTED, F = 0 lies ABOVE F = 1, and the \|F=0, mF=0> qubit state is the UPPER one. A(133)/A(137) = -2.469712 against the (mu_I/I) ratio -2.469686, agreeing to 1e-5 | Knab, Schupp, Werth, Europhys. Lett. 4, 1361 (1987); Hucul et al., Phys. Rev. Lett. 119, 100501 (2017); qutip_trap/species/ba133.py | the table stored +9925.45355459 MHz with the note '= zero-field splitting for I = J = 1/2', conflating the (positive) splitting with the (negative) signed A - the same failure class the `inverted` flag guards against in ca43.py and be9.py. The 12-figure value also had no provenance outside this repository (sole origin validation/scripts/check_atomic.py, itself uncited); the primary is Knab, Schupp and Werth 1987, NOT the Knab-Knoll-Scheerer-Werth Z. Phys. D 25, 205 (1993) g_J paper | corrected |
| `conv.be9_g_j_provenance` | g_J(2s 2S1/2) of 9Be+ = 2.00226239(31) | 4.5.6 species table 9Be+; 9.13 (the 119.446 G clock point); M0 | a literature review of 2026-09-08 reported that Shiga et al. 2011 measure only the RATIO g_I'/g_J and that be9.S12.g_J = 2.00226239(31), sourced to that paper, was therefore mis-cited. THE REPORT IS WRONG, and the reason it is wrong is that it read the abstract only: the abstract carries A_0, k and the ratio, but Sec. I of the body prints the absolute value and its provenance, read verbatim in the fetched full text -- 'The value of g_J for the ground electronic state of 9Be+ has been determined by measuring the 9Be+ cyclotron frequency and a hyperfine-Zeeman transition frequency at the same magnetic field [5]. The value is g_J = 2.002 262 39(31), calculated with the use of the best current value of the proton-electron mass ratio [6].' Shiga's [5] is Wineland, Bollinger, Itano, Phys. Rev. Lett. 50, 628 (1983) -- the MEASUREMENT, a cyclotron-versus-hyperfine-Zeeman frequency comparison, whose own printed value is 2.00226206(42) -- and [6] is Mohr, Taylor, Newell 2008, i.e. CODATA 2006, with which Shiga et al. re-reduce the digits. So the chain is measured 1983, re-reduced 2011, printed in Shiga 2011; the citation stands, the value is unchanged, and the source entry now names both papers and quotes the sentence. Wineland 1983's own text could not be read (APS 403); its title, authors, volume and pages 628-631 were confirmed through Crossref | Shiga, Itano, Bollinger, Phys. Rev. A 84, 012510 (2011), arXiv:1106.5760, Sec. I; Wineland, Bollinger, Itano, Phys. Rev. Lett. 50, 628 (1983); Mohr, Taylor, Newell, Rev. Mod. Phys. 80, 633 (2008); Dickopf et al., Nature 632, 757 (2024), doi:10.1038/s41586-024-07795-1, arXiv:2409.06306; species/be9.py, tests/test_species_gaps.py | the modern high-precision number that circulates for this quantity, 2.0022621287(24), is a CALCULATION and not a measurement: Dickopf et al. 2024 write 'the bound-electron g-factor g_s(9Be+) = -2.0022621287(24). For the latter, we use the calculations performed in ref. [22] and the updated nuclear recoil correction [33, 34]', and print it NEGATIVE in their sign convention. It is stored as be9.S12.g_J_theory tagged background, with the magnitude taken because Section 13's g-factor row governs, and it does NOT replace the measured value: the two agree (2.61e-7 apart, 0.84 sigma of the measurement's own bar) and swapping them moves the Section 9.13 clock point from 119.44615496 G / 1 207 495 853.379 Hz by 1.56e-5 G and 0.0093 Hz, 2000x inside the plan's own 20 Hz tolerance on that anchor, against 0.135 G and 81 Hz for the plan's g_J = 2.000 negative control. tests/test_species_gaps.py pins all four numbers, so a future swap cannot be silent. Also corrected: Dickopf et al. is Nature 632, 757-761 (2024), confirmed through Crossref on 2026-09-08; 'Nature 632, 673' circulates and is wrong. Note for 25Mg+, whose gap note used to imply 9Be+ was in the same position: it is not -- 9Be+ has an absolute g_J and 25Mg+ has none, measured or computed | verified |
| `conv.be9_linewidth` | Gamma(2p)/2pi of 9Be+ | 4.2.1; 4.3.2; 9.13 | four readings spanning 9%: 17.97 MHz (NIST ASD A_ki 1.1292e8 and 1.1285e8 s^-1, accuracy grade AAA <= 0.3%, theory), 19.4 MHz (Monroe et al. 1995, which PLAN.md 4.2.1 quotes), 19.6 MHz (Ozeri Table I) and 19.64 MHz (Andersen, Jessen, Soerensen 1969, beam-foil). The record uses Monroe's 19.4 MHz, which is the reading every Section 9.13 anchor assumes (P_SE = 8.706e-4) | NIST ASD (Yan, Tambasco, Drake 1998); Monroe et al. 1995; Ozeri 2007 Table I; Andersen et al. 1969; qutip_trap/species/be9.py | the spread is far outside every stated accuracy. The ASD value implies tau = 8.86 ns and an oscillator strength f = 0.498, the textbook ~0.5 for a Li-like ion, so ASD is self-consistent; 19.4/19.6 MHz need f = 0.543. No primary derivation of the ubiquitous trapped-ion '19.4 MHz / 8.2 ns' could be located (Langer's thesis states it uncited, Ozeri cites Andersen 1969, whose text was not obtainable). Same shape as the plan's own handling of 40Ca+'s 21.57/23.4 MHz | contested |
| `conv.be9_p32_hyperfine` | A(2p 2P3/2), B(2p 2P3/2) of 9Be+ | 4.5.6; 9.13 | the record uses the THEORY values A = -1.026(3) MHz and B = -2.29940(3) MHz, tagged background; no measurement of either exists | Puchalski and Pachucki, Phys. Rev. A 79, 032510 (2009); Bollinger et al., Phys. Rev. A 31, 2711 (1985); qutip_trap/species/be9.py | TWO unresolved tensions. (1) the theory A is 1.7x LARGER than Bollinger et al. 1985's experimental bound \|A(2P3/2)\| < 0.6 MHz. (2) the theory B is NEGATIVE while Q(9Be) = +0.0529(4) b is positive, and for a single p3/2 valence electron sign(B) = sign(Q) (87Rb and 133Cs both obey it), so either the paper uses another B or Q convention or one of the two needs correcting. Neither affects any Section 9.13 anchor (a 1 MHz excited-state splitting against THz detunings), but the values must never be read as measured. The -1.03(3) / -2.30(9) MHz pair that circulates is these numbers with invented uncertainties | contested |
| `conv.beam_phase_spectrum` | NoiseModel.beam_phase_noise -> phi_j(t) per beam; eps = [int S_phi F] (4 g^2/Delta^2)^2 | 6.3; 7.10 'the optical path difference between two Raman beams sets the beat-note phase' | each beam gets its own realization of the SAME declared spectrum, so a Raman pair's beat note sees phi_2(t) - phi_1(t) with twice one beam's variance (measured 2.00 x sigma^2 over 120 seeds) and a co-propagating pair (one shared path, declared as one beam) sees none. The builder reads the sampled Delta phi(t) with the convention of key_beam_phase_rad (phi_2 - phi_1 for a Raman or light-shift pair, -phi_1 for a single beam) and multiplies the beat note by e^{-i Delta phi(t)} beside a single-photon drive's own laser phase, so constant trajectories reproduce the quasi-static build to 1e-12 and a common-mode path drift cancels (tests/test_m7_beam_phase_builder.py). The Baldwin form's (4 g^2/Delta^2) enters as its SQUARE, so halving the two-photon-to-detuning ratio drops eps by exactly 4 | M7 audit E-9 (noise/model.py, noise/sampling.key_beam_phase_trajectory_rad, validation/noise_closed_forms.baldwin_phase_noise_infidelity; tests/test_m7_closed_forms.py; dynamics/hamiltonian._beam_phase_trajectory; tests/test_m7_beam_phase_builder.py) | before this the only laser-phase route was KEY_LASER_PHASE_TRAJECTORY on single-photon optical drives, so route (d) was unreachable for the phase-sensitive Raman geometries Section 6.3 names, and there was no per-beam or beat-note phase spectrum field at all. Kirchmair's 2e-3 per gate from incoherent carrier excitation and the Rabi-drift anchors (Kirchmair 1.4e-2, Harty 5e-4) are recorded as consistency anchors in validation/noise_closed_forms.py | extracted |
| `conv.beam_roles` | resonant vs far-detuned beams | 3.3; 7.3; 8.1 | a beam within 2e-3 of the vacuum wavelength of a tabulated E1 line is resonant light (cooling, detection, repumping) and never a gate drive; the scheduler's drive inference and the readout's detection-beam selection both read this rule, so one device list carries every beam | qutip_trap/light/roles.py | the M2 inference counted every beam and refused a device with a detection beam beside its Raman pair | background |
| `conv.beat_phase_at_gate_start` | phi_leg -> phi_leg + 2 pi mu_leg t_g | 7.10 Phase coherence between pulses; 4.4.1 Corrections beyond the leading order; 7.3 | with HardwareChain.phase_continuous = False every leg of an entangling waveform carries the phase offset 2 pi mu_leg t_g (red and blue opposite, the spin phase unchanged, the virtual-Z frame intact), so the beat note starts at zero phase at the gate start as it did in the t = 0 calibration; with phase-continuous tones the start phase zeta = 2 pi mu t_g is part of the gate and the tilt sin^2[(4 Omega-bar/mu) sin zeta] is reported in the intrinsic budget | Roos 2008 (psi = (4 Omega/delta) sin zeta); PLAN.md Section 7.10; qutip_trap/control/schedule.py | the builder's per-pulse phase_mode='reset' resets at every SEGMENT of a multi-segment waveform (0.2-cycle jumps at the segment edges of the fixture's AM pulse open the loops: leakage 0.31); the M6 finding on the two-ion fixture: the same calibrated gate has fidelity 0.99995 at zeta = 0 or pi and 0.98903 at zeta = +-pi/2 (1.1 % leakage), so a gate started 21.27 beat cycles into a circuit under continuous tones is not the gate that was calibrated | corrected |
| `conv.benchmark_budget_composition` | r_channel = sum_kinds n_kind eps_kind; F_gates = prod (1 - eps) | 10 M10; 6.8; 8.4; 9.6 | beside every benchmark: (i) the Section 9.6 closed-form scales per unit, summed per native gate kind from Diagnostics.intrinsic_budget; (ii) the Section 6.8 channel of every native gate kind the benchmark compiled to, from ONE one-gate circuit through run(level='GATE_LOCAL') (the step's Choi matrix, its ideal the GateTarget unitary), reduced to the benchmarked qubits with the crosstalk neighbours traced out in \|0> (E_keep(rho) = Tr_rest E(rho (x) \|0><0\|)) and converted to the average gate infidelity; (iii) the SPAM errors; composed as r_channel = sum n_kind eps_kind, F_gates = prod (1 - eps_kind)^n_kind, F(0) = prod_q [(1 - eps_prep)(1 - eps_D) + eps_prep eps_B], the GHZ readout factors (1 - sum eps_q) on the populations and prod (1 - 2 eps_q) on the contrast, the depolarizing heavy-output prediction (benchmarks/budget.py) | Magesan 2011 (additivity of average infidelities to first order); Wright et al. 2019 (the composite expected-fidelity product of Section 7.9); Sections 6.8 and 9.6 | an estimate, never a bound, and not in either direction: coherent errors of one Clifford's pulses partly cancel, so single-qubit RB on the example device measures r about 0.8 of r_channel (2.00e-5 measured against 2.455e-5 composed), while the coherent crosstalk rotations of a GHZ circuit's five consecutive carrier pulses on the same neighbour add in amplitude, so its exact register infidelity 2.33e-3 exceeds 1 - F_gates = 1.40e-3. The earlier text said 'about half' here and '0.8' in anchor.m10.single_qubit_rb and the README, and it read the simultaneous-RB comparison as a second factor 2 in the other direction; that factor was a unit mismatch between the joint decay and a per-Clifford composition (conv.simultaneous_rb_units), and once the units match the composition predicts the simultaneous decay to 3 %, so simultaneous RB is NOT evidence of errors adding in amplitude. The Section 9.6 scales are bounds on the coherent errors, are summed over each kind's whole schedule entry (the crosstalk on ions outside the benchmarked set included, so r_intrinsic bounds a LARGER error than r_channel measures) and are reported beside the prediction, never summed into it | background |
| `conv.bermudez_pauli_mapping` | eps -> Pauli channels, three variants | 6.8 (PLAN.md 920-923); 4.4.7 (610); M7, M10 | Section 6.8 lists 'Bermudez et al.'s three-variant mapping of eps onto Pauli channels for fault-tolerance studies is provided as well [verified]' as a deliverable and Section 4.4.7 repeats it, but the plan transcribes NO formulas for the three variants (one occurrence of 'three-variant' in the whole plan, with no equations), and the first release therefore omits it. What noise/summary.py does provide of Section 6.8 is complete: the entanglement infidelity and the average gate infidelity of a simulated channel, its Pauli twirl (which IS a mapping of a simulated channel onto a Pauli channel, p_Q from the Pauli transfer-matrix diagonal) and the depolarizing rate of conv.depolarizing_normalization. Implementing the three variants needs the source itself, not the plan | Bermudez et al., PRX 7, 041061 (2017), the fault-tolerance section; noise/summary.py; docs/limits.md | docs/limits.md filed the omission under 'open conventions the plan records rather than resolves', which the plan does not do -- it lists the mapping as a deliverable of Sections 6.8 and 4.4.7 -- and no ledger record carried the deviation at all; both are corrected, the limits page now naming it a deliverable the release omits | background |
| `conv.boundary_threshold_is_branch_scaled_on_both_sides` | SolverOptions.boundary_population_max | 5.5 | Section 5.5's one threshold: the population in the top two Fock levels above 1e-6 'of the population the pulse moves' repeats the run with the cap raised | dynamics/engine.py run_pulses (the boundary trip and the margin check); run/space.py cap_for; run/gate_local.py step_space; calibration/surrogate.py spot_check_space | M9a applied two thresholds: the boundary trip compared the BRANCH-relative top-level population against boundary_population_max while the margin check measured the populated range at boundary_population_max / branch_weight, MIXTURE-relative, so on a Fock branch of weight w the two differed by 1/w. Both now read the branch-scaled threshold min(boundary_population_max / w, 0.5): one threshold, one population (the mixture's, which is what the pulse moves), and a single state has w = 1 so no existing number moves. cap_for also now takes the run's boundary_population_max instead of a hard-coded 1e-6, so Section 5.5's per-test override for hot modes reaches the initial cap as well as the monitor (it used to give a loosened run an over-large cap and a tightened one a cap-raising retry). the third call site, calibration/surrogate.py's spot_check_space, took cap_for's own default until the M8 fixer gave it a tail keyword and passed opts.boundary_population_max from both surrogate_table call sites (None there resolves to SolverOptions().boundary_population_max, named explicitly), so all three now read one threshold | corrected |
| `conv.branch_enumeration_refuses_an_empty_mixture` | enumerate_branches | 5.3; 5.7 | the Fock sum keeps every branch of the diagonal initial mixture whose weight reaches SolverOptions.branch_weight_min and reports the dropped total; a cutoff that keeps NO state of some factor, or no product of the kept states, is refused with a RunError naming the cutoff and the most likely weight | qutip_trap/run/job.py | _truncated_weights used to fall back to [(argmax, 1.0)] when every probability sat below the cutoff, renormalizing a hard-truncated mixture to a pure state and reporting a dropped weight of zero; and when no PRODUCT reached the cutoff the enumeration returned an empty branch list, so the run evolved nothing and failed deep in the readout with 'the internal state has no population'. Both are reachable on a Doppler-only device: the 40Ca+ optical preset has two frozen modes at nbar = 10 whose joint ground-state weight is 8e-3, below the M7 tests' branch_weight_min = 1e-2 | corrected |
| `conv.branching_deficit` | sum_lo branching(lo -> e) + sum declared = 1 | 4.5.5; Appendix E Level | every upper level reached by at least one E1 transition must have its tabulated branchings plus its declared Level.untabulated_branching sum to 1 within 1e-9, and sum_{b q'} \|c\|^2 then equals the TABULATED total, never an unconditional 1 | qutip_trap/species/model.py; tests/test_m0a_branchings.py | an incomplete branching set used to be renormalized to unity over whatever channels happened to be tabulated, which put 100.0% of 40Ca+ P3/2's decay into the 393 nm cycling line and made its 5.9% of D-state leakage vanish. Level.untabulated_branching is a DEVIATION from Appendix E: an additive field with default () (the API-freeze test allows additions), used by 171Yb+'s 3D[3/2]1/2 for the 297.143 nm channel (audit item E2) | corrected |
| `conv.ca40_branching` | b(P1/2 -> S1/2), b(P3/2 -> S1/2) | 9.13; 8.1; 12 | the table carries Ramm et al. 2013's measured 0.93565(7)/0.06435(7) for the P1/2 split (adopted 2026-09-08) and Gerritsma et al. 2008's measured 0.93469/0.0587/0.00661 for the P3/2 split into S1/2/D5/2/D3/2 | Ramm et al., Phys. Rev. Lett. 111, 023004 (2013); Gerritsma et al., Eur. Phys. J. D 50, 13 (2008); Ozeri 2007 Table I; qutip_trap/species/ca40.py | PLAN.md 9.13 states 0.941 and 0.940. The table's previous P3/2 value 0.9412 came from Ozeri's f^-1 = 17, which PLAN.md:457 lists for 43Ca+ and NOT for 40Ca+ - a misattribution; its previous P1/2 value was Section 8.1's rounder '6% branch to D3/2', 6.8 % low, now kept as the contested cross-check ca40.P12.branching_to_D32_plan_8_1. Section 12 records 1:12, 1:16 and about 1:17.6 in different sources, a 30 % spread it calls a gap 'with no dedicated measurement'; the dedicated measurements DO exist: Ramm et al. 2013 give 0.93565(7)/0.06435(7) for P1/2 (Hettrich et al. 2015's independent 0.93572(25) agrees) and Gerritsma et al. 2008 the P3/2 split. Ramm's value was PROMOTED from cross-check to the branching the table uses on 2026-09-08 because it is what makes the 397 nm partial rate reproduce Hettrich's gamma_PS: 23.0526 x 0.93565 = 21.5691 MHz against his printed 21.57(8), where Section 8.1's 0.94 would give 21.6694 MHz, 1.2 sigma outside his bar (audit item E24) | corrected |
| `conv.ca40_linewidth_reading` | 21.57 MHz = gamma_PS/2pi (PARTIAL) | 9.13; 12; Appendix E Transition | Hettrich's Table: <S1/2\|\|d\|\|P1/2> = 2.8928(43) e a0, tau(4p 2P1/2) = 6.904(26) ns, branching 0.93572(25), gamma_PS = 2 pi x 21.57(8) MHz and gamma_PD = 2 pi x 1.482(8) MHz, so the TOTAL is 23.05 MHz | Hettrich et al., Phys. Rev. Lett. 115, 143003 (2015); Meir et al., Phys. Rev. A 101, 012509 (2020); PLAN.md 9.13 line 1385; qutip_trap/species/ca40.py | FLIPPED on 2026-09-08. The ca40 table used to read the plan's quoted 21.57 and 23.4 MHz as the TOTAL P1/2 and P3/2 rates; it now reads both as PARTIAL rates into S1/2 and takes the totals from MEASURED lifetimes instead (ca40.P12.lifetime_s = 6.904(26) ns, gamma/2pi = 23.0526 MHz, Hettrich; ca40.P32.lifetime_s = 6.639(42) ns, 23.9727 MHz, Meir et al. 2020). Three independent reasons, all agreeing: (1) Hettrich et al. 2015 print gamma_PS = 2 pi x 21.57(8) MHz explicitly as the partial P1/2 -> S1/2 rate, beside gamma_PD = 2 pi x 1.482(8) MHz and tau = 6.904(26) ns; (2) PLAN.md 9.13 (line 1385) itself reads both quoted linewidths as partial rates - '40Ca+ 397 nm with the quoted 21.57 MHz and 393 nm with 23.4 MHz read as partial rates: 2.045 e a0, 45.11 mW/cm^2 and 2.972 e a0, 50.25 mW/cm^2' - while asking that 'the species table must say which'; (3) as a TOTAL rate 21.57 MHz means tau(P1/2) = 7.379 ns, which no lifetime measurement supports (Hettrich 6.904(26) ns, Jin and Church 1993 7.098(20) ns). With Ramm et al. 2013's branching promoted to the one the table uses (conv.ca40_branching) the partial rate comes out at 21.5691 MHz, reproducing Hettrich's printed value to 4.0e-5, and the residual D3/2 partial rate 1.4834 MHz reproduces his gamma_PD inside its own 0.008 MHz bar - two identities the previous reading could not produce. Hettrich's printed <S1/2\|\|d\|\|P1/2> = 2.8928(43) e a0 is the same element in the normalization that omits the (2J+1) factor: sqrt(2) x 2.0455 = 2.8920, inside his uncertainty. The quoted linewidths stay in the table ONLY as contested cross-checks that nothing is built from (ca40.P12.linewidth_quoted_hz, ca40.P32.linewidth_quoted_hz). Every 40Ca+ rate in the tree moved: the movers are listed in anchor.ca40.gamma_dependent_repins, the 393 nm residual disagreement in anchor.ca40.p32_linewidth_readings | corrected |
| `conv.calibration_cache` | key = (Device.hash(), seed, surrogate, experiments, t0, digest(kwargs)) | 7.5; Appendix E Device.hash | tables are cached per device configuration and noise seed; a table whose device_hash differs from the device it is asked for is never returned, so a changed parameter invalidates the old configuration's tables and the new one has none until calibrate runs (run(table=None) builds the surrogate) | calibration/cache.py | - | background |
| `conv.calibration_graph` | crystal_image -> field -> micromotion -> modes -> rabi, stark, qubit frequency -> crosstalk -> entangling scans -> detection, heating | 7.5; Appendix E calibrate(); 9.17 Calibration dependency graph | calibrate(surrogate=False) runs the experiments in this order and never reorders; a downstream fit refuses to run while an entry group it reads is uncalibrated and its own entries (the groups PRODUCES names for it) become uncalibrated under its name, with the reason in CalibrationReport.refused; a group whose experiment was refused is not established even when it carries no entry to mark, and a group that is a seed because the caller did not ASK for its experiment stays a seed; seed entries (the derived values) are legitimate upstream inputs (the bootstrap) | PLAN.md Section 7.5 [corrected: critique, 2026-09-04]; calibration/experiments.py ORDER, UPSTREAM | the second revision's unordered experiment list and its stark, crosstalk and field entries without an experiment. The first M8 build recorded the refusal in the report but left the refused experiment's entries as schedulable SEEDS (refuse()'s keys argument was dead and both call sites passed ()): with micromotion['beta[2]'] uncalibrated the mode-frequency fit refused and the mode frequencies silently stayed the derived seeds, which is exactly the fallback Section 7.3 forbids, and a Bell circuit still scheduled 15 pulses from that table | corrected |
| `conv.calibration_refusal_marks_its_entries` | refuse(name) -> PRODUCES[name] entries become uncalibrated | 7.3; 7.5 Bootstrap, fits and failure; 9.17 Calibration dependency graph | PRODUCES maps each experiment to the entry groups it writes (field_scan -> field, micromotion_scan -> micromotion, sideband_spectroscopy -> modes, nbar, lamb_dicke, rabi_scan -> rabi, stark_scan -> stark, ramsey_frequency -> qubit_freq, crosstalk_scan -> crosstalk, crosstalk_phase, ms_scan -> ms, detection_histogram -> detection, heating_rate -> heating; crystal_image and parity_scan write none). A refused experiment marks exactly those entries uncalibrated under its own name, keeping their VALUES for the record, and the refusal cascades through UPSTREAM: an uncalibrated field refuses the micromotion, mode, Rabi, Stark and Ramsey-frequency fits in turn, and the uncalibrated Rabi entry then makes schedule() raise ScheduleError. An entry that is a seed because the caller did not ASK for its experiment stays a seed (calibrate(experiments=...) and the Section 7.5 bootstrap). A group whose producer was refused is treated as unestablished even when it holds no entry to mark, which no path in the present graph reaches but which keeps the rule true if micromotion_scan ever gains an upstream | calibration/experiments.py PRODUCES, refused_table, full_calibration refuse/check; tests/test_m8_calibration_fixes.py | the refuse() helper's keys argument was dead and both call sites passed (), so a refusal was recorded in the report while the mode frequencies the fit could not establish stayed schedulable derived seeds and a Bell circuit still scheduled 15 pulses from that table; the module docstring, conv.calibration_graph and PLAN 7.3's last clause all said otherwise | corrected |
| `conv.check_script_residual_class` | run_checks.py tolerance classes | M0 (Section 10) first CI job; Appendix D; 5.5 | every numeric token of a committed oracle is compared at rtol 1e-9 (atol 1e-12) except the residual class, a number below 1e-6 in magnitude printed with at most three significant digits (leaked populations, norm losses, element differences), compared to a factor 3; 9.6 % of the 8763 committed tokens fall in the residual class (check_circuits 160, check_transport 120, check_composite 76, check_comb 75) | validation/scripts/run_checks.py; tests/test_run_checks.py | a check script with no committed oracle fails the run; fresh numeric lines with no committed counterpart are counted and reported as a stale oracle; --update --only <stem> regenerates one oracle on purpose (M0 audit, 2026-09-07) | background |
| `conv.closure_amplitude_at_the_fitted_offset` | closure_offset_used_hz = closure_offset_hz | 7.5 item 4 | the leakage parabola over the scanned detuning offsets gives a CONTINUOUS closure offset, and the table applies it, so the cos(2 chi_1 s^2) amplitude fit is run at that offset: when the scanned grid does not already carry it, one extra amplitude scan is measured there and closure_offset_used_hz is the fitted offset, which the table then applies together with the scale | experiments/entangling.py ms_scan (amplitude_row); calibration/experiments.py (the ms block) | the scan snapped the parabola's minimum to the nearest SCANNED offset before fitting the amplitude while the table applied the continuous one: on the two-ion fixture, whose offsets are (-2, 0, +2) kHz and whose fitted minimum is a couple of hundred hertz, the closure scale was measured at 0 Hz and applied at the fitted offset | corrected |
| `conv.cnot_template_sign` | s in XX(s pi/4) | 7.7 CNOT; 4.4.2 | RY(v pi/2)_c, XX(s pi/4), RX(-s pi/2)_c RX(-v s pi/2)_t, RY(-v pi/2)_c = e^{i pi v s/4} CNOT for all four (s, v); XX(-pi/4) is exported as MS(0, pi, pi/2) (GPi(pi) = -X), so the angle stays in IonQ's [0, 0.25] turns | Maslov 2017 Fig. 1; PLAN.md Section 7.7; tests/test_compiler.py | the plan makes s the pair's physical sign 'from the simulator's own mode structure and detuning'; here the scheduler realizes either sign of the native MS from the waveform's signed chi by a pi on the second ion's tones (M4, Section 4.4.2), so the compiler's s is a free convention (default +1) and the physical sign never reaches the compiler | corrected |
| `conv.collapse_op_rate_units` | CollapseOp.rate_hz, the value corrected (the name is Appendix E's) | 5.6; 6.2; 6.3; 6.5; 13 | the field carries ndot in quanta/s, gamma_phi = 1/T_2, 2/tau and the intensity-noise and scattering densities, all ordinary rates in s^-1; heating_channels now reports ndot itself (400.0 for a 400 quanta/s mode, both the down and the up operator at n_bar_bath = None), motional dephasing 2/tau (250.0 at tau = 8 ms) and qubit dephasing 1/T_2 (0.6667 at T_2 = 1.5 s) | M7 audit E-18 (dynamics/channels.py, noise/scattering.py; tests/test_m7_dc_floor.py) | all six construction sites divided by 2 pi (dynamics/channels.py x5 through hz_from_rad_s and an explicit /TWO_PI, noise/scattering.py x1), reporting a number 6.28 times small: Section 5.6's 2 pi rule converts ANGULAR FREQUENCIES, not rates. The audit offered a rename to rate_per_s as an alternative, but Appendix E (PLAN.md:2561) declares rate_hz and tests/test_api_freeze.py enforces it, so the PLAN wins and only the value is corrected - a rate in Hz IS a rate in s^-1, and what was wrong was treating the value as an angular frequency, not the label. The field is reporting-only (nothing in the engine, the run or the noise layer reads it), so no dynamics change follows | corrected |
| `conv.collision_outcomes` | kick energy k_B T m_gas/m_ion; reorder from a configured permutation distribution | 6.7 'a heating kick drawn from a configured energy distribution whose scale is the neutral's thermal energy times the mass ratio' | <E_kick> = k_B T (m_gas/m_ion) x kick_scale_multiplier with the partial-pressure-weighted gas mass, drawn from an exponential (maximum entropy at fixed mean) or a Maxwell chi^2_3 of the same mean; <delta nbar> = <E_kick>/(hbar omega_m) on the mode it heats most (the softest). The plan states the scale, not its O(1) coefficient (the elastic head-on transfer fraction is 4 m_g/m_i and the momentum-kick estimate 3 m_g k_B T/(2 m_i), both of this order), so the coefficient is 1 and kick_scale_multiplier is the device's handle | M7 audit E-7 (noise/spectra.Collisions, noise/collisions.mean_kick_quanta/sample_kick_quanta/sample_reorder, run/job.py; tests/test_m7_collisions_physics.py, tests/test_run_noise.py) | the scale gives 7.4e4 quanta of a 1 MHz mode for H2 at 300 K on 171Yb+, ABOVE Section 6.7's parenthetical 'tens to thousands' (which is reached at a few MHz for a lighter ion or a heavier gas ratio); the formula the plan specifies is implemented rather than tuned to the parenthetical. Two limitations are declared in Diagnostics.approximations rather than hidden: a kick landing after the preparation discards the shot instead of adding its nbar to the already-evolved sample (the dynamical samples are propagated before the shot loop), and a reorder permutes RunState.order and discards the shot without re-deriving b_{i,m}, eta_{i,m} or the pair sign s of Section 7.7 for the shots that follow | derived |
| `conv.comb_explicit_folded_partition` | \|mu_j - mu_res\| <= 10/t_g explicit, the rest folded, never both | 4.3.7 (PLAN.md:515), 9.17 comb tone cut; light/comb.py | CombSpec.explicit_orders(resonance_hz, gate_time_s) is the ONE definition of the explicit set; tones() emits exactly those orders and comb_factor/stark4_hz exclude exactly those from the static l-sum, so the two sets partition the comb's beat notes. Both paths now place beat notes on the one grid l nu_rep + Delta_nu_M (CombSpec.beat_notes_hz), so a two-comb drive's tone set and Stark sum share it. At 80 MHz and a 100 us gate the 10/t_g cut is 100 kHz, far inside the tooth gap: the explicit set is {j_res} at both 10/t_g and 20/t_g, so the 9.17 regression's gate-phase change is exactly 0 (< 1e-4 rad). With a window widened until the l = j +- 1 neighbours cross, the folded shift alone moves by more than 1 Hz while the TOTAL (folded plus the retained off-resonant tones' own second-order shift) is invariant, which is the plan's convergence test with content | tests/test_comb_drive.py::test_near_resonant_explicit_far_detuned_folded_never_both, ::test_aom_offset_is_on_the_same_grid_in_both_paths | M2's stark4_hz summed every l with no exclusion, so every tone tones() kept explicit was simultaneously inside the static shift: 5510.08 Hz against 1152.74 Hz at the Lee point, a factor 4.78 of double-counted shift. aom_offset_hz was absent from the entire fourth-order path, so the two grids differed by Delta_nu_M. The ledger's anchor.m2.comb_numbers claim 'the rest folded into the fourth-order shift' is now true (M2 audit E7) | corrected |
| `conv.comb_resonance_guard` | \|delta_{n,a}\| > 1e-9 nu_rep | 4.3.7 ('the guard is delta_{n,a} != 0, never j != 0'), 12; light/comb.py | the l-sum's singularity guard is a stated threshold relative to the tooth spacing, SINGULAR_REL_TOL = 1e-9 times nu_rep, evaluated at the nearest FOLDED beat note (a beat note held explicit as a QobjEvo coefficient is not a term of the perturbative sum, which is what lets a comb LOCKED to the qubit still carry a shift from its far-detuned teeth). l = 0 is never skipped: it is the operative case for the Zeeman partners, where C_{10,11} = C_{10,1-1} = 0.988841 and that equality carries the elliptical-polarization cancellation | tests/test_comb_drive.py::test_the_resonant_comb_is_refused_rather_than_returning_a_huge_shift | M2 tested delta == 0.0 and np.any(den == 0.0) by exact float equality, which any comb whose rep rate nearly divides the splitting defeats: nu_rep = nu_q/158 (resonant to 1.9 microhertz) returned 1.12389628e+17 Hz with no error and no warning. The couplings half of the audit's recommendation is deliberately NOT a raise: \|delta_{n,a}\| below \|Omega_{n,a}\| is the source's own 'marginal at full power' band (Omega_0/2pi = 45.7 MHz against delta_00,10/2pi = 42.8 MHz at its own operating point), which PLAN.md:530 keeps while declining to claim the closed form there, so it is a RuntimeWarning naming both numbers. Refusing it would refuse the plan's operating point (M2 audit E6) | corrected |
| `conv.comb_sum_depth` | l_max = ceil(8/(pi nu_rep tau)) | 4.3.7, 9.15 C_{00,10} convergence; Appendix E CombSpec.pair_order_max; light/comb.py | the sech^2 fourth-order weight has half-width 1/(pi nu_rep tau) in the tooth separation l (189.470 at 120 MHz and 14 ps, 397.887 at 80 MHz and 10 ps) and its tail beyond k half-widths is about 4 e^{-2k} of the sum, so CombSpec.pair_order_max = None derives the depth as eight half-widths: 1516 at the Lee point and 3184 at the Mizrahi/APB one. The plan's 'sum to \|k\| >~ 1000' is then a consequence of the envelope; an explicit pair_order_max is kept as an override and validated against the derived depth | tests/test_comb_drive.py::test_sum_depth_comes_from_the_envelope_and_covers_both_operating_points | Appendix E declares the literal pair_order_max = 1200 with a floor of 1000. That is 2.6x too shallow at the plan's own second operating point (80 MHz, 10 ps, nu_q = 12.642812118466 GHz), where stark4_hz raised 'has not converged: 79673.08 vs 79680.64' and refused a legitimate configuration; the converged value is 79680.65 Hz. Five half-widths (the M2 audit's suggested k ~ 5) still leaves 1.7e-5 at the Lee point, above the 1e-6 doubling tolerance, so k = 8. comb_factor had NO doubling check at all and now takes the same one behind validate=True (M2 audit E5) | corrected |
| `conv.compiler_frame_absorption` | phi -> phi - theta; RZ_final discarded | 7.2 item 1; 7.6; 13 Virtual-Z propagation | every rz is absorbed into the phases of the later gpi, gpi2 and ms operations in time order (zz commutes); the compiled circuit carries only gpi, gpi2, ms, zz (the exported native set) and a residual per-qubit frame RZ(theta_q) that the computational-basis measurement cannot see; the whole-circuit check is RZ(frame) U_compiled = e^{i alpha} U_target | PLAN.md Sections 7.2 and 7.6 | keeping rz in the compiled circuit would break the IonQ JSON export of Section 7.6 (rz is not an exported gate) | verified |
| `conv.compiler_single_qubit_decomposition` | RZ(alpha) GPi2(0) RZ(beta) GPi2(0) RZ(gamma) | 7.2 item 1; 9.6 | GPi2(0) RZ(beta) GPi2(0) = RY(-beta) RX(pi) up to a phase, so (alpha, -beta, -gamma) are the ZYZ Euler angles of U X; a pure Z rotation is one virtual rz, a pi or pi/2 equatorial rotation one GPi or GPi2 plus an rz (H = RY(pi/2) Z is one GPi2 after a virtual Z(pi)) | PLAN.md Section 7.2; tests/test_compiler.py (20 random SU(2) targets to 1.5e-15) | the ZYZ solve reads a = arg V10 - arg V00 and c = arg V11 - arg V10 (each modulo 2 pi); solving the half-sum and half-difference for a and c leaves a pi branch that flips RY(b) to RY(-b), which the first draft of the compiler hit | recomputed here |
| `conv.composite_distance_global_phase` | d(U, V) = min_g \|\|U - e^{ig} V\|\|_2 | 4.3.5, 9.15 order ladder, 13 Gate fidelity measure; control/composite.py | CompositePulse.distance quotients the global phase out (g = arg Tr(V^dag U) for 2x2, the bisector of the two eigenphases of V^dag U), because F_K, F_C and F_avg all quotient it and the distance stands in for them below the float64 infidelity floor; the exact spectral norm stays available as operator_distance for the 1e-12 operator identities | tests/test_composite_pulses.py::test_order_ladder_at_theta_pi | M2 compared against +R(theta, phi) only, so short-CORPSE's n = (0, 1, 0) winding (which returns -R, a global phase the module's own comment records) gave the CONSTANT distance 2.0 at every eps: order_slope's sub-1e-8 fallback then substituted 4.0 and the reported detuning slope collapsed from the plan's 4.000 to 2e-16. The physics was right all along (1 - F_K gives 4.000); the measure was wrong, and the 9.15 order-ladder row for short_CORPSE would have failed. short_CORPSE is the only one of the 17 constructible (family, order) pairs that folds to -R (M2 audit E3) | corrected |
| `conv.composite_n_rep_is_a_record` | n_rep = 2^{2j-2} | Appendix E (PLAN.md:2800, 2802), 4.3.5, 6.9; control/composite.py, noise/decoupling.py | Appendix E's n_rep, 'P2j/N2j/B2j: repetitions of the cached T_{2j-2} block', is a RECORD of how segments was built (control.composite.suzuki_block_power: 1, 4, 16, 64 at j = 1, 2, 3, 4, the field's declared default 1 being the j = 1 value) and never a multiplier a consumer applies again; composite_segments expands pulse.segments verbatim, phases and areas alike | tests/test_m2_composite_segments.py | M2 had noise/decoupling.py's composite_segments multiply the whole segment tuple by n_rep while total_rotation_rad and duration_s ignored it, a factor-n_rep disagreement on the pulse duration that Section 4.3.5 makes the scheduler's second reported quantity (P2j at theta = pi/2, order 1, n_rep 3: 8.5 pi against 25.5 pi). Both 'active' readings are refuted and the refutation is a test: repeating the whole pulse multiplies the target angle, so target()/propagator() would disagree; repeating the corrector alone preserves the target exactly (every corrector in the Section 4.3.5 library folds to +I at eps = 0, measured to 5e-15) but drops P2's amplitude infidelity slope from 6.000 to 1.999, because the corrector cancels the TARGET pulse's error and a second copy has none to cancel. An explicit n_rep is accepted only when it equals the derived block power (M2 audit E4, E12) | derived |
| `conv.composite_segments_azimuth_once` | ControlSegment(area, phase, area/Omega) | 13 row 'Composite-pulse sequence order in time', 6.9; noise/decoupling.py | pulse.segments already carries the target azimuth phi_t on EVERY entry, the zeroth pulse included, so composite_segments passes the stored phase through unchanged | tests/test_m2_composite_segments.py | M2 added phi_t a second time, so every control segment came out at phi_t too high (BB1 at theta = pi, phi_t = 0.7: [1.4, 3.2235, 6.8704, 3.2235] against [0.7, 2.5235, 6.1704, 2.5235]). Invisible in every existing test because they all use phi_rad = 0 and both consumers (dc_polygon, amplitude_filter_function) are invariant under a rigid azimuth rotation; it bit _monte_carlo_dephasing, which emits Tone(0.0, seg.phi_rad, ...) into a real Schedule, and DecouplingSequence.segments()'s inner path, which did not double-add and therefore disagreed (M2 audit E4) | corrected |
| `conv.computational_ordering` | sigma_z = \|1><1\| - \|0><0\| | 5.2, 5.7, 7.6; hilbert/operators.py | qudit index 0 is the lower qubit level \|0> = Species.qubit[0], index 1 the upper; sigma_+ = \|1><0\|; the ENERGY sigma_z of H_int = (Delta/2) sigma_z and H_Stark puts the upper level up; the native-gate matrices act on (\|0>, \|1>) as printed | PLAN.md Sections 5.7 and 7.6; tests/test_native_pulses.py | the computational Pauli Z of control/native.py (Z\|0> = +\|0>) is the NEGATIVE of the energy sigma_z; the two are never aliased (M2) | derived |
| `conv.cp_template_exact` | CP(theta) = [RZ(theta/2) (x) RZ(theta/2)] ZZ(-theta/2) | 7.7 Controlled phase; 9.6 | exact up to a global phase for every theta, with ZZ(theta) = exp(-i theta/2 Z Z) the native zz or the wrapper W XX(theta/2) W_in (W = GPi2(pi/2)^(x2), W_in = GPi2(3 pi/2)^(x2), the M4 identity); Debnath's Fig. 2b template with the fixed RZ(sgn(theta) pi/2) pair is CP(theta) times RZ((sgn(theta) pi - theta)/2) on both qubits, overlap \|Tr\|/4 = cos^2((sgn(theta) pi - theta)/4) = 0.854 at pi/2 and 0.691 at pi/4, and appending RZ((theta - sgn(theta) pi)/2) on both fixes it | PLAN.md Section 7.7; tests/test_compiler.py | the plan's 0.854 and 0.691 belong to the fixed-rotation template; a bare ZZ(-theta/2) has the different defect RZ(-theta/2)^(x2), overlap cos^2(theta/4) (0.854 at pi/2 by coincidence, 0.5 at pi) | recomputed here |
| `conv.crosstalk_ratio` | eps_ij = Omega_j/Omega_i | 6.6 Addressing crosstalk; 13 Crosstalk ratio; 4.5.7 crosstalk convention | a Rabi (amplitude) ratio from beam i onto ion j, never an intensity ratio; for a two-photon Raman pair with both beams sharing one Gaussian profile Omega is proportional to the intensity, so the amplitude ratio equals the single-beam intensity ratio at the neighbour (2.2 % for a 2.5 um waist at 3.45 um spacing) | Fang 2022; Schindler 2013 (inverted and squared in print); calibration/experiments.py; calibration/surrogate.py | Schindler's printed 22/121 = 18 % is the amplitude ratio and 3.3 % the intensity ratio matching his stated < 3 % bound; the printed squared form would read 3025 % | corrected |
| `conv.curvature_naming` | d2nu_dB2, taylor_c2 | 13 Curvature naming; 4.5.1; 6.3 | d2nu_dB2 (Harty's 2.4 mHz/mG^2) and taylor_c2 = (1/2) d2nu/dB2 (Langer's 0.305 Hz/uT^2; 171Yb+'s 310.8 Hz/G^2) stored separately; accumulated phase 2 pi int [nu(B) - nu(B0)] dt with the bracket in Hz | Harty 2014; Langer 2005; derivation audit 2026-09-04 | one field named 'curvature' silently mixes a factor 2; the missing 2 pi is 6.28 in both of Langer's figures | corrected |
| `conv.dc_floor_channel_order` | m = control.order if channel in control.corrects else 0 | 6.9 'The dc floor and the max rule'; 9.15 'Leading coefficients at theta = pi' | c-hat is sequence- AND axis-dependent: CompositePulse.order is the order of the channels the pulse CORRECTS, so against any other channel the residual is O(eps^2) and the floor is the primitive's (m = 0, c-hat_1 = 1 for detuning and pi^2/4 for amplitude). SCROFULOUS's eps_d coefficient is the plan's 4.0, so its dephasing floor is 4x the primitive's | M7 audit E-1 (noise/decoupling.py filter_function; tests/test_m7_dc_floor.py) | using control.order unconditionally divided an O(eps^2) infidelity by eps^{2(m+1)}, inflating c-hat as eps^{-2m}: SK1 read 108x and BB1 19400x too high, so filter_function reported a 70 % infidelity (0.6998) for a BB1 pi pulse under 300 Hz dephasing noise. The whole CompositePulse branch was uncalled by any test | corrected |
| `conv.dc_floor_decoupling_sequences` | infidelity_dc = <1 - F> over a Gaussian frozen beta (Gauss-Hermite), not c-hat (2m+1)!! rel^{m+1} | 6.9 'the prescription the module copies is to evaluate both estimates and report the larger at every point' | for a DecouplingSequence or Schedule no source supplies a c-hat, and the plan's c-hat (2m+1)!! rel^{m+1} is only the leading term of the exact Gaussian average of 1 - (1/4)\|Tr U_c^dag U\|^2 under a constant beta, so the average itself is reported. It reproduces the composite-pulse c-hat floors to 5e-4 relative (2e-3 for SCROFULOUS, whose O(beta^4) remainder is the library's largest). On a 300 Hz Gaussian band with 4 us pi pulses over 200 us: free induction 0.1237 against a first-order 0.1386, Hahn 2.30e-5 against 1.26e-3, CPMG-2 8.91e-6 against 9.03e-6, CPMG-4 8.93e-6 against 1.29e-6 | M7 audit E-17 (noise/decoupling.frozen_noise_floor; tests/test_m7_dc_floor.py) | before this, infidelity = max(a1sq, 0.0) for every non-CompositePulse control: the first-order estimate alone, which the plan says 'reports spuriously low infidelities for precisely the band that dominates a real trap'. CPMG-4 is the case that bites (7x) | derived |
| `conv.dc_floor_detuning_normalization` | eps_d = 2 beta_d/Omega, so the detuning moment is 4 <beta_d^2> | 6.9; 4.3.5 (Mount's primitive); 13 'Dephasing noise variable' | leading_coefficient fits c-hat against Mount's R = exp[-(i/2) theta (1 + eps_a)(sigma_phi + eps_d sigma_z)]. Matching that against Section 6.9's H_0 = beta . sigma (no 1/2 on the Pauli vector) gives eps_a = beta_a/Omega but eps_d = 2 beta_d/Omega, because beta_d is HALF the splitting fluctuation while eps_d scales the whole splitting. Measured: the exact frozen average of a primitive pi pulse is 4(beta_d/Omega)^2 and sin^2(pi beta_a/2 Omega) -> (pi^2/4)(beta_a/Omega)^2 | M7 audit, beyond the report: verified against the Gauss-Hermite average of the exact frozen-noise 1 - (1/4)\|Tr U_c^dag U\|^2 (tests/test_m7_dc_floor.py) | filter_function paired c-hat_d with <beta_d^2>/Omega^2, under-reporting every detuning dc floor by 4^(m+1): the exact-to-reported ratio was 4.0000 at m = 0 (primitive, SK1, BB1, SCROFULOUS) and converged to 16 for CORPSE at m = 1 (33.41, 20.34, 17.07, 16.32 as <beta^2>/Omega^2 fell through 3.6e-5, 9.0e-6, 2.25e-6, 5.6e-7). The plan's own pinned floors (SK1 5.87365e-6 at var = 2.07e9/pi, Omega = 1.5e6) are the c-hat arithmetic and are unchanged | corrected |
| `conv.decay_amplitude_omega_cubed` | \|c_{e->b q'}\|^2 = Gamma_partial(e -> b)/Gamma_e | 4.5.5 Kramers-Heisenberg amplitudes; 13 Dipole-element normalization stack | <b\|d_q'\|e> = sqrt(3 pi eps0 hbar c^3 Gamma_e/omega_e^3) c_{e->b,q'}, so the normalization of the decay amplitudes carries omega_{e,b}^3 PER CHANNEL: each raw element is weighted by omega_{e,lo}^(3/2) before sum_{b q'} \|.\|^2 is normalized | PLAN.md 4.5.5; qutip_trap/species/raman.py; tests/test_m0a_branchings.py | AtomicStructure.decay_amplitudes normalized the BARE elements until 2026-09-07, giving a branching weight of Gamma_partial/omega^3 per channel instead of Gamma_partial. Measured error: 171Yb+ P1/2 shares 0.408694/0.591306 against the tabulated 0.99499/0.00501, so ScatteringBudget.leakage_per_s was 118.03x too large and the elastic and spin-flip rates 2.4x too small; 40Ca+ P1/2 6.65x and 0.639x. dynamics/multilevel.py already carried one omega^3 per line, so this was a local M0a bug, not a plan ambiguity (audit item E1) | corrected |
| `conv.depolarizing_normalization` | Lambda_eps(rho) = (1 - eps) rho + eps/(4^n - 1) sum_{P != I} P rho P | 13 Depolarizing normalization; 6.8; 9.10 Depolarizing conversions; M7, M10 | the depolarizing channel is written with the UNIFORM weight eps/(4^n - 1) on each of the 4^n - 1 non-identity Paulis, in which eps is exactly the entanglement infidelity, so the rate the simulator reports IS the entanglement infidelity and the average gate infidelity is d/(d + 1) eps, (2/3) eps at n = 1 and (4/5) eps at n = 2; as Kraus operators that is sqrt(1 - eps) 1 together with sqrt(eps/3) X, Y, Z on one qubit or sqrt(eps/15) P over the fifteen non-identity two-qubit Paulis, and sum_K K^dag K = 1 to 1e-12 for every eps in [0, 1] (noise/summary.py depolarizing_kraus, whose Choi state depolarizing_choi is built from); the parameter Qiskit's depolarizing_error takes is the mixing weight of the maximally mixed state, lambda = eps 4^n/(4^n - 1), so Chen et al.'s Forte medians 2.0e-4 (single-qubit) and 46.4e-4 (two-qubit, direct RB) enter Qiskit as 2.67e-4 and 49.5e-4 (noise/summary.py qiskit_depolarizing_lambda) | Chen et al. 2023; Trout et al. 2018 (the uniform p/3 and p/15 weights); Qiskit Aer depolarizing_error; noise/summary.py; tests/test_m10_benchmarks.py | Trout 2018's p/3 and p/15 are the same weights under a different name for the parameter, not a different channel; Qiskit's lambda is NOT that p, and passing a published p to Qiskit as a lambda understates the rate by 4^n/(4^n - 1), 33 % on one qubit and 6.7 % on two. Kept separate from conv.rb_error_rate, whose p is a decay BASE, and from conv.gate_fidelity_measure, whose F_K is a unitary overlap: this row is a channel RATE | verified |
| `conv.designated_transitions_must_exist` | cycling, repumps, shelving in {Transition.label} | Appendix E Species; 4.5.2 | a designated cycling, repump or shelving label must name a tabulated Transition, not merely two known level names | qutip_trap/species/model.py; tests/test_m0a_branchings.py | species('88Sr+').transition('S1/2-P1/2') (the cycling line), ('D3/2-P1/2') (1092 nm repump), species('40Ca+').transition('D5/2-P3/2') (854 nm repump) and species('171Yb+').transition('D3/2-3D[3/2]1/2') (935.2 nm repump) all raised KeyError. All four records were added in the same pass (audit items E6, E7) | corrected |
| `conv.detection_efficiency_once` | eps_sys R_o, R_bg | 13 Detection efficiency; 8.1, 8.2 | epsilon_sys enters once, in RecordModel.from_rates (scattered -> detected); the background rate is a separate additive Poisson rate; apparatus presets ingest a DETECTED rate by dividing epsilon_sys out once (rates_from_detected) | Crain 2019 (Eq. 3 corrected); Acton 2006 (alpha/eta) | Crain's printed Eq. 3 carries the scattered R_o in the no-jump exponent, which makes it vanish and changes the bright error at 11 us by a factor 8.7 (6.26e-3 corrected against 7.18e-4 printed) | verified |
| `conv.detuning` | delta = omega_drive - omega_transition | 13 Detuning, Detuning symbols | delta from the carrier, red negative; mu = beat-note detuning from the carrier; delta_{i,m} = mu_i - omega_m always two-indexed | RMP 2003; Run 2B consolidation | Landsman's delta measured from a mode; Mount's dimensionless delta | verified |
| `conv.detuning_symbols` | delta, mu, delta_{i,m}, delta-hat | 13 Detuning symbols; 7.5 Ramsey experiments | delta = omega_L - omega_0 from the carrier; mu = beat-note detuning from the carrier; delta_{i,m} = mu_i - omega_m always two-indexed; delta-hat = the dimensionless composite-pulse detuning error | Run 2B consolidation; experiments/single_ion.py | Landsman's delta is measured from a mode; Mount's delta is dimensionless | corrected |
| `conv.device_hash` | Device.hash() | Appendix E preamble | canonical serialization: fields in declaration order, floats to 12 significant digits, ndarrays as dtype plus C-order bytes, dicts by sorted key, callables by qualified name plus a source digest, Qobj excluded; a cross-process test asserts equality | 2026-09-04 architect critique | Python's hash() is not supplied by frozen=True for ndarray, dict and Qobj fields | corrected |
| `conv.dipole_normalization_stack` | <J\|\|d\|\|J'> | 13 Dipole-element normalization stack; 4.5.6 | Gamma_{J'->J} = omega^3 (2J+1) \|<J\|\|d\|\|J'>\|^2/(3 pi eps0 hbar c^3 (2J'+1)) is the total rate out of J' into the level J, b/tau; the stretched cycling element is sqrt(1/2) <J\|\|er\|\|J'> on a J = 1/2 -> 3/2 line; Edmonds' round bracket is sqrt(2j+1) larger | Steck QAO Eqs. 7.237-7.283; Rb87 Eqs. 34-42 | the 'total rate into a partial-rate formula' factor the second revision listed does not exist; stacking a reduced element into a stretched-element formula is a factor 2 on a D2 line [corrected: derivation audit, 2026-09-04] | corrected |
| `conv.doppler_optimizes_detuning_only` | optimize_detuning over Delta; s and k_hat fixed | 4.2.1 | Section 4.2.1 states the objective over three beam parameters: 'Delta, s and k_hat are beam parameters chosen to minimize the participation-weighted mean of the per-mode steady-state nbar'. Only a common frequency offset of one beam group is optimized; s and k_hat are inputs. The steady-state nbar at the Doppler limit is nearly s independent - (Gamma/4 nu)(1 + alpha/cos^2 theta_L) sqrt(1 + s) to the force model's accuracy - so the omission barely moves the objective, but the RATE, which sets the stage duration, does depend on s and is never optimized; k_hat enters through cos^2 theta_L per mode and a genuine three-beam geometry choice is the three_beam_optimum of Itano and Wineland instead | qutip_trap/prep/doppler.py | restriction stated rather than implemented; the three-beam apportionment closed form is available separately (anchor.m3.itano_three_beam_optimum) | contested |
| `conv.doppler_stage_producer` | nbar_D per mode from the rate framework; force model only for nu/Gamma < 0.1 | 4.2.1; 13 Doppler limit; 9.17 Doppler detuning per beam | the Doppler stage is the Section 4.2.2 rate framework at any nu/Gamma with W from the Bloch steady state; one detuning per beam group is chosen to minimize the participation-weighted mean nbar (uniform weights when no gate mode is given) and every mode's residual is reported; the RMP force model E = (hbar Gamma/8)(1 + alpha/cos^2 theta_L)[(1 + s) Gamma/(2\|Delta\|) + 2\|Delta\|/Gamma] is reported as a cross-check for modes with nu/Gamma < 0.1 only, its emission weight read from the same steady state | qutip_trap/prep/doppler.py; validation/scripts/check_cooling.py | the two-level closed forms hbar Gamma/2 and 0.35 hbar Gamma (0.480 against 0.336 mK at 20 MHz, ratio 1.4286, 9.5 against 6.5 quanta at 1 MHz) are oracles in prep/closed_forms.py, never the producer | corrected |
| `conv.drive_coefficient_and_phase_continuity` | c(t) = (1/2) sum_tones Omega(tau) e^{-i(mu t - phi(tau))} | 5.2, 7.10; dynamics/hamiltonian.py | the beat-note phase is mu t in ABSOLUTE time for a phase-continuous tone (mu tau under phase_mode='reset'; the FM integral int mu dtau for a detuning schedule); envelopes, phases and detunings are callables of the time since the pulse start, a constant is a square pulse, an array is uniformly sampled over the pulse and cubic-spline interpolated; the static factors are rabi_scale x eps_ij x J_0(beta) x prod_frozen e^{-eta^2/2} L_n(eta^2) | PLAN.md Section 7.10 (phase-continuous operation at the nominal qubit frequency, the mode the virtual-Z rule was pinned in) | coefficients are QuTiP 5.3 f(t, **kwargs) callables carrying a shared evaluation counter, never strings (Section 5.2) | derived |
| `conv.drop_test_debye_waller_spread` | DW_SPREAD_DROP_MAX | 5.2; 11.3 item 2; 9.8 row 3 | a mode is dropped when \|alpha_m(tau)\|^2 (2 nbar_m + 1) < 1e-6 AND \|chi_m(tau)\| < 1e-4 AND max_i eta_{i,m}^2 sqrt(nbar_m (nbar_m + 1)) < 3e-4 rad; otherwise, below the freeze pair, it is frozen | run/space.py DW_SPREAD_DROP_MAX, ModeContribution.dw_spread_rad, classify; tests/test_m6_space_classes.py; tests/test_run_circuits.py | a strengthening of Section 5.2's rule, not a deviation from it. The plan's pair reads the Section 4.4.3 LOOP integrals only, so a pulse that closes a mode's loop exactly leaves the pair at ~1e-33 however strongly the mode couples, and dropping the mode removes its exact Debye-Waller factor e^{-eta^2/2} L_n(eta^2) from every carrier pulse - which is the very thing Section 5.2 keeps the FROZEN class for ('samples n_m once per shot and multiplies Omega_i by the exact Debye-Waller factor of that Fock state'). The MEAN of the factor is absorbed by the calibration, because the spot check and the run drop the same mode and Section 4.4.7's s^2 rescaling takes the mode-independent part; the shot-to-shot SPREAD is not. To leading order in eta the factor is 1 - eta^2 (n + 1/2), so over a thermal state (Var n = nbar(nbar + 1)) the rotation angle's standard deviation is eta^2 sqrt(nbar (nbar + 1)). Measured on the three-ion GHZ fixture, whose tilt mode has eta = 0.0809 and a loop contribution of 3.5e-33: 1.500e-3 rad at nbar = 0.05, 9.256e-3 at nbar = 1 and 6.864e-2 at nbar = 10, i.e. sin^2(delta theta/2) = 5.6e-7, 2.1e-5 and 1.2e-3 of infidelity on a pi pulse. The threshold 3e-4 rad is matched to the 1e-6 of the alpha^2 row ((3e-4/2)^2 = 2.3e-8 of infidelity, comfortably inside it). Consequence for the fixtures: the three-ion tilt mode (mode 4), which M6 had just made genuinely dropped, is FROZEN again - correctly, since its Debye-Waller spread is 1.5e-3 rad, five times the threshold - while a mode with eta = 0.008 at nbar = 0.05 (spread 1.5e-5 rad) and the plan's own 9.17 row (\|alpha\|^2(2n+1) = 1e-7, \|chi\| = 1e-5 at eta = 1e-3) stay dropped. The summed spread of the modes that ARE dropped is reported beside the loop-pair bound as SpaceSelection.dropped_dw_spread_rad and in the selection notes, so a Section 9.8 row 3 comparison can add it | corrected |
| `conv.dropped_mode_has_no_dynamics` | HilbertSpace.dropped | 5.2; 11.3 item 2; 9.8 row 3 | the class follows the contribution pair alone - dropped when \|alpha_m\|^2 (2 n_m + 1) < 1e-6 AND \|chi_m\| < 1e-4, whatever couples to the mode; the coupling test (\|eta\| > 1e-12 for some drive) decides only the modes NO entangling gate touches (no contribution): frozen when a carrier pulse couples to them, dropped when nothing does. A dropped mode has no tensor factor (like a frozen one) and additionally no Debye-Waller factor and no Fock branch, so it evolves freely and is not modelled at all, which is what makes SpaceSelection.dropped_contribution the loss nothing absorbs | PLAN.md Section 5.2 line 822; qutip_trap/run/space.py, qutip_trap/hilbert/space.py, qutip_trap/dynamics/hamiltonian.py, qutip_trap/run/job.py | the M6 code intersected the drop test with `coupled`, returning frozen for any coupled mode below the drop pair. Since a contribution only ever exists for a coupled mode, the `dropped` return was DEAD and the effective rule was `eta = 0 -> dropped`: Diagnostics.dropped_contribution was identically (0.0, 0.0), the three-ion GHZ tilt mode (\|alpha\|^2 (2n+1) = 3.5e-33, chi = 0.0) was reported frozen, and every such mode still cost a Fock branch and a Debye-Waller factor of 1 - O(1e-33). A second defect sat behind it: both frozen and dropped modes went into HilbertSpace.frozen, whose members all receive the exact Debye-Waller factor and a Fock branch, so `dropped` had no dynamical meaning at all | corrected |
| `conv.e1_structure_required` | AtomicStructure needs an E1 record | 4.5.4; 4.5.5 | AtomicStructure refuses to build when species.transitions carries no E1 record unless the qubit pair IS a tabulated E2 pair (the I = 0 optical qubit of Section 4.5.7, which needs no intermediate sum) | qutip_trap/species/raman.py; tests/test_m0a_branchings.py | 88Sr+ had the E2 clock line as its ONLY transition, so couplings_from, light_shift_rad_s, raman_coupling_rad_s, scattering_rates and rayleigh_dephasing_rate all returned 0 or {} with no error while available() listed it as buildable (audit item E7, defect B7) | corrected |
| `conv.e2_ac_stark_shift` | delta_St = -sum_{c != driven} \|Omega_c\|^2/(4 Delta_c) | 4.5.7; 12 | the ac Stark shift of a driven \|S, m>-\|D, m'> component by the nine off-resonant components inside the Zeeman span, second order in the same Omega(m, m') table: Delta(m, m') = 2 pi[(E_D(m0') - E_S(m0)) - (E_D(m') - E_S(m))] is a pure Zeeman offset, a component shifts its lower level by +\|Omega\|^2/(4 Delta) and its upper by -\|Omega\|^2/(4 Delta), and the driven component is excluded because the Rabi dynamics treats it exactly | PLAN.md 4.5.7; qutip_trap/species/quadrupole.py; qutip_trap/light/raman.py | light/raman.py hardcoded stark_shift_hz = 0.0 for kind == 'optical_E2'. UNVALIDATED, as Section 4.5.7 and Section 12 tag the whole E2 dissipation block: there is no source number to reproduce, and the sums have no i gamma/2 (Section 4.5.6), so a component degenerate with the driven one raises rather than returning an infinity | contested |
| `conv.e2_decay_weights` | w(m, m') = \|Lambda(m, m')\|^2/sum_m \|Lambda(m, m')\|^2 | 4.5.7 Dissipation, dephasing and crosstalk; 9.14 3-j table and mask | the E2 collapse operators are sqrt(A w(m, m')) \|S, m><D, m'\| with the same A that fixed the Rabi frequency; the raw 3-j squares sum to 1/6 at every m' (so the normalization is a multiplication by 6) and the weights sum to 1 over m at fixed m', giving sum_m A w = A | PLAN.md 4.5.7; qutip_trap/species/quadrupole.py; tests/test_m0a_quadrupole_dissipation.py | lambda_3j was referenced only by the drive and by tests; no decay operator used it and no lumped sqrt(A) sigma_- existed either (shelf decay appears only as readout/presets.py's shelf_lifetime_s inside the M5 Markov chain). quadrupole_collapse_operators is deliberately NOT wired into any engine: the noise layer of Section 4.5.7 chooses between it and the plan's own escape hatch, 'a lumped sqrt(A) sigma_- is adequate at A ~ 0.86 s^-1 for microsecond gates' (audit item E9, defect B8) | verified |
| `conv.effective_sample_size` | n_eff = p(1 - p) S / Var(p_s) | 3.4; 8.6 | with S samples of M_s shots and per-sample frequencies p_s, the pooled estimate is p_hat = sum M_s p_s / sum M_s and Var(p_hat) = Var(p_s)/S, where Var(p_s) = sigma^2_between + p(1 - p)/M is the TOTAL per-sample variance; Var(p_s) is estimated by the shot-count-weighted sample variance sum_s M_s (p_s - p_hat)^2 / [(S - 1) sum_s M_s], which reduces to var(p_s, ddof=1)/S for equal M_s, and n_eff = p(1 - p)/Var(p_hat), the minimum over the bitstrings seen, capped at the shot count | one-way random-effects decomposition (standard); qutip_trap/run/job.py effective_sample_size | the M6 code added the within-sample binomial term mean[p_s(1 - p_s)/M_s]/S on top of np.var(ps, ddof=1)/S, which already contains it: the error bars were up to sqrt(2) too large whenever more than one sample was drawn (measured on 200 replicates of i.i.d. Bernoulli shots: n_eff/shots = 0.547 at S = 8, 0.510 at S = 64, 0.640 at S = 2, where 1.0 is correct). The between-only ANOVA component would be s^2_total - mean[p_s(1 - p_s)/M_s], and adding the within term back to THAT returns s^2_total/S exactly | corrected |
| `conv.effective_wavevector` | Delta k = k_1 - k_2 | 13 Effective wavevector | beam 1 couples to the upper level; \|Delta k\| = 2k sin(theta_cross/2) from the configured geometry; k for one beam, 0 for a microwave | RMP; Wineland 1998; Lee 2005 | Landsman's k = k_2 - k_1; Lee's arXiv v1 Eq. 2 inverts its own beam-to-level assignment [corrected: preprint only] | corrected |
| `conv.eit_plan_sign_and_composition` | Delta > 0, Omega_r^2 = 4 nu (nu + Delta), Omega_r = sqrt(sum Omega_j^2) | 4.2.3; 13 EIT detuning sign; 9.17 Morigi EIT fixture | A_+- = (Omega_g^2/gamma) gamma^2 nu^2/{gamma^2 nu^2 + 4 [Omega_r^2/4 - nu (nu -+ Delta)]^2}, <n>_S = A_+/(A_- - A_+) with the poles Delta = 0 and Omega_r = 2 nu raising CoolingError, the light shift (sqrt(Delta^2 + Omega_r^2) - \|Delta\|)/2 and the tuning Omega_r = 2 sqrt(nu (nu + Delta)); Morigi's Delta is the negative of the plan's | qutip_trap/prep/eit.py; tests/test_eit.py | no carrier recoil term (W = 0 at the dark resonance, Morigi 2003); the two-photon eta is the projected difference of the legs | corrected |
| `conv.electric_field_noise` | S_E | 13 Electric-field noise density; 4.1.5 | single-sided S_E; Gamma_h = e^2 S_E/(4 m hbar omega) | Brownnutt Eqs. 11-12 | two-sided gives e^2 S_E/(2 m hbar omega); Turchette's arXiv preprint prints Eq. 3 inconsistent by 2, the published PRA is consistent [corrected: preprint only]; the filter-function stack uses TWO-sided spectra, converted explicitly at the boundary | corrected |
| `conv.electric_field_noise_adapter` | S_E(omega) from a NoiseSpectrum | 13 Electric-field noise density; 4.1.5; 6.1 | the heating layer takes the NoiseSpectrum RECORD and evaluates 2 S^(2).value(\|omega\|): the spectrum's own folding onto \|omega\|, ZERO above the tabulated band (where the density is the white level, which value() adds exactly once), no second white term in NoiseModel.heating_rates_quanta_per_s | tests/test_m1_heating_spectrum.py; noise/spectra.py | single_sided_from_spectrum took the two arrays and did 2 np.interp(\|omega\|, \|grid\|, S): a symmetric two-sided tabulation (-omega_max..+omega_max, which NoiseSpectrum accepts) has a NON-monotonic \|grid\| on which np.interp silently returns a constant (a factor 10 low at DC on a Lorentzian-like table), and np.interp clamps at the right edge, so above the band the layer reported 2 S[-1] + white instead of white - exactly where micromotion_sideband_heating_rate evaluates S_E, at tens of MHz [corrected: M1 audit, fix batch] | corrected |
| `conv.emission_collapse_operators` | C_q = sqrt(omega^3/(3 pi eps0 hbar c^3)) T_q^- per decay line and polarization | 4.2.8; 4.5.2; 8.1 | one collapse operator per fine-structure decay line (lower, upper) and emitted polarization index q, the lower<-upper block of the dipole operator in C m times sqrt(g_LU); sum_q C_q^dag C_q = Gamma_{U->L} P_U by the Wigner-Eckart sum rule (residual 1e-16 in the tests); the excited-to-ground feeding term carries the product of two matrix elements that transfers excited coherence into ground coherence; with a mode each C_q is resolved over emission directions as C_q (x) D(-i eta_em u), entered INCOHERENTLY over directions; decay into excluded levels follows the leak policy include / sink / renormalize | qutip_trap/dynamics/multilevel.py (M3a) | never one operator per excited sublevel (Berkeland's Eq. 12 as printed), never a coherent sum over recoil classes (Joshi Eq. A.3); QuTiP's auto-tidyup zeroes elements below 1e-12, so the rate prefactor must multiply the 1e-29 C m elements before the Qobj is built (M3a implementation finding) | corrected |
| `conv.enr_factor_dims` | HilbertSpace.dims = [ions..., resolved d_m..., C(M + N_exc, N_exc)] | 5.1.1 (the ENR option, 'dimensions from shape'); 13 (ENR row); 11.3 item 1 | every joint operator and state an ENR space hands out is relabelled with the space's own dims, in which the excitation-number-restricted group is ONE tensor factor of dimension C(M + N_exc, N_exc); QuTiP's enr_identity / enr_destroy / enr_fock / enr_thermal_dm label the group by its per-mode dims (n_exc + 1 each, product C(M+N_exc,N_exc) only by accident of small cases), and a QobjEvo refuses to combine terms whose dims differ. Data is shared, not copied. Marginals of the group remain index sums over the ENR basis (never ptrace) | qutip_trap/hilbert/space.py (with_space_dims, applied by identity, embed, embed_many, product_state); tests/test_m9_scaling.py::test_every_joint_operator_and_state_of_an_enr_space_carries_the_spaces_dims | until 2026-09-08 identity(), number(), annihilation(), embed() and the joint states of an ENR space came back labelled [..., n_exc + 1, n_exc + 1] while the factorized drive term (FactorizedOperator, built on HilbertSpace.dims) said [..., C(M+N_exc,N_exc)], so the first ENR run through run() (the M9 fixer's end-to-end test, 2026-09-08) failed inside QobjEvo with 'has dims [[2, 2, 11, 13, 6], ...] but previous terms had dims [[2, 2, 11, 13, 3, 3], ...]' - after building a 37752-dimensional space that took 20 GB, because an explicit JOINT_EXACT above the Section 11.5 guards was not refused (conv.space_declaration_before_allocation). The unit tests of the ENR construction (tests/test_scaling_modes.py) never combined an ENR motional term with a drive term in one QobjEvo, which is why the mismatch survived M9a and M9b | corrected |
| `conv.entangling_angle` | chi_ij | 13 Entangling angle; 4.4.3 | chi_ij = sum_m eta_im eta_jm int int [Omega_i(t) Omega_j(t') + Omega_j(t) Omega_i(t')] ... with the pair sum over i < j; maximally entangling at chi = pi/4; XX(chi) = exp(-i chi sigma_x sigma_x) | Choi 2014; Maslov 2017; Debnath 2016; derivation audit 2026-09-04 | Choi's printed factor 2 holds only for proportional envelopes; Blumel's kernel with target pi/8 must be doubled; Roos's and Kirchmair's pi/8 comes from S without the 1/2 and must not be rescaled | corrected |
| `conv.entangling_scan_beliefs` | ms_scan, parity_scan, ms_phase_scan at (qubit_shifts_hz, mode_frequencies_hz) | 7.3; 7.5 item 4; 7.5 step 3 | the entangling scans run in the frame the machine programs (qubit_shifts_hz = nu_true - table.qubit_freq per ion, forwarded to exact_gate_check and to parity_after_analysis_pulse's engine) and build their GateModes at the frequencies the table believes (mode_frequencies_hz), so the spin-phase corrections they write carry the frame error run() will apply. Before the amplitude scan the pair's waveform is RE-SOLVED at the calibrated mode frequencies (surrogate_waveform(mode_frequencies_hz=...), keeping the surrogate's duration, beat-note placement rule and phase entries), which is PLAN 7.5 item 4's 'the closure amplitude the pulse solver predicts at the calibrated mode frequencies'; the scan then refines the closure offset and the amplitude. A supplied GateModes together with mode_frequencies_hz is refused rather than one of them discarded | calibration/experiments.py (the entangling block, _resolved_at_calibrated_modes); experiments/entangling.py _entangling_setup, _frame_shifts; calibration/entangling.py parity_after_analysis_pulse; tests/test_m8_calibration_fixes.py | full_calibration built both keyword arguments and neither reached anything: ms_scan called exact_gate_check with an explicit argument list that omitted qubit_shifts_hz, parity_after_analysis_pulse had no such parameter at all, and mode_frequencies_hz had no consumer on that path - both landed in **kw and were dropped. The entangling scans were therefore the only experiments of the whole calibration running with a PERFECT qubit frame, and the amplitude scan rigidly shifted and rescaled the surrogate's derived-mode solution, which is equivalent for one symmetric mode but not for a multi-segment AM pulse whose segment amplitudes depend on the mode spacing | corrected |
| `conv.entangling_sign` | s | 4.4.3; 7.6; 13 (M4) | a pulse applies exp(+i chi sigma sigma) with chi = sum_m chi_m signed by the detuning side (tones inside the sidebands give chi > 0); XX(chi) = exp(-i chi sigma sigma), so a positive chi is played with a pi on the second ion's tones (GPi(phi + pi) = -GPi(phi)) and a partial angle rescales every amplitude by sqrt((theta/2)/\|chi\|) | Choi 2014; derivation audit 2026-09-04 (row 13-1); check_two_qubit.py | Waveform.chi_m is SIGNED and, after the exact spot check, carries the measured angle; a solver landing on -pi/4 is not a failure | verified |
| `conv.epsilon_d_split` | eps_D = f P_total, reported beside eps_S = P_Raman | 9.7 row 'Scattering'; 4.5.5; 6.5 | f is DERIVED from the species' tabulated D-level branchings, weighted by the 1/Delta_e^2 excitation of the reachable excited levels, never typed in; for 171Yb+ at 355 nm it is 9.69e-3, so eps_D sits two decades below eps_S | M7 audit E-16 (light/scattering.d_level_branching / epsilon_s_and_d, validation/noise_closed_forms.epsilon_d_from_p_total; tests/test_m7_closed_forms.py). The Table II power regression itself is M0a's validation/ozeri_closed_forms.py and tests/test_m0a_ozeri_closed_forms.py, reused rather than duplicated | the module's own P_Rayleigh comes from the elastic amplitude sum and does NOT contain the D-level Raman channel (the D-level finals are counted in leakage_per_s and hence in eps_S), so Section 4.5.5's 'overstating the elastic rate by f P_total' is reported as the amount by which OZERI'S CLOSED FORM exceeds this one (ozeri_rayleigh_overstatement), not as a correction subtracted from it - subtracting it would clamp at zero and hide the fact that the two P_Rayleigh definitions differ | derived |
| `conv.factorized_isequal_is_structural` | isequal on FactorizedOperator | 11.3 item 4 | scale_a (x)_f A_f == scale_b (x)_f B_f iff A_f = c_f B_f for every factor f with scale_a prod_f c_f = scale_b | dynamics/kernels.py _isequal, _scalar_ratio | M9b compared a.scale * A_f against b.scale * B_f factor by factor, which multiplies the scale in once PER FACTOR: 2 (X (x) Y) and 1 ((2X) (x) (2Y)) differ by a factor two and passed that test, and a false 'equal' merges two distinct drive terms in QobjEvo.compress. The comparison is now the exact structural one above, over the union of the two factor sets with an omitted factor read as the identity, so a 'no' is a proof; the two-probe-vector fallback remains only for a factorized operator against another data type, where the exact answer would need one of the two assembled | corrected |
| `conv.filter_function_machinery` | R_ij(omega) = -i omega sum_l M_l(omega) Lambda^{(l-1)} | 6.9; 13 'Filter-function normalization stack', 'Toggling-frame control matrix', 'Dynamical-decoupling filter function' | per segment M_l = e^{i omega t_{l-1}}[I_c 1 + (I_1 - I_c) n n^T + I_s [n]_x] with the elementary integrals of cos, sin and 1 against e^{i omega s} (removable poles at omega = +-Omega_l); R^P(theta) = cos theta 1 + (1 - cos theta) n n^T + sin theta [n]_x is the adjoint of U^dag; the gated option switches the noise off during pulses and reproduces Biercuk's (-1)^{n+1} form to 1e-15 at both parities; the ungated (physical) finite-pulse form keeps a first-order transverse term at odd n (F ~ omega^2) | check_composite.py sections 9-15 reproduced by noise/decoupling.py (tests/test_decoupling.py) | XY4, XY8, KDD, CDD and (XY)^N compose per-pulse blocks with Lambda^{(l-1)} (never the scalar (-1)^l), return to identity to 1e-12 and match brute-force propagation to 1e-8 with a second-order rule (the check script's mpmath quadrature reaches 4e-10); no source validates them [background] | recomputed here |
| `conv.filter_function_normalization` | chi = (2/pi) int S_b F/omega^2 | 13 Filter-function normalization stack; 6.9 | four pieces pinned together: S two-sided in angular frequency with the e^{-i omega t} kernel (per rad/s); prefactor 1/(2 pi) two-sided or 1/pi one-sided; weight 1/omega^2; an explicit -i omega inside R_ij(omega) = -i omega int_0^tau dt R_ij(t) e^{+i omega t}, so only (F = omega^2 \|FT[R]\|^2, 1/omega^2) and (F = \|FT[R]\|^2, omega^0) are self-consistent | Green 2013 Eqs. 22-24; Kabytayev Eq. 3; noise/decoupling.py | mixing a 1/(2 pi) prefactor with a one-sided integral is a factor 2 in chi; dropping the explicit 1/omega^2 is a factor omega^2; a 1/omega^4 weight appears nowhere | corrected |
| `conv.filter_function_omega_min_fallback` | omega_min = 2 pi/(1000 tau) when neither the band nor an experiment duration sets it | 12; 6.9; Appendix E filter_function | the cutoff is, in order: the caller's omega_min_rad_s; the spectrum's own lowest positive tabulated frequency when its band starts above zero (the device declared it); 2 pi/experiment_duration_s (Appendix E and Section 12's rule); else three decades below the sequence, 2 pi/(1000 tau) | M7 audit E-22 (noise/decoupling.filter_function; tests/test_decoupling.py) | the third fallback is the simulator's invention, not the plan's 2 pi/T_total: it exists because a Gaussian or OU band starts at dc and carries no infrared edge of its own, and it is always reported beside d ln chi/d ln omega_min so a divergence cannot pass unnoticed (Section 12's 'no source supplies an infrared cutoff, so omega_min is the simulator's choice') | derived |
| `conv.floquet_branch_normalization` | beta in (-1, 1] | 13 Floquet branch and normalization; 4.1.7 | omega_j = beta_j Omega/2 on the principal branch; companion matrix F = [[0, 1], [-(A - 2Q cos 2tau), 0]] with U(0) = 1 and monodromy at tau = pi; \|tr M\| < 2 for stability | Landa 2012; House 2008 | House's Eq. 7 sign; unitarity of Gamma^-1(0) as a check; the paper-literal inverted C_-2 step (the Floquet-Lyapunov modes themselves are not built in M1) | corrected |
| `conv.floquet_phase_origin` | u(t) ~ e^{i nu t}[1 - (q/2) cos omega_rf t] | 13 Floquet function and rf phase origin; 4.1.1 | u_dot(0) = i nu (1 + q) at u(0) = 1 in the adopted sign; C0 = 1 + 3q^2/16 even in q | check_critique_v3.py; derivation audit 2026-09-04 | the RMP's quantum section, Wineland's trajectory and the plan's second revision use [1 + (q/2) cos], i nu (1 - q): the same physics half an rf period later | corrected |
| `conv.fm_closure_guard` | alpha_{a,m}(tau) = 0 for the gate ions, plural | 4.4.3; 7.8 (M4 audit 2026-09-07) | the FM family holds Omega constant and equal on both ions, so alpha_{i,m} ~ eta_{i,m} and closing one ion closes the other; the solver drove the residual from ion a alone and SKIPPED every mode with eta_a = 0 exactly, leaving ion b's loop open where b couples and a sits at a node. The residual is now driven from whichever gate ion couples more strongly and a row is skipped only when NEITHER couples | tests/test_shaping.py::test_fm_solver_closes_the_ion_at_a_mode_node; Egan 2021 (b_{8,2n} = 0) | measured on eta = {a: (0.08, 0), b: (0.07, 0.09)}: \|alpha_{b,1}\| fell from 0.0296 (eps_ent = 8.8e-4) to 6e-14 and every \|alpha\| is now below 1e-12; the same fix needed the least-squares gradient test disabled (gtol=None), since J^T r goes tiny long before the residual does and stopped the descent at \|alpha\| ~ 1e-6. On a real crystal the node eta is 1e-17 rather than an exact zero, which is why the defect was invisible outside a synthetic mode table | corrected |
| `conv.fock_sum_branches` | rho(0) = sum_b w_b \|b><b\| | 5.3; 5.7 Initial state; 3.4 | the prepared state is diagonal in the computational and Fock bases (pumped populations per ion, thermal weights per carried mode, the frozen modes' Fock states included), so its branches are evolved as pure states with sesolve and recombined with their weights, exact up to the branches below SolverOptions.branch_weight_min (1e-6) whose total weight is reported and renormalized; Diagnostics.trajectories counts the branches | qutip_trap/run/job.py; PLAN.md Section 5.3 (mesolve is a reference path below dimension ~100) | a joint-space mesolve at dimension 400 would need a Liouvillian of 1.6e5 x 1.6e5 with about 6e7 non-zeros per drive operator; the trajectory path for Lindblad channels is M7/M9b | background |
| `conv.four_ion_gate_local_fixture` | the four-ion GATE_LOCAL-against-JOINT_EXACT circuit | 9.8 row 1; 9.6; 5.4; 11.5 | Section 9.8 row 1: three- AND four-ion circuits of 3 to 5 gates at the resolved-mode sets of Section 9.6 agree in their final probabilities within the residual-displacement bound GATE_LOCAL reports | tests/test_m9_four_ion.py (slow); validation/scripts/check_circuits.py section 3b | M9a shipped the three-ion case only and README.md claimed both. The four-ion case is the GHZ4 circuit (h + three cnots, four gates) on check_circuits.py's four-ion chain, at freeze_chi_max_rad = 0.3 so that BOTH levels resolve the same single mode: measured \|chi\| per Moelmer-Soerensen step on that circuit is mode 4 / 5 / 6 / 7 = 0.038 / 0.1078 / 0.1255 / 0.8056 rad for the (0,1) and (2,3) gates and 0.1251 / 0.1123 / 0.0414 / 0.8395 for the (1,2) gate, so 0.3 rad separates the gate mode (mode 7) from the other three at every step. JOINT_EXACT then runs at dimension 192 (4 ions x d_m = 12) and each gate-local space is 48. check_circuits.py's hand-built space (modes 6 and 7 at d_m = 12, dimension 2304), which the audit and the fix brief both suggested, is NOT usable for the comparison: it is not what the criterion selects for either level, GATE_LOCAL's per-step step_space would resolve modes it freezes at \|chi\| = 0.11 to 0.13 rad (a 14 to 16 % error on a pi/4 angle, which discrepancy_bound does not cover), and no threshold selects exactly {6, 7} at both levels because mode 6 falls to 0.0414 on the (1,2) gate while mode 4 rises to 0.1251 there. At the default 0.05 rad all four x modes are resolved and the joint space is 139968, far above the Section 11.5 guard, so JOINT_EXACT cannot run at all - which is itself the reason Section 5.4 puts four-ion chains in GATE_LOCAL | recomputed here |
| `conv.frequencies` | omega = 2 pi nu | 13 Frequencies; 5.6 | internal frequencies angular in rad/s; public API in Hz, converted at the boundary with an explicit 2 pi | RMP 2003 (Leibfried et al.) | papers quote gamma = 2 pi x 19.6 MHz, gamma/2pi = 60 MHz and 19.7 MHz for gamma/2pi interchangeably; the two are distinct types at the public boundary (qutip_trap.units.Hz and RadPerS with rad_s_from_hz / hz_from_rad_s), while the package's internal signatures and dataclass fields carry the unit in the NAME (_hz, _rad_s, _hz_per_g, _per_s) as plain floats rather than as NewTypes - the M0 audit of 2026-09-07 asked for the NewTypes at every frequency boundary with a census test, and the deviation is recorded instead: an AST census on that date found 149 frequency-like parameter and field names in qutip_trap without a unit suffix over 56 distinct names (gamma, omega, mu, rates, omega_f, detuning, ...), every one a closed-form or internal argument whose docstring states its unit, so a suffix census would need an allow-list of that size and the NewType retyping would touch every module; the run-time rule that holds is the boundary conversion with an explicit 2 pi, checked by the constants and closed-form tests | corrected |
| `conv.frozen_spectator_fallback_draw_is_per_sample` | the frozen-spectator Fock draw in run_pulses | 5.2; 3.4 | Section 5.2: spectators='frozen' samples n_m once per shot from the mode's thermal distribution and multiplies Omega by the exact Debye-Waller factor of that Fock state | dynamics/engine.py run_pulses; run/job.py enumerate_branches | run() does not sample: it enumerates the frozen modes' Fock states as weighted branches, a deterministic quadrature over the same thermal distribution and strictly better than sampling, and passes each branch's tuple to run_pulses. The draw inside run_pulses is the fallback for a direct caller, which has no shot index at all - one call is one evolution - so it is keyed per SAMPLE, not per shot as Section 3.4's (sample, trajectory, shot, ion, channel) key would suggest, and it now says so in an engine note whenever it fires | corrected |
| `conv.frozen_spectator_shot_sample` | n_m per shot | 5.2, 3.4; dynamics/engine.py | a frozen mode's Fock state comes from the sample key frozen_n[m] or, absent that, from a thermal draw at the state's nbar with the keyed stream (sample, 0, 0, 0, 'frozen_n[m]'); it multiplies every drive on the shot by e^{-eta^2/2} L_n(eta^2) and is recorded in the engine report | PLAN.md Sections 3.4 and 5.2 | never the ensemble-mean factor exp[-eta^2(nbar + 1/2)] inside one shot (Section 13, Debye-Waller statistics) | derived |
| `conv.gate_fidelity_measure` | F_K = (1/4)\|Tr(U_id^dag U)\|^2 | 13 Gate fidelity measure; 4.3.5; 6.9 | for a single-qubit UNITARY error the internal measure is F_K = (1/4)\|Tr(U_id^dag U)\|^2, the normalization the composite-pulse library and the Section 6.9 filter functions are built to and the one that floors at 1/2 for a fully wrong qubit gate; F_C = \|Tr(U_id^dag U)\|/2 = sqrt(F_K) and F_avg = (2 F_C^2 + 1)/3 = (2 F_K + 1)/3 are exposed as conversions, so 1 - F_K = 2(1 - F_C) and 1 - F_avg = (4/3)(1 - F_C) at leading order, and every ingested c-coefficient is tagged with the measure it was quoted in (control/composite.py fidelity_k and the measure argument of the error models); at small spectral-norm distance E between unitaries 1 - F_C ~ E^2/2 while 1 - F_K ~ E^2, so the plan's 'converted as E^2/2' is the F_C form. The plan's row says to keep this separate from the RB row and the depolarizing row, and it is separate: conv.rb_error_rate converts a decay BASE p, conv.depolarizing_normalization a channel RATE eps, this row a unitary OVERLAP | Kabytayev et al. 2014 Sec. II B; Cummins et al. 2003 Eq. 7; Merrill and Brown Eqs. 10-11; control/composite.py | Low, Yoder and Chuang's F = \|\|U V^dag\|\| is identically 1 for a qubit under its own Eq. 2 norm and Brown, Harrow and Chuang's 1 - \|\|V^dag U\|\| identically 0 for unitaries; both are replaced by F_K. The row had no ledger record of its own although the M10 modules cite it by name, which Section 14.5's one-record-per-quantity rule asks for | corrected |
| `conv.gate_local_discrepancy_has_no_fudge` | max \|p_JOINT_EXACT - p_GATE_LOCAL\| | 9.8 row 1 | Section 9.8 row 1: final probabilities agree within the residual-displacement bound GATE_LOCAL reports; disagreement above it fails | tests/test_gate_local.py (Bell and three-ion GHZ, slow) | the Bell comparison asserted < discrepancy_bound + dropped_branch_weight + 1e-6 and the three-ion one < discrepancy_bound + 1e-5. The dropped_branch_weight term is the weight of the initial-mixture branches JOINT_EXACT did not evolve while GATE_LOCAL carried the whole register, and is licensed; the bare 1e-6 and 1e-5 were unrecorded slack and are removed, so both comparisons now hold to discrepancy_bound + dropped_branch_weight exactly as the row states | corrected |
| `conv.gate_local_residual_bound_pre_step` | GateLocalStep.residual_bound | 9.8; 5.4 | Section 9.8: the residual-displacement bound sum_m \|alpha_m\|^2 (2 nbar_m + 1) that GATE_LOCAL reports and the JOINT_EXACT comparison is held to | run/gate_local.py _gate_step | M9a evaluated 2 nbar_m + 1 at the occupation the step's own output left, so the reported bound depended on the gate's heating: conservative for a heating gate, an under-estimate for a cooling one, and in neither case reproducible from the step's inputs. It now reads the occupation the tracked motional model had when the step started | corrected |
| `conv.gate_local_workers_are_the_ones_used` | Diagnostics.workers on GATE_LOCAL | 11.3 item 9 | Section 11.3 item 9: trajectories and quasi-static samples are spread over the 18 cores with the parallel map | dynamics/tomography.py _run_tasks, TomographyRecord.workers; run/gate_local.py GateLocalStep.workers, GateLocalReport.workers; run/job.py | M9a reported worker_count(options) for every GATE_LOCAL run whether or not anything ran in parallel: the tomography runs its inputs in-process whenever the local space has no resolved mode (every carrier step), a cache hit runs nothing at all, and the walk iterates its quasi-static samples serially. The number reported is now the largest count any step actually used (the tomography's map, or the trajectories inside one engine run). Spreading the quasi-static samples over the workers, which Section 11.3 item 9 also asks for, is still not done: it is a performance narrowing with no Section 9 row, and Diagnostics.workers now states it truthfully instead of hiding it | corrected |
| `conv.gate_parameters` | turns | 13 Gate parameters; 7.6 | radians internally; IonQ turns at the API boundary (1 turn = 2 pi); phases enter as e^{+-2 pi i phi}, angles as cos(pi theta), sin(pi theta) | IonQ documentation | the documentation's prose that GPi2 at phi = 0.5 gives R_x(+pi/2) contradicts its own matrix (it is R_x(-pi/2)) | corrected |
| `conv.ghz_parity_bound` | (P_0 + P_1 + C)/2 = max_theta <GHZ_theta\| rho \|GHZ_theta> | 7.9 Entangling gate; 9.6 rows 1 and 2; M10 | P_0 = P(0...0) and P_1 = P(1...1) from the GHZ circuit H, CNOT chain; on fresh shots the parity <prod_q Z_q> after GPi2(phi) on every qubit oscillates as C cos(N phi + phi_0) with C = 2 \|rho_{0...0, 1...1}\| (only the sigma_+^{ox N} term carries e^{i N phi}; every other coherence lands in the phase-independent offset), fitted from at least four phases over one period 2 pi/N. Since P_0 = rho_{0...0,0...0} and P_1 = rho_{1...1,1...1}, (P_0 + P_1 + C)/2 = (rho_{0...0,0...0} + rho_{1...1,1...1})/2 + \|rho_{0...0,1...1}\| = max_theta <GHZ_theta\| rho \|GHZ_theta> with \|GHZ_theta> = (\|0...0> + e^{i theta}\|1...1>)/sqrt 2: an EQUALITY with the phase-optimised GHZ-class fidelity and therefore an UPPER bound on the fidelity against any FIXED-phase GHZ state. The simulator reports both exact numbers beside the measured bound, GHZResult.register_fidelity_max_phase (what the bound estimates) and GHZResult.register_fidelity (against the compiled circuit's own fixed-phase target), with the shot-noise uncertainties of P_0, P_1 and C combined in quadrature (benchmarks/ghz.py ghz_coherence; tests/test_m10_benchmarks.py) | Sackett et al. 2000; Leibfried et al. 2005; Wright et al. 2019 (the two-qubit form, Section 7.9 [verified]) | this record, benchmarks/ghz.py and docs/physics_notes.md said 'an equality for the ideal state, a lower bound in general' and the test asserted the bound BELOW the fixed-phase register fidelity within three sigma; the inequality runs the other way. The committed three-ion run already violated the claim -- bound 0.9935 against the exact fixed-phase 0.98520, the bound 0.008 ABOVE the number it was printed beside -- and the test passed only because its 3-sigma slack, 0.0185, was twice the discrepancy, so it could not detect a systematic phase error, which is the error the exact fidelity was added to catch. For the ideal three-ion \|GHZ_theta> at theta = 0.7 the contrast is 1 and the bound 1 while the fixed-phase fidelity is cos^2(theta/2) = 0.882421, a gap of 0.117579. Section 7.9's own 'is a lower bound (Wright 2019)' is about an imperfect analysis pulse degrading the MEASURED contrast, not about the direction of the inequality, so this is a deliberate deviation from the plan's phrasing. Unchanged: phi_0 carries N pi/2 from the rotation axis of GPi2 and the compiled circuit's residual virtual-Z frames, so the parity at phi = 0 is not the maximum (it vanishes for the real GHZ state) and only the contrast enters the bound | corrected |
| `conv.gradient_drive` | Device.gradient, kind = 'gradient' | 3.2; 4.4.5; 9.4 'Microwave gradient' (M4 audit 2026-09-07) | the near-field microwave-gradient drive is now device-level: GradientField carries the electrodes' oscillating gradient amplitude d(B_g . B_hat)/dr (T/m), its frequency omega_g, its axis, the two microwave tones' field amplitude B_1 and the gradient's phase; derive_gradient_drive returns Omega_mu = (B_x/2 hbar)<dn\|mu_x\|up> from the existing magnetic-dipole coupling, d omega_0/dB from the hyperfine-Zeeman diagonalization, and the per-(ion, mode) laboratory force w_{i,m} = (1/2)(d omega_0/dB) grad(B)(r_hat . e_hat_m) c_{i,m} sqrt(hbar/(2 m_i omega_m)) built from the mode's mass-weighted pattern exactly as a Lamb-Dicke parameter is; the builder's 'gradient' branch adds sum_{i,m} w_{i,m} sigma_z^i (a_m + a_m^dag) cos(omega_g t + phi_g) beside the drive's two microwave tones (BuilderOptions.gradient_form = 'bare', the default, so that J_2(4 Omega_mu/delta) and the J_0 = 0 decoupling emerge from the exact dynamics) or the eliminated (w_{i,m}/2) J_2 sigma_z^i (a_m e^{i(omega_g + 2 delta) t} + h.c.) alone ('dressed', recorded in approximations) | Srinivas et al., Nature 597, 209 (2021) Eq. 1; light/microwave.py; dynamics/hamiltonian.py | Section 13's r_0 with the TOTAL two-ion mass is confirmed by the derivation, not assumed: on a spatially antisymmetric mode w_{i,m} = +-2 Omega_g EXACTLY with Omega_g = (r_0/4) grad(B)(d omega_0/dB) and r_0 = sqrt(hbar/(2 M_total omega_m)), which is what makes the plan's (sigma_z1 - sigma_z2) normalization the right one (measured ratio -1.000000000/+1.000000000). Two things the audit's recommendation supposed do NOT hold: a gradient gate needs a FIELD-SENSITIVE qubit (d omega_0/dB vanishes at a clock point, so Srinivas's \|F=3,mF=3> <-> \|F=2,mF=2> of 25Mg+ and not the 212.78 G clock line of the 9.13 row), and solve_amplitude_modulation(kind='gradient') does NOT work unchanged, because the force is a Bessel FUNCTION of the tone amplitude (Omega_g J_2(4 Omega_mu/delta)) and not linear in it, so Choi's, Leung's and Blumel's linear closure algebra does not apply and Srinivas's Walsh sign modulation (eight segments over 740 us, the sign carried by the tones' half-difference phase) is the applicable family. WaveformKind gains 'gradient' (two legs, the microwave tones at -/+ delta) and the scheduler plays it through the same sigma_z spin-echo path as a light-shift waveform; the closure SOLVERS do not cover it | derived |
| `conv.hamiltonian_fingerprint_carries_the_device` | BuiltHamiltonian.fingerprint | 11.3 item 5; 5.2 | the fingerprint digest is (device.hash(), the space, the mode frequencies, the segment times, the pulses' sampled tones, the frozen Fock states, the noise sample's values and OU grids, the qubit shifts, the BuilderOptions with kernel normalized to auto) | dynamics/hamiltonian.py build_hamiltonian; dynamics/engine.py propagator cache; tests/test_m9_scaling.py | M9b omitted device.hash(), yet H(t) depends on the device through every Lamb-Dicke parameter, the beam waist and pointing (_pointing_factor) and the geometric phase Delta k . X_i. One engine over two devices differing only in beam 0's wavelength (355 nm -> 344.35 nm, etas {2: -0.08093, 3: 0.07858} -> {2: -0.08218, 3: 0.07979}, mode frequencies unchanged) served the first device's propagator for the second on an internal-state-only space and reported P1[0] = 0.4950194835 where the correct answer is 0.4948647699, an error of 1.5e-4, three orders above the solver tolerance and silent. No shipped call site reused an engine across devices, so it was latent; GATE_LOCAL's own extraction cache already keyed on device.hash() | corrected |
| `conv.hardware_chain` | Omega(t) -> quantized words, first-order response, jittered trains | 7.10 | the envelope of every tone through y' = (x - y)/tau_r with tau_r = aom_rise_s for laser light and 1/(2 pi B) of the amplifier for microwaves (B = inf is an ideal envelope), continuously across a pulse train, an 8 tau_r tail (e^-8 dropped), truncated at the next pulse on a shared ion; the drive's Stark shift follows the played light as delta_ref mean_tones (\|Omega_played(t)\|/Omega_ref)^p with p = stark_scaling_power(kind) and the reference at the programmed peak (the tail carries the decaying shift, not the programmed one); constant detunings to f_clk/2^bits, phases to 2 pi/2^bits, amplitudes to full_scale/2^bits without resampling an unfiltered envelope; rigid per-train jitter from the keyed seeds | standard instrumentation physics exposed as HardwareChain parameters (control/hardware.py) | an omitted parameter is ideal electronics and HardwareChain.describe() says so (Section 7.10's closing rule) | background |
| `conv.heating_master_equation` | Gamma(N_bar + 1), Gamma N_bar | 13 Heating master equation, Heating-rate meaning | rates Gamma(N_bar + 1) on a and Gamma N_bar on a^dagger; a quoted heating rate is d<n>/dt at n = 0, i.e. Gamma N_bar; thermal diffusion Gamma(2 N_bar + 1) | Wineland Eq. 62 (typo fixed); Brownnutt Eqs. 15-17 | the journal prints 2 a rho^dagger a^dagger, a typesetting artifact absent from both arXiv versions | corrected |
| `conv.heating_rate_meaning` | n_dot | 13 Heating-rate meaning; 4.1.5 | a quoted rate is d<n>/dt at n = 0, i.e. Gamma N_bar; thermal diffusion is Gamma(2 N_bar + 1); n_dot ~ omega^-(alpha+1) when S_E ~ omega^-alpha; the coherence of (\|n0> + \|m0>)/sqrt 2 decays at Gamma_h (n0 + m0 + 1) | Brownnutt 2015 | Trout's symmetric infinite-temperature pair {sqrt(n_dot) a, sqrt(n_dot) a^dag} is the N_bar >> 1 limit, never to be imported with a finite N_bar | verified |
| `conv.heavy_output_criterion` | mean(h_U) - 2 sqrt(h(1 - h)/n_c) > 2/3 over n_c >= 100 circuits | 10 M10; 4.6 (the four-qubit quantum-volume circuit of Pino et al. 2021) | square circuits of width n and depth d = n, every layer a uniform random permutation and a Haar-random SU(4) (Mezzadri's QR construction) on each of the floor(n/2) pairs, adjacent local unitaries merged per qubit; the heavy outputs of a circuit are the strings whose IDEAL probability exceeds the median of the ideal distribution (half the strings); h_U is the measured fraction of shots on them; the width passes when the mean over circuits of h_U clears 2/3 by two sigma AND the run has at least the protocol's 100 circuits ('we set n_c >= 100 circuits'), the confidence being Appendix C Eq. (32), [n_h - z sqrt(n_h (n_s - n_h/n_c))]/(n_c n_s) > 2/3 with z = 2, n_h the total heavy count and n_s the shots per circuit, which with h = n_h/(n_c n_s) reduces IDENTICALLY to mean - 2 sigma > 2/3 with the per-CIRCUIT binomial sigma = sqrt(h(1 - h)/n_c) (volume.py cross_confidence_sigma, heavy_output_pass; the same sigma the Qiskit Experiments reference implementation computes); the standard error of the mean of h_U over the circuits is reported beside it as QVResult.standard_error_of_the_mean and is never the criterion, and QVResult.threshold_cleared carries the confidence half of the criterion on its own, so a run below 100 circuits says so and clears no quantum volume; under a depolarizing composition h_pred = h_ideal (1 - eps) + eps/2 (benchmarks/volume.py) | Cross, Bishop, Sheldon, Nation and Gambetta, PRA 100, 032328 (2019), Appendix C Eq. (32); benchmarks/volume.py; tests/test_m10_benchmarks.py | - | corrected |
| `conv.hughes_smooth_gate_untested` | error flat to nbar = 9.4 | 4.4.6; 9.4 'Smooth gate' (M4 audit 2026-09-07) | theta_g = pi/4 as the maximally entangling angle of Eq. 4 is pinned (anchor.m4.hughes_gate_angle); the row's second claim, that the gate error is FLAT to nbar = 9.4, is not | Hughes et al. 2025 Eqs. 4, 9-10, 16, 19 with the plan's corrections | two independent blockers, both recorded rather than worked around. (1) The detuning ramp is not reconstructible: the plan itself records that Eq. 19 prints delta_max^{j} for delta_max^{-j} and that its constant b is dimensionally inconsistent as printed, so any ramp the simulator plays would be one of its own invention and 'flat to nbar = 9.4' would pin that invention and not the paper's schedule. (2) The temperature the claim names is out of reach of the exact displacement operators on the M4 fixture: nbar = 9.4 needs a Fock cap of about 68 levels, at which the Section 5.1.1 displacement oracle already reports expm against the analytic elements differing by 3e-9 at eta = 0.0786, ten thousand times the 1e-12 tolerance, so the run is refused before it starts. The FM machinery the section says the ramp would use IS in place and is now verified by exact integration (anchor.m4.solvers_verified_by_exact_integration): a callable detuning through waveform_from_callables plays through the engine, so what is missing is the source's ramp, not the capability | contested |
| `conv.hyperfine_dipole_element` | <F mF\|d_q\|F' mF'> | 13 Hyperfine dipole matrix elements; 4.5.2 | <F mF\|d_q\|F' mF'> = <J\|\|d\|\|J'> (-1)^{F'+J+1+I} sqrt((2F'+1)(2J+1)) {J J' 1; F' F I} (-1)^{F'-1+mF} sqrt(2F+1) (F' 1 F; mF' q -mF); q = m_lower - m_upper | Steck (Rb87 data, Eqs. 35-38) | the simulator reproduces the 171Yb+ 1/3 : 2/3 branching and the 50.8 mW/cm^2 I_sat as its ingest test (9.13) | verified |
| `conv.hyperfine_factors` | S_FF', b(F'->F) | 13 Hyperfine factors | S_FF' = (2F'+1)(2J+1){J J' 1; F' F I}^2 summing to one over excited F' (absorption); b(F'->F) = (2F+1)(2J'+1){...}^2 summing to one over ground F (emission) | Steck Rb87 Eqs. 40-42 | one 'hyperfine factor' used in both roles is off by (2F+1)(2J'+1)/((2F'+1)(2J+1)) | corrected |
| `conv.improved_sampling_single_segment` | mcsolve improved_sampling; deterministic_weights | 3.4; 5.3; 9.9 | with improved_sampling=True QuTiP evolves the no-jump trajectory once as a deterministic member of weight p_no-jump and gives the ntraj stochastic trajectories the residual weight, so the engine's per-member weights are res.deterministic_weights for res.deterministic_trajectories and res.runs_weights for res.trajectories; drawing uniformly from the stored trajectories instead biases every per-shot observable by p_no-jump. The engine uses it when the trajectory path is entered exactly ONCE in a schedule (SolverOptions.improved_sampling, default True); a multi-segment schedule keeps uniform-weight trajectories and says so in the run's notes | PLAN.md Section 5.3 line 827 and Section 3.4; QuTiP 5.3.1 McResult introspected | two names in the plan do not exist in QuTiP 5.3.1: the plan's `deterministic_weight_info` is `McResult.deterministic_weights` (a list, one entry per initial state, in initial-state order) beside `runs_weights` (per stochastic trajectory, in completion order and matched to the keyed seeds through res.seeds). The single-segment restriction is a real limit of this engine, not of QuTiP: the no-jump/jump split is a decomposition of the WHOLE evolution, so applied to each of the n segments a schedule is cut into it yields a jump expansion of 2^n weighted PURE members that cannot be merged into a density matrix on the trajectory path, and pruning it at branch_weight_min would discard exactly the jump weight the Lindblad channels exist to produce (on the M7 two-ion fixture the per-segment jump probability is 1e-5, so every jump branch sits below the 1e-6 floor after two segments). The bounded fix is a per-segment keyed resampling of the weighted mixture down to ntraj members, which changes every M7 and M9b trajectory number and was left to the coordinator | corrected |
| `conv.integrator_ladder_exception_types` | the escalation ladder's except clause | 5.3 | the ladder dop853 -> vern9 -> vern9 at atol 1e-8 with max_step = T_min/14 escalates on the integrator's own failure and records the retry | dynamics/evolve.py evolve; tests/test_m9_scaling.py | M9b caught bare Exception, so a mismatched e_op (ValueError) or an unsupported solver option (KeyError from method='diag') was recorded as an integrator failure, retried on every remaining rung on the same broken input and reported as RuntimeError('every rung of the integrator ladder failed'), hiding the programming error. The clause is now qutip.solver.integrator.IntegratorException, which is what QuTiP raises for 'larger nsteps is needed', 'excess work' and the scipy aborts the plan names; everything else propagates | corrected |
| `conv.intensity_noise_channel` | L = sqrt(D) H_drive(t), Gamma_I = D Omega^2/2 | 6.4; 9.16 row 4.4-8 | a two-photon drive scales as I (D = S_eps,white), a single-photon one as sqrt I (D/4); the carrier-contrast rate of sqrt(2k)(Omega/2) sigma_x is exactly k Omega^2 (the definition of Gamma_I checked analytically) | white multiplicative noise eps(t) on Omega with two-sided density D averages to -(D/2)[H_d, [H_d, rho]]; Bermudez's c_op = sqrt(2k) H_int is D = 2k | see anchor.m7.intensity_noise_factor_two | derived |
| `conv.ionq_json_encoding` | gpi, gpi2, ms, zz, nop | 7.6 JSON encoding of the native set; 9.13 | gpi/gpi2 take target and phase (turns); ms takes targets, phases (turns) and angle (turns, default 0.25); zz takes targets and angle only; the job body is {type: ionq.circuit.v1, input: {gateset, qubits, circuit}} | IonQ OpenAPI v0.4; qiskit-ionq, ionq-core-python, PennyLane-IonQ client code read 2026-09-04 | the 0 to 0.25 range of the ms angle is stated only by the PennyLane plugin's docstring | verified |
| `conv.kak_weyl_chamber` | U = e^{i phi} (A (x) B) exp[-i (a XX + b YY + c ZZ)] (C (x) D) | 7.2 (an arbitrary SU(4) as the standard set's U3 is an arbitrary SU(2)); 7.1; M10 | Q = M^dag U M = O_1 D O_2^T with O_1, O_2 real special orthogonal and D diagonal unitary (O_2 and D^2 from the complex-symmetric unitary Q^T Q, whose real and imaginary parts one real orthogonal matrix diagonalizes); (a, b, c) and phi from the four phases of D through the +-1 table of XX, YY, ZZ on the Bell states; the coefficients moved into the Weyl chamber pi/4 >= a >= b >= \|c\| >= 0 by pi/2 shifts absorbed as Paulis into A, B, by the axis permutations (S (x) S), (H (x) H), (R_x(pi/2) (x) R_x(pi/2)) and by the pair negations (Z, Y, X (x) 1); emitted as rxx(2a), (S (x) S) rxx(2b) (S^dag (x) S^dag), rzz(2c) with the local factors through decompose_single_qubit, so every Moelmer-Soerensen gate is played at an angle in [0, pi/2] and a generic SU(4) costs three; verified against the target up to a global phase (control/two_qubit.py) | Kraus and Cirac 2001; Zhang, Vala, Sastry and Whaley 2003; the magic-basis bidiagonalization of Makhlin 2002 as Cirq implements it | - | background |
| `conv.ladder_operators` | x0 | 13 Ladder operators | x = x0 (a + a^dagger), x0 = sqrt(hbar/(2 m omega)) | RMP Eq. 26 | Brownnutt arXiv v1 prints a with sqrt(hbar m omega); Ozeri 2007 and Baldwin 2021 put a factor 4 under the root | verified |
| `conv.lamb_dicke` | eta_{i,m} | 13 Lamb-Dicke parameter, Lamb-Dicke base; 4.1.7 | eta_{i,m} = (Delta k . e_m) c_{i,m} sqrt(hbar/(2 m_i omega_m)) with the ion's own mass and the mass-weighted unit-norm eigenvector component; C0 applied once in Crystal.lamb_dicke | Wineland 1998 Eq. 33; Monroe 2020; Home 2013 Eq. 19; Morigi-Walther Eq. 12 | Sorensen-Molmer's and Leung's per-ion COM eta already contains 1/sqrt N; Srinivas puts the total two-ion mass inside r0 | verified |
| `conv.large_mode_atol_floor` | atol = 1e-8 above d_m ~ 100, only when atol is the default | 5.3 (PLAN.md:825), 5.5, 9.9, 9.17 escalation ladder; dynamics/evolve.py | the keying of Section 5.3 relaxes atol to LARGE_MODE_ATOL = 1e-8 above LARGE_MODE_DIMENSION = 100 when and only when the caller left SolverOptions.atol at its 1e-10 default; a deliberately tightened or loosened atol is used as given. dynamics.evolve.convergence_check(run, options) is the named-observable companion to hilbert.truncation.halving_test: it runs a closure at both tolerance pairs and reports max_t \|delta p\| per observable with a summary string, and it is applied to the Section 9.2 carrier and blue-sideband fixtures (both converge below 1e-6) | tests/test_m2_convergence.py | M2 wrote max(atol, 1e-8), so a Section 5.5 tolerance-convergence run at d_m > 100 had its tightened atol clamped back to 1e-8 while only rtol tightened, and the convergence check reported a spuriously small change. The 9.17 escalation-ladder cases d_m = 121, 151 and 201 are now run (as a slow test): all three integrate on the first rung with no retry at the keyed 1e-8, and the plan's 'probably stiff' abort at d_m = 121 with atol 1e-10 does NOT reproduce on a one-mode detuned spin-dependent force, so it is specific to the Section 11.1 two-ion pulse and no failure is manufactured to exercise the ladder (M2 audit E9/B9, E10, E18) | corrected |
| `conv.laser_linewidth_phase_diffusion` | C_b = sqrt(delta omega_L/4)(P_upper(b) - P_lower(b)) | 8.1; 12 | Section 8.1 requires -(gamma/2 + delta omega_L/2) on the optical coherences; the Lindblad form supplies -gamma/2 by itself and delta omega_L was absent entirely (the M3a finding). MultiLevelOptions.laser_linewidth_rad_s (one entry per beam, default empty = an ideal laser) adds one phase-diffusion collapse operator per beam, the projector difference on the manifolds that beam connects times sqrt(delta omega_L/4), which damps that transition's coherence at exactly delta omega_L/2 and leaves every population and every within-manifold coherence untouched. Measured: the weak-drive excitation profile keeps its Lorentzian shape and broadens from full width Gamma to Gamma + delta omega_L at constant area (peak x Gamma/(Gamma + delta omega_L)) for delta omega_L/Gamma = 0.5, 1 and 2; rho_eg decays at gamma/2 + delta omega_L/2 and rho_ee still at gamma, both to 1e-9; R_o at delta omega_L = 0 is unchanged bit for bit | qutip_trap/dynamics/multilevel.py; tests/test_m3_linewidth.py; Berkeland and Boshier, Phys. Rev. A 65, 033413 (2002) Eq. 12 | the operators are excluded from the Section 4.2.8 decay sum rule through MultiLevelBuild.dephasing_slice (they carry no photon and no recoil), and Section 12's grant covers only the laser linewidth AS A SPECTRUM, which is weaker than this constant Lorentzian rate | corrected |
| `conv.layout_scripts_location` | validation/scripts/ | 3.2 Package layout; M0 (Section 10); Appendix D | the check and benchmark scripts of Appendix D are committed at the repository root (validation/scripts/) rather than under qutip_trap/validation/scripts/, so the first CI job runs them as standalone oracles without importing the package; qutip_trap/validation/ holds the closed forms the tests import | validation/scripts/README.md; run_checks.py; tests/test_provenance_and_layout.py | the layout guard derives the module list from the Section 3.2 fence and carries this relocation as its one exception | background |
| `conv.level_a_participation_guard` | sum_{i illuminated} c_{i,m}^2 >= 1e-4; optional W_m >= min_rate_per_s | 4.2 (first paragraph), 4.2.5; 9.12 row 1360 | level A refused a mode only on the beam-direction projection (k_hat . e_m)^2 < 1e-2; it now also refuses one whose ILLUMINATED ions barely participate, sum_i c_{i,m}^2 < PARTICIPATION_THRESHOLD = 1e-4, and (when the caller states a stage duration through min_rate_per_s) one whose W_m = cooling - heating falls below it. A 40Ca+-like coolant next to a 171Yb+-like qubit under strong radial confinement leaves the two qubit-dominated radial modes at a coolant participation of 6.6e-6 and 5.8e-6 and a relaxation rate below 1 s^-1 (tens of seconds), while returning a mathematically consistent nbar because the participation cancels between eta~ and eta: that is exactly Section 4.2's 'such a mode is not cooled at all' and it was reported as a prepared occupation before (the M3 finding) | qutip_trap/prep/rates.py; tests/test_sympathetic.py | participation_threshold=0.0 reports the steady state anyway, which is how the 30-second relaxation time is exhibited in the test | corrected |
| `conv.level_a_rates_participation` | heating_{i,m} = c_{i,m}^2 [S_i(+nu) + 2D_i], cooling_{i,m} = c_{i,m}^2 [S_i(-nu) + 2D_i] | 4.2 (level A), 4.2.2, 4.2.8 (ii, iii); 13 Cooling coefficients A_+- | per illuminated ion and mode the phonon rates come from the dipole-force spectrum of the ion's internal steady state under all its beams (exact in the saturation) plus the per-channel emission diffusion, with the mass-weighted participation folded into the zero-point length x0_eff = \|c\| sqrt(hbar/(2 m_i omega)); the mode rate carries W_k = sum_i c_{i,k}^2 and nbar = sum heating/(sum cooling - sum heating) is participation independent when the ions see one intensity (a two-ion COM mode: nbar equal to the single ion's to 1e-6, the rate halved when one ion is illuminated) | qutip_trap/prep/rates.py; tests/test_doppler.py | the W(Delta -+ nu) closed form with each beam shifted in turn is the weak-drive alternative, refused above Omega/Gamma = 0.1 (Section 4.2.8 vii); a mode whose projection on every cooling beam is below (k_hat . e_m)^2 = 1e-2 raises UncooledModeError instead of returning a steady state (Section 4.2) | derived |
| `conv.level_b_c_handoff_state` | PreparationStage.motional -> HilbertSpace.initial_state(states=...) | 4.2, 4.2.7 | Section 4.2 says the output of every stage is a per-mode thermal density matrix OR THE FOCK DISTRIBUTION FROM LEVEL B, and 4.2.7 adds OR THE LEVEL-C REDUCED MOTIONAL STATE; PreparationStage carried only Mapping[int, float] and prepare_state passed only thermal=nbar, so a non-thermal distribution was silently collapsed to its mean (the M3 finding). A stage now carries an optional per-mode motional density matrix that reaches initial_state(states=...) unchanged, with fock_distribution_state building the diagonal matrix of a level-B population vector; a later cooling stage on the same mode drops the explicit state again | qutip_trap/prep/sequence.py; tests/test_prep_sequence.py | nbar stays mandatory (the frozen modes and the provenance read it, and it is the fallback for a mode with no explicit state), and the explicit state's dimension is checked against the space's own truncation | corrected |
| `conv.light_shift_force` | Omega_LS | 4.4.4; 13 (M4) | Omega_gg = sum_e conj(Omega^(2)_eg) Omega^(1)_eg/(2 Delta_e) for each qubit level g under the beam pair; the light-shift drive is (1/2) sum_tones Omega_LS e^{-i(mu t - phi)} [w_dn P_0 + w_up P_1] (x) D + h.c. with Omega_LS = (Omega_upup - Omega_dndn)/2 and weights (Omega_dndn, Omega_upup)/Omega_LS (differing by exactly 2): Zhu's hbar Omega_j cos(Delta k . q_j + mu t) sigma_z^j plus the spin-independent force; the same beams' Raman coupling Omega_R rotates at mu - omega_0 and is kept unless its excitation is below 1e-12 | Wineland 2003 (two-photon coupling with g' = g); Zhu-Monroe-Duan 2006 Eq. 2; Ballance 2016; Baldwin 2021 | for a force on one level only (weights (-2, 0), the 40Ca+ S1/2-D5/2 qubit under 398 nm light, or Baldwin's \|up>) the single-qubit sigma_z phase is four times the two-body angle, which the spin echo of Section 4.4.4 cancels; the sign of a sigma_z force follows the detuning side alone (no phase flip) | derived |
| `conv.linewidth_gamma` | Gamma | 13 Linewidth Gamma; Appendix E Transition.gamma_hz | Gamma is the angular population decay rate of the upper level's TOTAL decay; gamma_hz = Gamma/2pi; the partial rate into a lower level is Gamma x branching | Noek 2013; Crain 2019 | an assertion checks Gamma/2pi equals the quoted MHz | verified |
| `conv.manifold_rates_mode_selection` | the slowest Liouvillian mode with weight on BOTH manifolds | 8.1 | the two-manifold coarse graining took the second-slowest Liouvillian mode (order[1]) and required it to connect the manifolds; with the D3/2 branch and its 935 nm repump in a 171Yb+ detection model that mode is a D-manifold relaxation and the call failed with 'the slow mode does not connect the two manifolds' (the M5 finding). The mode is now chosen by projecting every eigenvector onto the two manifolds' populations and taking the slowest one that moves population between them; the skipped intra-manifold rates are reported on ManifoldRates.intra_manifold_rates_per_s rather than hidden | qutip_trap/light/bloch.py (manifold_rates); tests/test_bloch.py | two further refusals replace silently negative rates: the two manifolds plus the decaying levels must carry the stationary population (closure_tol, naming the states that hold the rest), and neither manifold may be absorbing (weight_floor on the stationary weight). The 171Yb+ repump configuration hits both, because 3D[3/2]1/2 -> S1/2 at 297 nm is declared as untabulated branching and not as a Transition, so nothing returns from the D branch to S1/2 - the recorded gap of anchor.m3a.yb171_repump_level_gap, now a stated refusal instead of a number | corrected |
| `conv.mass_ion` | mass_u | 9.16 row 13-6; 5.6 | Species.mass_u is the ION mass, the cited atomic mass minus one electron mass (171Yb+: 170.93578 u from 170.9363302 u); the Lamb-Dicke anchor must state its mass | derivation audit 2026-09-04; AME via NIST | eta(N = 17) rounds to 0.0267 at 171 u and 0.0268 at the ion mass | recomputed here |
| `conv.mass_weighted_eigenvectors_parity` | c_i = sqrt(m_i) b_i | 13 Mass-weighted eigenvectors and parity; 4.1.7 | eigenvectors orthonormal in u_i = sqrt(m_i) z_i only; physical amplitudes carry 1/sqrt(m_i); inversion parity p = +1 means an ANTISYMMETRIC displacement pattern (centre ion at rest), exempt from uniform-field noise and uncoolable by a centre ion | Morigi-Walther 2001; Kielpinski 2000; Wubbena 2012; Home 2013; Landa 2012 | renormalizing in raw displacements is a 20 to 160% error invisible at mu = 1 (b1^2 = 0.40641 instead of 0.31608 at mu = 40/27); 'even' read as 'in-phase' inverts the rule | corrected |
| `conv.mathieu_sign` | a - 2q cos 2xi | 13 Mathieu equation; 4.1.1 | d^2x/dxi^2 + [a - 2q cos 2xi] x = 0, xi = omega_rf t/2, a = 4Z\|e\|U alpha/(m omega_rf^2), q = 2Z\|e\|U~ alpha'/(m omega_rf^2) | Leibfried et al. 2003 | Wineland 1998 and Berkeland 1998 use +2q cos (a half-period shift); factor-4 q definitions elsewhere; House 2008 has Q = -q and prints its first-order matrix with the wrong sign | verified |
| `conv.mathieu_sign_house` | Q_House = -q | 13 Mathieu sign in House 2008; 4.1.6 | the standard form u'' + (a - 2q cos 2tau) u = 0; House's x'' + [A + 2Q cos 2tau] x = 0 has Q = -q, and his Eq. 7 prints the first-order matrix's lower-left block with the wrong sign | House 2008 Eqs. 4, 7 | copied as printed, a stable trap (a_z = 0.005, q = 0.20) is reported unstable; trap/mathieu.py companion_matrix(lower_left_sign=+1) is the negative control (tests/test_mathieu.py) | corrected |
| `conv.mcsolve_weight_attributes` | deterministic_weights, runs_weights | 5.3 Solvers; 3.4; 9.13 QuTiP interface facts | with improved_sampling=True mcsolve evolves one deterministic no-jump member (McResult.deterministic_trajectories, weight McResult.deterministic_weights[0]) and gives the stochastic trajectories the residual weights McResult.runs_weights; shots are drawn from that weighted mixture | QuTiP 5.3.1 McResult (introspected, tests/test_m0_qutip_interface.py) | the plan names the weight record deterministic_weight_info; no such attribute exists in 5.3.1 | corrected |
| `conv.mean_count_curve` | n̄(τ) = εR_o[(R_b/k) τ + (R_d/k^2)(1 - e^{-kτ})] | 8.2; 7.5 item 5 | the calibration fit for (εR_o, R_d, R_b) from bright-prepared records; equals the exact chain's mean when only R_d and R_b act | Noek 2013 Eq. 4 with ṗ_1 = R_b p_0 - R_d p_1 | the fit is degenerate at bin-time windows (tens of us: R_b unconstrained, the least-squares returned 1e4 Hz); it needs windows long against 1/R_d and 1/R_b, as Noek's 60 ms, so calibrate_detection draws a separate long record set for the fit | verified |
| `conv.mg25_no_measured_g_j` | g_I/g_J, not g_J | 4.5.1; 9.13; 13 Sign of A_hfs and the g-factors | no measured absolute g_J of the 25Mg+ ground state exists: both measurements servo the field to the electronic transition and determine only the RATIO g_I/g_J = 9.299484(75)e-5 (Itano and Wineland 1981) or 9.299308313(60)e-5 (Brewer et al. 2019), which is what the table now stores | Itano and Wineland, Phys. Rev. A 24, 1364 (1981); Brewer et al., Phys. Rev. A 100, 013409 (2019); qutip_trap/species/mg25.py | mg25.S12.g_J carried 2.00226 sourced PLAN_9_13, which is 9Be+'s measured value transplanted, and the Section 9.13 212.78 G clock-point anchor depends on it at the 0.25 G level (g_J = 2.000 moves it to 213.025 G). g_J must NOT be back-solved from the ratio plus a tabulated mu_I: the corrected and uncorrected shielding conventions give 2.0045 or fail the 9Be+ cross-check at 4e-4. 25Mg+ therefore still raises IncompleteSpeciesTable, now for a DERIVED reason (audit item E21) | corrected |
| `conv.micromotion_amplitude_convention` | peak \| rms | 13 Micromotion amplitude convention; 4.1.1; Appendix E MicromotionIndex | declared explicitly: SIGNED peak amplitude u_1 = -(1/2) q u_0 for the stray-field term, the minus sign forced by conv.floquet_phase_origin (the adopted a - 2q cos 2xi origin makes the in-phase micromotion a contraction, x_mu = -(q_x/2) x_sec cos omega_rf t); beta = delta_k . u_1 is signed too, so sign(beta_ip) = -sign(q_x E_x) and it steps by pi across the compensated shim; the in-phase (nullable) and out-of-phase ((1/4) q R alpha phi_ac, not nullable) indices never collapse into one number; total adds in quadrature, and the builder consumes the pair as (hypot(ip, op), rf phase + -atan2(op, ip)) | Blakestad 2011 Sec. II; Berkeland 1998 | Blakestad's zone-E numbers are inconsistent in one convention (68 nm peak against 47 nm rms); the M1 code shipped +(1/2) q u_0 with abs() on both the amplitude and the index, i.e. the OTHER rf phase origin, which inverted every micromotion-sideband phase and the sign of the rf-photon correlation servo step while leaving the fitted null (a zero crossing) and J_0 (even) untouched [corrected: M1 audit, fix batch] | corrected |
| `conv.micromotion_compensation_loop` | device_with_compensation(device, {name: table.micromotion['shim[name]']}) | 7.5 (micromotion compensation); 4.1.1; 4.3.6 | run() programs the table's CALIBRATED shim entries onto the device it evolves; the crystal is re-solved at that setting, so the ions' displacement, the beams' intensity at the ions and the Lamb-Dicke parameters move together. A seed shim is not programmed (it is the device's own setting) and an uncalibrated one is reported in the run notes and leaves the device as it is. The reported device hash stays the UNCOMPENSATED device's, because no device parameter changed - only a programmed voltage. This closes the loop PLAN 7.5 asks for by name ('stores shims and beta in the table, so that compensation is calibrated, drifts with the stray field between calibrations and is re-nulled like a laboratory re-nulls it') | run/job.py (step 2b); experiments/micromotion.py device_with_compensation; tests/test_m8_calibration_fixes.py | nothing in run/, dynamics/ or control/ read table.micromotion: the shims and the residual beta were inert bookkeeping, the run always used the device's own Trap.shim_voltages_v / stray_field_v_per_m, and a stale compensation against a drifted stray field was not a reproducible failure mode at all (the entries WERE wired into UPSTREAM['sideband_spectroscopy'] as a gate, which is the other half of the requirement) | corrected |
| `conv.micromotion_correction` | C0 | 13 Micromotion correction; 4.1.1 | eta_eff = eta C0, C0 the e^{i nu t} Fourier coefficient of the exact Floquet function normalized by Im(u* u_dot) = nu: C0 = 1 + 3q^2/16 + O(q^4) (1.007741 at q = 0.2) | check_c0_floquet.py; Wineland Eqs. 71-72; Berkeland Eq. 21 | the RMP's (1 + q/2)^-1 is the u(0) = 1 normalization at one rf phase, an rf-clock artifact [corrected: derivation audit, 2026-09-04] | corrected |
| `conv.micromotion_jn_weights_dropped` | J_0(beta) only, the J_n^2 sideband weights dropped | 4.3.6 (PLAN.md:502), 13 row 'Micromotion factors on drives'; dynamics/hamiltonian.py | the default unlocked micromotion mode multiplies every drive coefficient by J_0(beta) and drops the incoherent J_n^2 micromotion-SIDEBAND weights entirely, although PLAN.md:502 says the unlocked average keeps 'only J_0 AND the J_n^2 weights' and Section 13's row asks for J_n(beta) on every drive. The machinery exists (micromotion='modulated' with a per-shot rf_phase_rad sample reproduces the full modulation) but nothing in qutip_trap/ produces KEY_RF_PHASE, so the weights are unreachable from a run | PLAN.md:502; qutip_trap/dynamics/hamiltonian.py micromotion='carrier_j0' | no Section 9.2 row is blocked (that row asks only for the J_0 reduction, 1.018161 at q = 0.3 and 2(C0 - 1) = 3.4 percent, both tested) but a micromotion-sideband-spectroscopy row would be; anchor.m2.micromotion_carrier_factor records that the default is the J_0 average without recording the omission, which this entry does (M2 audit E17, D) | derived |
| `conv.micromotion_static_spring` | u_0 = Q E/(m (Omega/2)^2 (a + q^2/2)) | 4.1.1; 9.12 Excess micromotion from a stray field | Trap.micromotion_beta balances the residual static force against the PSEUDOPOTENTIAL spring m (Omega/2)^2 (a + q^2/2), the period average of the driven Mathieu equation's particular solution, not against the exact exponent m nu^2 = m (Omega/2)^2 beta^2; the DC Fourier row gives x_0 = f/(a + q^2/2) exactly to O(q^4) and Berkeland's own omega is the pseudopotential secular frequency | driven Mathieu equation, DC Fourier row; Berkeland 1998 Eq. 16; qutip_trap/trap/model.py | the two springs differ by 0.39 q^2, 3.2 % at q = 0.28, so Trap.micromotion_beta gives \|beta\| = 0.034934 where the 9.12 row prints 0.034 (2 digits); that is why tests/test_trap_model.py bands the trap-level assertion at rel = 0.1 and pins the tight one against the standalone displacement_m helper [M1 audit, fix batch] | derived |
| `conv.mode_classes_and_tolerances` | dropped / frozen / resolved | 5.2; 11.3 item 2; 5.5 | per mode, from the Section 4.4.3 integrals of every played waveform with the prepared occupations: dropped when \|alpha\|^2 (2 nbar + 1) < 1e-6 and \|chi\| < 1e-4; frozen when \|alpha\|^2 (2 nbar + 1) < SolverOptions.freeze_alpha_max (1e-4) and \|chi\| < freeze_chi_max_rad (0.05); resolved otherwise, with the cap from the loop radius eta Omega/epsilon, the occupation and the Section 5.1.1 margin; a frozen mode's chi is absorbed by the exact spot check on the reduced space (the frozen run is calibrated to the same angle) and reported; modes only carrier pulses touch are frozen | PLAN.md Section 5.2 [corrected: critique, 2026-09-04]; qutip_trap/run/space.py | the plan names 'the residual-displacement tolerance' for freezing without a value; 1e-4 is the M6 default, an approximation of the truncation kind reported per run [background] | corrected |
| `conv.mode_dimension_max` | SolverOptions.mode_dimension_max | 5.1.1; 5.2; 5.3; 5.5 | the cap rule takes the populated range of the thermal mode displaced by the loop radius at the boundary threshold, plus the Section 5.1.1 margin for the mode's eta, clamped to mode_dimension_max (default 64) | dynamics/engine.py SolverOptions; run/space.py cap_requirement, cap_for, select_space; run/gate_local.py step_space; tests/test_m9_scaling.py | M9a fixed d_max = 64 in select_space and step_space with no way through run(), and when the rule wanted more it silently narrowed the DECLARED expected occupation range with it, so the Section 5.1.1 oracle check and the Section 5.5 margin check ran over less than the physics. Section 5.3 states that a Doppler-cooled nbar ~ 20 mode needs d_m >~ 150 for a boundary population below 1e-4 (measured here: the rule asks for d = 156 at nbar = 20, eta = 0.1, tail 1e-4) and Section 5.2 that long-chain transverse spectators within tens of kHz of the tones cannot be frozen and must be resolved: exactly the combination the clamp could not carry. The ceiling is now SolverOptions.mode_dimension_max, still 64 by default, and a clamp emits a SpaceSelection.notes entry naming the mode, the d the rule asked for, the expected range it asked for and the clamped range | corrected |
| `conv.mode_drift_differential_independence` | mode_drift_differential drawn as M independent per-mode offsets | 6.2 'never as M independent offsets' | the COMMON-mode part is correct and is one draw (KEY_RF_FRACTION, applied as frac x omega_m to every rf-derived family, conv.motional_dephasing_from_rf_noise); the DIFFERENTIAL dc part is drawn as n_modes independent normals of the Drift's rms | M7 audit E-21 / B-10 (noise/model.py:271-283, the size = n_modes draw) | Section 6.2 warns against exactly this: 'each has a correlation time and is sampled as a trajectory or a quasi-static value by the rule of Section 6.1, NEVER as M independent offsets, because independent sampling makes closure errors average as 1/sqrt(M) instead of adding and lets robustness tests pass for the wrong reason.' The physical fix is one draw per dc electrode projected through d omega_m/d V, and the MISSING DEVICE PARAMETER is that Jacobian: Trap carries omega_hz, shim_voltages_v and an optional geometry, but no d omega_m/d V per electrode, so the projection cannot be built from the present record. Recorded as a known deviation rather than silently left: a multi-mode FM/AM-FM robustness result obtained under it is optimistic by up to sqrt(M) | contested |
| `conv.mode_eigenvector_gauge` | b^(p), c^(k) | 13 Mode eigenvectors; 4.1.3 | orthonormal, axial eigenvalues increasing, last component positive; transverse families numbered ascending in frequency (Appendix E), so the COM is the last transverse mode | Marquet 2003; James 1998 | James's Table 2 eigenvectors ARE sign-consistent with the closed forms (an extracted item claimed otherwise) | verified |
| `conv.mode_index` | mode: int | 13 Mode index; 4.1.3; Appendix E | every mode index is a position in Crystal.modes, ordered axial, transverse_1, transverse_2, ascending frequency within a family; eigenvectors unit norm, last component positive | PLAN.md Appendix E | eigensolver output order, frequency order across families or (family, index) pairs, none declared in the second revision [corrected: critique, 2026-09-04] | corrected |
| `conv.motion_phase_default` | phi_m = 0 | 4.4.1; 4.4.3; 9.4 (M4 finding) | the played waveforms default to equal tone phases (phi_b = phi_r, the force Omega cos(mu t)); the carrier Omega cos(mu t - phi_m) rotates the spin at F(t) = (2 Omega/mu)[sin(mu t - phi_m) + sin phi_m], whose mean tilts the force axis by psi = (2 Omega/mu) sin phi_m | check_two_qubit.py; Roos 2008 (psi = (4 Omega_Roos/delta) sin zeta) | Section 4.4.3's sine beat-note convention (phi_b - phi_r = pi, force zero at t = 0) is a convention for the closed forms only: played literally on the check_ms_closure.py fixture (Omega/mu = 0.1) it tilts the axis by 0.202 rad and leaks sin^2 psi = 3.8% into \|du>, \|ud> (concurrence 0.9597 against 0.9999 with equal phases); the kernels carry phi_m explicitly and Choi's sin(mu t) e^{i omega t} is their phi_m = pi/2 case | recomputed here |
| `conv.motional_dephasing_from_rf_noise` | 2/tau = omega_m^2 S_V,white (rf-derived modes) | 6.2; 13 'Motional dephasing operator' | a white fractional rf-amplitude density S_V gives a white mode-frequency density omega_m^2 S_V and the coherence of \|n> + \|n'> decays at (n - n')^2/tau; the axial (dc) family does not follow the rf amplitude | Section 13 row L = a^dag a sqrt(2/tau) with omega_m proportional to V_rf for the transverse families | the tabulated band of the same spectrum is a common-mode fractional trajectory of the transverse modes, never M independent offsets (Section 6.2) | derived |
| `conv.ms_closure` | eta Omega/eps = 1/(2 sqrt K) | 13 Spin operator in MS formulas; 4.4.1 | the force on S_y is -(hbar eta Omega/2)(a^dagger e^{i eps t} + h.c.) for the plan's per-tone Omega; closure eta Omega/eps = 1/(2 sqrt K) in every spin normalization; targets -pi/2 on J_y^2, -pi/8 on S_y^2, pi/4 on sigma_y sigma_y | Sorensen-Molmer 2000; Kirchmair 2009; Roos 2008; check_ms_closure.py | the second revision halved the closure to 1/(4 sqrt K), a factor-2 force error caught by the derivation audit; Roos's Omega_c = \|eps\|/(4 eta) reads \|eps\|/(2 eta) in the plan's Omega | corrected |
| `conv.ms_gates_serialized_under_parallel_addressing` | start_ms = max(global_clock, clock[a], clock[b]) | 7.3 | under parallel addressing the per-ion clocks carry the single-qubit gates and an entangling gate advances BOTH its ions' clocks and the crystal-wide clock, so carrier pulses on distinct ions overlap and entangling gates never do; HardwareChain.parallel_addressing (additive field, default False) is the device model's permission and schedule(parallel=None)/run(parallel=None) read it, an explicit parallel=True against a chain that declares False being refused | PLAN.md Section 7.3 ('MS gates are serialized, one at a time per crystal in the first release'; single-qubit gates 'in parallel if the device model allows parallel addressing'); qutip_trap/control/schedule.py | the M6 scheduler started an entangling gate at max(clock[a], clock[b]) under parallel=True and never advanced the crystal clock, so two MS gates on disjoint pairs of a four-ion chain were both scheduled on [0, 50] us on the SAME global Raman pair (measured); Schedule.__post_init__ accepted it because it only forbids overlap on a shared ion. The flag was also a caller argument with no device field behind it, against 7.3's 'if the device model allows' | corrected |
| `conv.mu_i_shielding_convention` | mu_I corrected versus uncorrected | 13 Sign of A_hfs and the g-factors; 4.5.6 | the tables carry mu_I in THREE different conventions, each now declared per table: 9Be+ -1.177432 mu_N DIAMAGNETICALLY CORRECTED (Dickopf 2024's g_I gives -1.1774316344; the uncorrected NMR value is -1.17449(2), 0.25% away), 25Mg+ -0.85545 mu_N UNCORRECTED (Alder and Yu 1951 via Stone, which carries no correction flag for that row; no corrected 25Mg value exists, and Stone's corrected recommendation is -0.85533(3)), 43Ca+ -1.315350(9) mu_N FREE-ION (Hanley et al. 2021, explicitly uncorrected for the ion's bound electrons; Stone's bare-nucleus value is -1.31733(6)) | Stone INDC(NDS)-0794; Dickopf et al., Nature 632, 757 (2024); Hanley et al., arXiv:2105.10352 | PLAN.md 4.5.6 called the 43Ca+ value 'a thesis value 0.17% from the handbook number that fits f0 nine times better, effectively a fitted constant'. It is not fitted: it is the free-ion moment, which is the moment a trapped-ion Breit-Rabi Hamiltonian takes, and Hanley et al. print it to 9e-6. Mixing the conventions between species is a 0.2-0.25% error in g_I | corrected |
| `conv.multi_pair_waveform_angles` | Waveform.chi_m of a multi-pair GateModes | 9.17 'Waveform per (ion, leg)'; Section 10 M4 bullet '3 to 5 ions' (M4 audit 2026-09-07) | a GateModes built on more than two ions carries an entangling angle per pair, and the Waveform stores ONE pair's per-mode angles; the builders took the first pair's regardless of the pair the solver targeted, and the scheduler reads Waveform.chi_total_rad for both the s^2 amplitude rescale and the kernel sign | tests/test_shaping.py::test_multi_pair_waveform_stores_the_solved_pairs_angles | measured on a three-ion chain, pair (0, 2): chi(0,2) = pi/4 while the same envelope carries chi(0,1) = chi(1,2) = 0.449272, so the stored angle was 1.75x wrong and the amplitude rescale a factor sqrt(pi/4 / 0.449272) = 1.32 off. The solver's ``pair`` is now threaded into waveform_from_segmented/waveform_from_callables and a multi-pair GateModes with no pair named is REFUSED rather than defaulted | corrected |
| `conv.multilevel_frame_assignment` | f_level, delta_b = omega_b - (f_U - f_L) | 4.2.8 rotating frame; 5.3 | frame frequencies assigned manifold by manifold along the beams from the lowest included level at f = 0 so that every detuning sits on the diagonal; levels reached only by decay take the transition frequency as frame (the dissipator is invariant); a beam inconsistent with two assigned manifolds (two tones on one transition, a closed loop) leaves a residual beat, the Hamiltonian is time periodic, and the steady state is the fixed point of the one-period propagator (vern9, nsteps 1e7) period-averaged, the same machinery serving a polarization modulation; incommensurate beats fall back to propagating 1000/Gamma and averaging | qutip_trap/dynamics/multilevel.py (M3a) | the plan's own construction, tagged background; validated against the secular static model (5e-4 in populations, 2e-3 in rate at a 14.7 GHz beat) rather than against a source | background |
| `conv.native_ms_matrix` | MS(phi0, phi1, theta) | 7.6 IonQ cloud notation | MS(phi0, phi1, theta) = exp[-i pi theta GPi(phi0) (x) GPi(phi1)] with theta in turns in [0, 0.25]; GPi(phi) = [[0, e^{-2 pi i phi}], [e^{2 pi i phi}, 0]]; GPi2(phi) = (1/sqrt 2)[[1, -i e^{-2 pi i phi}], [-i e^{2 pi i phi}, 1]]; ZZ(theta) = diag(e^{-i pi theta}, e^{i pi theta}, e^{i pi theta}, e^{-i pi theta}); XX(chi) = MS(0, 0, theta) with chi = pi theta | IonQ documentation; qiskit-ionq, ionq-core-python, PennyLane-IonQ client code read 2026-09-04 | - | verified |
| `conv.no_stark_imbalance` | xi = (delta eta^2/\|eps\|)(phi/pi) | 4.4.1 (Stark compensation; M4 audit 2026-09-07) | waveform_from_segmented and the solvers accept an ``imbalance`` that splits the two legs' amplitudes, and nothing in the repository ever passes a non-zero value: the compensation LAW xi = (delta eta^2/\|eps\|)(phi/pi), the differential shift delta_ac^(C) = 2(Omega_r^2 - Omega_b^2)/delta = -8 Omega^2 xi/delta and the delta^(SB) = (eps/2) xi n term that breaks thermal-state operation are unimplemented, and so is the third-tone alternative | Kirchmair 2009 (Omega_b = Omega(1 + xi), Omega_r = Omega(1 - xi)); Innsbruck third-tone alternative (Section 4.4.1, last line) | control/table.py's Segment docstring claimed 'per-ion imbalance is a CalibrationTable entry', which does not exist and is not what the leg split is: the imbalance is carried by the two legs' own amplitudes, which the solvers set, and the docstring now says so. The closed forms also IGNORE a non-zero imbalance (integrals_segmented reads one amplitude per (ion, segment)), so a waveform built with imbalance != 0 carries the BALANCED pulse's chi_m and alpha_m | contested |
| `conv.noise_provenance` | NoiseSpectrum.provenance, Drift.provenance, NoiseModel.provenance_sentence() | 6.1 'An N-ion error budget assembled from them is stitched from at least three apparatus, and the report says so' | one tag per apparatus per rate ('author year, species, N ions'); NoiseModel.apparatus() unions the tags of every NON-QUIET rate (a zero rate contributes nothing to a budget and so no apparatus), undeclared_rate_count() counts the non-zero rates with no tag, and provenance_sentence() is emitted into Diagnostics.approximations whenever either is non-empty | M7 audit E-12 (noise/spectra.py, noise/model.py, run/job.py Diagnostics) | before this neither record had a provenance field and NoiseModel.extra was numbers only, so Section 6.1's reporting deliverable could not be produced at all | extracted |
| `conv.noise_routing` | route (b) Lindblad / (c) quasi-static / (d) sampled / (e) events | 6.1; 5.5; 13 | a NoiseSpectrum's tabulated band is synthesized per dynamical sample on a fixed grid (x(t) = sum_k sqrt(S(omega_k) d omega_k/pi)[a_k cos + b_k sin], <x^2> = (1/pi) int S d omega); its white_level is a Lindblad rate; Drift records are OU chains over the shot clock; collisions are Poisson events per shot | PLAN.md Section 6.1 (M7 implementation: noise/model.py, noise/processes.py) | no heuristic on a spectrum's shape decides its route: the two fields declare the split; a grid that cannot resolve the band refuses with the remedy (declare the band above it as white_level) | verified |
| `conv.nuclear_g_sign` | g_I | 13 Nuclear g-factor sign; 4.5.1 | g_I = -(mu_I/(I mu_N))(m_e/m_p), so H_Z = mu_B (g_J J_z + g_I I_z) B/hbar with one sign for both; 43Ca+ g_I = +2.0467e-4 from mu_I = -1.31535 mu_N | Steck; Harty 2014 | Harty's g_I^(N) = mu_I/(I mu_N) with a minus sign and Langer's g_I = -mu_I/(I mu_B) with a plus are self-consistent alternatives that must not be mixed: 83.76 kHz and 0.207 G off for 43Ca+ | recomputed here |
| `conv.observation_model` | P_declared = P_bright (1 - eps_B) + (1 - P_bright) eps_D; k ~ Binomial(shots, P_declared) | 7.5 Every fit reports an uncertainty; 8.4 | every experiment reads its populations as shots declared through the readout's (eps_B, eps_D), keyed by (sample, trajectory, shot, ion, channel); the fits are weighted least squares whose covariance is scaled by max(1, chi^2/dof), so an under-fitting model widens its own error bars; shots=None returns the exact populations (the M2 to M7 behaviour) | experiments/fitting.py Observation, weighted_fit | the first M8 build keyed the draws by (sample, point index, ion, outcome) only, so a calibration running the same experiment twice, or an experiment repeating a scan, replayed identical noise: the Stark scan read -12.67 Hz for BOTH beams of the fixture drive, exactly, and summed their uncertainties as if independent (3.3 sigma from the truth for a single 2.3 sigma draw counted twice); every experiment of a calibration and every sub-run now carries a stream label appended to the outcome key (Observation.stream, sub_stream) | background |
| `conv.one_rate_per_upper_level` | Transition.gamma_hz is the UPPER level's TOTAL rate | 13 Linewidth Gamma | every E1 transition out of one upper level carries the same gamma_hz, enforced in Species.__post_init__ and re-checked in AtomicStructure.total_decay_rate_rad_s | qutip_trap/species/model.py; qutip_trap/species/raman.py; tests/test_m0a_branchings.py | total_decay_rate_rad_s used set.pop() on the distinct rates, so two disagreeing gamma_hz returned an arbitrary one; a level with lifetime_s = None and two inconsistent transitions passed validation (audit item E8) | corrected |
| `conv.openqasm2_subset` | load_openqasm2 | 1.4; 7.6 | header, include (ignored, qelib1.inc gates built in), qreg/creg (flattened in declaration order), gate declarations expanded by inlining (the client SDKs' gpi/gpi2/ms/zz declarations import through their own u, rz, rxx, rzz bodies), register broadcasting, barrier ignored, trailing measure -> Circuit.measure, mid-circuit measure/reset kept as operations, expressions over pi with + - * / ^ and sin cos tan exp ln sqrt; undeclared gpi/gpi2/ms/zz take radians (the IR convention); if and opaque refused | OpenQASM 2.0 specification (Cross et al. 2017); qelib1.inc; PLAN.md Section 7.6 | u1(lambda) = e^{i lambda/2} RZ(lambda): imported as rz, the global phase dropped | verified |
| `conv.operator_order_templates` | right-to-left | 13 Operator order in templates; 7.7 | matrix product written right-to-left, circuit time left-to-right; every template checked numerically | Maslov 2017 Sec. 2.2 | Maslov's '.' reverses circuit order; IonQ's virtual-Z rule sign depends on this | corrected |
| `conv.optical_phase_beam_path_sign` | Delta phi_i = phi_2 - phi_1 | 5.2 (PLAN.md:808); 13 row 'Optical phase factor on sigma_+' (PLAN.md:1927); 4.3.7, 4.4.2 | Delta phi_i is the phase of the BEAT NOTE written E ~ cos(omega_L t - Delta k . r + Delta phi_i), so for a Raman pair with Delta k = k_1 - k_2 and E_j ~ cos(k_j . r - omega_j t + phi_j) it is phi_2 - phi_1, and for a single-beam optical drive (Delta k = k_1) it is -phi_1; the factor on sigma_+ is e^{+i(Delta k . X_i - Delta phi_i)}, so beam 1's sampled path phase enters as e^{+i phi_1} and beam 2's as e^{-i phi_2} | Monroe et al., RMP 2020 Eqs. 16, 18; tests/test_m2_optical_phase.py | M2 shipped beam_phase = phi_1 - phi_2 inside e^{-i beam_phase}, i.e. e^{+i Delta phi_i}: a sampled +0.4 rad on beam 1 played GPi2(-0.4) where the plan's convention gives GPi2(+0.4). The relative sign of Delta k . X_i and the optical phase is physical (PLAN.md:808), and it is the sign Section 4.3.7's phi_G = phi_{B,b} - phi_{B,r} cancellation and Section 4.4.2's spin/motion split refer to (M2 audit E2) | corrected |
| `conv.optical_phase_drift` | e^{+i(Delta k . X_i - Delta phi_i)} with X_i = X_i^(0) + u_0(i) | 13 row 'Optical phase factor on sigma_+' (PLAN.md:1927); 4.1.1 u_0 = Q E_dc/(m omega^2); 5.2 (PLAN.md:808) | the builder's geometric phase is Delta k . (X_j - X_i) with BOTH equilibrium positions carrying the shot's sampled stray-field displacement u_0 = Q E_dc/(m omega^2) (noise/sampling.py key_position_offset_m), so the surviving term is Delta k . (u_0(j) - u_0(i)); the addressed ion's own drift is a per-ion constant that the frame alignment of Section 7.5 step 3 absorbs and cancels out of the difference | Berkeland et al., J. Appl. Phys. 83, 5025 (1998) Eq. 16; tests/test_m2_optical_phase.py | M2 shipped the drift in the beams' INTENSITY only (_pointing_factor), keeping a 2.6e-7 amplitude effect and dropping a 0.51 rad phase for 171Yb+ at 355 nm counter-propagating with u_0 = 14.3 nm at 1 V/m; Section 13's row says 'beam-path AND ion-position drift stored as one scalar per ion per pulse' (M2 audit E1) | corrected |
| `conv.oracle_margin_rule` | margin >= 4 | 5.1.1 rule ii, 5.5; hilbert/space.py | HilbertSpace.displacement_factor asserts the interior elements against the analytic oracle to interior_tolerance(margin, eta) (the table row d = 2 margin, log-interpolated, floored at 1e-12) whenever the margin above the declared range is at least 4 levels; below the table's smallest row the difference is reported by oracle_status and the boundary monitor governs; required margins 6/10/20 at eta 0.1/0.5/1.0 are stored as an interpolated fixture | the Section 5.1.1 table (bench_numerics.py); tests/test_hilbert_layer.py | a fatal oracle at small caps would defeat the cap-raising retries of Section 5.5, which start from a deliberately small cap (M2 rule) | derived |
| `conv.per_ion_structures_and_selectivity` | models_per_ion(structures: Mapping[int, AtomicStructure] \| AtomicStructure, ...) | 4.2.5; 9.3 Sympathetic; 9.12 row 1360 | one stage call now holds a coolant and a qubit species: stage_rates and doppler_cooling take either one AtomicStructure for every illuminated ion or a per-ion mapping, and an illuminated ion with no structure is refused rather than given a guessed one. Section 9.12 row 1360 is pinned as the operator statement it is: the coolant's Hamiltonian and collapse operators act as X (x) 1 on a two-ion joint space, so the qubit ion's reduced density matrix - populations AND coherences of a general state with \|rho_01\| = 0.36 - is invariant under the whole cooling Liouvillian to below 1e-12 over 40 lifetimes while the coolant's own state moves by more than 1e-3 | qutip_trap/prep/rates.py; qutip_trap/prep/doppler.py; tests/test_sympathetic.py | none | derived |
| `conv.pgc_two_models` | xi = Delta s/(3 omega); W = (16/9) eta^2 Gamma s xi cos^2 2phi; H = (2/9) eta^2 Gamma s (8 xi^2 cos^4 2phi + 2 + sin^2 2phi) | 4.2.4; 13 Polarization-gradient depth parameter, Position dependence in the PGC model, Two saturation parameters | the analytic j_g = 1/2 <-> j_e = 1/2 model with the isotropic alpha = 1/3, the static-gradient per-ion average that raises at \|cos 2phi\|^2 < 1e-2, and the moving-gradient window W < delta < omega; the Lindblad layer is the M3a builder on two counter-propagating linear beams along B with recoil = minimal (the three-class kernel) and beam phases (2 phi, 0), so the sigma+- standing waves are offset by pi/2 automatically; xi is read from the D1 sigma Rabi frequency as Omega_1^2 Delta/(4 omega (Delta^2 + Gamma^2/4)) = (1/3) Delta s with s referenced to the D2 stretched line (Omega_1 = sqrt(2/3) Omega_stretched) | qutip_trap/prep/polarization_gradient.py; Joshi et al. 2020 | the 171Yb+ F = 1 -> F' = 0 scheme shares no coefficient with either model; only its beam projections and the bridge s_Joshi = (3/2) s0 (Gamma/2)^2/((Gamma/2)^2 + Delta^2) [background] are computed | verified |
| `conv.played_chain` | Omega_phys = Omega_req x Omega_derived/Omega_table | 7.3; 7.10 Modulator response; 7.5 | every pulse the scheduler emits carries the table's beliefs (Drive.programmed); the engine converts a requested Rabi frequency through the device's derived map divided by the calibrated one, the light shift to the derived shift at the played intensity (delta_derived sum_tones (Omega_phys/Omega_derived)^p), the crosstalk to the derived ratios of the listed neighbours; the identity for a surrogate table (seeds = derived values), so M2 to M7 are unchanged. SCOPE: the conversion applies to the kinds whose rf-power-to-Omega map the light layer derives from the beams and the atomic structure - raman, optical_E1, optical_E2. A microwave, state-dependent-light-shift (sigma_z sigma_z, Section 4.4.4) or microwave-gradient drive is played exactly as requested (control/played.py returns early), so a miscalibrated table produces NO over-rotation, detuning or crosstalk error for it: Section 7.10 exempts nothing and a light-shift drive's Omega is derivable from the beams (Section 4.4.4), so this is a narrowing of M8's central claim to two of four drive kinds, recorded here rather than left in an inline comment (M8 audit D1, open) | PLAN.md Section 7.10 ('the rf-power-to-Omega map is calibrated by the Rabi scan of Section 7.5 exactly as in the laboratory'); control/played.py | M2 to M7 played the table's Rabi frequency, Stark shift and crosstalk as the physics, which made a miscalibrated entry error-free by construction | background |
| `conv.played_zero_truth` | physical_drive | 7.3 (M8 played chain); 4.4.4 (M4 audit 2026-09-07) | the played chain converted a REQUESTED Rabi frequency into the physical one through the ratio Omega_true/Omega_believed, and fell back to ratio = 1 with the note 'no usable Rabi entry' whenever either was zero. That conflated a calibration gap with a beam geometry the device derives ZERO coupling for, in which case it played a pulse the beams cannot deliver under a note that was factually wrong | control/played.py; tests/test_light_shift_gate.py | Omega_true == 0 with a usable belief now RAISES and names the geometry; the 40Ca+ light-shift fixture's 729 nm E2 beam, whose Delta m = 0 quadrupole factor was exactly zero for k_hat = y_lab with pol = x_lab, was rotated instead of supplied (anchor.m4.light_shift_gate_ca40) | corrected |
| `conv.pm_solver` | solve_phase_modulation | 3.2; 4.4.3; 7.4; Section 10 M4 bullet (M4 audit 2026-09-07) | Section 4.4.3 supplies equations for AM (Zhu, Choi), FM (Leung) and AMFM (Blumel) and NAMES NO PM SOURCE, while 3.2's module comment, 7.4 and the M4 bullet all list PM as a deliverable: the plan is internally inconsistent. The solver implemented is the piecewise-constant per-segment MOTION phase at fixed amplitude and beat note, alpha_{i,m} = i eta_{i,m} Omega sum_k e^{i phi_k} F_k^{(m)} with the same symmetrized double integral and the phases inside, solved by least squares like the FM family's vertices, then scaled to \|chi\| = chi_target; the three sources above are the family's provenance and are [background], not equations the plan supplies | Milne et al., PRApplied 13, 024022 (2020); Green and Biercuk, PRL 114, 120502 (2015); Lu et al., Nature 572, 363 (2019) | the data model was widened rather than the Segment type: SegmentedEnvelope gains an optional per-segment phase offset and every integral sees the complex weight z_k = Omega_k e^{i phi_k}, the offsets live in the two legs' HALF-DIFFERENCE (so the ion's spin phase, the half-sum, never moves and the force axis stays put) and envelope_of recovers them instead of collapsing them to a sign. Milne's N + 1 phase segments for N modes needs their time-symmetric phase profile, which this general solver does not impose: at N + 1 = 3 segments it leaves \|alpha\| ~ 1e-2 on the two-ion fixture while Choi's 2N + 1 = 5 close both modes to 3e-17. The family also cannot use conv.motion_phase_default's tilt-free phi_m = 0, because the motion phase IS its modulation parameter, so Roos's spin-axis tilt psi = (2 Omega/mu) sin phi_m is intrinsic to it: the exact check measures 1.8e-2 leakage against 4.7e-4 (FM) and 4.7e-7 (Fourier AM) on the same fixture | background |
| `conv.polarization_components` | eps_q | 13 Polarization components; 4.5.3 | eps = sum_q eps_q e_q about B, e_{+-1} = -+(x +- i y)/sqrt 2, e_0 = z; helicity q_gamma = +1 (sigma+), 0 (pi), -1 (sigma-) for absorption \|F mF> -> \|F' mF + q_gamma>; operator index q_op = m_lower - m_upper = -q_gamma | Steck QAO Eqs. 7.188-7.190, 7.419-7.422; Rb87 Eqs. 34-37, 54-55 | Steck's tables list sigma+ under q = -1; extracted items interchanged the two labels | corrected |
| `conv.preparation_recipe` | Device.preparation: PreparationRecipe | 3.3; 3.4; 4.2.6 | the cooling beams at their detunings, the pulsed sideband schedule (beams, modes, pulses per order) and the pump beams are procedural device parameters carried on the Device; standard_recipe builds the 171Yb+ laboratory's recipe from the device's detection and global Raman beams (detection light shifted by -Gamma/2 then optimized over the gate modes' participation weights, pulsed Raman sideband cooling of the coupled modes with 10 second-order then 30 first-order pulses, the pump on F = I + 1/2 -> F' = I + 1/2 at 0.5 I_sat) and records every choice; run_preparation evaluates it with the M3 stages, the pumps' recoil heating added to the cooled occupations | qutip_trap/prep/recipe.py; PLAN.md Section 4.2.6 | Appendix E's Device has no such field; it is an additive field with default None (the API-freeze test allows additions), needed because the plan's prepare() stage has no other source for the laboratory's procedure | background |
| `conv.preparation_single_species_f0` | standard_recipe scope | 3.2; 3.4; 4.2.6 | standard_recipe builds a recipe only for a SINGLE-SPECIES crystal (prep/recipe.py _structure) whose qubit's lower level is an F = 0 hyperfine state (its pump is the F = I + 1/2 -> F' = I + 1/2 line of the cycling transition); every other qubit - optical, Zeeman, and any mixed-species crystal - needs an explicit Device.preparation, which run() honours. device/presets.ca40_optical_recipe is the worked example for a 40Ca+ optical qubit: 397 nm Doppler cooling at -Gamma/2 along the oblique path with the 866 nm D3/2 repump, no sideband stage, and a pump into S1/2 mJ = -1/2 by 397 nm sigma- light along B with the same repump (the I = 0 analogue of the 171Yb+ F = 1 -> F' = 1 pump) | qutip_trap/prep/recipe.py; qutip_trap/device/presets.py | the plan grants no species restriction (Section 3.2 lists 171Yb+, 40Ca+ optical, 43Ca+ clock and 137Ba+ presets, and Section 3.4's prepare stage carries no caveat), and until M6's fix the restriction was unrecorded and unescapable in practice: the refusal message named the escape hatch but no non-171Yb+ recipe existed anywhere in the repository, so run() was exercised on one species. The mixed-species half of the restriction stands: prep/ refuses a crystal of two species outright, while the crystal, mode and recoil layers all support one | background |
| `conv.preparation_stage_order` | Doppler -> sideband/EIT/PGC -> final pump | 4.2.6; 7.3; 9.17 Preparation stage order | PreparationSequence refuses a sequence that does not start with Doppler cooling, a sub-Doppler stage with no Doppler precooling before it, and a pump followed by a cooling stage with no later pump; the hand-off State takes each mode's nbar from the LAST stage that addressed it and each ion's internal state from its last pump (or an explicit state for an unpumped coolant/qubit) | qutip_trap/prep/sequence.py | the second revision printed the reverse order (pump first); run.prepare (Appendix E) waits for M6, where the Device carries the recipe | corrected |
| `conv.pseudopotential_error_estimate` | \|u_1\|/s | 4.1.7 close; 4.1.1; 9.13 Kaufmann inversion | Device.derived() reports pseudopotential_error = max_i \|u_1,i\| / min_ij \|r_i - r_j\|, with u_1 = -(q/2) u_0 the signed excess-micromotion amplitude and the denominator the smallest inter-ion distance: the fractional rf modulation of the Coulomb curvature (~1/r^3, so delta K/K ~ 3 \|u_1\|/s) that makes the pseudopotential modes of 4.1.3 inexact off the rf null. It is exactly 0 for a chain on the null with no stray field. Also reported: ion_height_m and trap_depth_ev on the surface path (4.1.6 'the module reports h and the trap depth with every device that uses it') and second_order_doppler[i] (4.1.1 'reports the implied second-order Doppler shift for completeness') | Kaufmann 2012; Landa 2012; qutip_trap/device/derived.py | PLAN 4.1.7 writes 'in units of the mode spacing'; the only length in the clause is the ION spacing and that is the reading taken, since the quantity is the small parameter of Kaufmann's mechanism. A single ion has no Coulomb coupling to modulate and gets a note instead of a number. Nothing reported any of these before [M1 audit, fix batch] | derived |
| `conv.pulsed_transfer_matrix` | W_k(t): a_n = cos^2(Omega_{n,n-k} t/2), b_n = sin^2(Omega_{n,n-k} t/2), Omega_{n,n-k} = Omega_0 \|<n-k\|D(i eta)\|n>\| | 4.2.8 Pulsed sideband cooling; 13 Sideband-cooling trapping condition | column-stochastic transfer matrices on the Fock populations with the exact Laguerre elements, coherences discarded between pulses, N pulses composed as an ordered product; the plan's Rabi convention traps population at Omega_{n-1,n} t = 2 m pi (the sources' half-Rabi m pi); the stranded state of order k is round(n'_zero + k) with the first Laguerre zero as a continuous degree; a repump between pulses is a column-stochastic recoil kernel sum_k Poisson(k) K^k | qutip_trap/prep/sideband.py; Che et al. 2017 Eq. 2; Rasmusson et al. 2021 Eqs. 6-7 | three schedule optimizations (shared duration, independent durations, one duration per order with higher orders first) with a coarse grid before the bounded scalar search, because the occupation is oscillatory in the pulse time (a bounded minimizer alone returned 6.7 against the global 3.1 on Rasmusson's fixed-pulse schedule) | verified |
| `conv.quadrupole_coupling` | Omega_{mm'} (E2) | 13 Quadrupole (E2) coupling; 4.5.7 | Omega_{mm'} = (e E_0 k/(2 hbar)) \|<S\|\|r^2 C^(2)\|\|D>\| \|Lambda(m, m')\| \|g^(q)\| with q = m - m', k = 2 pi/lambda_vac the single beam's wavenumber, C^(2)_q = sqrt(4 pi/5) Y_2q (Racah), rank-2 tensors normalized to 2/3, A = c alpha k^5 \|<\|\|>\|^2/(15 (2 j' + 1)) with the upper j'; carrier pi pulse at t = pi/Omega | James 1998; Roos 2000; species/quadrupole.py; light/raman.py | q = Delta m elsewhere; unit-normalized rank-2 tensors (sqrt(3/2) too large); r^2 Y_2m (sqrt(4 pi/5) too large); the lower-level 2j + 1 (sqrt 3 too small); air wavelengths in James's calcium panel and Roos's table; the Boulder Omega is half | corrected |
| `conv.quadrupole_element_normalization` | <v\|\|Q\|\|w> | 13 Reduced quadrupole element for D-state lifetimes; 4.5.7 | A = (1/15)(omega/c)^5 \|<v\|\|Q\|\|w>\|^2/(2 j_v + 1) with the UPPER-state degeneracy, prefactor 1.11995e18/lambda[A]^5 s^-1 on vacuum wavelengths (Transition.quadrupole_convention = johnson_1_15) | Kreuter et al. 2005 Eqs. 7-8; W. R. Johnson's lecture notes | the (1/75)(omega/c)^5 convention gives exactly one fifth, so its elements are sqrt 5 larger; air wavelengths shift tau by 0.15% | recomputed here |
| `conv.qubit_dephasing_from_field_noise` | gamma_phi = 2 pi^2 (d nu/dB)^2 S_B,white | 6.3; 13 'Qubit dephasing operator', 'Dephasing noise variable' | S_delta = (2 pi d1)^2 S_B (d1 in Hz/G, S_B in G^2/(rad/s)); gamma_phi = S_delta/2; the Ramsey coherence decays as e^{-gamma_phi t} (tests/test_noise_model.py) | Section 13 chain: H_deph = b sigma_z, b = pi delta nu, S_b = S_delta/4, L = sqrt(gamma/2) sigma_z decays the coherence at gamma = 2 S_b(0) | the white field level is a device declaration (NoiseSpectrum.white_level), never inferred from the band | derived |
| `conv.qubit_dephasing_operator` | L = sqrt(gamma_phi/2) sigma_z | 13 Qubit dephasing operator; 5.7; 6.3 | coherence decays at gamma_phi, so gamma_phi = 1/T2 for the white component; a measured T2 is split into quasi-static (Gaussian) and white (exponential) parts before use | PLAN.md Section 5.7; Ramsey test in 9.7 | conventions giving 2/T2 elsewhere [row added after the 2026-09-04 critique] | corrected |
| `conv.qutip_spectrum_sign` | S_qutip(omega) = int exp(-i omega tau) <dF(tau) dF(0)> dtau = W(Delta - omega) | 4.2.8 (iii); 9.3 Lamb-Dicke rate framework | for a weak two-level drive QuTiP's spectrum at omega equals the scattering rate at the detuning Delta - omega, so the heating coefficient is A_+ = S_qutip(+nu) + 2D and the cooling one A_- = S_qutip(-nu) + 2D, with 2D = sum_channels alpha_q eta_em^2 Gamma rho_ee (one alpha per polarization channel and one eta_em per line from its own wavenumber); at Omega = Gamma/2, Gamma/nu = 0.02: S(+nu) = 6.71368e-3 against eta^2 W(Delta - nu) = 6.71410e-3 s^-1, 2D = 1.074137e-2 against alpha eta_em^2 W(Delta) = 1.074135e-2 | qutip_trap/light/bloch.py; tests/test_level_c.py | the W(Delta -+ nu) closed form fed the SATURATED W is off by exactly 1 + s (nbar 9.7493e-5 against 6.4998e-5 at s = 0.5): Section 4.2.8 (vii)'s Omega << Gamma is enforced by rate_coefficients_from_model (M3a finding) | recomputed here |
| `conv.rabi_frequency` | Omega | 13 Rabi frequency; 4.3.1 | H = (hbar Omega/2) sigma_+ e^{...} + h.c., carrier pi pulse at Omega t = pi | RMP 2003; Monroe 2020; Lee 2005 | Wineland 1998, Monroe 1995 and Blumel 2021 use P = sin^2(Omega t), half the usual Rabi frequency | verified |
| `conv.rabi_from_intensity` | Omega = E_0 \|eps_q <F' m_F + q\|d_q\|F m_F>\|/hbar | 13 Rabi frequency from intensity; 4.5.2 | E_0 = sqrt(2 I/(eps_0 c)); on the cycling transition this reduces to Omega = Gamma sqrt(I/(2 I_sat)) and s = I/I_sat = 2 Omega^2/Gamma^2 there only | Steck; species/dipole.py; light/raman.py | Noek's s in the (Gamma/6, 2/3) detection form is I/(3 I_sat), the exact four-level result with one polarization share per sublevel (Sections 8.1, 8.8; derivation audit 2026-09-04) | corrected |
| `conv.raman_beam_labels` | Delta k = k_1 - k_2, mu = (omega_1 - omega_2) - omega_0 | 4.3.2, 13 Effective wavevector; light/raman.py | beam 1 is the higher-frequency beam ABSORBED from the lower qubit level and beam 2 the beam emitted into from the intermediate state, Omega_R = sum_e conj(Omega^(2)_{e up}) Omega^(1)_{e down}/(2 Delta_e); the tone at beat note omega_1 - omega_2 = omega_0 + mu drives \|down> -> \|up> with momentum k_1 - k_2 | Wineland 2003 Eq. 2.3; AtomicStructure.raman_coupling_rad_s(g1 = lower, g2 = upper, beam1, beam2) | Section 13's row wording 'beam 1 coupling to the upper level' is ambiguous (the intermediate level is 'upper' to both beams); the M0 pulses.py docstring repeated it and now states the frequency-ordering reading (M2) | derived |
| `conv.rayleigh_dissipator` | c = (1/2) sqrt(Gamma_el) sigma_z | 13 Rayleigh dephasing dissipator; 4.5.5 | dissipator prefactor Gamma_el/4 while the coherence decays at Gamma_el/2, both consistent; Raman operators sqrt(Gamma_du)\|u><d\| carry the plain rate | Uys et al. 2010 Eqs. 6-8 | sqrt(Gamma_el/2) sigma_z doubles the dephasing | recomputed here |
| `conv.rb_error_rate` | r = (1 - p)(2^n - 1)/2^n | 13 RB error rate; 7.9; 6.8 | an RB decay F(m) = A p^m + B reports r = (1 - p)(2^n - 1)/2^n, the average error per Clifford ((1 - p)/2 for one qubit, (3/4)(1 - p) for two); (4^n - 1)(1 - p)/4^n is the entanglement infidelity of the depolarizing channel with the same p and is reported under that name (noise/summary.py rb_error_per_clifford and depolarizing_entanglement_infidelity; benchmarks/rb.py RBResult.error_per_clifford and depolarizing_entanglement_infidelity) | Magesan, Gambetta and Emerson 2011; Wright et al. 2019; Chen et al. 2023 | the first version of the plan labelled the entanglement infidelity the RB error rate, 1.5x (n = 1) and 1.25x (n = 2) too large, which would have offset every RB comparison of M10; Wright's 'p is the gate fidelity'; Chen's Eq. 3 conflates decay bases with rates and prints a singular matrix | corrected |
| `conv.rb_sequence_inverse` | C_inv = (C_m ... C_1)^-1 | 7.9 Randomized benchmarking variants | m uniform Cliffords are closed by the inverse of THEIR PRODUCT, computed from the ideal matrices, recognized as a Clifford of its own class (two qubits) and compiled as one more Clifford; the survival is P(every benchmarked qubit reads 0); B is fixed at 1/2^n when fewer than three lengths are scanned or fix_offset is set, since a tiny decay leaves A and B degenerate (benchmarks/rb.py) | Egan et al. 2021; Mount et al. 2015; Magesan 2011 | the inverse of each Clifford, the extraction error Section 7.9 corrects | corrected |
| `conv.readout_chain_exact` | P(n \| start) by expm | 8.2; 5.7 | the count distribution over a window is one matrix exponential of the augmented generator (blocks Q - diag(lambda) with diag(lambda) one block up, an absorbing overflow block); the fast path samples the chain exactly (Gillespie) with Poisson counts at the piecewise rates, the trajectory path runs mcsolve on the class space with a photon-counting collapse operator | Markov-modulated Poisson process on the bright/dark/shelf chain (derived here); Crain Eq. 1 and Acton Eqs. 5-6 as its single-jump limits | the single-jump forms are the small-k-tau oracles: with one transition rate the exponential equals them to 1e-13; with both rates it differs by the second jump (1e-3 relative at Crain's 22 us). mcsolve needs the problem in units of the window (its collapse-time search has an absolute time tolerance) | derived |
| `conv.readout_crosstalk_added_counts` | leakage[\|i - j\|] eps_sys R_o,j | 8.5; 5.7 | a bright neighbour's leaked light is ADDED COUNTS on its neighbours at the configured PSF fraction AND extra off-resonant pumping on their chains (the 'depumping-reduced N' half, anchor.m5.crosstalk_depumping); the register confusion at configured crosstalk is factored by neighbour range (per-ion conditional tables over the ion's own LEVEL and its neighbours' start classes, exact for per-ion decisions on additive light with the neighbours' pumping frozen at their start class), dense to N = 12 | Wineland 1998 (degraded discrimination, not a changed eta_d); Burrell 2010 (PSF overlap); Acton 2006 Eq. 22 with the corrected I_sat | the product POVM is exact only at zero crosstalk: at 4 % leakage a dark 171Yb+ ion beside a bright one under the zero-threshold SNSPD protocol reads bright 34 % of the time (0.42 leaked counts in 22 us), the bounded, reported discrepancy of Section 9.5 | corrected |
| `conv.readout_detected_line` | R_det = eps_sys R_o on ONE line | 8.1 'Detected rate and background'; 13 Detection efficiency | epsilon_sys carries exactly one epsilon_filter and an interference filter passes one wavelength, so R_o counts photons on the species' CYCLING line only: detection_rates_for_ion passes line = '<lower><-<upper>' of species.cycling, rates_from_bloch derives the single non-sink line when the build has one and REFUSES to guess when it has several, and line=ALL_LINES is an explicit all-line diagnostic (the total scattering rate W(Delta) of Section 4.2.2), never a detected rate | qutip_trap/readout/fluorescence.py; tests/test_m5_detection_line.py | the first M5 implementation summed every non-sink decay line, so a 40Ca+ 397 + 866 nm detection model counted the repump's 866 nm photons: R_o was high by exactly 1 + Gamma(D3/2<-P1/2)/Gamma(S1/2<-P1/2) = 1.0638, i.e. 6.4 %, and eps_B, eps_D and the whole budget with it. Exact for 171Yb+ only because its detection build carries one line. A repump's manifold also enters the model only when the DEVICE has a beam on that line: without the beam the metastable level is a dead end whose own lifetime is the slowest Liouvillian mode and the bright/dark coarse graining has no two-manifold slow mode at all | corrected |
| `conv.readout_figure_of_merit` | eps = (eps_B + eps_D)/2 | 13 Readout figure of merit | eps = (1/2)(eps_B + eps_D), both reported | Myerson; Burrell; Harty; Christensen | Acton's min(F_B, F_D) | verified |
| `conv.readout_likelihood_first_order` | p_D = (1 - t_b/tau) M_N + (t_s/tau) S_N | 8.3 | the O(N) recursion in the log domain with log-sum-exp; the decay rate 1/tau is the dark class's rate of becoming bright (the shelf lifetime, or R_b); ties go to bright; the posterior error uses equal priors | Myerson 2008 Eqs. 1-2; the exact hidden-Markov forward likelihood derived here (Section 8.7 gap) | the exact forward likelihood on the chain's sub-bin kernel agrees with the first-order form to 1e-5 for the hypothesis matching the record and differs at the percent level for the hypothesis that must invoke a decay (its position inside the sub-bin), with the same decision; the linear brute force underflows near 227 sub-bins | verified |
| `conv.readout_micromotion_detection` | R_o(beta) = J_0(beta)^2 R(Delta) + J_1(beta)^2 [R(Delta - Omega_rf) + R(Delta + Omega_rf)] | 8.8 'Micromotion on the detection beam'; 13 'J_n(beta) on every drive' | the detection drive as a whole is phase modulated at the trap's rf frequency, so every rate the Bloch solve returns is the J-weighted sum of the carrier solve and the two first-sideband solves (three solves, the beams shifted by -/+ Omega_rf); readout_stage takes beta = k . u_1 for the SINGLE-photon wavevector of the shortest detection beam and Omega_rf from the trap's rf record, and reports the factor in the diagnostics | qutip_trap/readout/fluorescence.py; qutip_trap/run/job.py; tests/test_m5_detection_line.py | micromotion_detection_rate existed but had no caller: neither light/bloch.py nor dynamics/multilevel.py carries a modulation index, so the R_o the readout ran on was unmodulated. R_d and R_b carry the same weighting; being linear in intensity with no saturation denominator they barely move (sum_n J_n^2 = 1) while R_o loses the sideband share, so micromotion RAISES the leakage ratio R_d/R_o - an M5 finding, not in any source | corrected |
| `conv.readout_polarity_scheme` | ReadoutScheme | 13 Bright/dark polarity; 8.1, 8.4 | bright/dark is a property of the readout SCHEME stored as an enum: 171Yb+ direct fluorescence has \|1> = F = 1 bright; the 40Ca+ optical qubit has \|0> = S1/2 bright and \|1> = D5/2 the shelf; shelving transfer is a START distribution per level, so Pi_dark = sum_l p_l \|l><l\| is not rank one | Myerson 2008; Christensen 2020; Harty 2014 (direct fluorescence and shelving invert the polarity within one paper) | the qubit label carries no polarity; the code refuses a scheme in which no level fluoresces | verified |
| `conv.readout_povm_indexed_by_level` | POVM.per_ion[i][level, declared] | 5.7; 8.4; 9.5 row 'Budgets'; 9.17 'POVM fast-path domain' | the per-ion confusion is an (n_levels, 2) table P(declared \| internal LEVEL) with the scheme's shelving/mapping transfer channel folded into the rows, so the fast path indexes it by the projectively sampled level and samples no start class of its own; the register-wide form runs its OWN axis over levels and a neighbour's axis over the neighbour's start class (all the leaked light depends on), marginalizing the neighbours with P(bright \| level) | qutip_trap/readout/discriminate.py; tests/test_m5_fast_path_levels.py | the first M5 implementation sampled each ion's start class and then indexed the POVM by that class, applying the transfer channel TWICE: on a 40Ca+ shelving scheme with a 10 % transfer failure P(declared bright \| shelved level) came out 0.1925 instead of 0.1002 (a factor 1.92), and on Harty-like transfer eps_D was 6.04e-4 instead of 3.04e-4. Invisible to the existing test because YB_DIRECT has an ideal transfer, where class and level coincide. A d > 2 leakage level now gets its own row instead of borrowing a qubit level's start distribution | corrected |
| `conv.readout_povm_monte_carlo` | POVM.uncertainty = sqrt(1/(4 n_samples)) | 8.3 'strategy interface over the same record'; 8.4 | a threshold discriminator's confusion is exact through the count distributions; every other strategy of Section 8.3 (time-resolved ML, adaptive, first-photon, camera) has no closed form, so readout_stage estimates it from povm_samples sampled records per level per ion (default 2e4, seeded from the run's root seed) and the POVM declares the binomial error bar it earned. readout='full' builds no POVM at all - the record path REPLACES it (Section 5.7) - and the (eps_B, eps_D) report then comes from the run's own records, with nan for a qubit level the circuit never populated | qutip_trap/run/job.py readout_stage; tests/test_m5_run_discriminators.py | before this fix readout_stage built the product POVM unconditionally with n_samples = 0, and per_ion_confusion raises for anything but a threshold discriminator, so run(..., discriminator=TimeResolvedML(...)) raised in both readout modes: Section 8.3's strategy interface was advertised through run() and api.py but only the threshold strategy ran end to end | derived |
| `conv.repump_kernel_per_channel_alpha` | alpha_eff = sum_k p_k alpha_{q(k)}(chi) / sum_k p_k | 4.2.8 (ii); 4.2.2 | the pulsed-cooling repump kernel of run_preparation computed cos_chi and then called angular_factor(None, cos_chi), which returns 1/3 for ANY cos_chi: a hard-coded isotropic alpha with a dead argument, against light/recoil.py's own 'no scalar alpha is ever hard-coded' and Section 4.2.8 (ii)'s 'one angular factor per channel and mode axis' (the M3 finding). The scalar a one-dimensional Fock kernel needs is now the photon-rate-weighted mean of alpha_q(chi) over the repump beams' own emitted polarization channels in their steady state, read off the BlochModel the recipe already builds for the pump; a polarization-pure repumper on a mode along B returns 2/5 or 1/5 and an unpolarized cycle 1/3 | qutip_trap/light/bloch.py (emission_angular_factor, operator_angular_factor); qutip_trap/prep/recipe.py | the q = 9 vector-form sentinel of EmissionChannel.operator_qs no longer falls back to the isotropic 1/3 either: operator_angular_factor reads the channel's own tabulated alpha when the channel has a single polarization index and REFUSES a genuinely q-mixing one, whose alpha is the derived second moment of recoil.derived_angular_factors | corrected |
| `conv.response_phase_reference` | phi_leg -> phi_leg + sgn(mu) arctan(2 pi \|mu\| tau_r) | 7.10; 4.4.1 (Roos); 9.6 | the first-order response delays the envelope, so the bichromatic beat note advances arctan(2 pi mu tau_r) = 0.766 rad (the phase of H(mu) = 1/(1 - i 2 pi mu tau_r)) before the field arrives and Roos's spin-axis tilt returns after the per-gate reset: fidelity 0.99700 with 2.9e-3 leakage uncompensated, 0.99974 / 2.2e-4 with the linear mu tau_r reference, 0.99992 / 3.3e-5 with the arctan reference against 0.99987 / 4.5e-5 for an ideal modulator | check_noise.py 7 on the two-ion 171Yb+ fixture (mu/2pi = 3.06 MHz, tau_r = 50 ns: 2 pi mu tau_r = 0.96 rad) | the tone phases of every entangling pulse (scheduler and exact spot check alike) carry the arctan reference; the residual is what the exact check reports; a laboratory absorbs it in its phase calibration (M8's scans) | recomputed here |
| `conv.result_bit_order` | qubit0_lsb | 13 Result bit order; 8.6; 9.13 | qubit 0 is the least-significant bit of the decimal histogram key; {"5": 0.5, "7": 0.5} on three qubits <-> bitstrings 101, 111; shots ["6","1","0","7"] <-> 110, 001, 000, 111 | IonQ OpenAPI v0.4; qiskit-ionq and PennyLane-IonQ client code | PennyLane reverses to big-endian [verified against the client sources] | verified |
| `conv.rf_amplitude_pseudopotential` | V_rf peak | 13 rf amplitude and pseudopotential | V_rf is the PEAK of a cos(Omega_rf t) drive; psi = Q^2\|E\|^2/(4 m Omega^2) in joules (eV at the boundary); Blakestad's phi_ps = q(V_rf grad phi~_rf)^2/(4 m Omega_rf^2) is the same peak convention in volts | House; Wesenberg; Chiaverini; Madsen; Home; Kielpinski; derivation audit 2026-09-04 | reading the stored peak as rms doubles the depth and inflates every rf-set secular frequency by sqrt 2; as peak-to-peak quarters the depth and halves those frequencies; the second revision paired a halved depth with a sqrt 2-inflated frequency, which no single field factor gives | corrected |
| `conv.rf_phase_and_parametric_heating` | rf_phase_noise stored and unused; no 2 omega_sec parametric term | 6.2 [corrected]; 4.1.5 | Section 6.2 requires both: 'rf amplitude and phase noise spectra S_V(omega) and S_phi(omega) are device parameters that drive the Omega_rf +- omega_t sideband heating of Section 4.1.5 and a parametric-heating term at 2 omega_sec'. S_V is implemented on both routes (a common-mode fractional trajectory and the a^dag a sqrt(2/tau) operator from its white level); S_phi is stored and never read, and the 2 omega_sec term does not exist | M7 audit E-25 / A.1 (noise/model.py's rf_phase_noise field and its docstring; grep -rni parametric finds only surface-electrode geometry) | the MISSING DEVICE PARAMETER for the sideband channel is named: trap/heating.micromotion_sideband_heating_rate exists but needs the uncompensated stray field's rf GRADIENT at the ion, which the explicit-frequency Trap record does not carry (it has stray_field_v_per_m, a uniform field, not its rf-frequency gradient), so the channel is unreachable from a NoiseModel. The 2 omega_sec parametric term needs the fractional rf-amplitude density at 2 omega_sec, which S_V does supply - it is simply not implemented, and was previously recorded NOWHERE, which is the defect this record closes | contested |
| `conv.rotation_generators` | R(theta, phi) carries theta/2; XX(chi) carries chi with no 1/2 | 13 Rotation generators; 7.1; 9.7 | a single-qubit rotation carries theta/2 in the exponent, R(theta, phi) = exp[-i (theta/2)(cos phi sigma_x + sin phi sigma_y)], so GPi2(phi) = R(pi/2, phi) exactly and GPi(phi) = i R(pi, phi) (equal up to the global phase every fidelity measure quotients), with RZ(theta) = diag(e^{-i theta/2}, e^{i theta/2}); the two-qubit generator carries chi with NO 1/2, XX(chi) = exp(-i chi sigma_x (x) sigma_x), chi = pi/4 maximally entangling and the native MS angle theta = 2 chi in [0, pi/2]. No shared generator helper computes both: each is written where it is used (control/native.py gpi, gpi2, r_phi, rz, xx, ms, zz), because one factor 1/2 shared between the two conventions would halve every entangling angle. The consequence for the twirl is that Trout 2018's over-rotation probability p_xx = sin^2 alpha for exp(-i alpha XX) is the TWO-qubit case only, and it is exposed under that name (noise/summary.py over_rotation_twirl_probability) | Maslov 2017; Trout et al. 2018; control/native.py; control/two_qubit.py; noise/summary.py | reading Maslov's R gates and Trout's XX through one generator convention is a factor 2 in the entangling angle; the plan's row exists to forbid the shared helper, and the code has none | corrected |
| `conv.saturation_ceiling` | P_f^max = n_e/(n_e + n_g) | 13 Saturation ceiling of a closed manifold; 8.1 | P_f^max = (2J_f + 1)/((2J_i + 1) + (2J_f + 1)): Gamma/4 for 3 ground + 1 excited, Gamma/3 for a Lambda system, Gamma/2 for a two-level atom | Berkeland Eqs. 13-14 and 21 for the two special cases; the general form derived by the Run 5 consolidation | the two-level Lorentzian's Gamma/2 applied to a degenerate manifold; a 'naive four-level' 1/2 for the 171Yb+ detection line | background |
| `conv.saturation_intensity` | I_sat | 13 Saturation intensity, Saturation-intensity rate; 4.5.2; 9.13 | I_sat = pi h c Gamma_partial/(3 lambda_vac^3) with Gamma_partial = 2 pi gamma_hz x branching ANGULAR; 171Yb+ 369.5 nm: 50.83 mW/cm^2 | Steck; check_critique_v3.py | gamma_hz fed in unconverted gives 8.09 mW/cm^2 (a 2 pi in every saturation parameter); Acton prints hbar for h; the random-orientation hbar omega^3 Gamma/(4 pi c^2) is exactly 3x the cycling value [corrected: critique, 2026-09-04] | corrected |
| `conv.scattering_channels` | L_{q', k_hat} = sqrt(w_k) sum_{a,b} r_{a->b q'} \|b><a\| (x) D(i(eta_abs - eta_em)) | 4.5.5; 6.5; 4.2.8; 13 'Rayleigh dephasing dissipator', 'Recoil kernel discretization' | one operator per (emitted polarization, direction node): Rayleigh diagonal over the resolved levels (Gamma_el/4 dissipator prefactor automatically), one Raman operator per (a, b), the out-of-manifold rates summed into the SINK at d > 2 and reported at d = 2; the absorbed beam's e^{+i k_b x} multiplies the emission kick; six axis directions about B with weights alpha_par/2 and alpha_perp/2 are exact in the first and second moments for every dipole pattern and every mode axis; rates scale with the played intensity as (Omega/Omega_nom)^p, p the Stark scaling power | Uys 2010 Eqs. 6-8 (the diagonal Rayleigh operator), Ozeri 2007 (the recoil operator e^{i(k_L - k_s) x}), Joshi 2020 (the moment-exact direction quadrature); assembled in noise/scattering.py | the absorption factor in an eliminated jump operator is the plan's own construction (Section 13's 'never both in a collapse operator' rule is stated for the explicit multi-level builder); the recoil quanta per photon eta_abs^2 + alpha eta_em^2 are the test | background |
| `conv.scattering_channels_at_d_gt_2` | internal_levels > 2 => SolverOptions.scattering_channels = True, scattering_recoil = 'off' unless 'vector' was asked for | 4.5.5 'Leakage is therefore simulated, not estimated, whenever d > 2' | run(..., internal_levels = d > 2) turns the flag on and records it in the run's notes; internal_levels = 2 keeps the d = 2 per-pulse estimate path of Section 4.3.2. The automatic switch turns on the register operators (leakage into the SINK, spin flips, the differential Rayleigh operator) WITHOUT the recoil displacements: Section 4.5.5's d > 2 requirement is about leakage, and the recoil-dressed operators (one per emission direction, per pulse, per branch) cost 30 to 50x more on the two-ion fixture, so they remain an explicit choice (scattering_channels=True with scattering_recoil='minimal' or 'vector'); a caller who set scattering_recoil='vector' keeps it | M7 audit E-10 (run/job.py; tests/test_run_noise.py leakage test); 2026-09-08 consolidation | SolverOptions.scattering_channels defaults to False, and run(..., internal_levels=3) used to leave it off, so a d = 3 register carried a SINK level that nothing could populate and reported no leakage estimate for it either: the plan's automatic behaviour was opt-in. An explicit scattering_channels=False is overridden rather than refused, because the plan offers no d > 2 mode without leakage simulation | corrected |
| `conv.scattering_rate_object` | W(Delta) = sum_lines sum_k Tr(C_k^dag C_k rho_ss) | 13 Scattering-rate object; 4.2.8; 8.1 | one multi-level optical-Bloch steady state supplies the cooling rate W(Delta) in s^-1, the readout rates R_o, R_d, R_b and the dark-state physics; every photon rate is asserted against the equal-population ceiling n_e/(n_e + n_g) of the resonant closed manifold (Gamma/4 for the 171Yb+ F = 1 -> F' = 0 cycle); R_o is the rate of the CONDITIONAL bright state from the slow-manifold coarse graining, because the full steady state of a detected ion sits in F = 0 with weight R_d/(R_d + R_b) ~ 0.95 | qutip_trap/light/bloch.py (M3a) | the two-level Lorentzian saturates at Gamma/2 where the four-level manifold saturates at Gamma/4; the second revision's Gamma/6 prefactor with (2/3) s_o folded one polarization share too few | corrected |
| `conv.section_9_9_convergence_regime` | SolverOptions.convergence_check, Diagnostics.convergence, hilbert.truncation.convergence_report | 5.5; 9.9; 14.5; Appendix E | Section 9.9: tightening tolerances by 10x (and, for the integrator ladder, loosening by 10x and comparing) and raising truncation caps by 2 changes reported probabilities by less than the stated test tolerance, for every validation case | dynamics/engine.py SolverOptions; run/results.py Diagnostics; run/job.py; hilbert/truncation.py convergence_report, grown_caps, ConvergenceRegime; dynamics/evolve.py ConvergenceReport (M2); tests/test_m9_scaling.py | none of the three comparisons was ever run: halving_test was called only from one synthetic unit test, evolve.tightened was exported and never called, and the 'convergence' pytest marker declared in pyproject.toml was used by zero tests. The run level now carries the first arm (SolverOptions.convergence_check repeats the evolution at atol/10, rtol/10 and reports the change in the register populations as Diagnostics.convergence, an addition to Appendix E's Diagnostics; off by default because it triples a run's cost, and the register populations are compared rather than the sampled histogram because they are deterministic), and hilbert.truncation.convergence_report runs all three arms of one validation case as three dynamics.evolve.ConvergenceReport values: tolerances/10, tolerances x10 started from options x10 so the pair is (loose, options), and every resolved cap and the ENR excitation cap + 2 at unchanged tolerances (grown_caps respects Section 9.9's dimension ceiling by leaving a mode whose growth would cross it). The cap arm's report has tolerances == tightened_tolerances, which is how it says the tolerances did not move | corrected |
| `conv.seed_keys` | SeedSequence(root, spawn_key=(sample, trajectory, shot, ion, channel)) | 3.4; Appendix E SeedSpec | one root SeedSequence per run spawned by (sample, trajectory, shot, ion, channel); reproducibility over 1 and 18 workers is a 1e-12 tolerance test, not bitwise | PLAN.md Section 3.4; 2026-09-04 numerics critique | the shot slot was added because photon-count sampling is per shot per ion; MultiTrajResult accumulates in completion order (4.4e-16 measured) | corrected |
| `conv.servo_residual` | x_k - x_hat_{k-1}, x_hat_k = x_hat_{k-1} + (1 - e^{-2 pi f_s dt})(x_k - x_hat_{k-1}), x_hat_0 = x_0 | 7.5 Age, drift and cost; 6.2 | a Drift with a servo bandwidth is high-passed into its residual band over the shot clock: the residual variance of an OU drift of correlation time tau is sigma^2/(1 + 2 pi f_s tau) in the continuous limit and the drift's increment sigma^2 (2 dt/tau) for a lock re-armed every sample; the first sample carries no offset (the calibration measured it) | noise/model.py servo_residual; Drift.servo_bandwidth_hz | the first version sampled raw quasi-static drift against a once-computed table, double-counting the slow band a laboratory servos away | background |
| `conv.shim_field_response` | shim_voltages_v | 3.3; 4.1.1 | only the SURFACE path converts a shim voltage into a field (the named electrodes' field at the rf null); the explicit-frequency and rod/blade paths refuse non-zero shim voltages, because PLAN 4.1.1 states that no source gives the geometry factors kappa, R', Z0 or Phi'' for any electrode design, so Berkeland's rod map carries no shim -> field response | qutip_trap/trap/model.py; tests/test_trap_model.py | the rod/blade path accepted shims, dropped them from residual_field_v_per_m, and folded them into dc_voltages() where one named like the endcap silently changed a_z instead of producing a field [corrected: M1 audit, fix batch] | corrected |
| `conv.shot_blocks_per_sample` | sample k owns shots [sum_{j<k} M_j, sum_{j<=k} M_j) | 3.4; 8.6; 7.5 | shots are allocated in CONTIGUOUS BLOCKS, M_k = shots // S + 1 for the first shots % S samples, and sample k is drawn at the shot clock t0 + (sum_{j<k} M_j) T_rep; Diagnostics.shots_per_sample_realized reports the realized block sizes and Result.sample_of_shot the shot -> sample map Section 8.6 asks for | qutip_trap/run/job.py; PLAN.md Section 3.4 ('distributes shots round-robin over samples') | the plan says round-robin; blocks are the better physics and the deviation is deliberate. One quasi-static draw per contiguous block of the shot clock is what the hardware does, and it is the only allocation consistent with sample_times: round-robin would give every sample the same mean shot time, so an Ornstein-Uhlenbeck Drift over the sample times would produce no between-sample spread at all, and the effective sample size would then be the shot count by construction. The M6 code already allocated blocks; its own module docstring and README claimed round-robin | corrected |
| `conv.sideband_lineshape` | P(delta) = [Omega^2/(Omega^2 + delta^2)] sin^2((t/2) sqrt(Omega^2 + delta^2)) | 13 Sideband excitation lineshape; 7.5; 9.17 | the calibration fit function: a carrier pi pulse at Omega t = pi, half depth (the Lorentzian weight 1/2) at delta = Omega; fitted with weighted least squares to the fine scan of every line | PLAN.md Section 7.5 [corrected: critique, 2026-09-04]; experiments/fitting.py | Wineland's and Bluemel's Omega^2/(Omega^2 + delta^2/4) sin^2(t sqrt(Omega^2 + delta^2/4)) is the same curve at half the Omega: fitting it returns Omega/2 (the negative control the test suite keeps), never the fit function | corrected |
| `conv.sign_A_hfs_and_g_factors` | sign(A_hfs), g_S | 13 Sign of A_hfs and the g-factors; 4.5.6 | A_hfs is a signed input inferred from mu_I when not printed (9Be+, 43Ca+, 171Yb+ 3D[3/2]1/2 inverted); measured g_J replaces Lande; Steck's g_S = +2.0023193 (CODATA g_e is negative); nuclear Zeeman in Steck's form with a plus sign in H_B | Steck; Langer 2005; Harty 2014 | A > 0 removes the clock points (no 43Ca+ point in 1 to 4000 G, no 9Be+ root in 8 to 16 mT), the ingest test of the sign | corrected |
| `conv.simultaneous_rb_units` | r_q = (1 - p_q)/2 per qubit; (1 - p)(2^n - 1)/2^n per layer | 7.9 Randomized benchmarking variants; 6.6; 13 RB error rate; M10 | simultaneous RB draws INDEPENDENT single-qubit Clifford sequences on several ions and plays them interleaved, so the group is the product C1 x ... x C1 and Section 13's n-qubit Clifford formula does not describe the joint decay. Every benchmarked qubit's own MARGINAL survival P(qubit q reads its target bit) is therefore fitted to A p_q^m + B and reported as r_q = (1 - p_q)/2 (RBResult.marginal_error_per_clifford), one Clifford on ONE qubit the unit -- the unit of the r that qubit measures alone and of the per-qubit composition budget.predicted['r_channel.q{i}'] = (Cliffords per sequence) x sum_kinds (pieces per Clifford) x (the kind's GATE_LOCAL channel reduced to THAT ONE qubit) -- and RBResult.error_per_clifford is their mean. The JOINT survival is fitted too and its (1 - p)(2^n - 1)/2^n kept as RBResult.joint_error_per_layer, the error per LAYER of n Cliffords, which equals sum_q r_q to first order (0.2 % at r_q = 2e-5, 3 % at 3.4e-4 over m up to 512) and is compared with r_channel_joint_layer, the same composition over one layer with the channels reduced to ALL benchmarked qubits | Gambetta et al., PRL 109, 240504 (2012); Magesan 2011; benchmarks/rb.py; tests/test_m10_benchmarks.py | the first version reported the JOINT decay's (1 - p)(2^n - 1)/2^n as 'the error per Clifford' beside a budget whose unit was one single Clifford, a factor n apart, and the check script divided r_channel by 2 to compare them; anchor.m10.simultaneous_rb_crosstalk then read that factor 2 as a physical finding. Two independent qubits at r_q = r_channel = 3.426e-4 reproduce the committed joint fit p = 0.999080 to 3.5 % in (1 - p), so the composition already predicts the simultaneous decay. Separately, 'two_qubit = len(qs) == 2 and bool(run_kwargs.pop("pair", True))' short-circuited past its own pop on any qubit count but two, leaving pair= in the kwargs splatted into run() and raising TypeError: simultaneous RB on three ions, the natural use of a variant whose point is chain crosstalk, could not run at all | corrected |
| `conv.solver_integrators` | dop853, vern9 | 5.3 Solvers; Appendix E SolverOptions | the escalation ladder is dop853 then vern9; never a multistep method; atol 1e-10, rtol 1e-8 | PLAN.md Section 5.3; bench_numerics.py | - | recomputed here |
| `conv.solvers_read_the_device_modes` | gate_modes(mode_frequencies_hz=...), CalibrationTable.lamb_dicke | 7.3; 7.9; 9.17 (M4 audit 2026-09-07) | Section 7.3's contract is that the solvers work at what the calibration table says and never at the device's hidden truth. In fact every production caller builds GateModes from the device's crystal: gate_modes has a mode_frequencies_hz override that no caller passes, CalibrationTable.lamb_dicke is written by the sideband experiment and read by no solver, and gate_modes has no eta override at all | calibration/surrogate.py; run/space.py; calibration/experiments.py | recorded rather than wired: threading the table's beliefs into the surrogate and the resolved-mode selection changes what M8's calibration converges to and what M9a's mode classes select, which is a change to those milestones' contracts and not to M4's pulse algebra. The shaping docstring no longer claims the override is in force | contested |
| `conv.space_declaration_before_allocation` | HilbertSpace.check() | 11.5; 5.1; 5.4 | Section 11.5: the monitor refuses to build joint spaces above a configurable dimension (default 4096) or non-zero estimate (default 2e7) and directs the run to GATE_LOCAL | hilbert/space.py check, IDENTITY_CHECK_MAX_DIMENSION; run/space.py select_space, SpaceSelection.budget; run/job.py; tests/test_m9_scaling.py | M9a evaluated the guard AFTER the allocation: HilbertSpace.__post_init__ -> check() -> qt.tensor(*factor_identities()) formed an O(D)-non-zero CSR identity, and only then did run() call within_budget. Measured: 2 ions x 5 modes at d = 16 (dimension 4194304) cost +64 MB inside check(), and the eight-ion eight-mode case Section 5.4 cites would have needed about 86 GB there and died before the guard could reroute it. A Kronecker product's shape is the product of its factors' shapes, so check() now asserts shape == prod(dims) arithmetically and forms the joint identity only below IDENTITY_CHECK_MAX_DIMENSION = 65536; a HilbertSpace is therefore a DECLARATION that allocates nothing (every operator on it is lazy and cached), select_space evaluates within_budget on it and returns the verdict as SpaceSelection.budget, and run() reads that verdict. Deviation from the audit's recommendation: no separate SpaceDeclaration type and no HilbertSpace.declare classmethod were added, because after this fix the space object and the declaration are the same value and a parallel dataclass duplicating its six fields would drift An explicit level='JOINT_EXACT' above the guards used to proceed with a note ('JOINT_EXACT forced above the Section 11.5 guards'); on 2026-09-08 the ENR end-to-end test built a 37752-dimensional space that way ([2, 2, 11, 13, 66], the two resolved x modes beside a 66-dimensional ENR factor), took 20 GB of memory and an hour before QobjEvo rejected its term, so run() now REFUSES the explicit level above the guards with the dimension, the non-zeros and the two knobs named (raise joint_dimension_max / nnz_max to build it deliberately); the test runs at N_exc = 2 (dimension 3432) and pins the refusal at N_exc = 10 | corrected |
| `conv.spectator_dephasing_gaussian` | exp(-tau^2/alpha^2) | 8.5; 5.7 | a spectator's coherence during a neighbour's detection decays as a Gaussian; implemented as a quasi-static frequency offset drawn per shot from N(0, sqrt(2)/alpha), whose ensemble average reproduces the Gaussian exactly | Crain 2019 Fig. 3 (alpha = 94(5) ms at 200 um, 814(77) ms at 370 um, 1716(80) ms baseline) | a constant-rate sigma_z Lindblad term would give an exponential lineshape; the distance dependence between the two anchors is interpolated as a power law of exponent 3.5 (apparatus data, not physics) | verified |
| `conv.spin_motion_phases` | phi_s, phi_m | 13 Spin and motion phases; 4.3.4; 4.4.2 | phi_s = (phi_b + phi_r)/2, phi_m = (phi_b - phi_r)/2 of the two tone phases; the bichromatic force acts about phi_s + pi/2 in the same-Delta-k geometry (i eta(a + a^dag) carries an i), so MS(phi_0, phi_1, theta) sets phi_s,i = phi_i - pi/2 (control/schedule.py) | Haljan 2005; Lee 2005 (opposite sign, converted at ingest); derivation audit 2026-09-04 (row 4.3-5) | Lee writes phi_S = -(half-sum); the second revision's '+phi' axis would have certified a quarter-turn error | verified |
| `conv.stark_compensation` | mu_tone -> mu_tone + delta_St,believed | 7.5 item 7; 4.3.2 | every tone of a scheduled pulse is detuned by the table's differential light shift for the played amplitude; both legs of a bichromatic segment move together (the motion phase is untouched); a correct belief keeps the pulse resonant with the shifted qubit, a wrong one leaves a reproducible detuning. The tones are phase-continuous in absolute time (the builder plays e^{-i(2 pi mu t - phi)}), so every compensated tone also carries the phase 2 pi delta_s(0) t_start (compensation_phase_rad) that references the compensation detuning to the qubit frame at the pulse start, minus, inside a multi-segment gate, the frame 2 pi int delta_s dt the earlier segments accumulated; the frame update after the pulse is 2 pi int delta_s dt (stark_phase_rad, frame_after) | control/schedule.py single_qubit_pulse, entangling_pulses (stark_compensation=True) | an uncompensated -38 Hz light shift over the 100 us fixture gate costs about 1e-3 of fidelity (an uncompensated 0.024 rad sigma_z rotation per ion); the M6 surrogate carried no Rabi or Stark entry for the entangling drive's beams, so its gate had no light-shift physics at all; M8 seeds them. Without the phase reference the compensated axis rotates by 2 pi delta_s t_start: a compensated GPi2 one millisecond into a schedule lost 1.5e-2 = (2 pi 38 Hz 1 ms)^2/4 on the two-ion fixture while the same pulse at t = 0 was exact, and the Bell circuit through run() read 4.2e-3 against M6's 2.35e-3 (anchor.m8.compensation_phase_reference) | background |
| `conv.stark_scaling_with_amplitude` | delta_St(Omega) = delta_cal (Omega/Omega_cal)^p | 4.3.2; 7.3 | p = 1 for a two-photon (Raman or light-shift) drive, whose Rabi frequency and differential Stark shift are both proportional to the intensity; p = 2 for a single-photon optical drive (Omega proportional to the field) and a microwave drive (the ac Zeeman shift proportional to B_1^2) | Wineland 2003 Eq. 2.10 (shift proportional to intensity); PLAN.md Section 4.3.2 | the M4 scheduler scaled every Stark entry as (Omega/Omega_cal)^2; on a Raman waveform played at 550 times the derived carrier Rabi frequency of a weak beam pair that turned a -0.1 Hz shift into -70 kHz and tripped the boundary monitor (M6 finding, never exercised by the M4 tests, whose tables had no Stark entries) | corrected |
| `conv.static_error_primitive` | M(theta, phi; eps_a, eps_d) | 4.3.5 The static error primitive; 13 Detuning symbols | M = exp[-i theta {(1 + eps_a) rho(phi).sigma + eps_d sigma_z}/2], rho(phi) = (cos phi, sin phi, 0), exact for a square segment under time-independent errors; leading coefficients at theta = pi in 1 - F_K: primitive (pi^2/4) eps_a^2 and eps_d^2 exactly | Kabytayev 2014 Sec. II A; Merrill and Brown; Cummins 2003; Mount 2015; control/composite.py | Mount's R = exp[-(i/2) theta (1 + eps)(sigma_phi + delta Z)] and Cummins' quaternion pulse (axis (cos phi, sin phi, eps_d)/sqrt(1 + eps_d^2), angle theta sqrt(1 + eps_d^2)) identify eps_a = g and eps_d = f = delta/nu_1 with no rescaling on ingest | verified |
| `conv.surface_electrode_geometry` | Theta_i = phi_i/V | 13 Surface-electrode geometry; 4.1.6 | widths from gap centre to gap centre; ion height h; the ion at the minimum of the total potential, not the rf null; escape point a saddle from f''(w) = 0; rf pseudopotential exactly isotropic transversely at a 2D null, axes set by the dc; five-wire h = sqrt(a(a + 2b))/2 for FULL widths; two-rail null x0 = ac/(b + c), y0 = sqrt(abc(a + b + c))/(b + c) | House 2008; Wesenberg 2008; Nizamani 2012 | y0/h/d for one height; the rf Hessian angle used as a mode axis; a maximum finder for a saddle; the first plan version's sqrt(a(a + b)) with a as an unstated half-width | corrected |
| `conv.terminal_measurement_event` | ScheduledEvent('measure', ions, t_end, t_end + window) | 7.2 item 4; 3.4; 8.5 | the terminal measure (the circuit's targets plus trailing measure operations) is the schedule's one event, after the last pulse and dead time, of the calibration table's detection window (the detector's when the table has none); a measure, reset or recool before a later gate is refused with a ScheduleError naming Section 8.5; the engine integrates to the end of the pulses, never through the detection window | PLAN.md Section 7.2 item 4 [corrected: critique, 2026-09-04] | the M2 scheduler dropped the terminal measure and raised NotImplementedError for mid-circuit operations | verified |
| `conv.trajectory_count_two_phases` | target_tol -> n_est -> replay of n_est keyed seeds | 3.4; 5.4 | phase one runs mcsolve with target_tol on the population e_ops already assembled (SolverOptions.e_ops_for_target_tol gates it, SolverOptions.trajectory_target_tol sets the tolerance), under a SERIAL map so the firing point is reproducible, with ntraj as its cap and TARGET_TOL_MIN_TRAJECTORIES = 8 as its floor; phase two replays a keyed seed list of the estimated length | qutip_trap/dynamics/engine.py; QuTiP 5.3.1 mcsolve target_tol measured | trajectory_target_tol defaults to None (the fixed ntraj with its keyed seed list, the M6 behaviour), because QuTiP 5.3.1's target_tol accepts a zero-variance first batch: on a damped two-level system it returned 26 trajectories at tol 0.1 and 604 at 0.02, but 2 at 0.005, 0.001 and (0.005, 0.0), where the first two trajectories had both jumped and the estimated error was exactly 0. The plan's own Section 3.4 [corrected] note records the companion defect, that the firing point is scheduling dependent (200 trajectories serial against 5 parallel in one measured call), which is what the fixed-seed replay of phase two exists to repair | corrected |
| `conv.trajectory_grid_tau_c` | grid_oversample = 10 pi, i.e. dt = tau_c,min/10 | 5.5 'an independent fixed grid with Delta t <= tau_c/10' | the shortest correlation time a tabulated band carries is that of its fastest component, tau_c,min = 1/omega_max, so the rule reads dt <= 1/(10 omega_max); time_grid sets dt = pi/(omega_max x oversample), which meets it at oversample >= 10 pi = 31.4159 | M7 audit E-6 (noise/processes.TAU_C_OVERSAMPLE; tests/test_m7_noise_processes.py) | the previous default of 4 gave dt = 0.785/omega_max, 7.9x coarser than the plan's rule, and the constant tau_c/10 appeared nowhere in the repository. The generator remains the spectral method rather than the exact OU update, which is the more general choice for an arbitrary tabulated S and is what the periodogram test validates; the plan's 'exact Ornstein-Uhlenbeck update' sentence is a deviation recorded here | corrected |
| `conv.two_photon_rabi` | Omega = Omega_1 Omega_2/(2 Delta) | 13 Single-photon coupling symbol g; 4.3.2; 4.5.4 | the code stores Omega (the hbar Omega/2 convention) and two ingest maps: g = Omega/2 for Monroe 1995, Wineland 1998, Ozeri and Section 4.5.5's g_{b,r} = E<d>/(2 hbar); g = Omega for the Lee comb chain (g_0 = gamma sqrt(I/2 I_sat)); Monroe's Omega = g_1 g_2/Delta is Omega_1 Omega_2/(2 Delta) in plan units | Sections 4.2.2, 4.3.7, 4.5.5; calibration/surrogate.py; device/derived.py | one symbol meaning Omega/2 in two subsections and Omega in a third inside two-photon formulas that differ by exactly the factor 2 the plan hunts | corrected |
| `conv.two_qubit_clifford_classes` | \|C1\| = 24, \|C2\| = 11520 = 576 + 5184 + 5184 + 576 | 7.9; M10 | C1 is the closure of {H, S} modulo global phase (24 elements, of which 20 cost exactly ONE pulse through decompose_single_qubit -- 16 gpi2 and 4 gpi -- and the remaining 4, the order-4 Z-rotation subgroup {1, S, Z, S^dag} and one SIXTH of the group, cost no pulse at all, so the group average is 0.8333 pulses per Clifford, 0.6667 gpi2 and 0.1667 gpi; the decomposer's two-GPi2 ZXZXZ branch is never reached, a Clifford's ZYZ middle angle being 0, pi/2 or pi); C2 the closure of {H (x) 1, 1 (x) H, S (x) 1, 1 (x) S, CNOT} (11520); the double cosets L g L of L = C1 (x) C1 (576) by the core g in {1, CNOT, iSWAP, SWAP} have stabilizers \|L n g L g^-1\| = 576, 64, 64, 576 and sizes 576^2/stabilizer = 576, 5184, 5184, 576; a uniform element is a core drawn by class size and two independent uniform local elements l_1 g l_2 (every element reached by the same number of pairs); 0, 1, 2, 3 entangling gates per class, 1.5 on average; the class of any element is the rank of the off-diagonal block of its symplectic matrix (0 identity, 1 CNOT, 2 with a zero diagonal block SWAP, 2 otherwise iSWAP), and the decomposition is found by the 4 x 576 tensor-product search | Barends et al. 2014 Supplementary; benchmarks/clifford.py; check_benchmarks.py 1; tests/test_clifford.py | a phase key that rounded floats distinguished -0.0 from 0.0 and closed C1 to 311 'elements'; the key quantizes to integers. This record said 'eight of them' and anchor.m10.single_qubit_rb 'a third of the Cliffords' were pulse-free Z rotations, both against the code's own passing test (tests/test_clifford.py asserts 4): it is four of twenty-four, one sixth; and 'at most two GPi2 pulses' was a loose bound on a group whose pulse histogram is {0: 4, 1: 20} | recomputed here |
| `conv.uncalibrated_frame_refuses_the_run` | table.qubit_freq[i].status == 'uncalibrated' -> RunError | 7.3; 7.5 | qubit_shifts_hz[i] = nu_true(B) - table.qubit_freq[i] is the ONLY channel by which the table's frequency error reaches the physics (EngineSetup.qubit_shifts_hz -> dynamics/hamiltonian.py), so run() refuses an uncalibrated entry naming Section 7.3 instead of programming a shift of zero. A device with NO qubit-frequency entry at all keeps the note and the true-transition frame: there is no believed frame to be wrong about (the surrogate always seeds one, so nothing in the suite takes that path) | run/job.py (the qubit-frequency shifts); tests/test_m8_calibration_fixes.py | the fallback set shifts[i] = 0.0 for a missing OR uncalibrated entry, which put the frame exactly on the true transition and made an uncalibrated qubit frequency error-free by construction - the same class of defect conv.played_chain's corrected_form records for M2 to M7. control/schedule.py's carrier_rabi_hz already raised for an uncalibrated Rabi entry | corrected |
| `conv.validity_assertions` | eta^2(2n+1) << 1, Omega << Gamma, W << nu and << Gamma_min, Gamma' and gamma' << nu, Gamma > omega_R, s_aux << 1, \|delta_aux\| << Gamma_10 + Gamma_12 | 4.2.8 (vii) | the seven Section 4.2.8 (vii) conditions now RAISE ValidityError at run time instead of returning a float or a bool: assert_weak_drive on lorentzian_scattering_rate, stenholm_coefficients and eit_rate_coefficients (Omega/Gamma above 0.1, the same number as bloch.WEAK_DRIVE_MAX), assert_resolved_linewidth on sideband_floor (Gamma/nu above 0.1), assert_lamb_dicke on every level-A mode (eta^2 (2 nbar + 1) above 0.25, i.e. eta sqrt(2 nbar + 1) above 1/2, which the plan's own anchors 0.413 at 2 nbar + 1 = 21, 0.242 and 0.35 all satisfy), assert_adiabatic on every level-A mode (W above 0.1 nu or 0.1 min Gamma), EffectiveTwoLevel.assert_resolved (max(Gamma', gamma') above nu/5), assert_three_level_valid inside effective_two_level, and assert_doppler_recoil_limit inside doppler_cooling before any rate is computed (Gamma <= omega_R = hbar k^2/2m); before this only Omega << Gamma raised, and only on rate_coefficients_from_model and ion_mode_rates(method='closed_form') (the M3 finding) | qutip_trap/prep/validity.py; tests/test_m3_validity.py | every guard takes an explicit escape keyword (allow_saturation, allow_unresolved, allow_strong_coupling, allow_fast_cooling, allow_invalid, allow_recoil_limited) so a caller that deliberately evaluates a source's own out-of-regime fixture says so: Marzoli's Fig. 3 fixture violates \|delta_aux\| << Gamma_10 + Gamma_12 (delta_aux = -Gamma_10), the plan's Morigi EIT fixture violates Omega_g << gamma (17 against 20 MHz), Lechner's probe gives Omega_pi/gamma = 0.29, and the 7/12 and 13/20 sideband-floor normalizations are evaluated at Gamma/nu = 2; the report functions (EffectiveTwoLevel.resolved, effective_two_level_valid, StageRates.lamb_dicke_guard) keep their booleans and floats | corrected |
| `conv.virtual_z_concrete_sequence` | RZ(0.1) then GPi2(0) on \|0> | 5.2, 7.6, 9.6; control/schedule.py, dynamics/frames.py | the scheduler plays GPi2 at phase 0 - 0.1 and leaves the frame at 0.1; the simulated state equals GPi2(-0.1)\|0> and, with RZ(0.1) applied afterwards, GPi2(0) RZ(0.1)\|0> as multiplied matrices; GPi2(+0.1)\|0> is asserted to FAIL so the rule is pinned by the sequence, not by a sign convention | tests/test_native_pulses.py | phi -> phi - theta in time order (Section 7.6); the documentation's +theta holds in matrix order | recomputed here |
| `conv.virtual_z_propagation` | phi -> phi - theta | 13 Virtual-Z propagation; 7.6 | RZ(theta) shifts every later pulse phase phi -> phi - theta, gates read in time order: RZ(theta), GPi(0.5), GPi2(0) equals GPi(0.5 - theta), GPi2(-theta) followed by RZ(theta) | PLAN.md Section 7.6 (recomputed) | IonQ's printed +theta holds in matrix order; the second revision's 5.2 printed +theta in time order [corrected: critique, 2026-09-04] | corrected |
| `conv.wavelengths_vacuum` | lambda_vac | 4.5.7; 13 Quadrupole (E2) coupling | k = 2 pi/lambda_vac; every stored wavelength is a vacuum wavelength | James 1998; Roos 2000; NIST Sr II compilation | James's Fig. 4 quotes the calcium wavelengths in air (729.147 nm; vacuum 729.347 nm); an air value used as vacuum biases k by 274 ppm and gives the wrong laser frequency outright | corrected |
| `conv.worker_memory_cap` | workers <= floor(0.5 x physical memory / max(parent peak RSS, 512 MB)) | 11.3 item 9 (trajectory, branch and sample parallelism); 11.5 (the memory ceiling); 9.9 | QuTiP's parallel map forks its workers, so each starts as a copy-on-write image of the parent and Python's reference counting dirties every page a worker touches; a parent that has grown to P bytes over a long session can therefore turn N workers into up to N x P. worker_count now returns min(requested or available_cpu_count, floor(MEMORY_FRACTION_FOR_WORKERS x physical memory / max(peak RSS, MIN_PARENT_BYTES))) with MEMORY_FRACTION_FOR_WORKERS = 0.5 and MIN_PARENT_BYTES = 512 MB, so the workers' worst-case total stays inside half of physical memory: a 3 GB parent on a 48 GB machine gets 8 workers, a 6 GB one 4, a fresh process every CPU. The engine's mcsolve num_cpus and every map_tasks caller read worker_count, and the reports (EngineReport.workers, Diagnostics.workers) state the count actually used | qutip_trap/dynamics/parallel.py (memory_worker_cap, worker_count); QuTiP 5.3.1 qutip/solver/parallel.py uses multiprocessing.get_context('fork'); the two kernel panic reports of 2026-09-08 (/Library/Logs/DiagnosticReports/panic-full-2026-09-08-065112 and -122640: 'watchdog timeout: no checkins from watchdogd in 94 / 91 seconds') | the first release let SolverOptions.workers = None mean every CPU QuTiP sees (18 on the plan's machine) whatever the parent's size. The 2026-09-08 slow-suite runs, and the 20-agent fix pass before them with about 130 concurrent pytest processes, each ended in a kernel watchdog panic (the machine restarted at 01:30 and 11:14) once the multi-gigabyte pytest parents forked their 18 workers; nothing in the tests allocates that memory on its own except the ENR end-to-end test of 2026-09-08 (19.8 GB for a 37752-dimensional space, since refused by the guards): a per-file run of every slow test under a memory watchdog measured peaks of 0.35 GB (test_parallel, 18 workers), 1.2 GB (test_gate_local), 1.9 GB (test_m6_four_ion, dimension 2304), 2.8 GB (test_m9_four_ion), 2.6 GB (test_docs), 2.6 GB (test_run_noise), 2.8 GB (test_m7_closed_forms), 7.7 GB (test_level_c, the level-C Doppler limit on an 8-state x 70-Fock density matrix) and below 0.7 GB for the other 22 files. Section 9.9's 1-versus-N-worker identity is unchanged: the count is a tolerance-free bookkeeping quantity and the tests compare the report against worker_count rather than against the CPU count | corrected |
| `conv.zero_point_energy` | H_mot | 13 Zero-point energy; 4.1.3 | H = sum hbar omega_k a^dag a, zero-point energy dropped; x = x0 (a + a^dag) with x0 = sqrt(hbar/(2 m omega)) | Wineland 1998; Marquet 2003 | James keeps +hbar omega/2 per mode and uses the Kittel convention with a factor i and (a - a^dag), which the code never mixes in | verified |
| `conv.zz_wrapper_construction` | ZZ(theta) | 7.6; 12 (inferred); M4 control/schedule.py | ZZ(theta) = exp(-i (theta/2) Z Z) = [GPi2(pi/2) (x) GPi2(pi/2)] XX(theta/2) [GPi2(3 pi/2) (x) GPi2(3 pi/2)] since R_y(pi/2) X R_y(pi/2)^dag = -Z; played as two GPi2 wrappers around the MS(0, 0, theta) waveform with the dead time between | Chen 2023 (220 us duration difference); Quantinuum wrapper construction | the ZZ native gate has no published physical model; this construction is inferred and labelled so in the gate ids (zz[k]/wrap_in, /ms, /wrap_out) | background |

<!-- END generated: conventions -->
